[
  {
    "id": "testing-q-1",
    "topicId": "testing",
    "difficulty": "junior",
    "question": {
      "en": "What is the difference between unit tests, integration tests, and end-to-end tests?",
      "ru": "В чем разница между unit-тестами, интеграционными тестами и end-to-end тестами?"
    },
    "answer": {
      "en": "### Test Types Hierarchy\n\nUnit, integration, and e2e tests differ in **scope** (how much of the system runs) and **realism** (how close to real user behavior they get) — and these two things trade off against each other.\n\n- **Unit tests** exercise one function or module in isolation. Everything around it (DB, network, other modules) is faked or mocked. They run in milliseconds and pinpoint exactly which function broke.\n- **Integration tests** let several real pieces work together — for example, a service function that actually writes to a test database. They catch bugs at the boundaries (wrong SQL, mismatched data shapes) that unit tests can't see, since each unit test mocks those boundaries away.\n- **E2E tests** drive the whole application the way a real user would: open a browser, click buttons, wait for network calls to finish. They give the highest confidence that a feature works end to end, but they're slow and can fail for reasons unrelated to your code (a slow CI runner, a flaky animation).\n\nThe general rule of thumb is: write many unit tests, fewer integration tests, and only a handful of e2e tests for your most critical user flows (this ratio is usually drawn as a pyramid).\n\n```typescript\n// Unit: pure function, no dependencies\ntest('sum adds two numbers', () => {\n  expect(sum(2, 3)).toBe(5);\n});\n\n// Integration: real function talking to a real (test) database\ntest('createUser actually persists a row', async () => {\n  const user = await createUser({ name: 'John' });\n  const found = await db.users.findById(user.id);\n  expect(found.name).toBe('John');\n});\n\n// E2E: full browser flow through the real UI\ntest('user can sign up', () => {\n  cy.visit('/signup');\n  cy.get('input[name=email]').type('john@example.com');\n  cy.get('button[type=submit]').click();\n  cy.contains('Success').should('be.visible');\n});\n```\n\n**Common pitfall:** Assuming integration tests run faster than unit tests, or treating \"I have 100% unit test coverage\" as proof the app works — unit tests mock away exactly the boundaries where real bugs (bad queries, wrong API contracts) tend to live.",
      "ru": "### Иерархия видов тестов\n\nUnit, integration и e2e тесты отличаются по **охвату** (сколько системы реально выполняется) и по **реалистичности** (насколько близко к поведению настоящего пользователя) — и это два параметра, которые идут в противовес друг другу.\n\n- **Unit-тесты** проверяют одну функцию или модуль в изоляции. Всё вокруг (БД, сеть, другие модули) подменяется моками. Они выполняются за миллисекунды и точно указывают, какая именно функция сломалась.\n- **Интеграционные тесты** позволяют нескольким реальным частям работать вместе — например, функция сервиса реально пишет в тестовую БД. Они ловят баги на стыках (неправильный SQL, несовпадающие форматы данных), которые unit-тесты просто не увидят, потому что в них эти границы замоканы.\n- **E2E-тесты** прогоняют всё приложение так, как это делал бы реальный пользователь: открывают браузер, кликают, ждут завершения сетевых запросов. Они дают максимальную уверенность, что фича работает целиком, но выполняются медленно и могут падать по причинам, не связанным с вашим кодом (медленный CI-раннер, дёрганая анимация).\n\nОбщее правило: писать много unit-тестов, меньше интеграционных, и совсем немного e2e — только для самых критичных пользовательских сценариев (это соотношение обычно рисуют в виде пирамиды).\n\n```typescript\n// Unit: чистая функция, без зависимостей\ntest('sum складывает два числа', () => {\n  expect(sum(2, 3)).toBe(5);\n});\n\n// Integration: реальная функция работает с реальной (тестовой) БД\ntest('createUser реально сохраняет строку', async () => {\n  const user = await createUser({ name: 'John' });\n  const found = await db.users.findById(user.id);\n  expect(found.name).toBe('John');\n});\n\n// E2E: полный сценарий через реальный UI в браузере\ntest('пользователь может зарегистрироваться', () => {\n  cy.visit('/signup');\n  cy.get('input[name=email]').type('john@example.com');\n  cy.get('button[type=submit]').click();\n  cy.contains('Success').should('be.visible');\n});\n```\n\n**Частая ловушка:** Считать, что интеграционные тесты выполняются быстрее unit-тестов, или воспринимать \"100% покрытие unit-тестами\" как доказательство того, что приложение работает — unit-тесты как раз замокивают те границы, где чаще всего и живут реальные баги (плохие запросы, несовпадающие API-контракты)."
    },
    "tags": [
      "test-types",
      "unit-integration-e2e",
      "testing-strategy"
    ]
  },
  {
    "id": "testing-q-2",
    "topicId": "testing",
    "difficulty": "junior",
    "question": {
      "en": "Explain the basic structure of a Jest test with describe, it, and expect.",
      "ru": "Объясните базовую структуру Jest теста с describe, it и expect."
    },
    "answer": {
      "en": "### Jest Structure: describe, it/test, expect\n\nThese three functions form the vocabulary Jest tests are built from, and each has one job:\n\n- **`describe(name, fn)`** groups related tests under a shared label. It doesn't run any assertions itself — it's purely organizational, and it can be nested to reflect structure (a module, then a function inside it, then specific cases).\n- **`it(name, fn)`** (an alias of `test`) defines one actual test case. The name should read like a sentence describing the expected behavior (\"should return sum of two numbers\"), so a failing test name alone tells you what broke.\n- **`expect(value)`** wraps the actual result and is chained with a matcher (`.toBe`, `.toEqual`, `.toThrow`, etc.) that states what you expect that value to be. A test can contain multiple `expect` calls, but each should still be checking one logical behavior.\n\n```typescript\ndescribe('Calculator', () => {\n  describe('add()', () => {\n    it('returns the sum of two positive numbers', () => {\n      expect(add(2, 3)).toBe(5);\n    });\n\n    it('handles negative numbers correctly', () => {\n      expect(add(-2, 3)).toBe(1);\n    });\n\n    it('throws when given a non-number argument', () => {\n      expect(() => add(2, 'x' as any)).toThrow();\n    });\n  });\n});\n```\n\nNesting `describe` blocks doesn't share state between tests by default — each `it` runs independently, which is why `beforeEach`/`afterEach` exist for setup that multiple tests need.\n\n**Common pitfall:** Putting `expect()` calls directly inside a `describe` block instead of inside an `it` — Jest silently registers this as no test at all, since `describe`'s callback only runs to *register* tests, not to execute assertions.",
      "ru": "### Структура Jest: describe, it/test, expect\n\nЭти три функции — базовый словарь, из которого строятся тесты в Jest, и у каждой своя роль:\n\n- **`describe(name, fn)`** группирует связанные тесты под общим названием. Сам по себе он не выполняет никаких проверок — это чисто организационная обёртка, и её можно вкладывать друг в друга, отражая структуру (модуль → функция внутри него → конкретные случаи).\n- **`it(name, fn)`** (алиас `test`) описывает один конкретный тест-кейс. Название стоит писать как предложение, описывающее ожидаемое поведение (\"returns the sum of two positive numbers\"), тогда одно только имя упавшего теста уже говорит, что именно сломалось.\n- **`expect(value)`** оборачивает фактический результат и связывается с матчером (`.toBe`, `.toEqual`, `.toThrow` и т.д.), который описывает, каким должно быть это значение. В одном тесте может быть несколько вызовов `expect`, но по сути они должны проверять одно логическое поведение.\n\n```typescript\ndescribe('Calculator', () => {\n  describe('add()', () => {\n    it('returns the sum of two positive numbers', () => {\n      expect(add(2, 3)).toBe(5);\n    });\n\n    it('handles negative numbers correctly', () => {\n      expect(add(-2, 3)).toBe(1);\n    });\n\n    it('throws when given a non-number argument', () => {\n      expect(() => add(2, 'x' as any)).toThrow();\n    });\n  });\n});\n```\n\nВложенные `describe` по умолчанию не расшаривают состояние между тестами — каждый `it` выполняется независимо, поэтому для общей подготовки, нужной нескольким тестам, существуют `beforeEach`/`afterEach`.\n\n**Частая ловушка:** Писать вызовы `expect()` прямо внутри `describe`, а не внутри `it` — Jest молча зарегистрирует это как отсутствие теста вообще, потому что колбэк `describe` выполняется только для *регистрации* тестов, а не для выполнения проверок."
    },
    "tags": [
      "jest-basics",
      "describe-it-expect",
      "test-structure"
    ]
  },
  {
    "id": "testing-q-3",
    "topicId": "testing",
    "difficulty": "junior",
    "question": {
      "en": "What is mocking and why would you mock a function in a unit test?",
      "ru": "Что такое mocking и зачем вы бы создавали mock функции в unit-тесте?"
    },
    "answer": {
      "en": "### What Mocking Is and Why It Matters\n\nMocking means replacing a real dependency (a function, module, API call, database, timer) with a fake, controllable stand-in for the duration of a test. The goal is **isolation**: a unit test should fail only when the unit under test is actually broken, not because a network call timed out or an external service is down.\n\nMocking gives you two things a real dependency can't:\n1. **Control** — you decide exactly what the fake returns (a success, an error, an empty list), so you can test edge cases that are hard or impossible to trigger for real.\n2. **Observability** — a mock records how it was called (with what arguments, how many times), which lets you assert on *interactions*, not just on return values.\n\n```typescript\n// Without mocking: this test depends on a real network call\n// slow, flaky, and fails if the API is down for unrelated reasons\n\n// With mocking: isolate the function under test\nconst fetchUser = jest.fn();\nfetchUser.mockResolvedValue({ id: 1, name: 'John' });\n\ntest('greetUser calls fetchUser with the correct id and builds a greeting', async () => {\n  const greeting = await greetUser(1, fetchUser);\n  expect(fetchUser).toHaveBeenCalledWith(1);\n  expect(greeting).toBe('Hello, John!');\n});\n```\n\n**Common pitfall:** Mocking the function you're actually testing (or mocking too deep — every internal helper) instead of only its external dependencies. If you mock the thing under test, the test can pass even when that code is completely broken, because it's the fake, not the real implementation, that's running.",
      "ru": "### Что такое мокирование и зачем оно нужно\n\nМокирование — это замена реальной зависимости (функции, модуля, API-запроса, базы данных, таймера) на фейковую, полностью контролируемую заглушку на время теста. Цель — **изоляция**: unit-тест должен падать только тогда, когда реально сломан тестируемый юнит, а не потому что сетевой запрос завис по таймауту или упал внешний сервис.\n\nМок даёт две вещи, которые реальная зависимость дать не может:\n1. **Контроль** — вы сами решаете, что именно вернёт заглушка (успех, ошибку, пустой список), и можете протестировать edge-кейсы, которые сложно или невозможно вызвать по-настоящему.\n2. **Наблюдаемость** — мок запоминает, как его вызывали (с какими аргументами, сколько раз), что позволяет проверять сами *взаимодействия*, а не только возвращаемые значения.\n\n```typescript\n// Без мока: тест зависит от реального сетевого запроса —\n// медленно, нестабильно, падает из-за проблем, не связанных с кодом\n\n// С моком: изолируем тестируемую функцию\nconst fetchUser = jest.fn();\nfetchUser.mockResolvedValue({ id: 1, name: 'John' });\n\ntest('greetUser вызывает fetchUser с правильным id и строит приветствие', async () => {\n  const greeting = await greetUser(1, fetchUser);\n  expect(fetchUser).toHaveBeenCalledWith(1);\n  expect(greeting).toBe('Hello, John!');\n});\n```\n\n**Частая ловушка:** Мокировать саму тестируемую функцию (или уходить в мокирование слишком глубоко — каждый внутренний хелпер) вместо того, чтобы мокировать только внешние зависимости. Если замокать сам код под тестом, тест может проходить, даже когда этот код полностью сломан — ведь выполняется заглушка, а не реальная реализация."
    },
    "tags": [
      "mocking-basics",
      "jest-mock",
      "test-isolation"
    ]
  },
  {
    "id": "testing-q-4",
    "topicId": "testing",
    "difficulty": "junior",
    "question": {
      "en": "How do you test a React component that fetches data using React Testing Library?",
      "ru": "Как вы тестируете React компонент, который загружает данные, используя React Testing Library?"
    },
    "answer": {
      "en": "### Testing a Data-Fetching Component with React Testing Library\n\nThe core idea of React Testing Library (RTL) is to test components the way a user experiences them — through rendered text and elements, not internal state or implementation details. For a component that fetches data, that means: render it, mock the network call so you control what it returns, then wait for the UI to reflect the loaded data.\n\nFetching is asynchronous, so a plain `getByText` (which checks synchronously) will fail before the data has arrived. RTL's `findBy*` queries solve this — they return a promise that retries until the element appears or a timeout is reached.\n\n```tsx\ntest('renders the user name after data loads', async () => {\n  const mockFetch = jest\n    .spyOn(global, 'fetch')\n    .mockResolvedValue({\n      ok: true,\n      json: async () => ({ name: 'John' }),\n    } as Response);\n\n  render(<UserProfile userId={1} />);\n\n  // shows a loading state first\n  expect(screen.getByText(/loading/i)).toBeInTheDocument();\n\n  // findBy* waits for the async update, then resolves\n  const name = await screen.findByText('John');\n  expect(name).toBeInTheDocument();\n\n  mockFetch.mockRestore();\n});\n```\n\n`waitFor` is the more general tool underneath — you can use it to wait for any arbitrary condition — but for \"wait for this element to show up\", `findBy*` is shorter and reads more clearly.\n\n**Common pitfall:** Using `getByText` for content that only appears after an async update, and \"fixing\" the resulting error by wrapping it in `waitFor(() => getByText(...))` — that's exactly what `findByText` already does in one call, so reaching for the longer form is unnecessary and easy to get wrong (e.g. forgetting `await`).",
      "ru": "### Тестирование компонента, который загружает данные, с React Testing Library\n\nКлючевая идея React Testing Library (RTL) — тестировать компоненты так, как их видит пользователь: через отрендеренный текст и элементы, а не через внутренний стейт или детали реализации. Для компонента, который делает запрос за данными, это означает: отрендерить его, замокать сетевой вызов, чтобы контролировать, что он вернёт, и дождаться, пока UI отразит загруженные данные.\n\nЗапрос асинхронный, поэтому обычный `getByText` (который проверяет синхронно) упадёт раньше, чем данные придут. Для этого в RTL есть запросы `findBy*` — они возвращают промис, который повторяет попытку, пока элемент не появится или не истечёт таймаут.\n\n```tsx\ntest('показывает имя пользователя после загрузки данных', async () => {\n  const mockFetch = jest\n    .spyOn(global, 'fetch')\n    .mockResolvedValue({\n      ok: true,\n      json: async () => ({ name: 'John' }),\n    } as Response);\n\n  render(<UserProfile userId={1} />);\n\n  // сначала показывается состояние загрузки\n  expect(screen.getByText(/loading/i)).toBeInTheDocument();\n\n  // findBy* ждёт асинхронное обновление и затем резолвится\n  const name = await screen.findByText('John');\n  expect(name).toBeInTheDocument();\n\n  mockFetch.mockRestore();\n});\n```\n\n`waitFor` — это более общий инструмент под капотом, им можно дождаться вообще любого произвольного условия, но для задачи \"подождать, пока появится этот элемент\" `findBy*` короче и читается понятнее.\n\n**Частая ловушка:** Использовать `getByText` для контента, который появляется только после асинхронного обновления, и \"чинить\" возникающую ошибку, оборачивая вызов в `waitFor(() => getByText(...))` — это ровно то, что уже делает `findByText` в один вызов, поэтому обращаться к более длинной форме без причины не нужно, и в ней проще ошибиться (например, забыть `await`)."
    },
    "tags": [
      "react-testing-library",
      "component-testing",
      "async-testing"
    ]
  },
  {
    "id": "testing-q-5",
    "topicId": "testing",
    "difficulty": "junior",
    "question": {
      "en": "What is the difference between jest.fn(), jest.spyOn(), and jest.mock()?",
      "ru": "В чем разница между jest.fn(), jest.spyOn() и jest.mock()?"
    },
    "answer": {
      "en": "### jest.fn() vs jest.spyOn() vs jest.mock()\n\nAll three create test doubles, but they operate at different levels and answer different questions:\n\n- **`jest.fn()`** creates a brand-new mock function out of nothing. There's no \"real\" implementation behind it unless you provide one. Use it when you just need a callback or a dependency you fully control — for example, a prop passed into a component.\n- **`jest.spyOn(object, 'method')`** wraps an *existing* method on a real object. By default it still calls through to the original implementation (so you can just observe calls), but you can chain `.mockImplementation()` or `.mockReturnValue()` to override behavior while keeping the ability to restore the original afterward with `.mockRestore()`.\n- **`jest.mock('./module')`** replaces an entire module at the module-resolution level, before your test file even runs. Every import of that module anywhere in the code under test gets the mocked version. This is the right tool when a function three layers deep imports something you can't reach by spying on an object reference.\n\n```typescript\n// jest.fn: create a mock from scratch\nconst onSave = jest.fn();\nbutton.onClick(onSave);\n\n// jest.spyOn: watch (and optionally override) a real method\nconst spy = jest.spyOn(Math, 'random').mockReturnValue(0.5);\n// ... test code that calls Math.random() ...\nspy.mockRestore(); // restores the real Math.random\n\n// jest.mock: replace an entire module for every importer\njest.mock('./api', () => ({\n  fetchUser: jest.fn().mockResolvedValue({ id: 1 }),\n}));\n```\n\n**Common pitfall:** Reaching for `jest.mock('./module')` to replace a whole module when a single `jest.spyOn` on the specific method actually used would do — module-level mocking is more powerful but also more invasive, since it silently changes the module for the entire test file.",
      "ru": "### jest.fn() vs jest.spyOn() vs jest.mock()\n\nВсе три создают тестовые дублёры, но работают на разных уровнях и решают разные задачи:\n\n- **`jest.fn()`** создаёт совершенно новую мок-функцию с нуля. За ней нет никакой \"реальной\" реализации, если вы её не задали сами. Используйте её, когда нужен просто колбэк или полностью контролируемая зависимость — например, проп, передаваемый в компонент.\n- **`jest.spyOn(object, 'method')`** оборачивает *уже существующий* метод реального объекта. По умолчанию он всё ещё вызывает оригинальную реализацию (то есть вы просто наблюдаете за вызовами), но можно добавить `.mockImplementation()` или `.mockReturnValue()`, чтобы переопределить поведение, сохранив при этом возможность вернуть оригинал через `.mockRestore()`.\n- **`jest.mock('./module')`** заменяет целый модуль на уровне разрешения модулей — ещё до того, как выполнится сам тестовый файл. Каждый импорт этого модуля в любом месте тестируемого кода получит замоканную версию. Это правильный инструмент, когда функция где-то на три уровня глубже импортирует что-то, до чего не дотянуться через спай на ссылку объекта.\n\n```typescript\n// jest.fn: создаём мок с нуля\nconst onSave = jest.fn();\nbutton.onClick(onSave);\n\n// jest.spyOn: наблюдаем (и опционально переопределяем) реальный метод\nconst spy = jest.spyOn(Math, 'random').mockReturnValue(0.5);\n// ... тестовый код, вызывающий Math.random() ...\nspy.mockRestore(); // возвращает настоящий Math.random\n\n// jest.mock: заменяем целый модуль для всех, кто его импортирует\njest.mock('./api', () => ({\n  fetchUser: jest.fn().mockResolvedValue({ id: 1 }),\n}));\n```\n\n**Частая ловушка:** Тянуться к `jest.mock('./module')`, чтобы заменить целый модуль, когда достаточно одного `jest.spyOn` на конкретный используемый метод — мокирование на уровне модуля мощнее, но и куда более инвазивно, поскольку молча подменяет модуль для всего тестового файла."
    },
    "tags": [
      "jest-mocking",
      "jest-fn",
      "jest-spyon",
      "module-mocking"
    ]
  },
  {
    "id": "testing-q-6",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "What is the difference between a mock, stub, and spy in testing? When would you use each one?",
      "ru": "В чём разница между mock, stub и spy в тестировании? Когда использовать каждый?"
    },
    "answer": {
      "en": "### Mock vs Stub vs Spy\n\nThese are all \"test doubles\" — fake stand-ins for real dependencies — but they differ in what problem they solve, and mixing them up is a common source of confusing test code:\n\n- **Stub**: a canned, predetermined response. It doesn't care how it was called; it just returns whatever you told it to return. Use a stub when you need a dependency to *behave* a certain way so the code under test can run (e.g. \"the API returns this user object\").\n- **Spy**: wraps a *real* function and records how it was called, while (by default) still letting the real implementation run. Use a spy when you want to assert on interactions (\"was `analytics.track` called?\") without changing the actual behavior.\n- **Mock**: a double that both provides canned behavior *and* has built-in expectations about how it should be called — some mocking libraries will fail the test automatically if the expected calls don't happen. In everyday Jest usage, \"mock\" is often used loosely to mean any of the three, but the distinction matters when reasoning about what a test is actually verifying: return value, or interaction, or both.\n\nThe practical trade-off: stubs make tests simpler and more resilient to refactoring (they don't care about *how* something is called), while spies/mocks that assert on call arguments make tests more precise but more brittle — they can break when you refactor internals even if the *behavior* didn't change.\n\n```typescript\n// Stub: just returns a canned value, no assertions on how it's called\nconst fetchUser = jest.fn().mockResolvedValue({ id: 1, name: 'John' });\n\n// Spy: wraps the real implementation, only observes\nconst spy = jest.spyOn(logger, 'warn');\nrunRiskyOperation();\nexpect(spy).toHaveBeenCalledWith('operation degraded');\n\n// Mock-with-expectations: fails if the interaction contract isn't met\nconst onSave = jest.fn();\nform.submit(onSave);\nexpect(onSave).toHaveBeenCalledTimes(1);\nexpect(onSave).toHaveBeenCalledWith(expect.objectContaining({ valid: true }));\n```\n\n**Common pitfall:** Over-asserting on *how* a function was called (exact call order, exact argument shape) when the test only cares *whether* the right outcome happened — this couples tests tightly to implementation details and makes routine refactors fail tests that describe correct behavior.",
      "ru": "### Mock vs Stub vs Spy\n\nВсё это — \"тестовые дублёры\" (test doubles), фейковые замены реальных зависимостей, но они решают разные задачи, и путаница между ними — частая причина непонятных тестов:\n\n- **Stub** — заранее заданный, \"консервированный\" ответ. Ему всё равно, как его вызвали — он просто возвращает то, что вы ему сказали. Используйте stub, когда нужно, чтобы зависимость *вела себя* определённым образом, чтобы тестируемый код мог выполниться (например, \"API возвращает вот такой объект пользователя\").\n- **Spy** — оборачивает *реальную* функцию и запоминает, как её вызывали, при этом (по умолчанию) реальная реализация всё ещё выполняется. Используйте spy, когда хотите проверить взаимодействие (\"был ли вызван `analytics.track`?\"), не меняя реальное поведение.\n- **Mock** — дублёр, который одновременно и задаёт поведение, и содержит встроенные ожидания насчёт того, как его должны вызвать — некоторые mocking-библиотеки автоматически проваливают тест, если ожидаемых вызовов не было. В повседневном Jest-коде слово \"mock\" часто используют вольно, для обозначения любого из трёх, но разница важна, когда нужно понять, что именно тест проверяет: возвращаемое значение, взаимодействие, или и то, и другое.\n\nПрактический trade-off: стабы делают тесты проще и устойчивее к рефакторингу (им всё равно, *как* что-то вызывается), а spy/mock с проверкой аргументов вызова делают тесты точнее, но более хрупкими — они могут ломаться при рефакторинге внутренностей, даже если само *поведение* не изменилось.\n\n```typescript\n// Stub: просто возвращает заданное значение, без проверок вызова\nconst fetchUser = jest.fn().mockResolvedValue({ id: 1, name: 'John' });\n\n// Spy: оборачивает реальную реализацию, только наблюдает\nconst spy = jest.spyOn(logger, 'warn');\nrunRiskyOperation();\nexpect(spy).toHaveBeenCalledWith('operation degraded');\n\n// Mock с ожиданиями: тест падает, если контракт взаимодействия не соблюдён\nconst onSave = jest.fn();\nform.submit(onSave);\nexpect(onSave).toHaveBeenCalledTimes(1);\nexpect(onSave).toHaveBeenCalledWith(expect.objectContaining({ valid: true }));\n```\n\n**Частая ловушка:** Излишне проверять *как именно* была вызвана функция (точный порядок вызовов, точная форма аргументов), когда тесту на самом деле важно только *был ли достигнут правильный результат* — это жёстко привязывает тесты к деталям реализации, и обычный рефакторинг начинает ломать тесты, описывающие корректное поведение."
    },
    "tags": [
      "test-doubles",
      "mocking-libraries",
      "isolation"
    ]
  },
  {
    "id": "testing-q-7",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you properly test asynchronous code? Explain the difference between async/await assertions and promise-based assertions.",
      "ru": "Как правильно тестировать асинхронный код? Объясните разницу между async/await и promise-based assertions."
    },
    "answer": {
      "en": "### Testing Asynchronous Code: async/await vs Promise Chains\n\nJest needs to know when an async operation has finished before it can safely run your assertions — if the test function returns before the async work resolves, Jest considers the test done, and any assertions inside a still-pending callback simply never execute (and won't fail the test even if they'd fail).\n\nThere are two correct patterns:\n\n- **`async/await`**: mark the test function `async` and `await` every asynchronous call before asserting. This is the clearer, more readable option and is the default choice for new code.\n- **Returning a promise chain**: `return somePromise.then(...)`. This works because Jest awaits whatever the test function returns. It's mostly seen in older codebases or libraries that don't support `async/await` syntax well.\n\nThe trade-off isn't really style — it's correctness. Both are equally correct *if done right*, but async/await makes the \"did I forget to wait?\" mistake much harder to make, because a missing `await` next to another `await` in the same function reads oddly, whereas a missing `return` in front of a `.then()` chain is easy to miss at a glance.\n\n```typescript\n// async/await — the recommended default\nit('fetches user data', async () => {\n  const data = await fetchUser();\n  expect(data.name).toBe('John');\n});\n\n// Promise chain — must return, or the test finishes before the assertion runs\nit('fetches user data (promise style)', () => {\n  return fetchUser().then((data) => {\n    expect(data.name).toBe('John');\n  });\n});\n\n// Testing rejections explicitly\nit('rejects for an invalid id', async () => {\n  await expect(fetchUser(-1)).rejects.toThrow('Invalid id');\n});\n```\n\n**Common pitfall:** Writing `fetchUser().then(data => expect(data.name).toBe('John'))` without `return` (or `await`) in front of it. The test function returns immediately, Jest marks it as passed, and the assertion inside the `.then()` callback runs later — possibly after the test has already reported success, so a failing assertion there is silently swallowed.",
      "ru": "### Тестирование асинхронного кода: async/await vs цепочки промисов\n\nJest должен понимать, когда асинхронная операция завершилась, прежде чем можно безопасно выполнять ассерты — если функция теста возвращает управление раньше, чем асинхронная работа резолвится, Jest считает тест завершённым, и любые ассерты внутри всё ещё \"висящего\" колбэка просто никогда не выполнятся (и не провалят тест, даже если бы они провалились).\n\nЕсть два корректных паттерна:\n\n- **`async/await`**: пометить функцию теста как `async` и делать `await` перед каждым асинхронным вызовом до проверки. Это более понятный и читаемый вариант, выбор по умолчанию для нового кода.\n- **Возврат цепочки промисов**: `return somePromise.then(...)`. Это работает, потому что Jest дожидается того, что вернула функция теста. Чаще встречается в старом коде или в библиотеках, плохо поддерживающих синтаксис `async/await`.\n\nРазница здесь не столько в стиле, сколько в корректности. Оба варианта одинаково правильны *при верном использовании*, но с async/await гораздо сложнее случайно забыть подождать — пропущенный `await` рядом с другим `await` в той же функции выглядит странно, а вот пропущенный `return` перед `.then()` легко не заметить.\n\n```typescript\n// async/await — рекомендуемый вариант по умолчанию\nit('получает данные пользователя', async () => {\n  const data = await fetchUser();\n  expect(data.name).toBe('John');\n});\n\n// Цепочка промисов — обязательно нужен return, иначе тест завершится раньше ассерта\nit('получает данные пользователя (promise-стиль)', () => {\n  return fetchUser().then((data) => {\n    expect(data.name).toBe('John');\n  });\n});\n\n// Явная проверка отклонения промиса\nit('отклоняется для невалидного id', async () => {\n  await expect(fetchUser(-1)).rejects.toThrow('Invalid id');\n});\n```\n\n**Частая ловушка:** Писать `fetchUser().then(data => expect(data.name).toBe('John'))` без `return` (или `await`) перед этим. Функция теста завершается сразу, Jest помечает тест как пройденный, а ассерт внутри `.then()` выполняется позже — возможно, уже после того, как тест отчитался об успехе, поэтому упавший там ассерт просто молча теряется."
    },
    "tags": [
      "async-testing",
      "promises",
      "test-execution"
    ]
  },
  {
    "id": "testing-q-8",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "What is renderHook and how do you test custom React hooks with it? Provide an example with act().",
      "ru": "Что такое renderHook и как тестировать custom React hooks? Приведите пример с act()."
    },
    "answer": {
      "en": "### Testing Custom Hooks with renderHook and act()\n\nCustom hooks contain stateful logic but aren't components, so you can't just `render()` them — `renderHook` solves this by mounting a tiny invisible test component internally and giving you back a `result` object whose `.current` always reflects the hook's latest return value.\n\n`act()` matters because React batches state updates and flushes effects asynchronously. If you call something that triggers a state update *outside* of `act()`, React can't guarantee the DOM/state has settled before your next assertion runs — hence the \"not wrapped in act()\" warning, which is React telling you your assertion might be reading a stale value.\n\n```typescript\nimport { renderHook, act } from '@testing-library/react';\n\nfunction useCounter(initial = 0) {\n  const [count, setCount] = useState(initial);\n  const increment = () => setCount((c) => c + 1);\n  return { count, increment };\n}\n\ntest('increments the counter', () => {\n  const { result } = renderHook(() => useCounter());\n  expect(result.current.count).toBe(0);\n\n  act(() => {\n    result.current.increment();\n  });\n\n  expect(result.current.count).toBe(1);\n});\n\n// Hooks that depend on context/providers need a wrapper\ntest('useTheme reads from ThemeProvider', () => {\n  const wrapper = ({ children }) => (\n    <ThemeProvider theme=\"dark\">{children}</ThemeProvider>\n  );\n  const { result } = renderHook(() => useTheme(), { wrapper });\n  expect(result.current.theme).toBe('dark');\n});\n```\n\nThe practical trade-off with `renderHook` versus testing a hook indirectly through a real component: `renderHook` is faster to write and pinpoints hook bugs precisely, but it can pass even if the hook is awkward to actually wire into a real component (missing required context, unexpected re-render behavior) — so for hooks with complex integration surface, a component-level test is still worth having alongside it.\n\n**Common pitfall:** Calling `result.current.someMethod()` directly without wrapping it in `act()` — the state update happens, but Jest/React may warn (or in stricter setups, fail) because React can't confirm all pending updates were flushed before the next assertion reads `result.current`.",
      "ru": "### Тестирование кастомных хуков с renderHook и act()\n\nКастомные хуки содержат stateful-логику, но не являются компонентами, поэтому просто `render()` их не получится — `renderHook` решает это, монтируя внутри себя крошечный невидимый тестовый компонент и возвращая объект `result`, чьё поле `.current` всегда отражает последнее возвращаемое значение хука.\n\n`act()` важен, потому что React батчит обновления состояния и асинхронно применяет эффекты. Если вызвать что-то, вызывающее обновление состояния, *вне* `act()`, React не может гарантировать, что состояние/DOM устаканились до выполнения следующего ассерта — отсюда предупреждение \"not wrapped in act()\", которым React сообщает, что ваш ассерт может читать устаревшее значение.\n\n```typescript\nimport { renderHook, act } from '@testing-library/react';\n\nfunction useCounter(initial = 0) {\n  const [count, setCount] = useState(initial);\n  const increment = () => setCount((c) => c + 1);\n  return { count, increment };\n}\n\ntest('увеличивает счётчик', () => {\n  const { result } = renderHook(() => useCounter());\n  expect(result.current.count).toBe(0);\n\n  act(() => {\n    result.current.increment();\n  });\n\n  expect(result.current.count).toBe(1);\n});\n\n// Хукам, зависящим от контекста/провайдеров, нужен wrapper\ntest('useTheme читает значение из ThemeProvider', () => {\n  const wrapper = ({ children }) => (\n    <ThemeProvider theme=\"dark\">{children}</ThemeProvider>\n  );\n  const { result } = renderHook(() => useTheme(), { wrapper });\n  expect(result.current.theme).toBe('dark');\n});\n```\n\nПрактический trade-off `renderHook` против тестирования хука косвенно, через реальный компонент: `renderHook` быстрее писать, и он точно указывает на баг именно в хуке, но такой тест может проходить, даже если хук неудобно реально подключить к компоненту (не хватает нужного контекста, неожиданное поведение при перерендере) — поэтому для хуков со сложной интеграционной поверхностью стоит держать рядом ещё и тест на уровне компонента.\n\n**Частая ловушка:** Вызывать `result.current.someMethod()` напрямую, не оборачивая в `act()` — обновление состояния произойдёт, но Jest/React выдаст предупреждение (а в более строгих настройках — упадёт), потому что React не может подтвердить, что все отложенные обновления применились до того, как следующий ассерт прочитает `result.current`."
    },
    "tags": [
      "react-testing-library",
      "hooks-testing",
      "act-wrapper"
    ]
  },
  {
    "id": "testing-q-9",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "What are the pros and cons of snapshot testing? When should you avoid it?",
      "ru": "Какие плюсы и минусы snapshot тестирования? Когда его избегать?"
    },
    "answer": {
      "en": "### Snapshot Testing: Pros, Cons, and When to Avoid It\n\nA snapshot test renders something (a component, an object, an API response shape) and saves the output to a file; future runs compare the new output against that saved file and fail on any difference. The appeal is speed: you get a regression test for \"did anything change\" without writing a single explicit assertion.\n\nThat speed is also the core weakness. A snapshot doesn't encode *intent* — it just encodes \"whatever the output happened to be when I first ran this.\" When a snapshot test fails, a rushed developer's instinct is often to run `--updateSnapshot` and move on without actually reading the diff, which quietly turns the safety net into a rubber stamp.\n\nSnapshots work best for:\n- Stable, low-churn output (a design-system component's rendered markup, a serialized config object)\n- Large structured output where writing individual assertions for every field would be tedious and add little value\n\nSnapshots are a poor fit for:\n- Anything containing non-deterministic values (timestamps, random IDs, `Math.random()`) — the diff will \"fail\" every run for reasons unrelated to a real bug, training people to blindly re-approve\n- Business-critical logic where you want the test to fail loudly and specifically when behavior changes, not just \"something in this 200-line blob is different\"\n\n```typescript\n// Poor fit: non-deterministic content pollutes the snapshot\ntest('renders user card', () => {\n  expect(render(<UserCard id={Math.random()} createdAt={new Date()} />))\n    .toMatchSnapshot(); // fails almost every run for the wrong reason\n});\n\n// Better: targeted assertion on the thing that actually matters\ntest('renders the user name', () => {\n  render(<UserCard name=\"John\" />);\n  expect(screen.getByText('John')).toBeInTheDocument();\n});\n\n// Reasonable use of snapshot: a stable, self-contained component\ntest('renders the primary button markup', () => {\n  const { container } = render(<Button variant=\"primary\">Save</Button>);\n  expect(container.firstChild).toMatchSnapshot();\n});\n```\n\n**Common pitfall:** Treating a large snapshot diff during code review as \"probably fine\" and approving it without reading it line by line — snapshots only add value if every diff is actually inspected, since an unread diff can just as easily be hiding a real regression as a harmless formatting change.",
      "ru": "### Snapshot-тестирование: плюсы, минусы и когда его избегать\n\nSnapshot-тест рендерит что-то (компонент, объект, форму ответа API) и сохраняет результат в файл; при следующих запусках новый результат сравнивается с сохранённым файлом, и при любом расхождении тест падает. Привлекательность — в скорости: вы получаете регрессионный тест \"изменилось ли вообще что-то\", не написав ни одного явного ассерта.\n\nЭта же скорость — и главная слабость. Snapshot не кодирует *намерение* — он просто фиксирует \"каким результат оказался в момент первого запуска\". Когда snapshot-тест падает, у спешащего разработчика часто возникает рефлекс запустить `--updateSnapshot` и пойти дальше, реально не прочитав диф — это незаметно превращает защитную сетку в формальную печать одобрения.\n\nSnapshot хорошо подходит для:\n- Стабильного, редко меняющегося вывода (отрендеренная разметка компонента из дизайн-системы, сериализованный конфиг)\n- Крупного структурированного вывода, где писать отдельные ассерты на каждое поле было бы утомительно и почти бесполезно\n\nSnapshot плохо подходит для:\n- Всего, что содержит недетерминированные значения (таймстемпы, случайные ID, `Math.random()`) — диф будет \"падать\" почти при каждом запуске по причинам, не связанным с реальным багом, приучая людей бездумно переодобрять\n- Бизнес-критичной логики, где важно, чтобы тест падал громко и конкретно при изменении поведения, а не просто \"что-то в этом 200-строчном блоке отличается\"\n\n```typescript\n// Плохой случай: недетерминированные данные засоряют snapshot\ntest('рендерит карточку пользователя', () => {\n  expect(render(<UserCard id={Math.random()} createdAt={new Date()} />))\n    .toMatchSnapshot(); // падает почти каждый раз по неверной причине\n});\n\n// Лучше: точечный ассерт на то, что реально важно\ntest('рендерит имя пользователя', () => {\n  render(<UserCard name=\"John\" />);\n  expect(screen.getByText('John')).toBeInTheDocument();\n});\n\n// Разумное использование snapshot: стабильный самодостаточный компонент\ntest('рендерит разметку основной кнопки', () => {\n  const { container } = render(<Button variant=\"primary\">Save</Button>);\n  expect(container.firstChild).toMatchSnapshot();\n});\n```\n\n**Частая ловушка:** Считать большой snapshot-диф на код-ревью \"наверное нормальным\" и одобрять его, не читая построчно — snapshot приносит пользу только тогда, когда каждый диф реально изучается, потому что непрочитанный диф с равной вероятностью может скрывать как безобидное изменение форматирования, так и настоящую регрессию."
    },
    "tags": [
      "snapshot-testing",
      "test-strategy",
      "jest"
    ]
  },
  {
    "id": "testing-q-10",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you interpret code coverage metrics? What does 80% coverage mean and what are its limitations?",
      "ru": "Как интерпретировать метрики code coverage? Что означает 80% покрытие и в чём его ограничения?"
    },
    "answer": {
      "en": "### What Code Coverage Metrics Actually Tell You\n\nCoverage tools instrument your code and report which lines, branches, functions, or statements were *executed* while the test suite ran. That's the entire claim — coverage measures **execution**, not **verification**. A line can run a hundred times across your test suite and still never be checked against an expected outcome.\n\nThis is why \"80% coverage\" is a weak signal on its own: it says nothing about *which* 20% is untested (a rarely-hit edge case, or your most critical error-handling branch?), and it says nothing about whether the covered 80% has assertions strong enough to actually catch a regression.\n\n```typescript\nfunction divide(a: number, b: number) {\n  return a / b;\n}\n\n// This test gives divide() 100% line coverage...\nit('divides two numbers', () => {\n  divide(10, 2); // executed, but never asserted on!\n});\n\n// ...but says nothing about correctness, and the real edge case is missed\nit('divides two numbers correctly', () => {\n  expect(divide(10, 2)).toBe(5);\n});\nit('handles division by zero', () => {\n  expect(divide(10, 0)).toBe(Infinity); // or throws, depending on your contract\n});\n```\n\nThe practical takeaway for a team: use coverage as a *diagnostic* to find completely untested areas of the codebase (especially error-handling branches, which are easy to forget), not as a target to hit for its own sake. Chasing a coverage percentage as a KPI tends to produce exactly the first example above — tests that execute code without meaningfully checking it, since that's the fastest way to make the number go up.\n\n**Common pitfall:** Treating 100% coverage as equivalent to \"bug-free\" or \"well-tested\" — a codebase with 100% coverage and weak assertions is strictly worse than one with 70% coverage and strong, meaningful assertions on its critical paths, because the first one gives false confidence.",
      "ru": "### Что на самом деле показывают метрики покрытия кода\n\nИнструменты для замера покрытия инструментируют код и показывают, какие строки, ветки, функции или инструкции были *выполнены* во время прогона тестов. Это вся суть метрики — покрытие измеряет **выполнение**, а не **проверку**. Строка может выполниться сто раз за весь набор тестов и при этом ни разу не быть сверена с ожидаемым результатом.\n\nИменно поэтому \"80% покрытия\" сам по себе слабый сигнал: он ничего не говорит о том, *какие именно* 20% не покрыты (редкий edge-кейс или самая критичная ветка обработки ошибок?), и ничего не говорит о том, достаточно ли строгие ассерты у покрытых 80%, чтобы реально поймать регрессию.\n\n```typescript\nfunction divide(a: number, b: number) {\n  return a / b;\n}\n\n// Этот тест даёт divide() 100% покрытие по строкам...\nit('делит два числа', () => {\n  divide(10, 2); // выполнено, но ничего не проверено!\n});\n\n// ...но ничего не говорит о корректности, и реальный edge-кейс упущен\nit('корректно делит два числа', () => {\n  expect(divide(10, 2)).toBe(5);\n});\nit('обрабатывает деление на ноль', () => {\n  expect(divide(10, 0)).toBe(Infinity); // или бросает исключение — зависит от контракта\n});\n```\n\nПрактический вывод для команды: используйте покрытие как *диагностический* инструмент для поиска совсем непротестированных участков кода (особенно веток обработки ошибок, про которые легко забыть), а не как самоцель. Погоня за процентом покрытия как за KPI обычно приводит ровно к первому примеру выше — тестам, которые выполняют код, но по сути ничего не проверяют, потому что это самый быстрый способ поднять цифру.\n\n**Частая ловушка:** Считать 100% покрытия эквивалентом \"без багов\" или \"хорошо протестировано\" — кодовая база со 100% покрытием и слабыми ассертами строго хуже, чем база с 70% покрытия и строгими, содержательными проверками на критичных участках, потому что первая создаёт ложную уверенность."
    },
    "tags": [
      "code-coverage",
      "metrics",
      "test-quality"
    ]
  },
  {
    "id": "testing-q-11",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you integration-test a Node.js/NestJS API endpoint with supertest? What should such a test actually cover?",
      "ru": "Как протестировать API-эндпоинт Node.js/NestJS с помощью supertest? Что реально должен покрывать такой тест?"
    },
    "answer": {
      "en": "### Integration-Testing an API Endpoint with supertest\n\n`supertest` lets you send real HTTP requests against your app (either a running server or an in-memory instance) and assert on the actual response — status code, headers, body — instead of calling a controller function directly. This is a step up from a unit test: it exercises the routing, middleware (auth guards, validation pipes, error filters), and serialization together, which is exactly where a lot of real bugs live and where pure unit tests of a controller method in isolation won't catch anything.\n\nThe trade-off versus a pure unit test of the controller/service: supertest tests are slower and need more setup (usually a test database or an in-memory one, plus app bootstrapping), but they catch a whole class of bugs — wrong HTTP status on an error path, a validation pipe rejecting a valid payload, a guard blocking a request it shouldn't — that controller-only unit tests structurally can't see.\n\n```typescript\nimport * as request from 'supertest';\nimport { Test } from '@nestjs/testing';\nimport { AppModule } from '../src/app.module';\n\nlet app;\n\nbeforeAll(async () => {\n  const moduleRef = await Test.createTestingModule({\n    imports: [AppModule],\n  }).compile();\n  app = moduleRef.createNestApplication();\n  await app.init();\n});\n\nafterAll(async () => {\n  await app.close();\n});\n\nit('POST /users creates a user and returns 201', async () => {\n  const res = await request(app.getHttpServer())\n    .post('/users')\n    .send({ name: 'John', email: 'john@example.com' });\n\n  expect(res.status).toBe(201);\n  expect(res.body).toMatchObject({ name: 'John' });\n});\n\nit('POST /users returns 400 for an invalid payload', async () => {\n  const res = await request(app.getHttpServer())\n    .post('/users')\n    .send({ name: '' }); // missing required email, empty name\n\n  expect(res.status).toBe(400);\n});\n```\n\nA well-designed test here should cover: the happy path, at least one validation-failure path, and any auth/permission boundary the endpoint has — not just \"the endpoint returns 200 for good input\", since that's the one path least likely to have a real bug.\n\n**Common pitfall:** Pointing these tests at a shared development or staging database instead of an isolated test database — tests then either mutate real data or become flaky depending on what other processes did to that data, and a test suite that \"sometimes fails for no reason\" quickly loses the team's trust.",
      "ru": "### Интеграционное тестирование API-эндпоинта с supertest\n\n`supertest` позволяет отправлять реальные HTTP-запросы к приложению (либо к запущенному серверу, либо к in-memory инстансу) и проверять реальный ответ — статус-код, заголовки, тело — вместо прямого вызова функции контроллера. Это шаг вперёд по сравнению с unit-тестом: тест задействует роутинг, middleware (guard'ы авторизации, пайпы валидации, фильтры ошибок) вместе, а это как раз то место, где часто живут реальные баги и куда чистый unit-тест метода контроллера в изоляции просто не дотягивается.\n\nTrade-off по сравнению с чистым unit-тестом контроллера/сервиса: тесты с supertest медленнее и требуют больше настройки (обычно тестовая БД или in-memory вариант, плюс бутстрап приложения), но они ловят целый класс багов — неправильный HTTP-статус на пути ошибки, валидационный пайп, отклоняющий валидный payload, guard, блокирующий запрос, который не должен блокировать — которые unit-тесты одного только контроллера структурно не видят.\n\n```typescript\nimport * as request from 'supertest';\nimport { Test } from '@nestjs/testing';\nimport { AppModule } from '../src/app.module';\n\nlet app;\n\nbeforeAll(async () => {\n  const moduleRef = await Test.createTestingModule({\n    imports: [AppModule],\n  }).compile();\n  app = moduleRef.createNestApplication();\n  await app.init();\n});\n\nafterAll(async () => {\n  await app.close();\n});\n\nit('POST /users создаёт пользователя и возвращает 201', async () => {\n  const res = await request(app.getHttpServer())\n    .post('/users')\n    .send({ name: 'John', email: 'john@example.com' });\n\n  expect(res.status).toBe(201);\n  expect(res.body).toMatchObject({ name: 'John' });\n});\n\nit('POST /users возвращает 400 для невалидного payload', async () => {\n  const res = await request(app.getHttpServer())\n    .post('/users')\n    .send({ name: '' }); // отсутствует обязательный email, пустое имя\n\n  expect(res.status).toBe(400);\n});\n```\n\nХорошо продуманный тест здесь должен покрывать: happy path, хотя бы один путь с ошибкой валидации, и любую границу авторизации/прав, которая есть у эндпоинта — а не только \"эндпоинт возвращает 200 на хороший инпут\", потому что это как раз тот путь, где реальный баг наименее вероятен.\n\n**Частая ловушка:** Направлять такие тесты на общую dev- или staging-базу вместо изолированной тестовой БД — тогда тесты либо мутируют реальные данные, либо становятся нестабильными в зависимости от того, что с этими данными сделали другие процессы, а тестовый набор, который \"иногда падает без причины\", быстро теряет доверие команды."
    },
    "tags": [
      "supertest",
      "nestjs-testing",
      "http-integration-testing",
      "api-endpoint-testing"
    ]
  },
  {
    "id": "testing-q-12",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you test time-dependent code (setTimeout, setInterval, Date) in Jest using fake timers?",
      "ru": "Как протестировать код, зависящий от времени (setTimeout, setInterval, Date), в Jest с помощью fake timers?"
    },
    "answer": {
      "en": "### Testing Time-Dependent Code with Fake Timers\n\nCode that relies on `setTimeout`, `setInterval`, or the current `Date` is awkward to test with real timers: a test that actually waits out a 30-second timeout is slow, and one that races against a real clock is flaky. Jest's fake timers solve this by replacing the global timer functions with a controllable, synchronous implementation — instead of waiting for time to pass, you tell the test to \"advance time\" instantly.\n\n```typescript\nbeforeEach(() => {\n  jest.useFakeTimers();\n});\n\nafterEach(() => {\n  jest.useRealTimers();\n});\n\nit('calls the callback after the delay', () => {\n  const callback = jest.fn();\n  setTimeout(callback, 1000);\n\n  expect(callback).not.toHaveBeenCalled();\n\n  jest.advanceTimersByTime(1000); // instantly \"fast-forwards\" 1 second\n\n  expect(callback).toHaveBeenCalledTimes(1);\n});\n\nit('debounces repeated calls', () => {\n  const onChange = jest.fn();\n  const debounced = debounce(onChange, 300);\n\n  debounced();\n  debounced();\n  debounced();\n\n  jest.advanceTimersByTime(300);\n  expect(onChange).toHaveBeenCalledTimes(1); // only the last call fires\n});\n```\n\nFor code that depends on the current date (`Date.now()`, `new Date()`), the same fake-timer system can pin the clock to a fixed point so a test isn't dependent on which day it happens to run:\n\n```typescript\nit('marks a subscription as expired after its end date', () => {\n  jest.setSystemTime(new Date('2026-01-15'));\n  const sub = { expiresAt: new Date('2026-01-10') };\n  expect(isExpired(sub)).toBe(true);\n});\n```\n\nThe main trade-off: fake timers make time-based logic fast and deterministic to test, but they can mask real timing bugs that only show up with actual async scheduling (e.g. how your code interacts with real microtask/macrotask ordering), so critical timing-sensitive integrations may still deserve a slower, real-timer test alongside the fast fake-timer ones.\n\n**Common pitfall:** Forgetting to call `jest.useRealTimers()` after a test that used fake timers — the fake timers then leak into subsequent tests in the same file, causing unrelated `setTimeout`/`setInterval` calls elsewhere to silently never fire, which produces confusing, hard-to-trace failures far from the actual cause.",
      "ru": "### Тестирование кода, зависящего от времени, с fake timers\n\nКод, зависящий от `setTimeout`, `setInterval` или текущей `Date`, неудобно тестировать с реальными таймерами: тест, который реально ждёт 30-секундный таймаут, — медленный, а тест, который \"гонится\" за реальными часами, — нестабильный. Fake timers в Jest решают это, подменяя глобальные функции таймеров на контролируемую, синхронную реализацию — вместо ожидания реального времени вы говорите тесту \"промотать время вперёд\" мгновенно.\n\n```typescript\nbeforeEach(() => {\n  jest.useFakeTimers();\n});\n\nafterEach(() => {\n  jest.useRealTimers();\n});\n\nit('вызывает колбэк после задержки', () => {\n  const callback = jest.fn();\n  setTimeout(callback, 1000);\n\n  expect(callback).not.toHaveBeenCalled();\n\n  jest.advanceTimersByTime(1000); // мгновенно \"перематывает\" 1 секунду\n\n  expect(callback).toHaveBeenCalledTimes(1);\n});\n\nit('дебаунсит повторные вызовы', () => {\n  const onChange = jest.fn();\n  const debounced = debounce(onChange, 300);\n\n  debounced();\n  debounced();\n  debounced();\n\n  jest.advanceTimersByTime(300);\n  expect(onChange).toHaveBeenCalledTimes(1); // срабатывает только последний вызов\n});\n```\n\nДля кода, зависящего от текущей даты (`Date.now()`, `new Date()`), та же система fake timers позволяет зафиксировать часы на конкретной точке, чтобы тест не зависел от того, в какой день он реально запускается:\n\n```typescript\nit('помечает подписку как истёкшую после даты окончания', () => {\n  jest.setSystemTime(new Date('2026-01-15'));\n  const sub = { expiresAt: new Date('2026-01-10') };\n  expect(isExpired(sub)).toBe(true);\n});\n```\n\nОсновной trade-off: fake timers делают тестирование логики на времени быстрым и детерминированным, но могут маскировать реальные баги с таймингом, которые проявляются только при настоящем асинхронном планировании (например, как ваш код взаимодействует с реальным порядком микро-/макротасков), поэтому критичные к таймингу интеграции всё же могут заслуживать отдельного, более медленного теста с реальными таймерами.\n\n**Частая ловушка:** Забыть вызвать `jest.useRealTimers()` после теста, использовавшего fake timers — фейковые таймеры \"утекают\" в последующие тесты того же файла, из-за чего не связанные с этим вызовы `setTimeout`/`setInterval` в других местах молча перестают срабатывать, что даёт запутанные, трудноотслеживаемые падения далеко от настоящей причины."
    },
    "tags": [
      "fake-timers",
      "jest-timers",
      "time-dependent-code",
      "settimeout-testing"
    ]
  },
  {
    "id": "testing-q-13",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you unit-test state management logic (Redux/Zustand reducers, actions, selectors) in isolation from React components?",
      "ru": "Как протестировать логику стейт-менеджмента (Redux/Zustand редьюсеры, экшены, селекторы) в изоляции от React-компонентов?"
    },
    "answer": {
      "en": "### Testing State Management Logic in Isolation\n\nState-management logic — reducers, action creators, selectors, or a Zustand store's update functions — is usually plain functions operating on plain data, with no rendering involved. That's valuable: you can test the actual business logic of \"how does state change in response to this action\" without mounting a single component, which makes these tests fast and lets them pinpoint exactly whether a bug lives in the state logic or in how a component consumes it.\n\nThe core pattern is: call the reducer/action/store method directly with a known input state, and assert on the resulting state — no `render()`, no DOM.\n\n```typescript\n// Redux-style reducer — pure function, trivial to test\nfunction cartReducer(state = { items: [] }, action) {\n  switch (action.type) {\n    case 'ADD_ITEM':\n      return { ...state, items: [...state.items, action.payload] };\n    case 'REMOVE_ITEM':\n      return { ...state, items: state.items.filter((i) => i.id !== action.payload) };\n    default:\n      return state;\n  }\n}\n\nit('adds an item to the cart', () => {\n  const state = cartReducer(undefined, { type: 'ADD_ITEM', payload: { id: 1 } });\n  expect(state.items).toHaveLength(1);\n});\n\nit('removes an item by id', () => {\n  const initial = { items: [{ id: 1 }, { id: 2 }] };\n  const state = cartReducer(initial, { type: 'REMOVE_ITEM', payload: 1 });\n  expect(state.items).toEqual([{ id: 2 }]);\n});\n\n// Zustand store — test the store's actions directly, no provider needed\nconst useCartStore = create((set) => ({\n  items: [],\n  addItem: (item) => set((s) => ({ items: [...s.items, item] })),\n}));\n\nit('Zustand: addItem updates the store', () => {\n  useCartStore.getState().addItem({ id: 1 });\n  expect(useCartStore.getState().items).toHaveLength(1);\n});\n```\n\nA practical trade-off to be aware of: testing the store in isolation like this proves the state logic is correct, but it doesn't prove a component actually *reads* the right slice of state or *dispatches* the right action at the right time — that still needs a component-level test (with `renderHook` or full component rendering) for the parts where the wiring itself is the risk.\n\n**Common pitfall:** Testing a global store (Redux store, Zustand store) without resetting it between tests — since these stores are often module-level singletons, state mutated in one test can silently leak into the next test in the same file, making tests pass or fail depending on execution order rather than on their own logic.",
      "ru": "### Тестирование логики стейт-менеджмента в изоляции\n\nЛогика стейт-менеджмента — редьюсеры, экшен-криэйторы, селекторы, или функции обновления Zustand-стора — это обычно чистые функции, работающие с обычными данными, без какого-либо рендеринга. Это ценно: можно протестировать реальную бизнес-логику \"как меняется стейт в ответ на этот экшен\", не монтируя ни одного компонента, что делает такие тесты быстрыми и позволяет точно локализовать, живёт ли баг в логике стейта или в том, как компонент её потребляет.\n\nБазовый паттерн: вызвать редьюсер/экшен/метод стора напрямую с известным входным состоянием и проверить результирующее состояние — никакого `render()`, никакого DOM.\n\n```typescript\n// Redux-style редьюсер — чистая функция, тривиально тестируется\nfunction cartReducer(state = { items: [] }, action) {\n  switch (action.type) {\n    case 'ADD_ITEM':\n      return { ...state, items: [...state.items, action.payload] };\n    case 'REMOVE_ITEM':\n      return { ...state, items: state.items.filter((i) => i.id !== action.payload) };\n    default:\n      return state;\n  }\n}\n\nit('добавляет товар в корзину', () => {\n  const state = cartReducer(undefined, { type: 'ADD_ITEM', payload: { id: 1 } });\n  expect(state.items).toHaveLength(1);\n});\n\nit('удаляет товар по id', () => {\n  const initial = { items: [{ id: 1 }, { id: 2 }] };\n  const state = cartReducer(initial, { type: 'REMOVE_ITEM', payload: 1 });\n  expect(state.items).toEqual([{ id: 2 }]);\n});\n\n// Zustand store — тестируем экшены стора напрямую, провайдер не нужен\nconst useCartStore = create((set) => ({\n  items: [],\n  addItem: (item) => set((s) => ({ items: [...s.items, item] })),\n}));\n\nit('Zustand: addItem обновляет стор', () => {\n  useCartStore.getState().addItem({ id: 1 });\n  expect(useCartStore.getState().items).toHaveLength(1);\n});\n```\n\nПрактический trade-off, о котором стоит помнить: такое изолированное тестирование стора доказывает, что логика стейта корректна, но не доказывает, что компонент реально *читает* нужный кусок стейта или *диспатчит* нужный экшен в нужный момент — для частей, где риск именно в этой связке, всё равно нужен тест на уровне компонента (с `renderHook` или полным рендерингом компонента).\n\n**Частая ловушка:** Тестировать глобальный стор (Redux store, Zustand store) без сброса между тестами — поскольку такие сторы часто являются синглтонами на уровне модуля, состояние, изменённое в одном тесте, может незаметно \"утечь\" в следующий тест того же файла, из-за чего тесты проходят или падают в зависимости от порядка выполнения, а не от собственной логики."
    },
    "tags": [
      "state-management-testing",
      "redux-testing",
      "zustand-testing",
      "reducer-testing"
    ]
  },
  {
    "id": "testing-q-14",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "What are beforeEach/afterEach/beforeAll/afterAll for, and how do they help avoid order-dependent tests caused by shared state?",
      "ru": "Для чего нужны beforeEach/afterEach/beforeAll/afterAll, и как они помогают избежать тестов, зависящих от порядка выполнения из-за общего состояния?"
    },
    "answer": {
      "en": "### Test Lifecycle Hooks and Avoiding Order-Dependent Tests\n\n`beforeEach`/`afterEach` run before/after *every* test in their scope; `beforeAll`/`afterAll` run once for the whole suite (or `describe` block). Their real purpose isn't just \"reduce duplication\" — it's to guarantee that **each test starts from a known, identical state**, regardless of what other tests ran before it or what order the test runner picked.\n\nThis matters because a common source of confusing, hard-to-reproduce bugs in a test suite is shared mutable state: a module-level array, a database row, a singleton store, or a mocked module that one test mutates and the next test unknowingly inherits. Such a suite might pass entirely when run in file order but fail when run in parallel, in a different order, or with `--randomize` — because test B was silently depending on state that test A happened to leave behind.\n\n```typescript\ndescribe('UserRepository', () => {\n  let db;\n\n  beforeAll(async () => {\n    db = await connectTestDatabase(); // expensive: do this once\n  });\n\n  afterAll(async () => {\n    await db.close();\n  });\n\n  beforeEach(async () => {\n    await db.users.deleteMany({}); // cheap: reset state before every test\n  });\n\n  it('creates a user', async () => {\n    await db.users.insert({ name: 'John' });\n    expect(await db.users.count()).toBe(1);\n  });\n\n  it('starts with an empty table', async () => {\n    // if beforeEach didn't run, this would fail depending on test order\n    expect(await db.users.count()).toBe(0);\n  });\n});\n```\n\nThe trade-off between `beforeAll` and `beforeEach`: `beforeAll` is cheaper (runs once) but anything it sets up is implicitly shared and mutable across tests unless you're careful, while `beforeEach` is slightly more expensive but gives every test a clean slate — as a rule, use `beforeAll` only for genuinely expensive, read-only setup (opening a connection), and `beforeEach` for anything a test might mutate.\n\n**Common pitfall:** Relying on `beforeAll` to seed data that individual tests then mutate (e.g. inserting one user in `beforeAll` and having several tests update or delete it) — the tests then implicitly depend on running in a specific order, and the suite becomes \"green in CI, red locally\" or vice versa the moment someone reorders or parallelizes them.",
      "ru": "### Хуки жизненного цикла тестов и как избежать зависимости от порядка выполнения\n\n`beforeEach`/`afterEach` выполняются до/после *каждого* теста в своей области видимости; `beforeAll`/`afterAll` — один раз для всего набора (или блока `describe`). Их реальное назначение — не просто \"уменьшить дублирование\", а гарантировать, что **каждый тест стартует с известного, одинакового состояния**, независимо от того, какие тесты выполнялись до него и в каком порядке их выбрал раннер.\n\nЭто важно, потому что частый источник запутанных, трудно воспроизводимых багов в тестовом наборе — общее изменяемое состояние: массив на уровне модуля, строка в БД, синглтон-стор, или замоканный модуль, который один тест мутирует, а следующий незаметно это наследует. Такой набор может полностью проходить при запуске в порядке файлов, но падать при параллельном запуске, в другом порядке, или с `--randomize` — потому что тест B молча зависел от состояния, оставленного тестом A.\n\n```typescript\ndescribe('UserRepository', () => {\n  let db;\n\n  beforeAll(async () => {\n    db = await connectTestDatabase(); // дорого: делаем один раз\n  });\n\n  afterAll(async () => {\n    await db.close();\n  });\n\n  beforeEach(async () => {\n    await db.users.deleteMany({}); // дёшево: сбрасываем состояние перед каждым тестом\n  });\n\n  it('создаёт пользователя', async () => {\n    await db.users.insert({ name: 'John' });\n    expect(await db.users.count()).toBe(1);\n  });\n\n  it('начинает с пустой таблицы', async () => {\n    // если бы beforeEach не отработал, этот тест зависел бы от порядка выполнения\n    expect(await db.users.count()).toBe(0);\n  });\n});\n```\n\nTrade-off между `beforeAll` и `beforeEach`: `beforeAll` дешевле (выполняется один раз), но всё, что в нём настроено, неявно расшарено и изменяемо между тестами, если не быть осторожным, а `beforeEach` чуть дороже, зато даёт каждому тесту чистый лист — как правило, `beforeAll` стоит использовать только для действительно дорогой, read-only настройки (открытие соединения), а `beforeEach` — для всего, что тест потенциально может изменить.\n\n**Частая ловушка:** Полагаться на `beforeAll` для подготовки данных, которые затем мутируют отдельные тесты (например, вставить одного пользователя в `beforeAll`, а несколько тестов дальше обновляют или удаляют его) — тесты тогда неявно зависят от выполнения в конкретном порядке, и набор становится \"зелёным в CI, красным локально\" или наоборот, как только кто-то поменяет порядок или запустит их параллельно."
    },
    "tags": [
      "test-lifecycle-hooks",
      "shared-mutable-state",
      "test-order-independence",
      "beforeeach-aftereach"
    ]
  },
  {
    "id": "testing-q-15",
    "topicId": "testing",
    "difficulty": "middle",
    "question": {
      "en": "How do you test that a function throws the right error or that a promise rejects correctly? Why does testing failure paths matter?",
      "ru": "Как протестировать, что функция бросает правильную ошибку, или что промис корректно отклоняется? Почему важно тестировать пути с ошибками?"
    },
    "answer": {
      "en": "### Testing Error Paths: Thrown Errors and Rejected Promises\n\nIt's easy to write tests only for the happy path, but a function's error-handling behavior is often the part most likely to have a real bug — and the part most likely to matter in production, since that's exactly when users hit something unexpected. Jest has dedicated matchers for both synchronous throws and async rejections, and using the right one avoids a class of tests that silently pass without checking anything.\n\nFor **synchronous** code that throws, you have to wrap the call in a function so Jest can invoke it and catch the throw itself — calling the function directly inside `expect()` would throw *before* `expect` ever gets a chance to wrap it.\n\n```typescript\nfunction parseAge(input: string): number {\n  const age = Number(input);\n  if (Number.isNaN(age) || age < 0) {\n    throw new Error('Invalid age');\n  }\n  return age;\n}\n\nit('throws for a negative age', () => {\n  expect(() => parseAge('-5')).toThrow('Invalid age');\n});\n\n// Wrong: this throws immediately, expect() never runs\n// expect(parseAge('-5')).toThrow(); // TypeError before the assertion\n```\n\nFor **asynchronous** code, use `.rejects` (with `await`) to unwrap a rejected promise the same way `await` normally unwraps a resolved one:\n\n```typescript\nasync function fetchUser(id: number) {\n  if (id <= 0) throw new Error('Invalid id');\n  return api.get(`/users/${id}`);\n}\n\nit('rejects for an invalid id', async () => {\n  await expect(fetchUser(-1)).rejects.toThrow('Invalid id');\n});\n```\n\nTesting these paths matters beyond just \"coverage\": error handling is exactly the code that runs during an incident — a wrong error message, a swallowed exception, or a promise that rejects with the wrong shape can turn a minor issue into a confusing outage, precisely because nobody noticed the error path was broken until it mattered.\n\n**Common pitfall:** Writing `expect(someFunction()).toThrow()` for a function that throws synchronously — this throws immediately when `someFunction()` is evaluated as an argument, before `expect` is even called, so the test fails with an uncaught error instead of a clear assertion failure. The function call must be wrapped in an arrow function: `expect(() => someFunction()).toThrow()`.",
      "ru": "### Тестирование путей с ошибками: брошенные исключения и отклонённые промисы\n\nЛегко писать тесты только для happy path, но поведение обработки ошибок в функции часто — как раз то место, где вероятнее всего живёт реальный баг, и то место, которое чаще всего критично в проде, потому что именно тогда пользователи натыкаются на что-то неожиданное. В Jest есть отдельные матчеры и для синхронных исключений, и для асинхронных отклонений, и использование правильного помогает избежать целого класса тестов, которые молча проходят, ничего не проверяя.\n\nДля **синхронного** кода, который бросает исключение, вызов нужно обернуть в функцию, чтобы Jest мог вызвать её сам и поймать исключение — вызов функции напрямую внутри `expect()` бросит исключение *раньше*, чем `expect` успеет его обернуть.\n\n```typescript\nfunction parseAge(input: string): number {\n  const age = Number(input);\n  if (Number.isNaN(age) || age < 0) {\n    throw new Error('Invalid age');\n  }\n  return age;\n}\n\nit('бросает исключение для отрицательного возраста', () => {\n  expect(() => parseAge('-5')).toThrow('Invalid age');\n});\n\n// Неправильно: это бросит исключение сразу, expect() даже не выполнится\n// expect(parseAge('-5')).toThrow(); // TypeError ещё до ассерта\n```\n\nДля **асинхронного** кода используйте `.rejects` (вместе с `await`), чтобы развернуть отклонённый промис так же, как `await` обычно разворачивает зарезолвленный:\n\n```typescript\nasync function fetchUser(id: number) {\n  if (id <= 0) throw new Error('Invalid id');\n  return api.get(`/users/${id}`);\n}\n\nit('отклоняется для невалидного id', async () => {\n  await expect(fetchUser(-1)).rejects.toThrow('Invalid id');\n});\n```\n\nТестирование этих путей важно не только \"ради покрытия\": обработка ошибок — это ровно тот код, который выполняется во время инцидента — неверное сообщение об ошибке, проглоченное исключение или промис, отклоняющийся не той формой, могут превратить мелкую проблему в запутанный инцидент именно потому, что никто не заметил, что путь с ошибкой был сломан, пока это не стало критично.\n\n**Частая ловушка:** Писать `expect(someFunction()).toThrow()` для функции, которая бросает исключение синхронно — это бросит исключение сразу же при вычислении `someFunction()` как аргумента, ещё до вызова `expect`, и тест упадёт с неперехваченной ошибкой вместо понятного провала ассерта. Вызов функции нужно оборачивать в стрелочную функцию: `expect(() => someFunction()).toThrow()`."
    },
    "tags": [
      "error-testing",
      "exception-testing",
      "promise-rejection",
      "tothrow-matcher"
    ]
  },
  {
    "id": "testing-q-16",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "Explain the difference between the testing pyramid and testing trophy. When would you choose one over the other in a modern application?",
      "ru": "Объясните разницу между пирамидой тестирования и трофеем тестирования. Когда вы выбрали бы один подход над другим в современном приложении?"
    },
    "answer": {
      "en": "### Testing Pyramid vs Testing Trophy\n\nThe **pyramid** (Mike Cohn) recommends a large base of fast, cheap unit tests, a smaller layer of integration tests, and a thin top of slow, expensive e2e tests — the shape reflects \"write more of what's cheap and fast, less of what's slow and brittle.\" It was formulated with backend, logic-heavy systems in mind, where most complexity genuinely lives in isolated business logic that unit tests can cover well.\n\nThe **trophy** (Kent C. Dodds) inverts the emphasis for typical frontend/full-stack apps: a small base of unit tests, a large middle layer of integration tests, a thin top of e2e, and — critically — a base of static analysis (TypeScript, linting) underneath everything. The reasoning: in a UI-heavy app, most real bugs live at the *seams* — how a component talks to a hook, how a form talks to an API, how routing interacts with auth — and unit tests that mock those seams away don't test the thing that actually breaks. Confidence-per-test-written is higher for integration tests here, even though each one costs more to write and run than a unit test.\n\n```typescript\n// Pyramid mindset: heavy unit-test investment, seams get mocked\ntest('calculateDiscount() applies the correct percentage', () => {\n  expect(calculateDiscount(100, 0.1)).toBe(90);\n});\n\n// Trophy mindset: test the actual integration point a user would hit\ntest('applying a discount code updates the displayed total', async () => {\n  render(<Cart items={[{ price: 100 }]} />);\n  await userEvent.type(screen.getByLabelText(/discount code/i), 'SAVE10');\n  await userEvent.click(screen.getByRole('button', { name: /apply/i }));\n  expect(await screen.findByText('$90.00')).toBeInTheDocument();\n});\n```\n\nNeither shape is universally \"correct\" — the real skill is recognizing which one matches where your system's actual risk lives. A library with complex pure algorithms benefits from the pyramid's unit-heavy base; a UI-heavy product, or a system built from several independently deployed services where the real risk is at integration boundaries, benefits from the trophy's integration-heavy middle. For microservices specifically, the trophy's logic extends naturally to favoring contract tests at the API boundary over unit-testing each service's internals in isolation, since the boundary is exactly where the actual coordination risk lives.\n\n**Common pitfall:** Treating either shape as a fixed rule to apply everywhere rather than a heuristic to reason from — a candidate who says \"always the pyramid\" or \"always the trophy\" without connecting it to *where the risk actually lives in this specific system* is reciting a diagram, not reasoning about testing strategy.",
      "ru": "### Testing Pyramid vs Testing Trophy\n\n**Пирамида** (Майк Кон) рекомендует большую базу из быстрых, дешёвых unit-тестов, слой поменьше из интеграционных тестов и тонкую верхушку из медленных, дорогих e2e-тестов — форма отражает идею \"пишите больше того, что дёшево и быстро, меньше того, что медленно и хрупко\". Она формулировалась с оглядкой на backend-системы с большим объёмом логики, где реальная сложность действительно живёт в изолированной бизнес-логике, которую unit-тесты хорошо покрывают.\n\n**Трофей** (Кент Си Доддс) переворачивает акцент для типичных фронтенд/фуллстек приложений: небольшая база unit-тестов, большой средний слой интеграционных тестов, тонкая верхушка e2e, и — что важно — база статического анализа (TypeScript, линтинг) под всем этим. Логика такая: в UI-тяжёлом приложении реальные баги чаще всего живут на *стыках* — как компонент общается с хуком, как форма общается с API, как роутинг взаимодействует с авторизацией — а unit-тесты, замокивающие эти стыки, не проверяют как раз то, что реально ломается. Уверенность на один написанный тест выше именно у интеграционных тестов, даже если каждый из них дороже писать и выполнять, чем unit-тест.\n\n```typescript\n// Мышление пирамиды: тяжёлая инвестиция в unit-тесты, стыки замокиваются\ntest('calculateDiscount() применяет правильный процент', () => {\n  expect(calculateDiscount(100, 0.1)).toBe(90);\n});\n\n// Мышление трофея: тестируем реальную точку интеграции, с которой столкнётся пользователь\ntest('применение промокода обновляет отображаемую сумму', async () => {\n  render(<Cart items={[{ price: 100 }]} />);\n  await userEvent.type(screen.getByLabelText(/discount code/i), 'SAVE10');\n  await userEvent.click(screen.getByRole('button', { name: /apply/i }));\n  expect(await screen.findByText('$90.00')).toBeInTheDocument();\n});\n```\n\nНи одна из форм не является универсально \"правильной\" — реальный навык здесь в том, чтобы понимать, какая из них соответствует тому, где в вашей системе реально живёт риск. Библиотека со сложными чистыми алгоритмами выигрывает от unit-тяжёлой базы пирамиды; UI-тяжёлый продукт, или система из нескольких независимо задеплоенных сервисов, где реальный риск — на границах интеграции, выигрывает от интеграционно-тяжёлой середины трофея. Конкретно для микросервисов логика трофея естественно продолжается в пользу contract-тестов на границе API вместо unit-тестирования внутренностей каждого сервиса в изоляции, потому что риск координации живёт именно на этой границе.\n\n**Частая ловушка:** Воспринимать любую из форм как фиксированное правило, применимое везде, а не как эвристику, от которой нужно рассуждать — кандидат, говорящий \"всегда пирамида\" или \"всегда трофей\" без привязки к тому, *где в этой конкретной системе реально живёт риск*, пересказывает картинку, а не рассуждает о стратегии тестирования."
    },
    "tags": [
      "testing-pyramid",
      "testing-trophy",
      "testing-strategy",
      "microservices-testing"
    ]
  },
  {
    "id": "testing-q-17",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "How would you implement contract testing between a frontend and backend service? What tools would you use and why?",
      "ru": "Как бы вы реализовали контрактное тестирование между фронтенд и бэкенд сервисом? Какие инструменты вы бы использовали и почему?"
    },
    "answer": {
      "en": "### Contract Testing: Implementation and Consumer-Driven vs Provider-Driven\n\nContract testing verifies that a consumer (e.g. a frontend, or another microservice) and a provider (a backend API) agree on the shape of their interaction — without spinning up both services together the way an e2e test would. Each side is tested independently against a shared **contract** (a recorded set of expected requests and responses), which means a broken integration is caught the moment either side changes, in each side's own CI pipeline, rather than discovered later in a slow, flaky end-to-end environment.\n\nThere are two ways to drive who defines the contract:\n\n- **Consumer-driven contracts (CDC)**: the consumer writes tests describing exactly what it needs from the provider; this generates a contract file, which the provider then replays against its real implementation to verify it satisfies every consumer's actual needs. This fits well when there are many consumers and a shared provider — the provider gets a single source of truth for \"what does anyone actually depend on,\" which also protects against breaking a consumer nobody remembered to check with.\n- **Provider-driven contracts**: the provider publishes its own contract (often auto-generated from an OpenAPI/Swagger spec) describing what it promises to support, and consumers validate against that. This fits better when the provider is the stable, well-governed source of truth and consumers are expected to adapt to it — a public API is a typical case.\n\n```typescript\n// Consumer-driven: the frontend (consumer) defines what it needs\nimport { PactV3 } from '@pact-foundation/pact';\n\nconst provider = new PactV3({ consumer: 'WebApp', provider: 'UserService' });\n\ntest('fetches user by id', async () => {\n  await provider\n    .given('user 1 exists')\n    .uponReceiving('a request for user 1')\n    .withRequest({ method: 'GET', path: '/users/1' })\n    .willRespondWith({ status: 200, body: { id: 1, name: 'John' } })\n    .executeTest(async (mockServer) => {\n      const user = await fetchUser(1, mockServer.url);\n      expect(user.name).toBe('John');\n    });\n});\n\n// The provider side later replays this same contract against its real implementation\n// in its own CI pipeline, independently of the consumer's codebase.\n```\n\nThe practical payoff is decoupled deployability: teams can deploy independently and still get fast, reliable feedback the moment an API change would actually break someone, instead of relying on a full e2e suite (or worse, production) to find out.\n\n**Common pitfall:** Treating contract tests as a substitute for either integration or business-logic tests — a contract test verifies the *shape* of an interaction (status codes, field presence, types), not that the underlying business logic behind it is correct. A provider can pass every contract test while returning a completely wrong discount calculation, as long as the response shape matches what was agreed.",
      "ru": "### Contract testing: реализация и consumer-driven vs provider-driven\n\nContract testing проверяет, что consumer (например, фронтенд или другой микросервис) и provider (backend API) согласны насчёт формы своего взаимодействия — без реального одновременного запуска обоих сервисов, как это делает e2e-тест. Каждая сторона тестируется независимо против общего **контракта** (записанного набора ожидаемых запросов и ответов), а это значит, что сломанная интеграция ловится в момент, когда меняется любая из сторон, в собственном CI-пайплайне каждой стороны, а не обнаруживается позже, в медленном и нестабильном e2e-окружении.\n\nЕсть два способа определить, кто задаёт контракт:\n\n- **Consumer-driven contracts (CDC)**: consumer пишет тесты, описывающие ровно то, что ему нужно от provider'а; это генерирует файл контракта, который provider затем прогоняет против своей реальной реализации, чтобы убедиться, что удовлетворяет реальные нужды каждого consumer'а. Это хорошо подходит, когда есть много consumer'ов и один общий provider — provider получает единый источник истины о том, \"от кого что реально зависит\", что заодно защищает от поломки consumer'а, про которого забыли спросить.\n- **Provider-driven contracts**: provider публикует собственный контракт (часто автоматически сгенерированный из OpenAPI/Swagger-спеки), описывающий, что он обещает поддерживать, а consumer'ы валидируются против него. Это лучше подходит, когда provider — стабильный, хорошо управляемый источник истины, а consumer'ы должны под него подстраиваться — типичный пример: публичное API.\n\n```typescript\n// Consumer-driven: фронтенд (consumer) определяет, что ему нужно\nimport { PactV3 } from '@pact-foundation/pact';\n\nconst provider = new PactV3({ consumer: 'WebApp', provider: 'UserService' });\n\ntest('получает пользователя по id', async () => {\n  await provider\n    .given('user 1 exists')\n    .uponReceiving('a request for user 1')\n    .withRequest({ method: 'GET', path: '/users/1' })\n    .willRespondWith({ status: 200, body: { id: 1, name: 'John' } })\n    .executeTest(async (mockServer) => {\n      const user = await fetchUser(1, mockServer.url);\n      expect(user.name).toBe('John');\n    });\n});\n\n// Сторона provider'а позже прогоняет этот же контракт против своей реальной\n// реализации, в своём собственном CI-пайплайне, независимо от кода consumer'а.\n```\n\nПрактическая выгода — независимая деплоеспособность: команды могут деплоиться независимо и всё равно получать быструю, надёжную обратную связь в момент, когда изменение API реально что-то сломает, вместо того чтобы полагаться на полный e2e-набор (или того хуже — на прод), чтобы это выяснить.\n\n**Частая ловушка:** Считать contract-тесты заменой интеграционных тестов или тестов бизнес-логики — contract-тест проверяет *форму* взаимодействия (статус-коды, наличие полей, типы), а не то, что бизнес-логика за этим взаимодействием корректна. Provider может пройти все contract-тесты, возвращая совершенно неверный расчёт скидки, если форма ответа при этом совпадает с согласованной."
    },
    "tags": [
      "contract-testing",
      "pact",
      "consumer-driven-contracts",
      "provider-driven-contracts"
    ]
  },
  {
    "id": "testing-q-18",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "Describe property-based testing and provide an example. When is it more valuable than example-based testing?",
      "ru": "Опишите property-based тестирование и приведите пример. Когда оно более ценно, чем тестирование на примерах?"
    },
    "answer": {
      "en": "### Property-Based Testing\n\nExample-based tests assert a specific output for a specific input: \"given 2 and 3, sum returns 5.\" Property-based testing instead asserts an **invariant** — a property that should hold true for *any* valid input — and lets a library generate hundreds of random inputs (including edge cases you wouldn't think to write by hand: empty arrays, negative numbers, Unicode strings, deeply nested objects) to try to break that invariant.\n\n```typescript\nimport fc from 'fast-check';\n\n// Property: addition is commutative, for any two integers\nfc.assert(\n  fc.property(fc.integer(), fc.integer(), (a, b) => {\n    return sum(a, b) === sum(b, a);\n  })\n);\n\n// Property: sorting an array never changes its length or its set of elements\nfc.assert(\n  fc.property(fc.array(fc.integer()), (arr) => {\n    const sorted = mySort(arr);\n    return sorted.length === arr.length &&\n      new Set(sorted).size === new Set(arr).size;\n  })\n);\n```\n\nWhen a property-based test finds a failing input, it doesn't just report the first random value that broke it — most libraries (fast-check included) perform **shrinking**: they automatically search for the smallest, simplest input that still reproduces the failure. Finding that `sort([3, -7, 0])` breaks something is far less useful than the shrunk result telling you it actually breaks on `[-1, 0]`.\n\nThis technique shines for pure functions with mathematical properties (sorting, serialization round-trips, parsers, data transformations) where the space of possible inputs is large and manual examples tend to miss corner cases. It's a poor fit for testing UI interactions or business rules that don't have a clean invariant to state (there's no simple mathematical property describing \"the checkout button should be disabled while a discount code is being validated\").\n\nA common practical pairing is combining property-based generation with a network-mocking layer like MSW: instead of hand-writing a handful of fixed API responses, you generate a wide range of randomized-but-valid response shapes and assert your API client parses all of them correctly — this catches client-side parsing bugs that fixed example responses would never surface.\n\n```typescript\nimport { http, HttpResponse } from 'msw';\nimport { setupServer } from 'msw/node';\nimport fc from 'fast-check';\n\nconst server = setupServer(); // handlers registered per-test below\n\ntest('client correctly parses any valid user id shape', () => {\n  fc.assert(\n    fc.property(fc.integer({ min: 1 }), (id) => {\n      server.use(\n        http.get('/api/user', () => HttpResponse.json({ id, name: 'User' }))\n      );\n      return parseUserResponse({ id, name: 'User' }).id === id;\n    })\n  );\n});\n```\n\n**Common pitfall:** Reaching for property-based testing as a wholesale replacement for example-based tests. The two complement each other — a handful of example-based tests document specific, meaningful business scenarios clearly for future readers, while property-based tests hunt for the edge cases those examples wouldn't think to cover. Dropping the examples in favor of only properties makes test failures harder to read, since a property failure reports \"the invariant broke for input X\" rather than a named, intention-revealing scenario.",
      "ru": "### Property-based тестирование\n\nExample-based тесты проверяют конкретный результат для конкретного входа: \"для 2 и 3, sum вернёт 5\". Property-based тестирование вместо этого проверяет **инвариант** — свойство, которое должно выполняться для *любого* валидного входа — и позволяет библиотеке сгенерировать сотни случайных входных данных (включая edge-кейсы, которые вручную вы бы не додумались написать: пустые массивы, отрицательные числа, Unicode-строки, глубоко вложенные объекты), пытаясь сломать этот инвариант.\n\n```typescript\nimport fc from 'fast-check';\n\n// Свойство: сложение коммутативно для любых двух целых чисел\nfc.assert(\n  fc.property(fc.integer(), fc.integer(), (a, b) => {\n    return sum(a, b) === sum(b, a);\n  })\n);\n\n// Свойство: сортировка массива никогда не меняет его длину и набор элементов\nfc.assert(\n  fc.property(fc.array(fc.integer()), (arr) => {\n    const sorted = mySort(arr);\n    return sorted.length === arr.length &&\n      new Set(sorted).size === new Set(arr).size;\n  })\n);\n```\n\nКогда property-based тест находит \"ломающий\" вход, он не просто сообщает первое случайное значение, которое всё сломало — большинство библиотек (включая fast-check) выполняют **шринкинг** (shrinking): они автоматически ищут наименьший, самый простой вход, который всё ещё воспроизводит поломку. Узнать, что `sort([3, -7, 0])` что-то ломает, гораздо менее полезно, чем получить уменьшенный результат, показывающий, что реально ломается на `[-1, 0]`.\n\nЭта техника отлично работает для чистых функций с математическими свойствами (сортировка, round-trip сериализации, парсеры, трансформации данных), где пространство возможных входов велико, а ручные примеры склонны упускать угловые случаи. Она плохо подходит для тестирования UI-взаимодействий или бизнес-правил, у которых нет чистого инварианта для формулировки (нет простого математического свойства, описывающего \"кнопка оформления заказа должна быть недоступна, пока проверяется промокод\").\n\nЧастое практическое сочетание — комбинировать property-based генерацию со слоем мокирования сети вроде MSW: вместо того чтобы вручную писать пару фиксированных ответов API, вы генерируете широкий диапазон случайных, но валидных форм ответа и проверяете, что ваш API-клиент корректно парсит их все — это ловит баги парсинга на стороне клиента, которые фиксированные примерные ответы никогда бы не выявили.\n\n```typescript\nimport { http, HttpResponse } from 'msw';\nimport { setupServer } from 'msw/node';\nimport fc from 'fast-check';\n\nconst server = setupServer(); // хендлеры регистрируются в каждом тесте ниже\n\ntest('клиент корректно парсит любую валидную форму id пользователя', () => {\n  fc.assert(\n    fc.property(fc.integer({ min: 1 }), (id) => {\n      server.use(\n        http.get('/api/user', () => HttpResponse.json({ id, name: 'User' }))\n      );\n      return parseUserResponse({ id, name: 'User' }).id === id;\n    })\n  );\n});\n```\n\n**Частая ловушка:** Использовать property-based тестирование как полную замену example-based тестов. Эти два подхода дополняют друг друга — пара example-based тестов ясно документирует конкретные, значимые бизнес-сценарии для будущих читателей кода, а property-based тесты охотятся за угловыми случаями, которые эти примеры не додумались бы покрыть. Отказ от примеров в пользу одних только свойств делает падения тестов менее читаемыми, поскольку падение property-теста сообщает \"инвариант сломался для входа X\", а не именованный, раскрывающий намерение сценарий."
    },
    "tags": [
      "property-based-testing",
      "fast-check",
      "generative-testing",
      "msw"
    ]
  },
  {
    "id": "testing-q-19",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "How do you test microservices in isolation while ensuring end-to-end flows work? Compare MSW and contract testing approaches.",
      "ru": "Как вы тестируете микросервисы в изоляции, убедившись, что end-to-end потоки работают? Сравните подходы MSW и контрактного тестирования."
    },
    "answer": {
      "en": "### Testing Microservices in Isolation: MSW vs Contract Testing\n\nThese two techniques solve different halves of the same problem — how do you get confidence in a service that depends on other services, without needing all of them running together for every test run?\n\n**Mock Service Worker (MSW)** intercepts HTTP requests at the network level and returns responses you define, all inside the consumer's own test process. It's fast (no network, no other service needs to even exist), and it lets you simulate scenarios that are hard to trigger against a real dependency on demand (a 500 error, a slow response, a specific edge-case payload). Its weakness: the mocked responses are only as accurate as whoever wrote them — nothing guarantees the real provider actually behaves the way your MSW handlers assume it does, so MSW can give you a fully green test suite that would still break in production the moment the real API's shape changes.\n\n**Contract testing** closes exactly that gap: the provider's real implementation is verified against the same expectations the consumer's tests rely on, so a drift between \"what the consumer assumes\" and \"what the provider actually returns\" gets caught in each side's own CI, independent of the other.\n\n```typescript\n// MSW: fast, fully isolated, but the response shape is only as good as this definition\nconst server = setupServer(\n  http.get('/api/orders/:id', () =>\n    HttpResponse.json({ id: 1, status: 'shipped' })\n  )\n);\n\ntest('renders order status', async () => {\n  render(<OrderStatus orderId={1} />);\n  expect(await screen.findByText('shipped')).toBeInTheDocument();\n});\n\n// Contract test: verifies the REAL OrderService actually returns this shape\n// (runs in OrderService's own CI, replaying the consumer-defined contract)\n```\n\nThe practical strategy most teams land on: use MSW for the bulk of fast, everyday tests (including deliberately simulating errors and edge cases), and layer contract tests on top specifically to catch drift at the actual API boundary — MSW gives you speed and edge-case coverage, contract tests give you the guarantee that your assumptions about the real service are still true.\n\n**Common pitfall:** Relying on MSW alone and treating a fully green MSW-based test suite as proof the integration works — MSW tests a consumer's *reaction* to a response shape you invented; it says nothing about whether the real provider still produces that shape, which is exactly the gap contract testing exists to close.",
      "ru": "### Тестирование микросервисов в изоляции: MSW vs contract testing\n\nЭти две техники решают разные половины одной и той же проблемы — как получить уверенность в сервисе, который зависит от других сервисов, не запуская их все вместе при каждом прогоне тестов?\n\n**Mock Service Worker (MSW)** перехватывает HTTP-запросы на уровне сети и возвращает заданные вами ответы, всё это внутри собственного тестового процесса consumer'а. Это быстро (нет сети, другому сервису даже не нужно существовать), и позволяет симулировать сценарии, которые сложно вызвать по требованию на реальной зависимости (ошибку 500, медленный ответ, конкретный edge-кейс payload). Слабость: замоканные ответы настолько точны, насколько точен тот, кто их написал — ничто не гарантирует, что реальный provider реально ведёт себя так, как предполагают ваши MSW-хендлеры, поэтому MSW может дать полностью зелёный набор тестов, который всё равно сломается в проде, как только изменится форма реального API.\n\n**Contract testing** закрывает ровно этот пробел: реальная реализация provider'а проверяется против тех же ожиданий, на которые опираются тесты consumer'а, поэтому расхождение между \"что предполагает consumer\" и \"что реально возвращает provider\" ловится в CI каждой стороны независимо от другой.\n\n```typescript\n// MSW: быстро, полностью изолированно, но форма ответа хороша ровно настолько,\n// насколько хорошо она здесь определена\nconst server = setupServer(\n  http.get('/api/orders/:id', () =>\n    HttpResponse.json({ id: 1, status: 'shipped' })\n  )\n);\n\ntest('отображает статус заказа', async () => {\n  render(<OrderStatus orderId={1} />);\n  expect(await screen.findByText('shipped')).toBeInTheDocument();\n});\n\n// Contract-тест: проверяет, что РЕАЛЬНЫЙ OrderService реально возвращает эту форму\n// (выполняется в собственном CI OrderService, прогоняя контракт, заданный consumer'ом)\n```\n\nПрактическая стратегия, к которой приходит большинство команд: использовать MSW для основной массы быстрых повседневных тестов (включая намеренную симуляцию ошибок и edge-кейсов), а поверх добавлять слой contract-тестов специально для отлова расхождений на реальной границе API — MSW даёт скорость и покрытие edge-кейсов, contract-тесты дают гарантию, что ваши предположения о реальном сервисе всё ещё верны.\n\n**Частая ловушка:** Полагаться только на MSW и воспринимать полностью зелёный набор тестов на MSW как доказательство, что интеграция работает — тесты на MSW проверяют *реакцию* consumer'а на придуманную вами форму ответа; они ничего не говорят о том, продолжает ли реальный provider выдавать именно эту форму — а это ровно тот пробел, для закрытия которого существует contract testing."
    },
    "tags": [
      "microservices-testing",
      "msw",
      "contract-testing",
      "mocking"
    ]
  },
  {
    "id": "testing-q-20",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "Explain your approach to performance testing in a fullstack application. How would you measure and prevent regressions?",
      "ru": "Объясните ваш подход к тестированию производительности в fullstack приложении. Как бы вы измеряли и предотвращали регрессии?"
    },
    "answer": {
      "en": "### Performance Testing Strategy in a Fullstack Application\n\nPerformance testing needs to cover two genuinely different layers, and treating them as one thing is itself a common mistake: **frontend/perceived performance** (how fast the page feels to load and become interactive) and **backend/system performance** (how fast the server responds and how it behaves under concurrent load).\n\n**Frontend**: Core Web Vitals — LCP (Largest Contentful Paint, how fast the main content appears), INP (Interaction to Next Paint, responsiveness to user input, which replaced FID), and CLS (Cumulative Layout Shift, visual stability) — are the standard metrics because they're what Google's ranking and real-user-monitoring tools measure, and they correlate reasonably well with actual perceived quality. Measure them with Lighthouse (lab data, run in CI) and real-user-monitoring/RUM (field data, from actual visitors) — lab data is reproducible and good for catching regressions in CI, field data reflects what real users on real devices/networks actually experience, and the two can diverge significantly.\n\n**Backend**: response latency (p50/p95/p99, not just average — the tail latency is what users actually notice), database query time, and throughput under concurrent load, typically tracked with APM tools (DataDog, New Relic) in production and load-testing tools in CI/staging.\n\n```typescript\n// Lab-data check in CI (Playwright + web-vitals)\ntest('LCP stays within budget', async ({ page }) => {\n  await page.goto('/');\n  const lcp = await page.evaluate(() => new Promise((resolve) => {\n    new PerformanceObserver((list) => {\n      const entries = list.getEntries();\n      resolve(entries[entries.length - 1].startTime);\n    }).observe({ type: 'largest-contentful-paint', buffered: true });\n  }));\n  expect(lcp).toBeLessThan(2500); // 2.5s budget\n});\n```\n\nThe key architectural piece for *preventing* regressions rather than just detecting them after the fact is a **performance budget wired into CI**: fail the build if a tracked metric (bundle size, LCP, p95 API latency) crosses a threshold, the same way a failing test blocks a merge. Without an enforced budget, performance tends to degrade gradually — no single PR looks bad enough to block on its own, but the cumulative drift over months is very real.\n\n**Common pitfall:** Measuring only frontend metrics (or only backend metrics) and declaring \"performance testing done\" — a fast page backed by a slow API still feels slow to the user, and a fast API behind a JS bundle that blocks rendering for three seconds is invisible to backend-only monitoring. Both layers need their own budget and their own regression check.",
      "ru": "### Стратегия performance-тестирования в fullstack-приложении\n\nPerformance-тестирование должно покрывать два реально разных слоя, и восприятие их как одного целого — уже само по себе частая ошибка: **фронтенд/воспринимаемая производительность** (насколько быстро страница ощущается загруженной и интерактивной) и **backend/системная производительность** (насколько быстро отвечает сервер и как он ведёт себя под конкурентной нагрузкой).\n\n**Frontend**: Core Web Vitals — LCP (Largest Contentful Paint, насколько быстро появляется основной контент), INP (Interaction to Next Paint, отзывчивость на действия пользователя, заменивший FID) и CLS (Cumulative Layout Shift, визуальная стабильность) — стандартные метрики, потому что именно их измеряют инструменты ранжирования и real-user-monitoring Google, и они достаточно хорошо коррелируют с реально воспринимаемым качеством. Измеряйте их через Lighthouse (lab-данные, прогон в CI) и real-user-monitoring/RUM (полевые данные от реальных посетителей) — lab-данные воспроизводимы и хороши для отлова регрессий в CI, полевые данные отражают то, что реально испытывают пользователи на реальных устройствах/сетях, и эти два набора данных могут значительно расходиться.\n\n**Backend**: латентность ответа (p50/p95/p99, а не только среднее — именно \"хвостовая\" латентность реально заметна пользователям), время запросов к БД и throughput под конкурентной нагрузкой, обычно отслеживаются через APM-инструменты (DataDog, New Relic) в проде и инструменты нагрузочного тестирования в CI/staging.\n\n```typescript\n// Проверка lab-данных в CI (Playwright + web-vitals)\ntest('LCP остаётся в рамках бюджета', async ({ page }) => {\n  await page.goto('/');\n  const lcp = await page.evaluate(() => new Promise((resolve) => {\n    new PerformanceObserver((list) => {\n      const entries = list.getEntries();\n      resolve(entries[entries.length - 1].startTime);\n    }).observe({ type: 'largest-contentful-paint', buffered: true });\n  }));\n  expect(lcp).toBeLessThan(2500); // бюджет 2.5с\n});\n```\n\nКлючевой архитектурный элемент для *предотвращения* регрессий, а не просто их обнаружения постфактум — это **бюджет производительности, встроенный в CI**: валить сборку, если отслеживаемая метрика (размер бандла, LCP, p95 латентность API) пересекает порог, точно так же, как упавший тест блокирует мердж. Без закреплённого бюджета производительность обычно деградирует постепенно — ни один отдельный PR не выглядит достаточно плохим, чтобы блокировать его самостоятельно, но накопленный за месяцы дрейф вполне реален.\n\n**Частая ловушка:** Измерять только фронтенд-метрики (или только backend-метрики) и объявлять \"тестирование производительности готово\" — быстрая страница поверх медленного API всё равно ощущается медленной для пользователя, а быстрый API за JS-бандлом, блокирующим рендеринг на три секунды, невидим для мониторинга, следящего только за backend'ом. Обоим слоям нужен свой бюджет и своя проверка на регрессии."
    },
    "tags": [
      "performance-testing",
      "core-web-vitals",
      "regression-prevention",
      "lighthouse"
    ]
  },
  {
    "id": "testing-q-21",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "How do you test shared components in a micro-frontend architecture using Module Federation? What are the specific risks?",
      "ru": "Как тестировать общие компоненты в архитектуре микрофронтендов с Module Federation? В чём здесь специфические риски?"
    },
    "answer": {
      "en": "### Testing Shared Components Across Module Federation Micro-Frontends\n\nModule Federation lets independently built and deployed applications share code (components, hooks, utilities) at runtime rather than at build time — a \"host\" application loads a \"remote\" module dynamically, often resolving the actual version of a shared dependency (React, a design-system library) only when the app runs in the browser. This introduces a class of bugs that don't exist in a monolith: things that work perfectly in each MFE's own isolated test suite can still break in production because of how the *pieces* combine at runtime.\n\nThe two risks specific to this architecture:\n\n1. **Shared dependency version mismatches.** If MFE A was built against React 18.2 and MFE B (which it loads at runtime) was built against React 18.0, Module Federation's singleton sharing can silently resolve to one version at runtime — and subtle behavioral differences (or, worse, two copies of React loading if the singleton config is wrong) can produce bugs that no single MFE's isolated test suite would ever catch, because each one tests fine against its *own* dependency version.\n2. **Contract drift on the shared interface.** A shared component or hook exposed by one team can change its props/API in a way that quietly breaks a consuming MFE owned by a different team — and since each MFE deploys independently, there's no single build step where \"does this change break a consumer\" gets checked automatically, unlike in a monorepo where a type error would fail the build.\n\n```typescript\n// Consuming a remote module — this import is resolved at runtime, not build time\nconst RemoteButton = lazy(() => import('remoteApp/Button'));\n\n// A component-level test in the consuming MFE's own suite passes...\ntest('renders the checkout button', () => {\n  render(<Suspense fallback={null}><RemoteButton label=\"Pay\" /></Suspense>);\n  // ...but this test used whatever version of RemoteButton was available\n  // at test time (often mocked or pinned), not necessarily what's actually\n  // deployed for the remote in production right now.\n});\n```\n\nThe practical mitigation strategy layers three things: contract tests (or generated TypeScript types published from the shared library) on the interface between host and remote, so a breaking prop change is caught by the *provider's* CI, not discovered by the consumer in production; integration smoke tests that actually load the real deployed remote (against a staging/canary URL) rather than a local mock, to catch real runtime resolution issues; and strict `shared` version ranges in the Module Federation config, with CI alerts on drift.\n\n**Common pitfall:** Testing each micro-frontend fully in isolation (mocking every remote import) and treating a green test suite in every MFE as proof the composed application works — this is exactly the gap Module Federation's runtime composition creates: every piece can be \"tested and green\" individually while the actual runtime combination breaks, because no single test suite ever exercised the real integration point.",
      "ru": "### Тестирование общих компонентов между микрофронтендами на Module Federation\n\nModule Federation позволяет независимо собранным и задеплоенным приложениям шарить код (компоненты, хуки, утилиты) в рантайме, а не на этапе сборки — \"host\"-приложение динамически загружает \"remote\"-модуль, часто разрешая реальную версию общей зависимости (React, библиотеки дизайн-системы) только когда приложение реально запущено в браузере. Это порождает класс багов, которых не существует в монолите: то, что прекрасно работает в изолированном тестовом наборе каждого отдельного MFE, всё равно может сломаться в проде из-за того, как *части* соединяются в рантайме.\n\nДва риска, специфичных именно для этой архитектуры:\n\n1. **Несовпадение версий общих зависимостей.** Если MFE A собран под React 18.2, а MFE B (который он загружает в рантайме) собран под React 18.0, механизм singleton-шаринга Module Federation может молча разрешить одну версию в рантайме — и тонкие поведенческие различия (или, что хуже, загрузка двух копий React, если singleton-конфиг неверен) могут дать баги, которые изолированный тестовый набор ни одного из MFE никогда не поймает, потому что каждый из них тестируется нормально против *своей* версии зависимости.\n2. **Расхождение контракта общего интерфейса.** Общий компонент или хук, который выставляет одна команда, может изменить свои пропсы/API так, что незаметно сломает потребляющий его MFE другой команды — и поскольку каждый MFE деплоится независимо, нет единого этапа сборки, где автоматически проверялось бы \"не ломает ли это изменение consumer'а\", в отличие от монорепозитория, где ошибка типов провалила бы сборку.\n\n```typescript\n// Потребление remote-модуля — этот импорт разрешается в рантайме, не при сборке\nconst RemoteButton = lazy(() => import('remoteApp/Button'));\n\n// Тест на уровне компонента в собственном наборе потребляющего MFE проходит...\ntest('рендерит кнопку оформления заказа', () => {\n  render(<Suspense fallback={null}><RemoteButton label=\"Pay\" /></Suspense>);\n  // ...но этот тест использовал ту версию RemoteButton, что была доступна\n  // во время теста (часто замоканную или зафиксированную), не обязательно ту,\n  // что реально задеплоена для remote в проде прямо сейчас.\n});\n```\n\nПрактическая стратегия смягчения риска складывается из трёх слоёв: contract-тесты (или сгенерированные TypeScript-типы, публикуемые из общей библиотеки) на интерфейсе между host и remote, чтобы ломающее изменение пропа ловилось в CI *провайдера*, а не обнаруживалось потребителем в проде; интеграционные smoke-тесты, реально загружающие задеплоенный remote (против staging/canary URL), а не локальный мок, чтобы ловить реальные проблемы разрешения в рантайме; и строгие диапазоны версий `shared` в конфиге Module Federation с алертами в CI на расхождение.\n\n**Частая ловушка:** Тестировать каждый микрофронтенд полностью изолированно (замокивая каждый remote-импорт) и воспринимать зелёный набор тестов в каждом MFE как доказательство, что собранное приложение работает — это ровно тот пробел, который создаёт рантайм-композиция Module Federation: каждая часть может быть \"протестирована и зелена\" по отдельности, пока реальная композиция в рантайме ломается, потому что ни один тестовый набор никогда не задействовал реальную точку интеграции."
    },
    "tags": [
      "module-federation",
      "micro-frontend-testing",
      "shared-dependencies",
      "runtime-integration"
    ]
  },
  {
    "id": "testing-q-22",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "What is mutation testing, and how does it help you measure whether your test suite is actually effective, beyond code coverage?",
      "ru": "Что такое mutation testing, и как оно помогает понять, реально ли эффективен ваш набор тестов, за пределами code coverage?"
    },
    "answer": {
      "en": "### Mutation Testing: Measuring Test Suite Effectiveness\n\nCode coverage answers \"did my tests *execute* this line?\" — it says nothing about whether the tests would actually *notice* if that line's logic were wrong. Mutation testing directly attacks that blind spot: a tool (Stryker for JS/TS) automatically introduces small deliberate bugs (\"mutants\") into your source code — flipping a `>` to `>=`, changing `&&` to `||`, replacing a return value — then re-runs your test suite against each mutated version. If at least one test fails, the mutant is \"killed\" (your suite caught the bug). If every test still passes, the mutant \"survived\" — meaning your tests exercised that code path but weren't actually checking anything meaningful about it.\n\n```typescript\n// Original code\nfunction isEligibleForDiscount(age: number, memberYears: number) {\n  return age >= 65 || memberYears > 10;\n}\n\n// A mutant Stryker might generate: changed >= to >\nfunction isEligibleForDiscount(age: number, memberYears: number) {\n  return age > 65 || memberYears > 10;  // mutant: boundary shifted\n}\n\n// If this test suite doesn't include a boundary case at age === 65,\n// the mutant survives — coverage would still show 100% for this line,\n// but the test suite can't actually distinguish >= from >\nit('is eligible past the age threshold', () => {\n  expect(isEligibleForDiscount(70, 0)).toBe(true); // doesn't test the boundary\n});\n\n// A test that would kill the mutant above:\nit('is eligible exactly at the age threshold', () => {\n  expect(isEligibleForDiscount(65, 0)).toBe(true); // catches >= vs > boundary bugs\n});\n```\n\nThe output is a **mutation score** (percentage of mutants killed), which is a meaningfully different — and usually much harder-to-inflate — number than a coverage percentage. A codebase can sit at 95% line coverage and 40% mutation score at the same time, and the mutation score is the more honest signal about whether refactoring that code safely is actually protected by tests.\n\nThe trade-off is cost: mutation testing runs your entire test suite once per mutant, so it's computationally expensive (minutes to hours depending on codebase size) and isn't something you run on every commit — teams typically run it periodically (nightly, or on a schedule) or scoped to just the files touched in a PR, rather than as a blocking check on every push.\n\n**Common pitfall:** Chasing a high mutation score as a target the same way teams sometimes chase coverage percentage — a mutation score is a diagnostic for finding weak spots in an existing suite, not a KPI to game; writing tests specifically to kill trivial mutants (rather than to verify meaningful behavior) produces the same false confidence that chasing raw coverage numbers does.",
      "ru": "### Mutation testing: измерение эффективности тестового набора\n\nCode coverage отвечает на вопрос \"выполнили ли мои тесты эту строку?\" — но ничего не говорит о том, заметят ли тесты, если логика этой строки окажется неверной. Mutation testing напрямую атакует это слепое пятно: инструмент (Stryker для JS/TS) автоматически вносит небольшие намеренные баги (\"мутанты\") в исходный код — меняет `>` на `>=`, `&&` на `||`, подменяет возвращаемое значение — и затем прогоняет ваш тестовый набор против каждой мутированной версии. Если хотя бы один тест падает, мутант \"убит\" (набор поймал баг). Если все тесты по-прежнему проходят, мутант \"выжил\" — значит, тесты выполнили этот участок кода, но по сути ничего значимого в нём не проверили.\n\n```typescript\n// Исходный код\nfunction isEligibleForDiscount(age: number, memberYears: number) {\n  return age >= 65 || memberYears > 10;\n}\n\n// Мутант, который может сгенерировать Stryker: >= заменено на >\nfunction isEligibleForDiscount(age: number, memberYears: number) {\n  return age > 65 || memberYears > 10;  // мутант: сдвинута граница\n}\n\n// Если в наборе тестов нет граничного случая при age === 65,\n// мутант выживает — coverage всё равно покажет 100% для этой строки,\n// но набор тестов на самом деле не отличает >= от >\nit('имеет право на скидку после возрастного порога', () => {\n  expect(isEligibleForDiscount(70, 0)).toBe(true); // не проверяет границу\n});\n\n// Тест, который убил бы мутанта выше:\nit('имеет право на скидку ровно на возрастном пороге', () => {\n  expect(isEligibleForDiscount(65, 0)).toBe(true); // ловит баги на границе >= vs >\n});\n```\n\nРезультат — **mutation score** (процент убитых мутантов), число, значимо отличающееся от процента покрытия — и обычно его гораздо сложнее искусственно завысить. Кодовая база может одновременно иметь 95% покрытия по строкам и 40% mutation score, и mutation score — более честный сигнал о том, действительно ли безопасный рефакторинг этого кода защищён тестами.\n\nTrade-off — стоимость: mutation testing прогоняет весь тестовый набор один раз на каждого мутанта, поэтому вычислительно это дорого (от минут до часов, в зависимости от размера кодовой базы), и это не то, что запускают на каждый коммит — команды обычно запускают его периодически (ночью или по расписанию) либо ограничивают файлами, затронутыми в конкретном PR, а не как блокирующую проверку на каждый пуш.\n\n**Частая ловушка:** Гнаться за высоким mutation score как за самоцелью — точно так же, как иногда гонятся за процентом coverage — mutation score является диагностикой для поиска слабых мест в существующем наборе, а не KPI, который нужно \"накручивать\"; написание тестов специально ради убийства тривиальных мутантов (а не ради проверки значимого поведения) даёт ту же ложную уверенность, что и погоня за голыми цифрами покрытия."
    },
    "tags": [
      "mutation-testing",
      "stryker",
      "test-effectiveness",
      "test-quality"
    ]
  },
  {
    "id": "testing-q-23",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "Describe a strategy for testing microservices end-to-end with Playwright while minimizing flakiness and maintaining test performance. How do you handle async operations?",
      "ru": "Опишите стратегию для end-to-end тестирования микросервисов с помощью Playwright, минимизируя flakiness и поддерживая производительность тестов. Как вы обрабатываете асинхронные операции?"
    },
    "answer": {
      "en": "### E2E Testing Microservices with Playwright: Minimizing Flakiness\n\nThe single biggest source of flakiness in e2e tests against a system built from multiple services isn't Playwright itself — it's using time-based waits (`waitForTimeout`, hardcoded `sleep`) to paper over the fact that you don't actually know when an async operation (a network call, a service-to-service event, a database write) has finished. A fixed delay is either too short (flaky failures under load) or too long (slow suite), and it's guessing rather than actually knowing.\n\nThe fix is to wait for the actual *signal* that the operation completed, not a fixed amount of time: a specific element appearing, a network response resolving, or a particular DOM state being reached.\n\n```typescript\n// Flaky: guesses how long the async chain across services will take\nawait page.goto('/checkout');\nawait page.waitForTimeout(2000); // fragile guess\nawait page.click('[data-testid=\"submit\"]');\n\n// Reliable: waits for the actual signal\nawait page.goto('/checkout');\nawait page.waitForLoadState('networkidle');\nawait page.locator('[data-testid=\"submit\"]').waitFor({ state: 'visible' });\nawait page.click('[data-testid=\"submit\"]');\nawait page.waitForResponse((res) => res.url().includes('/api/orders') && res.status() === 200);\nawait expect(page.getByText('Order confirmed')).toBeVisible();\n```\n\nFor a system spanning multiple services, a second common source of flakiness is *shared test environments*: if several test runs (or CI jobs) hit the same staging environment concurrently, one test's data mutations can affect another's assertions. The fix is either fully isolated per-run test data (unique IDs/namespaces per test run) or running the whole stack in ephemeral, per-run infrastructure (docker-compose spun up fresh per CI job).\n\nPerformance matters alongside reliability: Playwright supports running tests in parallel across worker processes, and mocking non-critical downstream services (with MSW or similar) for tests that aren't specifically verifying that integration keeps the suite fast without sacrificing the confidence that matters — reserve fully-wired, all-services-real e2e runs for the small set of critical paths where that confidence is actually worth the cost.\n\n**Common pitfall:** Reaching for a hardcoded `waitForTimeout()` as a quick fix the moment a test is flaky, instead of identifying the actual condition the test should be waiting for — this \"solves\" the symptom in the short term while leaving the test just as fragile (now merely slower), since the real race condition is still there, just less likely to be hit under normal load.",
      "ru": "### E2E-тестирование микросервисов с Playwright: борьба с флейкистостью\n\nСамый крупный источник флейкистости в e2e-тестах для системы из нескольких сервисов — это не сам Playwright, а использование временных ожиданий (`waitForTimeout`, жёстко заданный `sleep`), которые маскируют тот факт, что вы на самом деле не знаете, когда завершилась асинхронная операция (сетевой вызов, событие между сервисами, запись в БД). Фиксированная задержка либо слишком мала (нестабильные падения под нагрузкой), либо слишком велика (медленный набор тестов) — это угадывание, а не реальное знание.\n\nРешение — ждать реальный *сигнал* о завершении операции, а не фиксированное количество времени: появление конкретного элемента, завершение сетевого ответа, достижение определённого состояния DOM.\n\n```typescript\n// Флейки: угадывает, сколько времени займёт асинхронная цепочка между сервисами\nawait page.goto('/checkout');\nawait page.waitForTimeout(2000); // хрупкое угадывание\nawait page.click('[data-testid=\"submit\"]');\n\n// Надёжно: ждёт реальный сигнал\nawait page.goto('/checkout');\nawait page.waitForLoadState('networkidle');\nawait page.locator('[data-testid=\"submit\"]').waitFor({ state: 'visible' });\nawait page.click('[data-testid=\"submit\"]');\nawait page.waitForResponse((res) => res.url().includes('/api/orders') && res.status() === 200);\nawait expect(page.getByText('Order confirmed')).toBeVisible();\n```\n\nДля системы из нескольких сервисов второй частый источник флейкистости — *общие тестовые окружения*: если несколько прогонов тестов (или CI-джобов) одновременно бьют в одно и то же staging-окружение, мутации данных в одном тесте могут повлиять на проверки в другом. Решение — либо полностью изолированные тестовые данные на каждый прогон (уникальные ID/неймспейсы на прогон), либо запуск всего стека в эфемерной, создаваемой заново на каждый прогон инфраструктуре (docker-compose, поднимаемый с нуля на каждой CI-джобе).\n\nПроизводительность важна наравне с надёжностью: Playwright поддерживает параллельный запуск тестов в нескольких воркер-процессах, а мокирование некритичных нижестоящих сервисов (через MSW или аналог) для тестов, которые не проверяют конкретно эту интеграцию, держит набор быстрым без потери той уверенности, которая реально важна — полные прогоны e2e со всеми реальными сервисами стоит оставить для небольшого набора критичных сценариев, где такая уверенность действительно стоит своей цены.\n\n**Частая ловушка:** Хвататься за жёстко заданный `waitForTimeout()` как за быстрое решение в момент, когда тест флейкует, вместо того чтобы определить реальное условие, которого тест должен дожидаться — это \"решает\" симптом в краткосрочной перспективе, оставляя тест таким же хрупким (только теперь ещё и более медленным), потому что реальная гонка состояний никуда не делась, просто стала менее вероятной при обычной нагрузке."
    },
    "tags": [
      "playwright",
      "e2e-testing",
      "microservices-testing",
      "test-flakiness"
    ]
  },
  {
    "id": "testing-q-24",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "How do you approach visual regression testing for UI components? How is it different from snapshot testing, and what are the common pitfalls?",
      "ru": "Как подойти к visual regression testing для UI-компонентов? Чем это отличается от snapshot testing, и какие тут частые ловушки?"
    },
    "answer": {
      "en": "### Visual Regression Testing\n\nVisual regression testing captures an actual **rendered screenshot** of a component or page and compares it pixel-by-pixel (or via perceptual diffing) against a stored baseline image, flagging any visual difference for human review. This is a fundamentally different mechanism from snapshot testing: a Jest snapshot serializes a DOM tree or a JSON structure as text, so it catches structural changes (a new prop, a different class name) but is blind to anything purely visual — a snapshot test won't notice that a CSS change made a button's padding wrong, or that a font failed to load, because the underlying markup can be identical while the rendered pixels are completely broken.\n\n```typescript\n// Playwright's built-in screenshot comparison\ntest('primary button renders correctly', async ({ page }) => {\n  await page.goto('/storybook/button--primary');\n  await expect(page.locator('[data-testid=\"button\"]'))\n    .toHaveScreenshot('primary-button.png');\n});\n\n// Tools like Percy/Chromatic work similarly but run comparisons across\n// multiple browsers/viewports and provide a review UI for approving diffs\n```\n\nBecause rendering is affected by the operating system's font rendering, GPU, and browser version, visual regression tests are usually run against a fixed, containerized environment (Docker, or a cloud service like Percy/Chromatic) rather than on developers' individual machines — otherwise \"it passed for me\" becomes routine, since a screenshot taken on macOS can differ pixel-for-pixel from the same page rendered on the Linux CI runner.\n\nThe main trade-offs versus DOM/JSON snapshot testing: visual regression catches an entire category of bugs (CSS regressions, font/layout issues, broken responsive breakpoints) that structural snapshots are blind to, but it's slower, needs infrastructure to keep rendering consistent, and — just like snapshot testing — is only useful if diffs are actually reviewed rather than rubber-stamped. Unlike a DOM snapshot, a visual diff also needs a human to judge whether a pixel difference is a real regression or an acceptable rendering variance (anti-aliasing, sub-pixel rounding).\n\n**Common pitfall:** Running visual regression tests without pinning the rendering environment (same OS, same browser version, same fonts installed) — tests then produce constant false-positive diffs from environment noise rather than real changes, and teams respond by ignoring or auto-approving diffs, which defeats the entire purpose of the check.",
      "ru": "### Visual regression testing\n\nVisual regression testing захватывает реальный **отрендеренный скриншот** компонента или страницы и сравнивает его попиксельно (или через перцептивный diff) с сохранённым эталонным изображением, помечая любое визуальное различие для ревью человеком. Это принципиально другой механизм по сравнению со snapshot testing: Jest-снапшот сериализует DOM-дерево или JSON-структуру как текст, поэтому он ловит структурные изменения (новый проп, другое имя класса), но слеп ко всему чисто визуальному — snapshot-тест не заметит, что изменение CSS сломало отступы кнопки, или что не загрузился шрифт, потому что базовая разметка может быть идентичной, пока отрендеренные пиксели полностью сломаны.\n\n```typescript\n// Встроенное сравнение скриншотов в Playwright\ntest('основная кнопка рендерится правильно', async ({ page }) => {\n  await page.goto('/storybook/button--primary');\n  await expect(page.locator('[data-testid=\"button\"]'))\n    .toHaveScreenshot('primary-button.png');\n});\n\n// Инструменты вроде Percy/Chromatic работают похоже, но сравнивают\n// в нескольких браузерах/вьюпортах и дают UI для ревью и одобрения дифов\n```\n\nПоскольку на рендеринг влияет рендеринг шрифтов операционной системы, GPU и версия браузера, тесты visual regression обычно запускают против фиксированного контейнеризированного окружения (Docker, или облачный сервис вроде Percy/Chromatic), а не на индивидуальных машинах разработчиков — иначе \"у меня прошло\" становится нормой, ведь скриншот, снятый на macOS, может пиксель-в-пиксель отличаться от той же страницы, отрендеренной на Linux CI-раннере.\n\nОсновные trade-off по сравнению со snapshot testing DOM/JSON: visual regression ловит целую категорию багов (регрессии CSS, проблемы шрифтов/вёрстки, сломанные адаптивные брейкпоинты), к которым структурные снапшоты слепы, но это медленнее, требует инфраструктуры для стабильного рендеринга, и — точно так же, как snapshot testing — полезно, только если дифы реально изучаются, а не проштамповываются. В отличие от DOM-снапшота, визуальный diff также требует, чтобы человек оценил, является ли пиксельное отличие реальной регрессией или допустимым отклонением рендеринга (сглаживание, субпиксельное округление).\n\n**Частая ловушка:** Запускать visual regression тесты без фиксации окружения рендеринга (та же ОС, та же версия браузера, те же установленные шрифты) — тесты тогда выдают постоянные ложноположительные дифы из-за шума окружения, а не реальных изменений, и команды в ответ начинают игнорировать или автоматически одобрять дифы, что сводит на нет весь смысл проверки."
    },
    "tags": [
      "visual-regression-testing",
      "chromatic",
      "percy",
      "screenshot-testing"
    ]
  },
  {
    "id": "testing-q-25",
    "topicId": "testing",
    "difficulty": "senior",
    "question": {
      "en": "How do you approach load and stress testing a backend service? What tools would you use and what do you actually measure?",
      "ru": "Как подойти к load- и stress-тестированию backend-сервиса? Какие инструменты использовать и что реально измерять?"
    },
    "answer": {
      "en": "### Load and Stress Testing a Backend Service\n\nLoad testing and functional testing answer fundamentally different questions. A passing integration test tells you an endpoint returns the right response *for one request at a time*; it says nothing about what happens when a thousand requests hit it concurrently — connection pool exhaustion, a database query that's fine at low volume but locks a table under contention, a memory leak that only surfaces after sustained traffic. Load testing exists specifically to find that class of bug before real users do.\n\nA few distinct testing shapes, each answering a different question:\n- **Load testing**: simulate expected normal-to-peak traffic (e.g. your typical Black Friday volume) and verify latency/error rates stay acceptable.\n- **Stress testing**: push volume well past expected peak to find the actual breaking point and *how* the system fails — does it degrade gracefully (slower responses, graceful rejection) or fail catastrophically (crashes, cascading failures across dependent services)?\n- **Soak testing**: sustain moderate load for an extended period (hours) to catch issues that only appear over time — memory leaks, connection pool leaks, gradual resource exhaustion that a short test wouldn't reveal.\n\n```javascript\n// k6 script: ramp up virtual users and assert on latency/error thresholds\nimport http from 'k6/http';\nimport { check } from 'k6';\n\nexport const options = {\n  stages: [\n    { duration: '2m', target: 100 },  // ramp up to 100 concurrent users\n    { duration: '5m', target: 100 },  // sustain\n    { duration: '2m', target: 0 },    // ramp down\n  ],\n  thresholds: {\n    http_req_duration: ['p(95)<500'], // 95% of requests under 500ms\n    http_req_failed: ['rate<0.01'],   // error rate under 1%\n  },\n};\n\nexport default function () {\n  const res = http.get('https://api.example.com/orders');\n  check(res, { 'status is 200': (r) => r.status === 200 });\n}\n```\n\nWhat actually matters to measure: latency percentiles (p95/p99, not the average — the average hides exactly the tail latency that real users experience as \"the app feels slow\"), error rate under load, and throughput (requests/second the system can sustain before latency or errors start climbing). Just as important as the metrics is watching *where* the system actually bottlenecks — CPU, database connections, an external API's own rate limit, a synchronous call that should have been queued — since that's what turns a load test from \"confirmed it's slow\" into \"found the actual fix.\"\n\n**Common pitfall:** Running a load test against a staging environment that's provisioned very differently from production (smaller instance size, no read replicas, a different connection pool config) and treating the results as representative — the numbers from a mismatched environment tell you almost nothing about how production will actually behave under the same load.",
      "ru": "### Load- и stress-тестирование backend-сервиса\n\nНагрузочное тестирование и функциональное тестирование отвечают на принципиально разные вопросы. Прошедший интеграционный тест говорит, что эндпоинт возвращает правильный ответ *на один запрос за раз*; он ничего не говорит о том, что происходит, когда по нему одновременно бьёт тысяча запросов — исчерпание пула соединений, запрос к БД, нормальный при малом объёме, но блокирующий таблицу под конкуренцией, утечка памяти, проявляющаяся только при устойчивой нагрузке. Нагрузочное тестирование существует именно для того, чтобы найти этот класс багов раньше, чем это сделают реальные пользователи.\n\nНесколько разных форм тестирования, каждая отвечает на свой вопрос:\n- **Load testing**: симулирует ожидаемую обычную-пиковую нагрузку (например, типичный объём в \"чёрную пятницу\") и проверяет, что латентность/частота ошибок остаются приемлемыми.\n- **Stress testing**: толкает объём далеко за ожидаемый пик, чтобы найти реальную точку отказа и понять, *как именно* система ломается — деградирует ли она плавно (более медленные ответы, аккуратный отказ) или ломается катастрофически (крэши, каскадные отказы у зависимых сервисов)?\n- **Soak testing**: поддерживает умеренную нагрузку в течение длительного времени (часы), чтобы поймать проблемы, проявляющиеся только со временем — утечки памяти, утечки пула соединений, постепенное исчерпание ресурсов, которое короткий тест бы не выявил.\n\n```javascript\n// Скрипт k6: наращивает виртуальных пользователей и проверяет пороги латентности/ошибок\nimport http from 'k6/http';\nimport { check } from 'k6';\n\nexport const options = {\n  stages: [\n    { duration: '2m', target: 100 },  // наращивание до 100 конкурентных пользователей\n    { duration: '5m', target: 100 },  // удержание\n    { duration: '2m', target: 0 },    // снижение\n  ],\n  thresholds: {\n    http_req_duration: ['p(95)<500'], // 95% запросов быстрее 500мс\n    http_req_failed: ['rate<0.01'],   // частота ошибок ниже 1%\n  },\n};\n\nexport default function () {\n  const res = http.get('https://api.example.com/orders');\n  check(res, { 'status is 200': (r) => r.status === 200 });\n}\n```\n\nЧто реально важно измерять: перцентили латентности (p95/p99, а не среднее — среднее как раз скрывает ту \"хвостовую\" латентность, которую реальные пользователи ощущают как \"приложение тормозит\"), частоту ошибок под нагрузкой и throughput (запросов в секунду, которое система способна выдерживать до того, как латентность или ошибки начнут расти). Не менее важно, чем сами метрики, — наблюдать, *где именно* система реально упирается в узкое место: CPU, соединения с БД, собственный rate limit внешнего API, синхронный вызов, который стоило бы поставить в очередь — именно это превращает нагрузочный тест из \"подтвердили, что тормозит\" в \"нашли реальный фикс\".\n\n**Частая ловушка:** Запускать нагрузочный тест против staging-окружения, устроенного совсем иначе, чем прод (меньший размер инстанса, нет read-реплик, другая конфигурация пула соединений), и воспринимать результаты как показательные — цифры из несоответствующего окружения почти ничего не говорят о том, как прод реально поведёт себя под той же нагрузкой."
    },
    "tags": [
      "load-testing",
      "stress-testing",
      "k6",
      "performance-under-load"
    ]
  }
]