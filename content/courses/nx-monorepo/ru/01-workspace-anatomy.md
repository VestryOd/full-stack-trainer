# Анатомия workspace

## Теория

### nx.json — конфигурация самого Nx

`nx.json` в корне — единственный файл, который описывает поведение *самого Nx* (а не отдельных проектов). Три ключевых блока, с которыми вы будете сталкиваться постоянно:

- **`plugins`** — список зарегистрированных плагинов с опциями. Это не просто "установленные пакеты": каждая запись здесь означает "этот плагин сканирует репу и *выводит* targets из конфигов инструментов". Запись `@nx/vite/plugin` — причина, по которой у проекта с `vite.config.ts` есть `build` и `serve`, хотя в его project.json пусто.
- **`targetDefaults`** — дефолты для одноимённых target-ов всех проектов: `"build": { "dependsOn": ["^build"], "cache": true }` означает "любой build сначала собирает зависимости и кешируется". Разберём в главе 03.
- **`namedInputs`** — именованные наборы файлов (`default`, `production`, `sharedGlobals`), от которых зависит хеш кеша. Центральная тема главы 04.

Ещё встречаются `generators` (дефолтные опции генераторов — чтобы каждый `nx g @nx/react:lib` в этой репе по умолчанию получал нужный bundler и линтер), `defaultBase` (для affected) и `release` (версионирование/публикация).

### Что вообще делает папку "проектом"

Nx не сканирует "всё подряд": узлы графа появляются из трёх источников. Папка становится проектом, если в ней есть **`project.json`**, или **`package.json`** (классика package-based), или её "усыновил" плагин из `nx.json`, чей `createNodes` сматчился на конфиг инструмента (так `@nx/playwright` создаёт e2e-проект из `playwright.config.ts`). Понимание этого списка снимает главную растерянность в чужой репе: "почему `nx show projects` показывает проект, у которого нет project.json?"

### project.json vs inferred targets: до и после crystal

Это самое крупное поведенческое изменение в истории Nx, и в рабочих репах вы встретите обе эпохи:

- **Старый мир (до Nx 17.2)**: у каждого проекта — "толстый" `project.json`, где каждый target прописан руками: executor, options, configurations, outputs. Десятки строк на проект, дублирование по всей репе, и правка "поменять outDir всем" — это массовый find-and-replace.
- **Project crystal (Nx 18+, мейнстрим с 19)**: targets *выводятся* (inferred) плагинами из конфигов самих инструментов. Есть `vite.config.ts` — плагин `@nx/vite` даёт проекту `build`, `serve`, `preview`, `test`, прочитав опции прямо из этого конфига. `project.json` худеет до имени, типа и tags — или исчезает совсем.

Итоговая конфигурация target-а собирается из трёх слоёв — и в незнакомой репе важно понимать, какой слой "победил":

```
              приоритет: выше = сильнее
┌────────────────────────────────────────────────────┐
│ 3. project.json проекта (и ключ "nx"               │
│    в package.json) — точечные переопределения      │
├────────────────────────────────────────────────────┤
│ 2. nx.json: targetDefaults — дефолты для всех      │
│    одноимённых target-ов workspace                 │
├────────────────────────────────────────────────────┤
│ 1. inferred targets от плагинов (crystal):         │
│    vite.config.ts → build / serve / preview / test │
└────────────────────────────────────────────────────┘
          итог: nx show project shell --web
```

Инструмент, который делает эти слои видимыми, — **`nx show project shell --web`**: интерактивная страница со ВСЕМИ target-ами проекта, где у каждой опции подписан источник (какой плагин или файл её принёс). Без `--web` та же команда печатает итоговый JSON в терминал. Это ответ на вопрос "что вообще запустится при `nx build shell`" — и им мы будем пользоваться в каждой главе.

> **Версии.** Имена inferred target-ов — не константы Nx, а опции плагина в `nx.json`: `"serveTargetName": "serve"`. В одних репах dev-сервер — это `nx serve app`, в других — `nx dev app`, потому что кто-то (или другая версия генератора) назвал target иначе. Прежде чем жаловаться, что "команда из туториала не работает", посмотрите `plugins` в nx.json своей репы.

### apps/ vs libs/ — почему такое деление

Конвенция Nx: **приложения тонкие, библиотеки толстые** (ориентир — 20% кода в apps, 80% в libs). Приложение — это точка сборки и деплоя: роутинг, композиция, конфигурация. Вся логика, UI-компоненты и работа с данными живут в libs — даже если приложение одно. Причины прагматичные: только у libs бывают границы (`tags` + module boundaries, глава 06), только мелкие libs дают точный affected (изменил `catalog/data-access` — тесты `checkout` не запускаются), и только вынесенный в lib код можно переиспользовать в будущем втором приложении — а в этом курсе их будет четыре. Сами имена папок — конвенция: `workspaceLayout` в nx.json может их поменять, а новые preset-ы используют `packages/`.

## В рабочем монорепо

- `npx nx show projects` — плоский список всех проектов репы. Быстрее, чем `nx graph`, когда нужно просто узнать имена и масштаб.
- `npx nx show project <имя> --web` — главная команда главы: все реальные targets проекта и источник каждой опции. Ищите подписи "inferred by @nx/..." против "from project.json".
- `cat nx.json | python3 -m json.tool` — блок `plugins` есть и непустой → репа на crystal, конфиги инструментов = источник правды. Блока нет, а project.json толстые → старый стиль, правда живёт в project.json.
- `find . -name project.json -not -path "*/node_modules/*" -exec wc -l {} +` — толщина project.json по всей репе за секунду: файлы по 10 строк — crystal, по 100+ — явные executors.
- `npx nx list @nx/react` — какие генераторы и executors даёт установленный плагин; работает для любого `@nx/*`.

## Что добавляем в проект

Генерируем первое приложение — `shell`, будущий host микрофронтенда (глав 10–11). Пока это обычное React-приложение на Vite; заодно workspace получает первые плагины в `nx.json` и `tsconfig.base.json`.

## Практическое задание

**Вход:** workspace `mini-shop` из главы 00 (пустой, один коммит).

**Задача:**

1. Добавить плагин React командой `npx nx add @nx/react` и посмотреть diff: что изменилось в `package.json` и `nx.json`?
2. Сгенерировать приложение `shell` в `apps/shell`: bundler — vite, стили — css, тесты — vitest, e2e — не нужен. Сначала прогнать генератор с `--dry-run` и прочитать список файлов, потом — по-настоящему.
3. Изучить результат и письменно ответить:
   - Сколько target-ов задекларировано в `apps/shell/project.json` — и сколько показывает `nx show project shell`? Откуда взялась разница?
   - Что появилось в `plugins` в nx.json и какие имена target-ов там заданы?
   - Почему `tsconfig.base.json` появился в корне только сейчас, а не в главе 00?
4. Запустить `nx serve shell` (проверить в браузере) и `nx build shell`; найти на диске артефакт сборки и файл, в котором задан его путь.

**Edge cases на подумать:**

- Что произойдёт при повторном запуске того же генератора? А с флагом `--dry-run`?
- Как переименовать target `serve` в `dev` для всех vite-проектов сразу, не трогая ни один project.json?
- Почему `nx add @nx/react` лучше, чем `npm install -D @nx/react` вручную?

## Разбор решения

Установка плагина и генерация:

```bash
npx nx add @nx/react
# ставит @nx/react ТОЧНО той же версии, что nx, и запускает его init-генератор

npx nx g @nx/react:app shell --directory=apps/shell --dry-run
# CREATE apps/shell/project.json ... — только план, диск не тронут

npx nx g @nx/react:app shell --directory=apps/shell \
  --bundler=vite --style=css --unitTestRunner=vitest \
  --e2eTestRunner=none --linter=eslint
```

> **Версии.** С Nx 18 путь проекта задаётся как есть (`--directory=apps/shell` — буквально эта папка); в старых версиях (`projectNameAndRootFormat=derived`) Nx сам конструировал путь из имени и вложенных папок, и та же команда могла положить проект в другое место. `nx add` появился в Nx 17 — до него плагин ставили руками, и главный риск был поставить версию, не совпадающую с ядром (глава 00).

Что появилось (плюс мелочи вроде favicon — состав зависит от версии):

```
mini-shop/
├── apps/shell/
│   ├── index.html            # dev-вход vite
│   ├── project.json          # ТОНКИЙ: имя, тип, tags — targets нет
│   ├── vite.config.ts        # источник правды для build/serve/test
│   ├── tsconfig.json         # + tsconfig.app.json, tsconfig.spec.json
│   └── src/
│       ├── main.tsx          # bootstrap: createRoot().render(<App/>)
│       ├── styles.css
│       └── app/
│           ├── app.tsx
│           └── app.spec.tsx  # vitest, гоняется тем же vite.config.ts
├── tsconfig.base.json        # появился с первым TS-проектом
└── eslint.config.mjs         # в новых версиях — flat config
```

`apps/shell/project.json` — весь, целиком:

```json
{
  "name": "shell",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/shell/src",
  "tags": []
}
```

Target-ов здесь **ноль**. При этом:

```bash
npx nx show project shell
# {
#   "name": "shell",
#   "targets": {
#     "build":   { "command": "vite build", ... },
#     "serve":   { "command": "vite serve", ... },
#     "preview": { "command": "vite preview", ... },
#     "test":    { "command": "vitest", ... },
#     "lint":    { "command": "eslint .", ... }
#   }
# }
```

Все пять target-ов — inferred: их принесли плагины, зарегистрированные `nx add` в nx.json. Обратите внимание: crystal-таргеты запускают **CLI самого инструмента** (`vite build` — та же команда, что вы набрали бы руками), Nx добавляет вокруг неё граф, кеш и pipeline; как это устроено — глава 03.

```json
{
  "plugins": [
    {
      "plugin": "@nx/vite/plugin",
      "options": {
        "buildTargetName": "build",
        "serveTargetName": "serve",
        "previewTargetName": "preview",
        "testTargetName": "test"
      }
    },
    { "plugin": "@nx/eslint/plugin", "options": { "targetName": "lint" } }
  ],
  "generators": {
    "@nx/react": { "application": { "babel": true, "style": "css", "linter": "eslint", "bundler": "vite" } }
  }
}
```

Ключевые решения:

- Ответ на edge case про переименование: поменять `"serveTargetName": "serve"` на `"dev"` в этом блоке — и у **всех** vite-проектов репы target станет `nx dev <проект>`. Ни один файл проекта не трогается — вот что значит "target-ы принадлежат плагину, а не проекту".
- `generators."@nx/react"` — записанные ответы визарда: следующий `nx g @nx/react:lib` в этой репе молча возьмёт те же опции. Так команда получает единообразные проекты без "у Пети lib на styled-components, у Маши на css".
- `tsconfig.base.json` появился только сейчас, потому что его создаёт init-генератор первого JS/TS-плагина, а не `create-nx-workspace`: пустому workspace он не нужен. Пока в нём нет `paths` — первый алиас появится с первой либой в главе 02.

Запуск и сборка:

```bash
npx nx serve shell     # vite dev-server, по умолчанию http://localhost:4200
npx nx build shell     # артефакт: dist/apps/shell/
```

Путь артефакта задан не "где-то в Nx", а в `apps/shell/vite.config.ts` → `build.outDir` — плагин прочитал его оттуда и сообщил Nx как output этого target-а (важно для кеша, глава 04). Повторный запуск генератора поверх существующего проекта упадёт на конфликте имён — генераторы идемпотентности не обещают, для проверки "что изменится" и существует `--dry-run`.

## Проверь себя

1. В project.json проекта нет ни одного target-а, но `nx build` для него работает. Объясни механику: кто и из чего создал этот target?
2. В каком порядке сливаются три слоя конфигурации target-а и какой побеждает? Где это увидеть для конкретного проекта?
3. Почему "тонкие apps, толстые libs" — это не эстетика, а требование механики Nx? Назови минимум две фичи, которые деградируют, если вся логика лежит в приложении.
4. Какие три вещи делают папку "проектом" в глазах Nx? Почему `nx show projects` может показывать больше проектов, чем есть файлов project.json?
5. В одной репе dev-сервер запускается `nx serve app`, в другой — `nx dev app`. Где физически лежит причина различия и что это говорит о природе имён target-ов?

<details>
<summary>Ответы</summary>

1. Плагин из `nx.json` (например, `@nx/vite/plugin`) при построении графа сканирует репу своим `createNodes`, находит `vite.config.ts`, читает его и *выводит* target-ы (`build`, `serve`, ...) с уже заполненными опциями. Target существует только в памяти графа — в файлах проекта его нет; конфиг инструмента и есть источник правды. Поэтому правка `vite.config.ts` меняет поведение `nx build` без единого изменения в project.json.
2. Снизу вверх: inferred targets плагинов → поверх них `targetDefaults` из nx.json → поверх точечные переопределения из project.json (или ключа `"nx"` в package.json проекта). Побеждает project.json. Итоговый результат слияния показывает `nx show project <имя>` (с `--web` — ещё и источник каждой опции).
3. Во-первых, affected: если вся логика в одном app, любое изменение затрагивает "весь app" — пересобирается и перетестируется всё, гранулярность теряется. Во-вторых, module boundaries: tags и boundary-правила (глава 06) вешаются на библиотеки — монолитному app нечего ограничивать. В-третьих, переиспользование: код из app нельзя импортировать в другое приложение (и в этом курсе их будет четыре), из lib — можно.
4. `project.json` в папке, `package.json` в папке, либо плагин из nx.json, чей `createNodes` сматчился на конфиг инструмента в этой папке. Третий источник и объясняет "лишние" проекты: например, e2e-проект, созданный `@nx/playwright` из одного `playwright.config.ts`, — файла project.json у него может не быть.
5. В nx.json, в опциях плагина: `"serveTargetName": "serve"` или `"dev"`. Имена target-ов — не зашитые в Nx константы, а конфигурируемые опции конкретной репы; поэтому первый рефлекс в чужом workspace — не гуглить "правильную" команду, а посмотреть `plugins` в nx.json или `nx show project <имя>`.

</details>

## Частая ошибка

Разработчик из single-app мира открывает `apps/shell/project.json`, видит пять строк без единого target-а — и делает вывод, что "конфигурация где-то прячется, Nx — магия". Дальше два плохих сценария. Первый: он находит в интернете статью 2022 года (мир до crystal) и вписывает в project.json полный target `build` с executor и options — теперь в репе **два источника правды**: его ручной target молча перекрывает inferred, и правки vite.config.ts частично перестают влиять на сборку. Второй: он правит `vite.config.ts`, не зная, что конкретная опция переопределена в `targetDefaults`, и не понимает, почему ничего не меняется. Лекарство одно и то же: `nx show project shell --web` — увидеть итоговую конфигурацию и *источник* каждой опции, прежде чем что-либо править.

Родственный рефлекс — создать новое приложение копированием папки существующего ("скопирую shell, переименую — быстрее, чем разбираться с генераторами"). В скопированном project.json остаётся старое имя (конфликт при построении графа), tags и алиасы едут, а половина файлов, которые генератор бы адаптировал, остаются от донора. Генератор с `--dry-run` покажет полный список того, что должно быть создано и изменено, — это и есть честный ответ на вопрос "что входит в новый проект".
