# Роутинг

## Теория

### Конфигурация маршрутов

Маршруты — обычный массив объектов, передаваемый в `provideRouter(routes)`:

```ts
export const routes: Routes = [
  { path: '', pathMatch: 'full', redirectTo: 'tickets' },
  {
    path: 'tickets',
    title: 'Tickets',                    // заголовок вкладки, ставит роутер
    loadComponent: () => import('./tickets/ticket-list').then((m) => m.TicketList),
    children: [
      {
        path: 'new',
        loadComponent: () => import('./tickets/ticket-form').then((m) => m.TicketForm),
      },
      {
        path: ':id',
        loadComponent: () => import('./tickets/ticket-detail').then((m) => m.TicketDetail),
      },
    ],
  },
  {
    path: 'admin',
    canMatch: [adminGuard],              // отсекает маршрут до загрузки чанка
    providers: [provideAdminTools()],    // свой EnvironmentInjector (глава 04)
    loadChildren: () => import('./admin/admin.routes').then((m) => m.ADMIN_ROUTES),
  },
  { path: '**', loadComponent: () => import('./core/not-found').then((m) => m.NotFound) },
];
```

Сразу стоит отметить три вещи:

- Порядок важен: маршруты проверяются сверху вниз, а `'**'` всегда идёт последним.
- `path: ':id'` идёт **после** `path: 'new'`, иначе `/tickets/new` совпадёт с параметром.
- `providers` на маршруте создаёт собственный инжектор, и живёт он столько же, сколько маршрут.

### Что происходит при навигации

```
Что происходит между кликом по ссылке и появлением экрана
┌───────────────────────────────────────────────────┐
│ URL: /admin/reports?range=7d                      │
└───────────────────────────────────────────────────┘
                          │  сопоставление
                          ▼
┌───────────────────────────────────────────────────┐
│ подбор маршрута сверху вниз + canMatch            │
│ false → маршрут пропускается, пробуем следующий   │
└───────────────────────────────────────────────────┘
                          │  маршрут найден
                          ▼
┌───────────────────────────────────────────────────┐
│ загрузка ленивого чанка                           │
│ loadComponent / loadChildren + providers маршрута │
└───────────────────────────────────────────────────┘
                          │  код загружен
                          ▼
┌───────────────────────────────────────────────────┐
│ canDeactivate текущего → canActivate нового       │
│ false → навигация отменяется, URL не меняется     │
└───────────────────────────────────────────────────┘
                          │  доступ есть
                          ▼
┌───────────────────────────────────────────────────┐
│ resolve: навигация ждёт данные                    │
│ ошибка → навигация падает                         │
└───────────────────────────────────────────────────┘
                          │  данные готовы
                          ▼
┌───────────────────────────────────────────────────┐
│ создание компонентов в router-outlet              │
│ входы заполняет withComponentInputBinding         │
└───────────────────────────────────────────────────┘
canMatch отсекает маршрут до загрузки чанка, canActivate — после:
для ленивых разделов с ролями это разница в мегабайтах
```

Навигация — не «показать компонент», а конвейер с несколькими точками отказа. Понимание порядка даёт две вещи. Вы знаете, куда вставлять проверку. И вы понимаете, почему «страница мигнула и вернулась»: гард отработал после начала навигации.

### Гарды

Четыре гарда и один резолвер, и различает их момент выполнения:

| хук | когда выполняется | что значит `false` | типичный случай |
|---|---|---|---|
| `canMatch` | при подборе маршрута | пробуем следующий маршрут | фича-флаг, роль |
| `canActivate` | после подбора, до `resolve` | навигация отменяется | требуется вход |
| `canActivateChild` | для каждого дочернего | навигация отменяется | защита раздела |
| `canDeactivate` | при уходе с маршрута | остаёмся на месте | несохранённая форма |
| `resolve` | после гардов, до компонента | ошибка навигации | данные до рендера |

Гард — обычная функция, выполняемая в контексте инъекции, поэтому внутри доступен `inject()`:

```ts
export const authGuard: CanActivateFn = (route, state) => {
  const auth = inject(AuthService);
  const router = inject(Router);

  if (auth.isLoggedIn()) return true;

  // вернуть можно boolean, UrlTree или RedirectCommand — последний
  // позволяет задать ещё и параметры навигации
  return new RedirectCommand(router.parseUrl('/login'), {
    state: { returnTo: state.url },
  });
};
```

Ключевое различие, которое спрашивают на собеседованиях: **`canMatch` работает на этапе сопоставления, `canActivate` — после**. Практических следствий два.

Первое: `canMatch: false` не отменяет навигацию, а заставляет роутер продолжить перебор. По одному пути можно показать разные компоненты разным ролям.

Второе: `canMatch` отсекает маршрут **до** загрузки ленивого чанка (отдельного файла сборки, который скачивается по требованию), а `canActivate` — уже после. Для админ-раздела это разница между «пользователь не скачал админку» и «скачал, но не увидел».

Старый `canLoad` помечен deprecated в пользу `canMatch`; классовые гарды (`implements CanActivate`) — тоже.

### Чтение состояния маршрута

Прочитать состояние маршрута можно четырьмя способами:

| способ | что даёт | когда брать |
|---|---|---|
| `input()` + `withComponentInputBinding` | сигнал, роутер обновляет сам | в компоненте — по умолчанию |
| `toSignal(route.params)` | сигнал из Observable | в сервисе или гарде |
| `route.snapshot.paramMap` | значение на момент создания | одноразовое чтение |
| `route.params.subscribe` | поток изменений | старый код, глава 09 |

Снимок не обновляется при смене `:id`, если компонент переиспользован. Это источник бага «открыл другой тикет, а данные прежние».

Современный путь — `withComponentInputBinding()`: роутер сам подставляет параметры маршрута, query-параметры и `data` во **входы** компонента, по совпадению имён.

```ts
provideRouter(routes, withComponentInputBinding());

// компонент на маршруте /tickets/:id?tab=history
export class TicketDetail {
  readonly id = input.required<string>();       // из параметра маршрута
  readonly tab = input('details');              // из query-параметра
}
```

Никакого `ActivatedRoute`, никаких подписок, и входы обновляются при смене параметра — даже если компонент переиспользуется. Собственного сигнального API у `ActivatedRoute` нет: вне компонентов (в сервисе, гарде) параметры превращают в сигнал через `toSignal(route.params, { requireSync: true })`.

Отдельно про `snapshot`: он валиден на момент создания компонента. При навигации с `/tickets/1` на `/tickets/2` роутер переиспользует тот же инстанс, потому что меняется только параметр. Значит, `snapshot` останется прежним, а `ngOnInit` повторно не вызовется. Это самая частая ловушка Angular-роутинга.

### Ленивая загрузка

`loadComponent` грузит один компонент, `loadChildren` — набор маршрутов (обычно файл `feature.routes.ts`, экспортирующий `Routes`). Оба принимают функцию с динамическим `import()`, поэтому сборщик автоматически выделяет чанк.

Предзагрузка настраивается фичей. `withPreloading(PreloadAllModules)` тянет все ленивые чанки после старта приложения. Своя стратегия может быть уже: например, грузить только маршруты, помеченные `data: { preload: true }`.

`provideRouter` принимает необязательные фичи. Две из них включены в проекте этой главы:

| фича | что делает |
|---|---|
| `withComponentInputBinding()` | подставляет параметры маршрута, query-параметры и `data` во входы компонентов |
| `withInMemoryScrolling({ … })` | восстанавливает позицию скролла и включает переход к якорям |

Остальные берут по случаю, каждая — одна строка конфигурации:

| фича | когда нужна |
|---|---|
| `withViewTransitions()` | анимация переходов через View Transitions API |
| `withRouterConfig({ onSameUrlNavigation: 'reload' })` | повторная навигация на тот же URL должна проходить заново |
| `withHashLocation()` | хостинг не умеет отдавать `index.html` на любой путь, поэтому URL становятся hash-адресами |
| `withNavigationErrorHandler(fn)` | одно место, где логируют или редиректят при ошибке навигации |
| `withDebugTracing()` | вывод каждого шага навигации в консоль при отладке |

В v22 добавились две экспериментальные фичи. `withExperimentalPlatformNavigation()` интегрируется с браузерным Navigation API, а `withExperimentalAutoCleanupInjectors` сам уничтожает неиспользуемые маршрутные инжекторы.

### Резолверы: нужны ли

`ResolveFn` задерживает навигацию, пока данные не готовы. Плюс — компонент открывается уже с данными и не мигает пустотой. Минусов три:

- Пользователь смотрит на старую страницу, не понимая, что происходит.
- Ошибка резолвера ломает навигацию целиком.
- Данные приходят в `data`, а не в сигналы, и обновлять их потом нечем.

Практическая позиция: резолвер уместен для того, что действительно должно быть готово до рендера. Например: проверить существование сущности и увести на 404, подставить `title`, дозагрузить конфиг раздела. Данные самого экрана лучше грузить в компоненте через `httpResource` (глава 08). Тогда состояние загрузки и ошибки становится частью UI (user interface, пользовательского интерфейса), а не навигации.

## Параллели с React

- **Роутер — часть фреймворка.** В React вы выбираете библиотеку и версию, и API между мажорами меняется заметно. В Angular роутер один, обновляется вместе с фреймворком и в `ng update` мигрируется схематиками. Меньше свободы, но и меньше решений: конфигурация маршрутов в проекте выглядит одинаково.
- **Гарды против компонентов-обёрток.** Типичный React-приём — `<RequireAuth><Dashboard/></RequireAuth>`: проверка выполняется **во время рендера**, то есть компонент уже смонтирован, а чанк уже загружен. Гард в Angular выполняется до создания компонента, а `canMatch` — вообще до загрузки кода. Практическая разница: в React защита от «случайно скачал админку» требует отдельной работы, в Angular это одна строка конфигурации.
- **Резолвер против `loader` в data-роутере React.** Идея та же — данные до рендера. Но в React Router `loader` — основной способ загрузки данных. В Angular резолвер конкурирует с `httpResource`, который отдаёт `loading`/`error` как сигналы. Поэтому в Angular резолверы стоит применять реже, чем `loader` в React.
- **Параметры маршрута.** `useParams()` возвращает значения на каждом рендере, и React-компонент обычно перемонтируется при смене ключа. В Angular компонент переиспользуется, а входы обновляются. Аналог `key` не нужен. Но нужно писать код, реагирующий на изменение входа: `computed`, а не разовое чтение в `ngOnInit`.
- **Где ломается привычка:** ожидание, что при смене `/tickets/1` → `/tickets/2` экран «пересоздастся». Он не пересоздаётся: тот же инстанс, тот же `snapshot`, `ngOnInit` не вызывается. Код, написанный по React-привычке «прочитать параметр при монтировании», молча показывает старые данные.

## Что увидишь в legacy-коде

- **`RouterModule.forRoot(routes)` / `forChild(routes)`** в NgModules и `loadChildren: './admin/admin.module#AdminModule'` строкой (до Angular 8). Сейчас — `provideRouter` и динамический `import()`.
- **Классовые гарды и резолверы:** `@Injectable({providedIn:'root'}) export class AuthGuard implements CanActivate` и `class TicketResolver implements Resolve<Ticket>`, перечисленные в маршруте как классы. Функциональные аналоги короче и не требуют регистрации.
- **`canLoad: [AuthGuard]`** — предшественник `canMatch`, помечен deprecated. Разница: `canLoad` только предотвращал загрузку чанка, `canMatch` ещё и позволяет продолжить перебор маршрутов.
- **`route.params.subscribe(...)` в `ngOnInit`** с ручной отпиской. И `route.snapshot.paramMap.get('id')` без реакции на изменения. Это два поколения решений одной задачи, которую сегодня закрывает `withComponentInputBinding`.
- **`data: { roles: ['admin'] }` + один универсальный гард**, читающий `route.data`. Приём рабочий. Но с функциональными гардами чаще пишут фабрику: `canActivate: [hasRole('admin')]`.
- **Кастомный `RouteReuseStrategy`** с `shouldDetach`/`shouldAttach` для «вкладок с сохранением состояния». В v22 для этого добавили официальный `destroyDetachedRouteHandle()`, потому что раньше отсоединённые view нередко утекали.

## Что добавляем в проект

Support Desk получает настоящую навигацию. Это список тикетов, детальная страница по `:id`, форма создания и ленивый админ-раздел, закрытый ролью через `canMatch`. Параметры приходят во входы компонентов, а уход с недозаполненной формы перехватывается `canDeactivate`.

## Практическое задание

**Вход:** проект из главы 06 (сторы, директива, пайп).
**Выход:** четыре маршрута, ленивый админ-раздел и две работающие защиты.

Требования:

1. Настройте `provideRouter` с `withComponentInputBinding()` и восстановлением позиции скролла. Маршруты: `/tickets` (список), `/tickets/new` (форма-заглушка), `/tickets/:id` (деталь), `/admin` (ленивый), `**` (404). Проверьте, что порядок `new` и `:id` имеет значение.
2. `TicketDetail` получает `id` **входом**, а не через `ActivatedRoute`. Тикет берётся из `TicketStore` по `id` через `computed`. Убедитесь, что при переходе с `/tickets/101` на `/tickets/102` содержимое меняется, и объясните, почему `ngOnInit` при этом не вызывается.
3. Админ-раздел: отдельный файл `admin.routes.ts` с двумя дочерними маршрутами, подключённый через `loadChildren`, и `canMatch`-гард по роли. Проверьте в DevTools (вкладка Network), что чанк админки не скачивается пользователем без роли.
4. `canDeactivate` для формы: если в форме есть несохранённые изменения, уход запрашивает подтверждение. Гард должен работать с типизированным компонентом, а не с `any`.
5. Резолвер: для `/tickets/:id` проверьте существование тикета и при отсутствии верните редирект на 404 (`RedirectCommand` или `UrlTree`). Обсудите в комментарии, почему сами данные тикета вы через резолвер **не** передаёте.
6. Ссылки: используйте `routerLink` с массивом сегментов и `routerLinkActive`; программную навигацию оставьте только там, где она действительно нужна (после сохранения формы).

Edge cases на подумать:

- Что произойдёт с `/tickets/new`, если поставить его после `/tickets/:id`?
- Компонент детали переиспользуется при смене `:id`. Что случится с состоянием, которое вы держите в его полях, и что — с `input()`?
- `canMatch` вернул `false`, а других подходящих маршрутов нет. Что увидит пользователь?
- Query-параметр `?tab=history` привязан ко входу `tab`. Что окажется во входе при `?tab=history&tab=notes`?
- Почему `providers` на маршруте админки — это не то же самое, что `providers` в корневом конфиге, и когда эти сервисы уничтожатся?

## Разбор решения

`src/app/app.routes.ts`:

```ts
import { Routes } from '@angular/router';
import { adminMatchGuard } from './admin/admin-guard';
import { ticketExistsGuard } from './tickets/ticket-exists-guard';
import { unsavedChangesGuard } from './core/unsaved-changes-guard';

export const routes: Routes = [
  { path: '', pathMatch: 'full', redirectTo: 'tickets' },
  {
    path: 'tickets',
    title: 'Tickets',
    loadComponent: () => import('./tickets/ticket-list').then((m) => m.TicketList),
  },
  {
    // Важно: статический сегмент объявляется раньше параметра,
    // иначе /tickets/new совпадёт с :id и id станет строкой "new"
    path: 'tickets/new',
    title: 'New ticket',
    loadComponent: () => import('./tickets/ticket-form').then((m) => m.TicketForm),
    canDeactivate: [unsavedChangesGuard],
  },
  {
    path: 'tickets/:id',
    loadComponent: () => import('./tickets/ticket-detail').then((m) => m.TicketDetail),
    // резолвер здесь только проверяет существование и уводит на 404;
    // сами данные компонент читает из стора (а в главе 08 — из httpResource)
    resolve: { exists: ticketExistsGuard },
  },
  {
    path: 'admin',
    // canMatch, а не canActivate: без роли маршрут не совпадёт вовсе,
    // и чанк админки не будет загружен
    canMatch: [adminMatchGuard],
    loadChildren: () => import('./admin/admin.routes').then((m) => m.ADMIN_ROUTES),
  },
  { path: '**', loadComponent: () => import('./core/not-found').then((m) => m.NotFound) },
];
```

Обратите внимание на форму: здесь маршруты — плоские соседи, а не вложенный `children` из блока теории. Детальная страница — самостоятельный экран, поэтому она не дочерний маршрут списка. Правило порядка работает в обеих формах: статический `tickets/new` объявлен раньше `tickets/:id`.

`src/app/app.config.ts`:

```ts
export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideRouter(
      routes,
      // роутер сам заполняет входы компонентов параметрами маршрута,
      // query-параметрами и data — по совпадению имён
      withComponentInputBinding(),
      withInMemoryScrolling({
        scrollPositionRestoration: 'enabled',
        anchorScrolling: 'enabled',
      }),
    ),
    ...provideAppConfig(),
    ...provideTicketRules(),
  ],
};
```

`src/app/tickets/ticket-detail.ts`:

```ts
import { Component, computed, inject, input, numberAttribute } from '@angular/core';
import { RouterLink } from '@angular/router';
import { TicketStore } from './ticket-store';

@Component({
  selector: 'app-ticket-detail',
  imports: [RouterLink],
  templateUrl: './ticket-detail.html',
})
export class TicketDetail {
  private readonly store = inject(TicketStore);

  // Вход заполняет роутер. transform: numberAttribute — потому что из URL
  // всё приходит строкой, а стор работает с числовым id
  readonly id = input.required({ transform: numberAttribute });
  // query-параметр ?tab=… попадает в одноимённый вход
  readonly tab = input<'details' | 'history'>('details');

  // Реакция на смену :id — не хук, а зависимость: computed пересчитается
  // сам, когда роутер обновит вход, даже если компонент переиспользован
  protected readonly ticket = computed(() =>
    this.store.tickets().find((t) => t.id === this.id()) ?? null,
  );
}
```

`src/app/tickets/ticket-exists-guard.ts` — резолвер с редиректом:

```ts
import { ResolveFn, RedirectCommand, Router } from '@angular/router';
import { inject } from '@angular/core';
import { TicketStore } from './ticket-store';

export const ticketExistsGuard: ResolveFn<boolean> = (route) => {
  const store = inject(TicketStore);
  const router = inject(Router);
  const id = Number(route.paramMap.get('id'));

  if (store.tickets().some((t) => t.id === id)) return true;

  // RedirectCommand вместо простого UrlTree: можно передать ещё и
  // параметры навигации (state, replaceUrl, skipLocationChange)
  return new RedirectCommand(router.parseUrl('/not-found'), {
    state: { reason: `Ticket ${id} not found` },
  });
};
```

`src/app/admin/admin-guard.ts` и `admin.routes.ts`:

```ts
export const adminMatchGuard: CanMatchFn = () => {
  const user = inject(CurrentUser);
  // false здесь означает "маршрут не подходит" — роутер продолжит перебор
  // и в итоге попадёт на '**'. Чанк админки при этом не загружается
  return user.roles().includes('admin');
};

// admin.routes.ts — отдельный файл, который и становится ленивым чанком
export const ADMIN_ROUTES: Routes = [
  {
    path: '',
    loadComponent: () => import('./admin-shell').then((m) => m.AdminShell),
    // providers на маршруте: сервисы живут, пока активен раздел,
    // и уничтожаются при уходе из него (глава 04)
    providers: [AdminMetrics],
    children: [
      { path: '', pathMatch: 'full', redirectTo: 'reports' },
      {
        path: 'reports',
        loadComponent: () => import('./admin-reports').then((m) => m.AdminReports),
      },
      {
        path: 'users',
        loadComponent: () => import('./admin-users').then((m) => m.AdminUsers),
      },
    ],
  },
];
```

`src/app/core/unsaved-changes-guard.ts` — типизированный `canDeactivate`:

```ts
// Контракт вместо any: гард работает с любым компонентом,
// который умеет ответить на вопрос "можно ли уходить"
export interface HasUnsavedChanges {
  readonly hasUnsavedChanges: Signal<boolean>;
}

export const unsavedChangesGuard: CanDeactivateFn<HasUnsavedChanges> = (component) => {
  if (!component.hasUnsavedChanges()) return true;
  return confirm('You have unsaved changes. Leave this page?');
};
```

Шаблон списка со ссылками:

```html
<a
  [routerLink]="['/tickets', ticket.id]"
  [queryParams]="{ tab: 'details' }"
  routerLinkActive="is-active"
>
  {{ ticket.title }}
</a>

<!-- router-outlet — место, куда роутер вставит компонент маршрута -->
<router-outlet />
```

Ответы на edge cases:

- `/tickets/new` совпадёт с `/tickets/:id`, и в `id` попадёт строка `"new"`. С `transform: numberAttribute` вы получите `NaN`, тикет не найдётся и экран покажет «не найдено». Ошибка проявится как «форма создания не открывается». Правило: статические сегменты объявляются раньше параметрических.
- Поля компонента сохранятся: инстанс тот же, конструктор и `ngOnInit` не выполняются повторно. `input()` обновится, и всё, что от него выведено через `computed`, пересчитается. Отсюда правило: состояние экрана, зависящее от параметра, должно быть выведено из входа, а не записано в поле при инициализации.
- Роутер продолжит перебор и дойдёт до `'**'` — пользователь увидит страницу 404, а не «доступ запрещён». Если нужен явный отказ, вместо `canMatch` берут `canActivate` с редиректом на `/forbidden`: это как раз пример, когда «отменить навигацию» лучше, чем «пропустить маршрут».
- При повторяющемся ключе значение будет одно, а не массив: последнее из строки запроса (`'notes'`). Фича берёт значения из `paramMap`-подобной структуры, а такая структура хранит по одному значению на ключ. Если нужен массив, читайте `queryParamMap.getAll('tab')` через `ActivatedRoute`.
- `providers` на маршруте создают **отдельный environment-инжектор** для этого маршрута. Сервисы создаются при входе в раздел и уничтожаются при выходе, а не живут всё время работы приложения. Это ровно тот случай, когда состояние должно жить дольше компонента, но короче приложения.

## Проверь себя

1. Объясните своими словами разницу между `canMatch` и `canActivate`. Затем приведите случай, когда неверный выбор ухудшает не безопасность, а размер загружаемого кода.
2. Почему при переходе `/tickets/1` → `/tickets/2` не срабатывает `ngOnInit`, и как правильно реагировать на смену параметра?
3. Что делает `withComponentInputBinding()` и почему после него `ActivatedRoute` в компонентах почти не нужен?
4. Когда резолвер оправдан, а когда данные лучше грузить в самом компоненте? Назовите два минуса резолвера.
5. Что произойдёт при `canMatch: false`, если ни один другой маршрут не подошёл, и чем это отличается от `canActivate: false`?

<details>
<summary>Ответы</summary>

1. `canMatch` выполняется на этапе сопоставления URL с маршрутами. Если он вернул `false`, маршрут считается неподходящим и роутер продолжает перебор: по одному пути можно показать разные компоненты разным ролям. Гард `canActivate` выполняется уже после того, как маршрут выбран, и `false` там отменяет навигацию целиком. Случай про размер кода — ленивый админ-раздел. Если закрыть его `canActivate`, роутер сначала выберет маршрут и **загрузит чанк** админки, и только потом проверит доступ. Обычный пользователь скачает код, который никогда не увидит. С `canMatch` маршрут отсеивается до загрузки, и чанк не запрашивается.
2. Роутер переиспользует инстанс компонента, когда маршрут тот же и изменился только параметр. Компонент не уничтожается и не создаётся заново, поэтому конструктор и `ngOnInit` не вызываются. Реагировать нужно не хуком, а зависимостью. Получите параметр как `input()` (с `withComponentInputBinding`) и выведите из него всё остальное через `computed`. Тогда при смене `:id` вход обновится, а зависимые вычисления пересчитаются автоматически. А `snapshot`-подход — «прочитал в `ngOnInit` и положил в поле» — покажет старые данные.
3. Он включает автоматическую привязку параметров маршрута, query-параметров и `data` к **входам** компонента по совпадению имён. Компоненту больше не нужно инжектить `ActivatedRoute`, подписываться на `params` и отписываться: вход — это сигнал, который роутер обновляет сам, и он работает даже при переиспользовании инстанса. `ActivatedRoute` остаётся нужен для нетипичных случаев: чтение `queryParamMap.getAll()`, доступ к родительским маршрутам, работа вне компонента (там параметры превращают в сигнал через `toSignal`).
4. Резолвер оправдан, когда что-то действительно обязано быть готово **до** рендера и влияет на саму навигацию. Например: проверка существования сущности с уходом на 404, подстановка заголовка, дозагрузка конфигурации раздела. Минусов два. Первый: навигация «висит». Пользователь остаётся на старой странице без индикации, пока резолвер не завершится, и чем медленнее сеть, тем сильнее ощущение зависания. Второй: ошибка резолвера ломает навигацию целиком. Состояние загрузки и ошибки нельзя показать в интерфейсе экрана, потому что оно оказывается за пределами компонента. Данные самого экрана лучше грузить в компоненте (`httpResource`, глава 08), где `loading` и `error` — часть UI.
5. Роутер продолжит перебор маршрутов и, не найдя совпадения, дойдёт до `'**'`. Пользователь увидит страницу «не найдено», при этом URL останется тем, который он запросил. При `canActivate: false` навигация отменяется. URL не меняется вовсе, пользователь остаётся на текущем экране — если гард не вернул явный редирект. Отсюда практический выбор. «Этого раздела для вас не существует» — это `canMatch`. «Раздел есть, но нужно войти» — это `canActivate` с редиректом на логин.

</details>

## Частая ошибка

Первая — чтение параметра из `snapshot` в `ngOnInit`. Код выглядит безупречно и работает при первом открытии: `const id = this.route.snapshot.paramMap.get('id')`, затем загрузка данных. Проблема всплывает, когда пользователь переходит с одного тикета на другой изнутри приложения — например, по ссылке «следующий».

Роутер переиспользует компонент, и `ngOnInit` не вызывается. `snapshot` остаётся прежним, поэтому на экране остаются данные предыдущего тикета при новом URL в адресной строке. Ошибка кажется мистической, потому что перезагрузка страницы «чинит» её.

Правильный путь — не разовое чтение, а зависимость. Возьмите `input()` с `withComponentInputBinding` плюс `computed` от него, и обновление будет приходить само.

Вторая — `canActivate` там, где нужен `canMatch`, для ленивых разделов. С точки зрения доступа обе защиты работают: пользователь без роли админку не увидит. Но с `canActivate` роутер сначала выбирает маршрут, **скачивает чанк** админ-раздела и только затем спрашивает гард. То есть код, который пользователь не имеет права выполнять, всё равно оказывается у него в браузере.

Кроме лишнего трафика это ещё и неверный сигнал: содержимое админки видно любому, кто откроет вкладку Network. Замена одной строки (`canActivate` → `canMatch`) убирает и загрузку, и утечку структуры раздела. При этом ни один гард не даёт границы безопасности: сервер обязан проверять права на каждый запрос сам.

Обратная сторона: `canMatch: false` приводит на 404, а не на «доступ запрещён». Поэтому там, где пользователю нужно объяснить причину, гарды комбинируют: `canMatch` по фича-флагу, `canActivate` с редиректом — по авторизации.
