[
  {
    "id": "javascript-quiz-1",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nconsole.log(typeof null);\nconsole.log(null instanceof Object);\n```",
      "ru": "Что будет выведено в консоль?\n```js\nconsole.log(typeof null);\nconsole.log(null instanceof Object);\n```"
    },
    "options": {
      "en": [
        "\"object\" then false",
        "\"object\" then true",
        "\"null\" then false",
        "\"null\" then true"
      ],
      "ru": [
        "\"object\" а затем false",
        "\"object\" а затем true",
        "\"null\" а затем false",
        "\"null\" а затем true"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "typeof null returns \"object\" due to a historical bug in JavaScript that was never fixed for compatibility reasons. However, null instanceof Object returns false because instanceof checks the prototype chain, and null has no prototype. This demonstrates the difference between typeof (which checks the type tag) and instanceof (which checks prototype chain inheritance).",
      "ru": "typeof null возвращает \"object\" из-за исторической ошибки в JavaScript, которая никогда не была исправлена для совместимости. Однако null instanceof Object возвращает false, потому что instanceof проверяет цепочку прототипов, а null не имеет прототипа. Это демонстрирует разницу между typeof (который проверяет тег типа) и instanceof (который проверяет наследование прототипов)."
    }
  },
  {
    "id": "javascript-quiz-2",
    "topicId": "javascript",
    "question": {
      "en": "What will be the output of this code?\n```js\nfunction outer() {\n  let x = 10;\n  function inner() {\n    console.log(x);\n  }\n  return inner;\n}\nconst fn = outer();\nlet x = 20;\nfn();\n```",
      "ru": "Какой будет результат выполнения этого кода?\n```js\nfunction outer() {\n  let x = 10;\n  function inner() {\n    console.log(x);\n  }\n  return inner;\n}\nconst fn = outer();\nlet x = 20;\nfn();\n```"
    },
    "options": {
      "en": [
        "20",
        "10",
        "undefined",
        "ReferenceError: x is not defined"
      ],
      "ru": [
        "20",
        "10",
        "undefined",
        "ReferenceError: x is not defined"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "This demonstrates closure: the inner function captures the variable x from the outer function's scope at the time of creation, not at execution time. Even though a new variable x with value 20 is declared later in the global scope, inner still references the x from outer's scope (value 10). The closure maintains its reference to the original lexical environment.",
      "ru": "Это демонстрирует замыкание: функция inner захватывает переменную x из области видимости функции outer в момент создания, а не в момент выполнения. Несмотря на то, что позже в глобальной области видимости объявлена новая переменная x со значением 20, inner все еще ссылается на x из области видимости outer (значение 10). Замыкание сохраняет ссылку на исходную лексическую среду."
    }
  },
  {
    "id": "javascript-quiz-3",
    "topicId": "javascript",
    "question": {
      "en": "When studying the event loop, which of the following correctly describes the order of execution in JavaScript?",
      "ru": "При изучении цикла событий, какое из следующих утверждений правильно описывает порядок выполнения в JavaScript?"
    },
    "options": {
      "en": [
        "Macrotasks (setTimeout) execute before microtasks (Promise.then), as they are prioritized by the event loop",
        "Microtasks and macrotasks are executed in the order they were added to the queue, with no priority difference",
        "Microtasks (Promise.then) execute before macrotasks (setTimeout), and both are processed after the call stack is empty",
        "All async operations are queued as macrotasks regardless of whether they're Promises or timers"
      ],
      "ru": [
        "Макротаски (setTimeout) выполняются перед микротасками (Promise.then), так как они приоритизируются циклом событий",
        "Микротаски и макротаски выполняются в порядке, в котором они были добавлены в очередь, без разницы в приоритете",
        "Микротаски (Promise.then) выполняются перед макротасками (setTimeout), и оба обрабатываются после очистки стека вызовов",
        "Все асинхронные операции ставятся в очередь как макротаски независимо от того, Promise это или таймер"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The event loop processes the call stack first, then all microtasks (Promise.then, queueMicrotask), and only then moves to macrotasks (setTimeout, setInterval). This is crucial for understanding async behavior in JavaScript—promises resolve before setTimeout callbacks execute, even if setTimeout has 0 delay.",
      "ru": "Цикл событий сначала обрабатывает стек вызовов, затем все микротаски (Promise.then, queueMicrotask), и только потом переходит к макротаскам (setTimeout, setInterval). Это критически важно для понимания асинхронного поведения в JavaScript—промисы разрешаются раньше, чем выполняются обработчики setTimeout, даже если setTimeout имеет задержку 0."
    }
  },
  {
    "id": "javascript-quiz-4",
    "topicId": "javascript",
    "question": {
      "en": "What concept allows a generator function to pause execution and resume later with the ability to yield intermediate values?",
      "ru": "Какая концепция позволяет функции-генератору приостановить выполнение и возобновить его позже с возможностью выдачи промежуточных значений?"
    },
    "options": {
      "en": [
        "Async functions that automatically pause at await points and handle Promise resolution internally",
        "The prototype chain, which allows methods to be inherited and executed in sequence",
        "Hoisting, which moves function declarations to the top of their scope for controlled execution",
        "Generators with the yield keyword, which return an iterator object that can be controlled via .next()"
      ],
      "ru": [
        "Асинхронные функции, которые автоматически приостанавливаются в точках await и внутренне обрабатывают разрешение Promise",
        "Цепочка прототипов, которая позволяет методам наследоваться и выполняться последовательно",
        "Всплытие, которое перемещает объявления функций в начало их области видимости для управляемого выполнения",
        "Генераторы с ключевым словом yield, которые возвращают объект итератора, управляемый через .next()"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Generators are functions declared with function* that use yield to pause execution and return an iterator. Each call to .next() resumes from the last yield point and executes until the next yield or return. This enables lazy evaluation and fine-grained control over function execution flow, distinct from async/await which is specifically designed for Promise handling.",
      "ru": "Генераторы—это функции, объявленные с function*, которые используют yield для приостановки выполнения и возврата итератора. Каждый вызов .next() возобновляет выполнение с последней точки yield и выполняется до следующего yield или return. Это позволяет ленивое вычисление и точное управление потоком выполнения функции, в отличие от async/await, которые специально предназначены для обработки Promise."
    }
  },
  {
    "id": "javascript-quiz-5",
    "topicId": "javascript",
    "question": {
      "en": "In JavaScript coercion, what will be the result of this expression?\n```js\nconst result = \"5\" + 3 - 2;\nconsole.log(result);\nconsole.log(typeof result);\n```",
      "ru": "При преобразовании типов в JavaScript, какой будет результат этого выражения?\n```js\nconst result = \"5\" + 3 - 2;\nconsole.log(result);\nconsole.log(typeof result);\n```"
    },
    "options": {
      "en": [
        "6 and \"number\"",
        "51 and \"string\"",
        "\"51\" and \"string\"",
        "NaN and \"number\""
      ],
      "ru": [
        "6 и \"number\"",
        "51 и \"string\"",
        "\"51\" и \"string\"",
        "NaN и \"number\""
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The expression evaluates left to right: \"5\" + 3 uses string concatenation (+ operator prefers string coercion), resulting in \"53\" (a string). Then \"53\" - 2 uses numeric coercion for the minus operator, converting \"53\" to 53 and subtracting 2 to get 51 as a number. The minus operator always coerces to numbers, unlike plus which coerces to strings when one operand is a string.",
      "ru": "Выражение вычисляется слева направо: \"5\" + 3 использует конкатенацию строк (оператор + предпочитает преобразование в строку), результатом является \"53\" (строка). Затем \"53\" - 2 использует числовое преобразование для оператора минус, преобразуя \"53\" в 53 и вычитая 2 для получения 51 в виде числа. Оператор минус всегда преобразует в числа, в отличие от плюса, который преобразует в строки, если один из операндов является строкой."
    }
  },
  {
    "id": "javascript-quiz-6",
    "topicId": "javascript",
    "question": {
      "en": "What is logged to the console?\n```js\nconst obj = { value: 42 };\nconst fn = function() { return this.value; };\nconst boundFn = fn.bind({ value: 100 });\nconst result = boundFn.call({ value: 200 });\nconsole.log(result);\n```",
      "ru": "Что выводится в консоль?\n```js\nconst obj = { value: 42 };\nconst fn = function() { return this.value; };\nconst boundFn = fn.bind({ value: 100 });\nconst result = boundFn.call({ value: 200 });\nconsole.log(result);\n```"
    },
    "options": {
      "en": [
        "200",
        "100",
        "42",
        "undefined"
      ],
      "ru": [
        "200",
        "100",
        "42",
        "undefined"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Once a function is bound with bind(), its this context is permanently fixed and cannot be changed by call() or apply(). The boundFn is bound to { value: 100 }, so even though call() tries to set this to { value: 200 }, the bound context takes precedence and returns 100.",
      "ru": "После использования bind(), контекст this функции фиксируется и не может быть изменён с помощью call() или apply(). boundFn привязана к объекту { value: 100 }, поэтому несмотря на попытку call() установить this на { value: 200 }, привязанный контекст имеет приоритет и возвращает 100."
    }
  },
  {
    "id": "javascript-quiz-7",
    "topicId": "javascript",
    "question": {
      "en": "What is the output of this code?\n```js\nfor (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 0);\n}\n```",
      "ru": "Какой результат выполнения этого кода?\n```js\nfor (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 0);\n}\n```"
    },
    "options": {
      "en": [
        "0, 1, 2",
        "undefined, undefined, undefined",
        "3, 3, 3",
        "1, 2, 3"
      ],
      "ru": [
        "0, 1, 2",
        "undefined, undefined, undefined",
        "3, 3, 3",
        "1, 2, 3"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "var is function-scoped, not block-scoped. By the time the setTimeout callbacks execute (after the loop completes), i has already been incremented to 3. All three callbacks reference the same i variable, so they all log 3. This is a classic closure issue that let would solve by creating a new binding for each iteration.",
      "ru": "var имеет функциональную область видимости, а не блочную. К моменту выполнения обратных вызовов setTimeout (после завершения цикла) переменная i уже увеличена до 3. Все три обратных вызова ссылаются на одну и ту же переменную i, поэтому все выводят 3. Это классическая проблема замыканий, которую решит использование let с созданием новой привязки для каждой итерации."
    }
  },
  {
    "id": "javascript-quiz-8",
    "topicId": "javascript",
    "question": {
      "en": "Which statement correctly describes what happens when you use the spread operator on a string in ES6+?",
      "ru": "Какое утверждение правильно описывает, что происходит при использовании оператора распространения на строку в ES6+?"
    },
    "options": {
      "en": [
        "It converts the string to an array of individual characters, including surrogate pairs as single characters",
        "It returns the string unchanged because strings are immutable",
        "It throws a TypeError because spread operator only works with arrays",
        "It converts the string to an array of individual characters, treating surrogate pairs as individual code units"
      ],
      "ru": [
        "Он преобразует строку в массив отдельных символов, включая суррогатные пары как одиночные символы",
        "Он возвращает строку без изменений, потому что строки неизменяемы",
        "Он выбрасывает ошибку TypeError, потому что оператор spread работает только с массивами",
        "Он преобразует строку в массив отдельных символов, рассматривая суррогатные пары как отдельные кодовые единицы"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The spread operator iterates using the string's iterator, which splits the string by UTF-16 code units, not Unicode characters. This means emoji and other characters outside the Basic Multilingual Plane may be split across multiple array elements. The option claiming surrogate pairs are kept as single characters incorrectly suggests proper Unicode handling; the option saying the string is returned unchanged ignores that spread creates an array; the option predicting a TypeError is wrong because spread works on any iterable.",
      "ru": "Оператор распространения использует итератор строки, который разбивает строку по кодовым единицам UTF-16, а не по символам Unicode. Это означает, что emoji и другие символы вне Основной многоязычной плоскости могут быть разделены на несколько элементов массива. Вариант, где суррогатные пары остаются одиночными символами, неправильно предполагает правильную обработку Unicode; вариант, где строка возвращается без изменений, игнорирует, что spread создаёт массив; вариант с TypeError неверен, потому что spread работает с любым итерируемым объектом."
    }
  },
  {
    "id": "javascript-quiz-9",
    "topicId": "javascript",
    "question": {
      "en": "In JavaScript, what is the primary difference between how the prototype chain and the scope chain resolve property lookups?",
      "ru": "В JavaScript в чём основное различие между тем, как цепь прототипов и цепь области видимости разрешают поиск свойств?"
    },
    "options": {
      "en": [
        "The prototype chain searches upward through object inheritance, while the scope chain searches through lexical environments where the code was written",
        "The scope chain only applies to var/let/const variables, while the prototype chain applies to object properties and methods",
        "Both chains search in the same way; they just apply to different data types",
        "The scope chain uses === comparison while the prototype chain uses == comparison"
      ],
      "ru": [
        "Цепь прототипов ищет вверх через наследование объектов, в то время как цепь области видимости ищет в лексических окружениях, где был написан код",
        "Цепь области видимости применяется только к переменным var/let/const, в то время как цепь прототипов применяется к свойствам и методам объектов",
        "Обе цепи ищут одинаково; они просто применяются к разным типам данных",
        "Цепь области видимости использует сравнение ===, а цепь прототипов использует сравнение =="
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The scope chain resolves variable/identifier lookups based on where code is lexically written (lexical scoping), moving outward through parent scopes. The prototype chain resolves property lookups by traversing the [[Prototype]] link of objects at runtime. The option restricting the scope chain to var/let/const variables conflates different concepts; the option claiming both chains search in the same way ignores their fundamental differences; the option about === versus == is about equality operators, not lookup mechanisms.",
      "ru": "Цепь области видимости разрешает поиск переменных/идентификаторов на основе того, где код лексически написан (лексическое связывание), движась наружу через родительские области. Цепь прототипов разрешает поиск свойств путём обхода связи [[Prototype]] объектов во время выполнения. Вариант, ограничивающий цепь области видимости переменными var/let/const, путает разные концепции; вариант, утверждающий, что обе цепи ищут одинаково, игнорирует их фундаментальные различия; вариант про === и == относится к операторам равенства, а не к механизмам поиска."
    }
  },
  {
    "id": "javascript-quiz-10",
    "topicId": "javascript",
    "question": {
      "en": "What is logged?\n```js\nPromise.resolve()\n  .then(() => { throw new Error('A'); })\n  .catch(e => console.log('Caught:', e.message))\n  .then(() => { throw new Error('B'); })\n  .catch(e => console.log('Caught:', e.message))\n  .finally(() => console.log('Finally'));\n```",
      "ru": "Что выводится в консоль?\n```js\nPromise.resolve()\n  .then(() => { throw new Error('A'); })\n  .catch(e => console.log('Caught:', e.message))\n  .then(() => { throw new Error('B'); })\n  .catch(e => console.log('Caught:', e.message))\n  .finally(() => console.log('Finally'));\n```"
    },
    "options": {
      "en": [
        "Caught: A, Finally",
        "Caught: A, Caught: B, Finally",
        "Caught: A, Caught: B",
        "Caught: A, then an unhandled rejection for B"
      ],
      "ru": [
        "Caught: A, Finally",
        "Caught: A, Caught: B, Finally",
        "Caught: A, Caught: B",
        "Caught: A, затем необработанное отклонение для B"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The first then() throws error A, caught by the first catch() which logs and returns undefined (resolving the promise). The second then() executes and throws error B, which is caught by the second catch() and logged. Finally runs after all handlers. The error B is not unhandled because it's caught before finally executes.",
      "ru": "Первый then() выбрасывает ошибку A, которую перехватывает первый catch() и выводит результат, возвращая undefined (разрешая promise). Второй then() выполняется и выбрасывает ошибку B, которую перехватывает второй catch() и выводит. Finally выполняется после всех обработчиков. Ошибка B не остаётся необработанной, потому что она перехватывается перед выполнением finally."
    }
  },
  {
    "id": "javascript-quiz-12",
    "topicId": "javascript",
    "question": {
      "en": "What demonstrates the correct understanding of closure scope?\n```js\nconst functions = [];\nfor (var i = 0; i < 3; i++) {\n  functions.push(() => i);\n}\nconsole.log(functions[0](), functions[1](), functions[2]());\n```",
      "ru": "Что правильно демонстрирует понимание области действия замыкания?\n```js\nconst functions = [];\nfor (var i = 0; i < 3; i++) {\n  functions.push(() => i);\n}\nconsole.log(functions[0](), functions[1](), functions[2]());\n```"
    },
    "options": {
      "en": [
        "0, 1, 2 because each iteration captures i",
        "undefined, undefined, undefined because i is out of scope",
        "3, 3, 3 because all closures reference the same i variable",
        "0, 0, 0 because arrow functions create block scope"
      ],
      "ru": [
        "0, 1, 2 потому что каждая итерация захватывает i",
        "undefined, undefined, undefined потому что i вне области действия",
        "3, 3, 3 потому что все замыкания ссылаются на одну переменную i",
        "0, 0, 0 потому что стрелочные функции создают блочную область"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "All three arrow functions close over the same i variable (function scope, not block scope with var). By the time the functions are called, the loop has completed and i equals 3. All closures reference this final value. Using let instead of var would create a new binding per iteration. This is a fundamental closure and hoisting concept.",
      "ru": "Все три стрелочные функции ссылаются на одну и ту же переменную i (область функции, а не блочная область с var). К моменту вызова функций цикл завершён и i равна 3. Все замыкания ссылаются на это финальное значение. Использование let вместо var создало бы новую привязку на каждую итерацию."
    }
  },
  {
    "id": "javascript-quiz-13",
    "topicId": "javascript",
    "question": {
      "en": "Consider the event loop behavior with microtasks and macrotasks. What order will be logged?\n```js\nconsole.log('start');\nPromise.resolve().then(() => console.log('promise'));\nsetTimeout(() => console.log('timeout'), 0);\nconsole.log('end');\n```",
      "ru": "Рассмотрим поведение цикла событий с микротасками и макротасками. В каком порядке будет выведено?\n```js\nconsole.log('start');\nPromise.resolve().then(() => console.log('promise'));\nsetTimeout(() => console.log('timeout'), 0);\nconsole.log('end');\n```"
    },
    "options": {
      "en": [
        "start, promise, timeout, end",
        "start, end, timeout, promise",
        "start, timeout, promise, end",
        "start, end, promise, timeout"
      ],
      "ru": [
        "start, promise, timeout, end",
        "start, end, timeout, promise",
        "start, timeout, promise, end",
        "start, end, promise, timeout"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Synchronous code (start, end) executes first. Then the microtask queue (Promise callback) executes before the next macrotask. setTimeout is a macrotask that executes last. This order demonstrates the event loop priority: call stack → microtasks → macrotasks. Understanding this is critical for async behavior prediction.",
      "ru": "Синхронный код (start, end) выполняется первым. Затем очередь микротасок (обратный вызов Promise) выполняется перед следующей макротаской. setTimeout является макротаской, которая выполняется последней. Этот порядок демонстрирует приоритет цикла событий: стек вызовов → микротаски → макротаски."
    }
  },
  {
    "id": "javascript-quiz-14",
    "topicId": "javascript",
    "question": {
      "en": "Which statement correctly explains how `this` binding works with arrow functions versus regular functions?",
      "ru": "Какое утверждение правильно объясняет, как привязка `this` работает со стрелочными функциями в сравнении с обычными функциями?"
    },
    "options": {
      "en": [
        "Arrow functions inherit `this` from their enclosing lexical context and cannot be rebound",
        "Arrow functions have their own `this` binding that can be changed with .call() or .apply()",
        "Regular functions always have `this` bound to the global object",
        "Both arrow and regular functions have identical `this` binding behavior in all contexts"
      ],
      "ru": [
        "Стрелочные функции наследуют `this` из окружающего лексического контекста и не могут быть переподключены",
        "Стрелочные функции имеют собственную привязку `this`, которую можно изменить с помощью .call() или .apply()",
        "Обычные функции всегда имеют `this`, привязанный к глобальному объекту",
        "Стрелочные и обычные функции имеют одинаковое поведение `this` во всех контекстах"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Arrow functions do not have their own `this` binding; they lexically inherit `this` from the surrounding scope at definition time. Methods like .call(), .apply(), and .bind() cannot change an arrow function's `this`. Regular functions have dynamic `this` binding determined at call time. This distinction is crucial for understanding method behavior and callback patterns.",
      "ru": "Стрелочные функции не имеют собственной привязки `this`; они лексически наследуют `this` из окружающей области в момент определения. Методы .call(), .apply() и .bind() не могут изменить `this` стрелочной функции. Обычные функции имеют динамическую привязку `this`, определяемую во время вызова. Это различие критично для понимания поведения методов и паттернов обратных вызовов."
    }
  },
  {
    "id": "javascript-quiz-15",
    "topicId": "javascript",
    "question": {
      "en": "What is the result of type coercion in this comparison?\n```js\nconsole.log('5' + 3);\nconsole.log('5' - 3);\nconsole.log(true == 1);\nconsole.log(true === 1);\n```",
      "ru": "Каков результат приведения типов в этом сравнении?\n```js\nconsole.log('5' + 3);\nconsole.log('5' - 3);\nconsole.log(true == 1);\nconsole.log(true === 1);\n```"
    },
    "options": {
      "en": [
        "'53', 2, true, true",
        "'53', 2, true, false",
        "'53', '2', true, false",
        "'8', '2', true, true"
      ],
      "ru": [
        "'53', 2, true, true",
        "'53', 2, true, false",
        "'53', '2', true, false",
        "'8', '2', true, true"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The + operator with a string performs string concatenation ('5' + 3 = '53'). The - operator converts both operands to numbers ('5' - 3 = 2). The == operator performs type coercion, converting true to 1 (true == 1 is true). The === operator checks both value and type without coercion (true === 1 is false). Understanding coercion rules is essential for avoiding bugs.",
      "ru": "Оператор + со строкой выполняет конкатенацию строк ('5' + 3 = '53'). Оператор - преобразует оба операнда в числа ('5' - 3 = 2). Оператор == выполняет приведение типов, преобразуя true в 1 (true == 1 is true). Оператор === проверяет значение и тип без приведения (true === 1 is false). Понимание правил приведения типов критично для избежания ошибок."
    }
  },
  {
    "id": "javascript-quiz-16",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nconst config = Object.freeze({ retries: 2, db: { host: 'localhost' } });\nconfig.db.host = '10.0.0.1';\nconsole.log(config.db.host);\nconsole.log(Object.isFrozen(config));\nconsole.log(Object.isFrozen(config.db));\n```",
      "ru": "Что будет выведено в консоль?\n```js\nconst config = Object.freeze({ retries: 2, db: { host: 'localhost' } });\nconfig.db.host = '10.0.0.1';\nconsole.log(config.db.host);\nconsole.log(Object.isFrozen(config));\nconsole.log(Object.isFrozen(config.db));\n```"
    },
    "options": {
      "en": [
        "localhost, then true, then true",
        "10.0.0.1, then true, then true",
        "10.0.0.1, then true, then false",
        "A TypeError, because config is frozen"
      ],
      "ru": [
        "localhost, затем true, затем true",
        "10.0.0.1, затем true, затем true",
        "10.0.0.1, затем true, затем false",
        "Выбрасывается TypeError, ведь config заморожен"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "`Object.freeze` works one level deep. It locks the own properties of the object you pass and nothing below them. `config.db` points to a separate object that was never frozen, so the write to `config.db.host` goes through and prints 10.0.0.1. `Object.isFrozen(config)` is true, while `Object.isFrozen(config.db)` is false.\n\nWhy the other answers are wrong:\n\n- The answer that keeps `localhost` assumes the freeze walks the whole object tree. It does not.\n- The answer where both checks are true repeats that mistake. `isFrozen` reports only on the object it is given.\n- No `TypeError` appears, because the code never writes to a frozen property. Such a write throws only in strict mode. In non-strict code it fails silently.\n\nTo lock a whole tree, walk it yourself and freeze every nested object.",
      "ru": "`Object.freeze` работает на один уровень. Он запирает собственные свойства того объекта, который вы передали, и ничего глубже. `config.db` — отдельный объект, его никто не замораживал, поэтому запись в `config.db.host` проходит и печатается 10.0.0.1. `Object.isFrozen(config)` даёт true, а `Object.isFrozen(config.db)` — false.\n\nПочему остальные ответы неверны:\n\n- Ответ, где остаётся `localhost`, предполагает, что заморозка идёт по всему дереву объектов. Это не так.\n- Ответ, где обе проверки дают true, повторяет ту же ошибку. `isFrozen` отвечает только про тот объект, который ему передали.\n- `TypeError` не появится: код ни разу не пишет в замороженное свойство. Такая запись бросает ошибку только в строгом режиме, а в нестрогом молча ничего не делает.\n\nЧтобы запереть всё дерево, обойдите его сами и заморозьте каждый вложенный объект."
    }
  },
  {
    "id": "javascript-quiz-17",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nfunction tag(strings, ...values) {\n  return strings.length + ':' + values.join('-');\n}\nconst user = 'Ada';\nconst role = 'admin';\nconsole.log(tag`${user} is an ${role}`);\n```",
      "ru": "Что будет выведено в консоль?\n```js\nfunction tag(strings, ...values) {\n  return strings.length + ':' + values.join('-');\n}\nconst user = 'Ada';\nconst role = 'admin';\nconsole.log(tag`${user} is an ${role}`);\n```"
    },
    "options": {
      "en": [
        "2:Ada-admin",
        "15:, because the tag receives the finished string",
        "1:Ada-admin, because empty pieces at the edges are not passed",
        "3:Ada-admin, because strings keeps the empty edge pieces"
      ],
      "ru": [
        "2:Ada-admin",
        "15:, ведь в тег приходит уже готовая строка",
        "1:Ada-admin, ведь пустые куски по краям не передаются",
        "3:Ada-admin, ведь strings хранит и пустые куски по краям"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "A tag function receives the literal cut around its placeholders as the first argument, and the placeholder values after it. The literal here is `${user} is an ${role}`, so `strings` is ['', ' is an ', '']. The empty piece before the first placeholder is kept, and so is the one after the last. That makes `strings.length` 3 while `values` holds 2 entries. `strings` always has exactly one element more than `values`.\n\nWhy the other answers are wrong:\n\n- The answer 2:Ada-admin counts the placeholders instead of the string pieces.\n- The answer 1:Ada-admin drops the empty edge pieces, which the language keeps.\n- The answer 15: assumes the tag gets the finished text 'Ada is an admin' as one string, and 15 is its length. A tag exists precisely to see the parts separately: `strings.raw` even shows the text with escape sequences left alone.",
      "ru": "Тег-функция получает первым аргументом литерал, разрезанный вокруг подстановок, а дальше — сами значения подстановок. Здесь литерал `${user} is an ${role}`, поэтому `strings` — это ['', ' is an ', '']: пустой кусок перед первой подстановкой и пустой после последней сохраняются. Отсюда `strings.length` равно 3, а в `values` два элемента. В `strings` всегда ровно на один элемент больше, чем в `values`.\n\nПочему остальные ответы неверны:\n\n- Ответ 2:Ada-admin считает подстановки вместо кусков строки.\n- Ответ 1:Ada-admin выбрасывает пустые куски по краям, а язык их сохраняет.\n- Ответ 15: считает, что в тег приходит готовый текст 'Ada is an admin' одной строкой, и 15 — его длина. Но тег и нужен, чтобы видеть части отдельно: `strings.raw` даже покажет текст с необработанными управляющими последовательностями."
    }
  },
  {
    "id": "javascript-quiz-18",
    "topicId": "javascript",
    "question": {
      "en": "Which statement correctly handles an asynchronous operation and is guaranteed to execute cleanup code?",
      "ru": "Какое утверждение правильно обрабатывает асинхронную операцию и гарантирует выполнение кода очистки?"
    },
    "options": {
      "en": [
        "fetch(url).then(res => res.json()).catch(err => console.log(err)).finally(() => cleanup())",
        "async function getData() { try { await fetch(url); } catch(e) {} } — cleanup runs immediately after",
        "Promise.resolve(fetch(url)).then(cleanup) — cleanup always executes last",
        "await fetch(url); cleanup(); — cleanup guaranteed to run even if fetch fails"
      ],
      "ru": [
        "fetch(url).then(res => res.json()).catch(err => console.log(err)).finally(() => cleanup())",
        "async function getData() { try { await fetch(url); } catch(e) {} } — cleanup запускается сразу после",
        "Promise.resolve(fetch(url)).then(cleanup) — cleanup всегда выполняется последним",
        "await fetch(url); cleanup(); — cleanup гарантировано запустится даже если fetch не удастся"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The finally() method in Promise chains is specifically designed to execute code after both success and failure paths, ensuring cleanup always runs. The try/catch option has no guarantee if an error occurs after the try block. The Promise.resolve(fetch(url)).then(cleanup) option executes cleanup on resolution only. The plain await fetch(url); cleanup(); option will never reach cleanup if fetch throws.",
      "ru": "Метод finally() в цепочках Promise специально разработан для выполнения кода после успешного и неудачного выполнения, гарантируя выполнение очистки. Вариант с try/catch не гарантирует выполнение, если ошибка произойдет после блока try. Вариант с Promise.resolve(fetch(url)).then(cleanup) выполняет cleanup только при разрешении. Вариант с обычным await fetch(url); cleanup(); никогда не достигнет cleanup если fetch выбросит ошибку."
    }
  },
  {
    "id": "javascript-quiz-19",
    "topicId": "javascript",
    "question": {
      "en": "What does the following code output?\n```js\nconsole.log('' == 0);\nconsole.log('' === 0);\nconsole.log(Boolean(''));\n```",
      "ru": "Что выведет следующий код?\n```js\nconsole.log('' == 0);\nconsole.log('' === 0);\nconsole.log(Boolean(''));\n```"
    },
    "options": {
      "en": [
        "false, false, true",
        "true, false, false",
        "true, true, false",
        "false, true, false"
      ],
      "ru": [
        "false, false, true",
        "true, false, false",
        "true, true, false",
        "false, true, false"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The == operator coerces both operands: empty string converts to 0 via type coercion, so '' == 0 is true. The === operator performs no coercion, so '' === 0 is false. Boolean('') is false because empty string is falsy. This highlights the difference between loose and strict equality and type coercion rules.",
      "ru": "Оператор == приводит оба операнда: пустая строка преобразуется в 0 через приведение типов, поэтому '' == 0 это true. Оператор === не выполняет приведение, поэтому '' === 0 это false. Boolean('') это false, так как пустая строка falsy. Это показывает разницу между нестрогим и строгим равенством и правилами приведения типов."
    }
  },
  {
    "id": "javascript-quiz-20",
    "topicId": "javascript",
    "question": {
      "en": "In JavaScript's prototype chain, which statement is true about property lookup?",
      "ru": "В цепочке прототипов JavaScript какое утверждение верно о поиске свойства?"
    },
    "options": {
      "en": [
        "Properties are always searched up the prototype chain first, regardless of whether they exist on the object",
        "The prototype chain search skips the constructor function and only checks inherited prototypes",
        "Properties are searched first on the object itself, then up the prototype chain until found or chain ends",
        "hasOwnProperty() returns true for inherited properties if they exist anywhere in the prototype chain"
      ],
      "ru": [
        "Свойства всегда сначала ищутся вверх по цепочке прототипов, независимо от того, существуют ли они на объекте",
        "Поиск в цепочке прототипов пропускает функцию конструктора и проверяет только унаследованные прототипы",
        "Свойства сначала ищутся на самом объекте, затем вверх по цепочке прототипов до нахождения или конца цепочки",
        "hasOwnProperty() возвращает true для унаследованных свойств, если они существуют где-то в цепочке прототипов"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "JavaScript uses own-first property lookup: the engine checks the object's own properties immediately, and only traverses the prototype chain if the property is not found. This is fundamental to prototype-based inheritance and performance optimization. The option that always searches the prototype chain first reverses the order. The option that skips the constructor function is incorrect because the constructor is part of lookup. The option about hasOwnProperty returning true for inherited properties is false—hasOwnProperty only returns true for non-inherited properties.",
      "ru": "JavaScript использует поиск 'собственный первый': движок сначала проверяет собственные свойства объекта и только потом обходит цепочку прототипов, если свойство не найдено. Это фундаментально для наследования на основе прототипов и оптимизации производительности. Вариант, где поиск всегда начинается с цепочки прототипов, инвертирует порядок. Вариант, где поиск пропускает функцию конструктора, неправ, так как конструктор является частью поиска. Вариант про hasOwnProperty для унаследованных свойств неверен — hasOwnProperty возвращает true только для неунаследованных свойств."
    }
  },
  {
    "id": "javascript-quiz-22",
    "topicId": "javascript",
    "question": {
      "en": "What does the following code output?\n```js\nconst obj = { x: 1 };\nconst handler = {\n  get(target, prop) {\n    return prop in target ? target[prop] : 'NOT_FOUND';\n  }\n};\nconst proxy = new Proxy(obj, handler);\nconsole.log(proxy.x);\nconsole.log(proxy.y);\n```",
      "ru": "Что выводит следующий код?\n```js\nconst obj = { x: 1 };\nconst handler = {\n  get(target, prop) {\n    return prop in target ? target[prop] : 'NOT_FOUND';\n  }\n};\nconst proxy = new Proxy(obj, handler);\nconsole.log(proxy.x);\nconsole.log(proxy.y);\n```"
    },
    "options": {
      "en": [
        "undefined and 'NOT_FOUND'",
        "1 and undefined",
        "Error is thrown",
        "1 and 'NOT_FOUND'"
      ],
      "ru": [
        "undefined и 'NOT_FOUND'",
        "1 и undefined",
        "Выбрасывается ошибка",
        "1 и 'NOT_FOUND'"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The Proxy's get trap intercepts property access. When accessing proxy.x, the trap checks if 'x' exists in the target object (it does), so it returns target.x which is 1. When accessing proxy.y, the 'in' operator returns false since y is not in the object, so it returns the string 'NOT_FOUND'. Proxies are ES6+ features that allow custom behavior for object operations.",
      "ru": "Ловушка get прокси перехватывает доступ к свойствам. При обращении к proxy.x ловушка проверяет, существует ли 'x' в целевом объекте (существует), поэтому она возвращает target.x, что равно 1. При обращении к proxy.y оператор 'in' возвращает false, так как y нет в объекте, поэтому возвращается строка 'NOT_FOUND'. Прокси — это функция ES6+, которая позволяет настраивать поведение операций с объектами."
    }
  },
  {
    "id": "javascript-quiz-23",
    "topicId": "javascript",
    "question": {
      "en": "Which statement best describes what happens with this code?\n```js\nfunction outer() {\n  let count = 0;\n  return function inner() {\n    count++;\n    return count;\n  };\n}\nconst counter = outer();\ncounter(); // 1\ncounter(); // 2\ncounter(); // 3\n```",
      "ru": "Какое утверждение лучше всего описывает, что происходит с этим кодом?\n```js\nfunction outer() {\n  let count = 0;\n  return function inner() {\n    count++;\n    return count;\n  };\n}\nconst counter = outer();\ncounter(); // 1\ncounter(); // 2\ncounter(); // 3\n```"
    },
    "options": {
      "en": [
        "The inner function forms a closure and retains access to the count variable across multiple calls",
        "Each call to counter() creates a new count variable, so count resets to 0 each time",
        "The count variable is stored in the global scope and shared between all counter calls",
        "The inner function loses its reference to count after the outer function returns"
      ],
      "ru": [
        "Внутренняя функция создаёт замыкание и сохраняет доступ к переменной count при множественных вызовах",
        "Каждый вызов counter() создаёт новую переменную count, поэтому count сбрасывается на 0 каждый раз",
        "Переменная count хранится в глобальной области видимости и используется всеми вызовами counter",
        "Внутренняя функция теряет ссылку на count после возврата из внешней функции"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "This is a classic closure example. The inner function maintains a persistent reference to the count variable from its parent scope, even after outer() has returned. Each call to counter() increments the same count variable, creating the closure behavior. The other options incorrectly describe how scope and variable lifetime work in JavaScript.",
      "ru": "Это классический пример замыкания. Внутренняя функция сохраняет постоянную ссылку на переменную count из области видимости родителя, даже после возврата из outer(). Каждый вызов counter() увеличивает одну и ту же переменную count, создавая поведение замыкания. Другие варианты неправильно описывают, как работают область видимости и время жизни переменной в JavaScript."
    }
  },
  {
    "id": "javascript-quiz-24",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged?\n```js\nvar x = 10;\nlet y = 20;\nfunction test() {\n  console.log(x);\n  console.log(y);\n  var x = 30;\n  let y = 40;\n}\ntest();\n```",
      "ru": "Что будет выведено?\n```js\nvar x = 10;\nlet y = 20;\nfunction test() {\n  console.log(x);\n  console.log(y);\n  var x = 30;\n  let y = 40;\n}\ntest();\n```"
    },
    "options": {
      "en": [
        "10 and 20",
        "undefined and ReferenceError",
        "undefined and 20",
        "10 and ReferenceError"
      ],
      "ru": [
        "10 и 20",
        "undefined и ReferenceError",
        "undefined и 20",
        "10 и ReferenceError"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Due to hoisting, var x is hoisted to the top of the function scope and initialized as undefined. The first console.log(x) logs undefined. The let y declaration creates a Temporal Dead Zone from the top of the block until the declaration is reached. Accessing y before its declaration throws a ReferenceError. var is hoisted with undefined, while let/const are hoisted but not initialized (TDZ).",
      "ru": "Из-за всплытия var x поднимается в верх области функции и инициализируется как undefined. Первый console.log(x) выводит undefined. Объявление let y создаёт Temporal Dead Zone от начала блока до объявления. Доступ к y до её объявления выбрасывает ReferenceError. var всплывает с инициализацией undefined, в то время как let/const всплывают, но не инициализируются (TDZ)."
    }
  },
  {
    "id": "javascript-quiz-25",
    "topicId": "javascript",
    "question": {
      "en": "What will the Promise chain output?\n```js\nPromise.resolve(1)\n  .then(x => Promise.resolve(x + 1))\n  .then(x => x + 1)\n  .then(console.log);\n```",
      "ru": "Что выведет цепь Promise?\n```js\nPromise.resolve(1)\n  .then(x => Promise.resolve(x + 1))\n  .then(x => x + 1)\n  .then(console.log);\n```"
    },
    "options": {
      "en": [
        "2",
        "Promise { 3 }",
        "3",
        "1"
      ],
      "ru": [
        "2",
        "Promise { 3 }",
        "3",
        "1"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The first then receives 1, and returns a resolved Promise with value 2. The second then receives the unwrapped value 2 (not the Promise object), adds 1 to get 3, and returns 3. The third then receives 3 and logs it. When a then callback returns a Promise, it is automatically unwrapped. The final output is 3, not a Promise object, because console.log receives the resolved value.",
      "ru": "Первый then получает 1 и возвращает разрешённый Promise со значением 2. Второй then получает развёрнутое значение 2 (не объект Promise), добавляет 1 и получает 3, возвращая 3. Третий then получает 3 и выводит его. Когда обратный вызов then возвращает Promise, он автоматически развёртывается. Итоговый вывод — 3, а не объект Promise, потому что console.log получает разрешённое значение."
    }
  },
  {
    "id": "javascript-quiz-26",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nconsole.log(typeof typeof 42);\n```",
      "ru": "Что будет выведено в консоль?\n```js\nconsole.log(typeof typeof 42);\n```"
    },
    "options": {
      "en": [
        "number",
        "undefined",
        "object",
        "string"
      ],
      "ru": [
        "number",
        "undefined",
        "object",
        "string"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The inner typeof 42 returns 'number' (a string). The outer typeof then evaluates typeof 'number', which returns 'string'. The typeof operator always returns a string representation of the type.",
      "ru": "Внутренний typeof 42 возвращает 'number' (строка). Внешний typeof затем вычисляет typeof 'number', что возвращает 'string'. Оператор typeof всегда возвращает строковое представление типа."
    }
  },
  {
    "id": "javascript-quiz-27",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nconst rows = [[1, 2], [3, 4], [5, 6]];\nlet seen = 0;\nouter:\nfor (const row of rows) {\n  for (const value of row) {\n    if (value === 4) break outer;\n    seen++;\n  }\n}\nconsole.log(seen);\n```",
      "ru": "Что будет выведено в консоль?\n```js\nconst rows = [[1, 2], [3, 4], [5, 6]];\nlet seen = 0;\nouter:\nfor (const row of rows) {\n  for (const value of row) {\n    if (value === 4) break outer;\n    seen++;\n  }\n}\nconsole.log(seen);\n```"
    },
    "options": {
      "en": [
        "3, because break outer leaves both loops at once",
        "5, because break outer ends only the inner loop",
        "4, because seen++ runs before the check",
        "A SyntaxError, because a label may not stand before a loop"
      ],
      "ru": [
        "3, ведь break outer выходит сразу из двух циклов",
        "5, ведь break outer завершает только внутренний цикл",
        "4, ведь seen++ выполняется до проверки",
        "Синтаксическая ошибка: метка не может стоять перед циклом"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "A labelled `break` leaves the loop that carries the label, not just the loop it sits in. `outer:` labels the outer `for...of`, so `break outer` stops both loops as soon as `value` is 4. The first row counts two values, the second row counts one more and then breaks, so `seen` is 3.\n\nWhy the other answers are wrong:\n\n- The answer 5 treats `break outer` as a plain `break`. Then the inner loop would end, the outer one would move on to [5, 6] and add two more.\n- The answer 4 puts `seen++` before the check, but the `if` is the first statement in the body.\n- There is no syntax error. A label in front of a loop is valid JavaScript, and `continue outer` is allowed too: it jumps to the next iteration of the labelled loop.",
      "ru": "`break` с меткой выходит из того цикла, на котором стоит метка, а не только из того, внутри которого он написан. Метка `outer:` относится к внешнему `for...of`, поэтому `break outer` останавливает оба цикла, как только `value` равно 4. В первой строке посчитаны два значения, во второй — ещё одно, дальше выход. Значит, `seen` равно 3.\n\nПочему остальные ответы неверны:\n\n- Ответ 5 считает `break outer` обычным `break`: тогда внутренний цикл закончится, внешний перейдёт к [5, 6] и добавит ещё два.\n- Ответ 4 ставит `seen++` перед проверкой, но первым в теле идёт `if`.\n- Синтаксической ошибки нет. Метка перед циклом — обычный JavaScript, и `continue outer` тоже разрешён: он переходит к следующей итерации помеченного цикла."
    }
  },
  {
    "id": "javascript-quiz-28",
    "topicId": "javascript",
    "question": {
      "en": "Which statement accurately describes what happens when a Promise is rejected without a catch handler?\n",
      "ru": "Какое утверждение точно описывает, что происходит при отклонении Promise без обработчика catch?"
    },
    "options": {
      "en": [
        "The error is silently ignored and execution continues",
        "An UnhandledPromiseRejectionWarning is emitted, but execution continues",
        "The entire program terminates immediately",
        "The rejection is queued for the next microtask cycle"
      ],
      "ru": [
        "Ошибка молча игнорируется и выполнение продолжается",
        "Выпускается UnhandledPromiseRejectionWarning, но выполнение продолжается",
        "Вся программа немедленно завершается",
        "Отклонение ставится в очередь на следующий цикл микротасков"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "In Node.js and modern browsers, an unhandled Promise rejection emits a warning or error event but does not stop program execution. The rejection is detected asynchronously after the current event loop cycle, allowing time to attach a handler.",
      "ru": "В Node.js и современных браузерах необработанное отклонение Promise выпускает предупреждение или событие ошибки, но не останавливает выполнение программы. Отклонение обнаруживается асинхронно после текущего цикла событий, что дает время для добавления обработчика."
    }
  },
  {
    "id": "javascript-quiz-29",
    "topicId": "javascript",
    "question": {
      "en": "What is the output of this code?\n```js\nfunction outer() {\n  let count = 0;\n  return function inner() {\n    return ++count;\n  };\n}\nconst counter1 = outer();\nconst counter2 = outer();\nconsole.log(counter1(), counter1(), counter2());\n```",
      "ru": "Какой результат этого кода?\n```js\nfunction outer() {\n  let count = 0;\n  return function inner() {\n    return ++count;\n  };\n}\nconst counter1 = outer();\nconst counter2 = outer();\nconsole.log(counter1(), counter1(), counter2());\n```"
    },
    "options": {
      "en": [
        "1 2 3",
        "1 1 1",
        "1 2 1",
        "3 3 3"
      ],
      "ru": [
        "1 2 3",
        "1 1 1",
        "1 2 1",
        "3 3 3"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Each call to outer() creates a new closure with its own count variable. counter1 and counter2 maintain separate count values. counter1() increments its closure's count to 1, then to 2. counter2() is a separate closure, so its count increments to 1.",
      "ru": "Каждый вызов outer() создает новое замыкание со своей переменной count. counter1 и counter2 имеют отдельные значения count. counter1() увеличивает count в своем замыкании до 1, затем до 2. counter2() — это отдельное замыкание, поэтому его count увеличивается до 1."
    }
  },
  {
    "id": "javascript-quiz-30",
    "topicId": "javascript",
    "question": {
      "en": "Consider this ES6 class inheritance scenario. What is logged?\n```js\nclass Animal {\n  constructor(name) {\n    this.name = name;\n  }\n}\nclass Dog extends Animal {\n  constructor(name, breed) {\n    super(name);\n    this.breed = breed;\n  }\n}\nconst dog = new Dog('Rex', 'Labrador');\nconsole.log(dog instanceof Dog, dog instanceof Animal, dog instanceof Object);\n```",
      "ru": "Рассмотрим этот сценарий наследования класса ES6. Что выводится?\n```js\nclass Animal {\n  constructor(name) {\n    this.name = name;\n  }\n}\nclass Dog extends Animal {\n  constructor(name, breed) {\n    super(name);\n    this.breed = breed;\n  }\n}\nconst dog = new Dog('Rex', 'Labrador');\nconsole.log(dog instanceof Dog, dog instanceof Animal, dog instanceof Object);\n```"
    },
    "options": {
      "en": [
        "true false false",
        "true true false",
        "false true true",
        "true true true"
      ],
      "ru": [
        "true false false",
        "true true false",
        "false true true",
        "true true true"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The instanceof operator checks the prototype chain. dog is an instance of Dog (direct class), Animal (parent class via inheritance), and Object (the root of all objects' prototype chains in JavaScript). All three checks return true.",
      "ru": "Оператор instanceof проверяет цепочку прототипов. dog является экземпляром Dog (прямой класс), Animal (родительский класс через наследование) и Object (корень всех цепочек прототипов объектов в JavaScript). Все три проверки возвращают true."
    }
  },
  {
    "id": "javascript-quiz-31",
    "topicId": "javascript",
    "question": {
      "en": "What is logged to the console?\n```js\nconst obj = { a: 1 };\nconst handler = {\n  get(target, prop) {\n    return target[prop] * 2;\n  }\n};\nconst proxy = new Proxy(obj, handler);\nconsole.log(proxy.a);\n```",
      "ru": "Что выводится в консоль?\n```js\nconst obj = { a: 1 };\nconst handler = {\n  get(target, prop) {\n    return target[prop] * 2;\n  }\n};\nconst proxy = new Proxy(obj, handler);\nconsole.log(proxy.a);\n```"
    },
    "options": {
      "en": [
        "2",
        "1",
        "undefined",
        "{ a: 1 }"
      ],
      "ru": [
        "2",
        "1",
        "undefined",
        "{ a: 1 }"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The Proxy's get trap intercepts property access and returns target[prop] * 2. Since obj.a is 1, the handler returns 1 * 2 = 2. Proxy handlers allow you to intercept and customize operations on objects.",
      "ru": "Ловушка get в Proxy перехватывает доступ к свойству и возвращает target[prop] * 2. Так как obj.a равна 1, обработчик возвращает 1 * 2 = 2. Обработчики Proxy позволяют перехватывать и настраивать операции над объектами."
    }
  },
  {
    "id": "javascript-quiz-32",
    "topicId": "javascript",
    "question": {
      "en": "What is the result of this expression?\n```js\n(function() {\n  console.log(x);\n  var x = 5;\n  console.log(x);\n})();\n```",
      "ru": "Какой результат этого выражения?\n```js\n(function() {\n  console.log(x);\n  var x = 5;\n  console.log(x);\n})();\n```"
    },
    "options": {
      "en": [
        "ReferenceError: x is not defined",
        "undefined, then 5",
        "null, then 5",
        "5, then 5"
      ],
      "ru": [
        "ReferenceError: x не определен",
        "undefined, затем 5",
        "null, затем 5",
        "5, затем 5"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Due to hoisting, var x is declared (but not initialized) at the top of the function scope. The first console.log accesses the uninitialized variable, which is undefined. After assignment on line 3, it becomes 5. This demonstrates function hoisting and the temporal dead zone for var declarations.",
      "ru": "Из-за hoisting переменная var x объявляется (но не инициализируется) в начале области видимости функции. Первый console.log обращается к неинициализированной переменной, которая равна undefined. После присваивания она становится 5. Это демонстрирует hoisting и временную мёртвую зону для var."
    }
  },
  {
    "id": "javascript-quiz-33",
    "topicId": "javascript",
    "question": {
      "en": "Which statement best describes the relationship between closures and the event loop in JavaScript?",
      "ru": "Какое утверждение лучше всего описывает связь между замыканиями и циклом событий в JavaScript?"
    },
    "options": {
      "en": [
        "Closures are executed by the event loop in a specific order",
        "The event loop creates closures automatically for all functions",
        "Closures capture variables at creation time; the event loop executes callbacks when the call stack is empty",
        "Closures prevent the event loop from processing macrotasks"
      ],
      "ru": [
        "Замыкания выполняются циклом событий в определённом порядке",
        "Цикл событий автоматически создаёт замыкания для всех функций",
        "Замыкания захватывают переменные во время создания; цикл событий выполняет обратные вызовы, когда стек вызовов пуст",
        "Замыкания предотвращают обработку макрозадач циклом событий"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Closures are a scoping mechanism: an inner function keeps access to the variables of its outer scope. The event loop is an execution model that runs callbacks after the call stack empties and microtasks finish. They are independent concepts that work together in async code. The option where the event loop executes closures confuses the two roles. The option about the event loop creating closures automatically is false. The option about closures blocking macrotasks describes something closures cannot do.",
      "ru": "Замыкания — это механизм области видимости: внутренняя функция сохраняет доступ к переменным внешней области. Цикл событий — это модель выполнения, которая запускает обратные вызовы после того, как стек вызовов опустел и микрозадачи завершились. Это независимые концепции, работающие вместе в асинхронном коде. Вариант, где цикл событий выполняет замыкания, путает эти две роли. Вариант про автоматическое создание замыканий циклом событий просто неверен. Вариант про блокировку макрозадач приписывает замыканиям то, чего они не делают."
    }
  },
  {
    "id": "javascript-quiz-34",
    "topicId": "javascript",
    "question": {
      "en": "What is the output of this code?\n```js\nPromise.resolve(1)\n  .then(x => {\n    console.log('A');\n    return Promise.reject(new Error('fail'));\n  })\n  .then(x => console.log('B'))\n  .catch(e => console.log('C'))\n  .then(x => console.log('D'));\n```",
      "ru": "Какой результат этого кода?\n```js\nPromise.resolve(1)\n  .then(x => {\n    console.log('A');\n    return Promise.reject(new Error('fail'));\n  })\n  .then(x => console.log('B'))\n  .catch(e => console.log('C'))\n  .then(x => console.log('D'));\n```"
    },
    "options": {
      "en": [
        "A, B, C, D",
        "A, C",
        "A, B",
        "A, C, D"
      ],
      "ru": [
        "A, B, C, D",
        "A, C",
        "A, B",
        "A, C, D"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Promise.resolve(1) logs 'A'. The rejection skips the next .then(), so 'B' is not logged. The .catch() handles the error and logs 'C'. Since .catch() returns a resolved promise, the final .then() executes and logs 'D'. This tests understanding of promise chains and error handling.",
      "ru": "Promise.resolve(1) логирует 'A'. Отклонение пропускает следующий .then(), поэтому 'B' не логируется. .catch() обрабатывает ошибку и логирует 'C'. Так как .catch() возвращает разрешённый promise, финальный .then() выполняется и логирует 'D'."
    }
  },
  {
    "id": "javascript-quiz-35",
    "topicId": "javascript",
    "question": {
      "en": "What does typeof typeof x return when x is undefined?",
      "ru": "Что возвращает typeof typeof x, когда x не определён?"
    },
    "options": {
      "en": [
        "\"string\"",
        "\"undefined\"",
        "\"object\"",
        "ReferenceError"
      ],
      "ru": [
        "\"string\"",
        "\"undefined\"",
        "\"object\"",
        "ReferenceError"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "typeof undefined returns the string \"undefined\". Then typeof \"undefined\" returns \"string\" because the result of typeof is always a string. This tests understanding of the typeof operator's return type and how it handles undefined values without throwing errors.",
      "ru": "typeof undefined возвращает строку \"undefined\". Затем typeof \"undefined\" возвращает \"string\", потому что результат typeof всегда является строкой. Это проверяет понимание возвращаемого типа оператора typeof и то, как он обрабатывает undefined без ошибок."
    }
  },
  {
    "id": "javascript-quiz-37",
    "topicId": "javascript",
    "question": {
      "en": "Consider the following code. What is the value of `result`?\n```js\nlet x = 5;\nlet result = (function() {\n  let x = 10;\n  return x + 5;\n})();\nconsole.log(result);\nconsole.log(x);\n```",
      "ru": "Рассмотрите следующий код. Какое значение у `result`?\n```js\nlet x = 5;\nlet result = (function() {\n  let x = 10;\n  return x + 5;\n})();\nconsole.log(result);\nconsole.log(x);\n```"
    },
    "options": {
      "en": [
        "result is 10, x is 10",
        "result is 15, x is 5",
        "result is 20, x is 5",
        "result is 15, x is 10"
      ],
      "ru": [
        "result равен 10, x равен 10",
        "result равен 15, x равен 5",
        "result равен 20, x равен 5",
        "result равен 15, x равен 10"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The IIFE (Immediately Invoked Function Expression) creates its own scope with a local `x` variable set to 10. Inside the function, `x + 5` evaluates to 15. The outer `x` remains unchanged at 5 because the inner `x` is scoped to the function. Variable shadowing prevents the inner `x` from affecting the outer one.",
      "ru": "IIFE (Немедленно вызываемое функциональное выражение) создаёт свою область видимости с локальной переменной `x`, равной 10. Внутри функции `x + 5` вычисляется как 15. Внешняя переменная `x` остаётся неизменной на уровне 5, так как внутренняя `x` ограничена областью видимости функции. Затенение переменной предотвращает влияние внутренней `x` на внешнюю."
    }
  },
  {
    "id": "javascript-quiz-38",
    "topicId": "javascript",
    "question": {
      "en": "What will be the output of this code?\n```js\nconsole.log(typeof undefined);\nconsole.log(typeof null);\nconsole.log(typeof {});\nconsole.log(typeof []);\n```",
      "ru": "Каков будет результат выполнения этого кода?\n```js\nconsole.log(typeof undefined);\nconsole.log(typeof null);\nconsole.log(typeof {});\nconsole.log(typeof []);\n```"
    },
    "options": {
      "en": [
        "\"undefined\", \"null\", \"object\", \"array\"",
        "\"undefined\", \"object\", \"object\", \"array\"",
        "\"undefined\", \"object\", \"object\", \"object\"",
        "\"undefined\", \"null\", \"object\", \"object\""
      ],
      "ru": [
        "\"undefined\", \"null\", \"object\", \"array\"",
        "\"undefined\", \"object\", \"object\", \"array\"",
        "\"undefined\", \"object\", \"object\", \"object\"",
        "\"undefined\", \"null\", \"object\", \"object\""
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The `typeof` operator returns \"undefined\" for undefined, \"object\" for null (this is actually a historical bug in JavaScript), \"object\" for plain objects, and \"object\" for arrays (arrays are objects in JavaScript). The operator does not distinguish between different object types.",
      "ru": "Оператор `typeof` возвращает \"undefined\" для undefined, \"object\" для null (это исторический баг в JavaScript), \"object\" для обычных объектов и \"object\" для массивов (массивы — это объекты в JavaScript). Оператор не различает разные типы объектов."
    }
  },
  {
    "id": "javascript-quiz-39",
    "topicId": "javascript",
    "question": {
      "en": "What will this code log?\n```js\nvar a = 1;\nlet b = 2;\nconst c = 3;\n\nconsole.log(typeof a);\nconsole.log(typeof b);\nconsole.log(typeof c);\nconsole.log(typeof d);\n```",
      "ru": "Что выведет этот код?\n```js\nvar a = 1;\nlet b = 2;\nconst c = 3;\n\nconsole.log(typeof a);\nconsole.log(typeof b);\nconsole.log(typeof c);\nconsole.log(typeof d);\n```"
    },
    "options": {
      "en": [
        "\"number\", \"number\", \"number\", ReferenceError",
        "\"number\", \"number\", \"number\", \"object\"",
        "\"1\", \"2\", \"3\", ReferenceError",
        "\"number\", \"number\", \"number\", \"undefined\""
      ],
      "ru": [
        "\"number\", \"number\", \"number\", ReferenceError",
        "\"number\", \"number\", \"number\", \"object\"",
        "\"1\", \"2\", \"3\", ReferenceError",
        "\"number\", \"number\", \"number\", \"undefined\""
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The first three logs output \"number\" as expected. However, `typeof d` returns \"undefined\" instead of throwing an ReferenceError because `typeof` is a special operator that safely handles undeclared variables. It returns \"undefined\" when checking an undeclared identifier, making it unique among operators.",
      "ru": "Первые три логирования выводят \"number\", как и ожидается. Однако `typeof d` возвращает \"undefined\" вместо того, чтобы выбросить ReferenceError, так как `typeof` — это специальный оператор, который безопасно работает с необъявленными переменными. Он возвращает \"undefined\" при проверке необъявленного идентификатора, что делает его уникальным среди операторов."
    }
  },
  {
    "id": "javascript-quiz-40",
    "topicId": "javascript",
    "question": {
      "en": "Which statement best describes what happens with hoisting in this code?\n```js\nconsole.log(x);\nvar x = 5;\n```",
      "ru": "Какое утверждение лучше всего описывает, что происходит с поднятием (hoisting) в этом коде?\n```js\nconsole.log(x);\nvar x = 5;\n```"
    },
    "options": {
      "en": [
        "x is undefined because the variable declaration is hoisted but not the initialization",
        "x is 5 because var declarations are hoisted to the top with their values",
        "A ReferenceError is thrown because x is not yet defined",
        "x is null because var defaults to null before assignment"
      ],
      "ru": [
        "x равен undefined, потому что объявление переменной поднято, но не инициализация",
        "x равен 5, потому что объявления var поднимаются в верх со своими значениями",
        "Выбрасывается ReferenceError, потому что x еще не определен",
        "x равен null, потому что var по умолчанию равен null перед присваиванием"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "JavaScript hoists variable declarations but not their initializations. The code is interpreted as if `var x;` appears at the top, then `console.log(x);` logs `undefined`, then `x = 5;` executes. This hoisting behavior is specific to `var`; `let` and `const` are also hoisted but exist in a Temporal Dead Zone until their declaration is reached.",
      "ru": "JavaScript поднимает объявления переменных, но не их инициализацию. Код интерпретируется так, как если бы `var x;` находился в верхней части, затем `console.log(x);` выводит `undefined`, а затем выполняется `x = 5;`. Такое поведение поднятия специфично для `var`; `let` и `const` также поднимаются, но находятся в Temporal Dead Zone до достижения их объявления."
    }
  },
  {
    "id": "javascript-quiz-41",
    "topicId": "javascript",
    "question": {
      "en": "What is logged to the console?\n```js\nconst obj = { value: 42 };\nconst getValue = function() { return this.value; };\nconst boundGetValue = getValue.bind(obj);\nconsole.log(boundGetValue.call({ value: 100 }));\n```",
      "ru": "Что выведется в консоль?\n```js\nconst obj = { value: 42 };\nconst getValue = function() { return this.value; };\nconst boundGetValue = getValue.bind(obj);\nconsole.log(boundGetValue.call({ value: 100 }));\n```"
    },
    "options": {
      "en": [
        "100",
        "42",
        "undefined",
        "TypeError"
      ],
      "ru": [
        "100",
        "42",
        "undefined",
        "TypeError"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The bind() method permanently sets the 'this' context to the obj object with value 42. Subsequent calls to call() or apply() cannot override a bound function's context, so 42 is logged. This demonstrates that bind creates a hard-bound function that ignores later context changes.",
      "ru": "Метод bind() постоянно устанавливает контекст 'this' на объект obj со значением 42. Последующие вызовы call() или apply() не могут переопределить контекст привязанной функции, поэтому выводится 42. Это демонстрирует, что bind создаёт функцию с жёсткой привязкой, которая игнорирует позднейшие изменения контекста."
    }
  },
  {
    "id": "javascript-quiz-42",
    "topicId": "javascript",
    "question": {
      "en": "What will this code print?\n```js\nconst nums = [10, 9, 100, 1];\nconsole.log(nums.sort().join(','));\nconsole.log([...nums].sort((a, b) => a - b).join(','));\n```",
      "ru": "Что напечатает этот код?\n```js\nconst nums = [10, 9, 100, 1];\nconsole.log(nums.sort().join(','));\nconsole.log([...nums].sort((a, b) => a - b).join(','));\n```"
    },
    "options": {
      "en": [
        "1,9,10,100 both times",
        "1,10,100,9 both times, because the copy is sorted already",
        "A TypeError, because sort() needs a comparator",
        "1,10,100,9 and then 1,9,10,100"
      ],
      "ru": [
        "1,9,10,100 в оба раза",
        "1,10,100,9 в оба раза, ведь копия уже отсортирована",
        "Выбрасывается TypeError: sort() требует компаратор",
        "1,10,100,9, а затем 1,9,10,100"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "`sort()` without a comparator turns every element into a string and compares those strings. The string '10' is smaller than '9', because the comparison starts at the first character. So the first line prints 1,10,100,9. The comparator `(a, b) => a - b` compares the numbers themselves, so the copy prints 1,9,10,100.\n\nTwo details worth keeping in mind:\n\n- `sort()` sorts in place and returns the same array, so `nums` is already in string order by the time the copy is made.\n- Since ECMAScript 2019 `sort` has to be stable: elements that compare equal keep their original order.\n\nThe answer with 1,9,10,100 twice assumes the default sort understands numbers. The answer with 1,10,100,9 twice ignores the comparator passed on the second call. Nothing is thrown, because the comparator is optional.",
      "ru": "`sort()` без компаратора превращает каждый элемент в строку и сравнивает строки. Строка '10' меньше строки '9', потому что сравнение начинается с первого символа. Поэтому первая строка печатает 1,10,100,9. Компаратор `(a, b) => a - b` сравнивает сами числа, и копия печатает 1,9,10,100.\n\nДве детали, о которых стоит помнить:\n\n- `sort()` сортирует на месте и возвращает тот же массив, поэтому к моменту копирования `nums` уже стоит в строковом порядке.\n- Начиная с ECMAScript 2019 `sort` обязан быть устойчивым: равные при сравнении элементы сохраняют исходный порядок.\n\nОтвет с 1,9,10,100 дважды считает, что сортировка по умолчанию понимает числа. Ответ с 1,10,100,9 дважды не замечает компаратор во втором вызове. Ошибки не будет: компаратор необязателен."
    }
  },
  {
    "id": "javascript-quiz-43",
    "topicId": "javascript",
    "question": {
      "en": "Which statement correctly describes the difference between typeof and instanceof?",
      "ru": "Какое утверждение правильно описывает разницу между typeof и instanceof?"
    },
    "options": {
      "en": [
        "typeof checks inheritance chain; instanceof checks primitive type names",
        "instanceof works for primitives; typeof only works for objects",
        "Both are identical and can be used interchangeably for all data types",
        "typeof returns a string and works well with primitives; instanceof checks if an object is an instance of a constructor"
      ],
      "ru": [
        "typeof проверяет цепь наследования; instanceof проверяет имена примитивных типов",
        "instanceof работает для примитивов; typeof работает только для объектов",
        "Оба идентичны и могут использоваться взаимозаменяемо для всех типов данных",
        "typeof возвращает строку и хорошо работает с примитивами; instanceof проверяет, является ли объект экземпляром конструктора"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "typeof returns a string representing the type and works well for primitives (returns 'number', 'string', 'boolean', etc.) but returns 'object' for null and arrays. instanceof checks if an object was created by a specific constructor and traverses the prototype chain, making it unsuitable for primitives. Each tool serves different purposes.",
      "ru": "typeof возвращает строку, представляющую тип, и хорошо работает с примитивами (возвращает 'number', 'string', 'boolean' и т.д.), но возвращает 'object' для null и массивов. instanceof проверяет, был ли объект создан определённым конструктором, и проходит по цепи прототипов, что делает его непригодным для примитивов. Каждый инструмент служит разным целям."
    }
  },
  {
    "id": "javascript-quiz-44",
    "topicId": "javascript",
    "question": {
      "en": "In JavaScript's event loop, what happens when a Promise callback is resolved while a synchronous task is still executing?",
      "ru": "В цикле событий JavaScript что происходит, когда обратный вызов Promise разрешается, пока выполняется синхронная задача?"
    },
    "options": {
      "en": [
        "The Promise callback is placed in the microtask queue and executes after the current synchronous task completes but before the next macrotask",
        "The Promise callback immediately interrupts the synchronous task and executes",
        "The Promise callback is placed in the macrotask queue and waits for all other tasks to complete",
        "The Promise callback is lost if synchronous code is running"
      ],
      "ru": [
        "Обратный вызов Promise помещается в очередь микротасков и выполняется после завершения текущей синхронной задачи, но до следующего макротаска",
        "Обратный вызов Promise немедленно прерывает синхронную задачу и выполняется",
        "Обратный вызов Promise помещается в очередь макротасков и ждёт завершения всех остальных задач",
        "Обратный вызов Promise теряется, если выполняется синхронный код"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Promise callbacks (.then, .catch, .finally) are placed in the microtask queue, which has higher priority than the macrotask queue (setTimeout, setInterval). The event loop processes all microtasks before moving to the next macrotask, ensuring Promises execute promptly after the current synchronous code finishes.",
      "ru": "Обратные вызовы Promise (.then, .catch, .finally) помещаются в очередь микротасков, которая имеет более высокий приоритет, чем очередь макротасков (setTimeout, setInterval). Цикл событий обрабатывает все микротаски перед переходом к следующему макротаску, обеспечивая быстрое выполнение Promise после завершения текущего синхронного кода."
    }
  },
  {
    "id": "javascript-quiz-45",
    "topicId": "javascript",
    "question": {
      "en": "What is the result of type coercion in this expression?\n```js\nconst result = '5' + 3 - 2;\nconsole.log(result);\n```",
      "ru": "Каков результат приведения типов в этом выражении?\n```js\nconst result = '5' + 3 - 2;\nconsole.log(result);\n```"
    },
    "options": {
      "en": [
        "'51'",
        "51",
        "6",
        "'53' - 2"
      ],
      "ru": [
        "'51'",
        "51",
        "6",
        "'53' - 2"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The + operator has left-to-right associativity. First, '5' + 3 uses string concatenation (+ converts 3 to string) resulting in '53'. Then '53' - 2 forces numeric coercion on both operands (- always converts to numbers), so '53' becomes 53, and 53 - 2 = 51. This demonstrates that coercion rules differ between operators.",
      "ru": "Оператор + имеет левосторонний приоритет. Сначала '5' + 3 использует конкатенацию строк (+ конвертирует 3 в строку), в результате получается '53'. Затем '53' - 2 заставляет приведение обоих операндов к числам (- всегда конвертирует в числа), поэтому '53' становится 53, и 53 - 2 = 51. Это демонстрирует, что правила приведения различаются между операторами."
    }
  },
  {
    "id": "javascript-quiz-46",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nconst obj = {\n  value: 42,\n  getValue: function() {\n    return this.value;\n  }\n};\nconst fn = obj.getValue;\nconsole.log(fn());\n```",
      "ru": "Что будет выведено в консоль?\n```js\nconst obj = {\n  value: 42,\n  getValue: function() {\n    return this.value;\n  }\n};\nconst fn = obj.getValue;\nconsole.log(fn());\n```"
    },
    "options": {
      "en": [
        "42",
        "null",
        "undefined",
        "ReferenceError"
      ],
      "ru": [
        "42",
        "null",
        "undefined",
        "ReferenceError"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "When a method is extracted from an object and called as a standalone function, `this` is bound to the global object (or undefined in strict mode), not the original object. Since the global object doesn't have a `value` property, `this.value` returns undefined.",
      "ru": "Когда метод извлекается из объекта и вызывается как отдельная функция, `this` привязывается к глобальному объекту (или undefined в strict mode), а не к исходному объекту. Поскольку глобальный объект не имеет свойства `value`, `this.value` возвращает undefined."
    }
  },
  {
    "id": "javascript-quiz-47",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged to the console?\n```js\nfunction pick() {\n  try {\n    return 'try';\n  } finally {\n    return 'finally';\n  }\n}\nfunction clean() {\n  try {\n    throw new Error('boom');\n  } finally {\n    return 'cleanup';\n  }\n}\nconsole.log(pick(), clean());\n```",
      "ru": "Что будет выведено в консоль?\n```js\nfunction pick() {\n  try {\n    return 'try';\n  } finally {\n    return 'finally';\n  }\n}\nfunction clean() {\n  try {\n    throw new Error('boom');\n  } finally {\n    return 'cleanup';\n  }\n}\nconsole.log(pick(), clean());\n```"
    },
    "options": {
      "en": [
        "try cleanup",
        "finally, then the Error from clean() escapes",
        "finally cleanup",
        "try, then the Error from clean() escapes"
      ],
      "ru": [
        "try cleanup",
        "finally, затем ошибка из clean() вылетает наружу",
        "finally cleanup",
        "try, затем ошибка из clean() вылетает наружу"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "A `return` inside `finally` replaces whatever the `try` block was about to do. In `pick` the `try` block prepares the value 'try', then `finally` runs and returns 'finally' instead. In `clean` the `try` block throws, but the `return` in `finally` drops the exception as well, so the call quietly returns 'cleanup'.\n\nWhy the other answers are wrong:\n\n- The answers that start with try assume the first `return` wins. `finally` always runs before the function really returns, and its own `return` overrides the pending one.\n- The answers where the Error escapes miss that a `return` in `finally` swallows an exception, not only a return value.\n\nThat is why linters warn about returning from `finally`: the error disappears without a trace.",
      "ru": "`return` внутри `finally` отменяет то, что собирался сделать блок `try`. В `pick` блок `try` готовит значение 'try', затем срабатывает `finally` и возвращает вместо него 'finally'. В `clean` блок `try` бросает ошибку, но `return` в `finally` выбрасывает и её, поэтому вызов тихо возвращает 'cleanup'.\n\nПочему остальные ответы неверны:\n\n- Ответы, которые начинаются с try, считают, что побеждает первый `return`. Но `finally` всегда выполняется до того, как функция действительно вернёт значение, и его собственный `return` перебивает готовое.\n- Ответы, где ошибка вылетает наружу, не учитывают: `return` в `finally` съедает исключение, а не только возвращаемое значение.\n\nПоэтому линтеры и ругаются на `return` из `finally`: ошибка исчезает без следа."
    }
  },
  {
    "id": "javascript-quiz-48",
    "topicId": "javascript",
    "question": {
      "en": "How does JavaScript's event loop prioritize tasks? Select the correct ordering of execution.",
      "ru": "Как Event Loop JavaScript приоритизирует задачи? Выберите правильный порядок выполнения."
    },
    "options": {
      "en": [
        "Synchronous code → Microtasks → Macrotasks → Rendering",
        "Macrotasks → Synchronous code → Microtasks → Rendering",
        "Synchronous code → Macrotasks → Microtasks → Rendering",
        "Synchronous code → Rendering → Microtasks → Macrotasks"
      ],
      "ru": [
        "Синхронный код → Микротаски → Макротаски → Отрисовка",
        "Макротаски → Синхронный код → Микротаски → Отрисовка",
        "Синхронный код → Макротаски → Микротаски → Отрисовка",
        "Синхронный код → Отрисовка → Микротаски → Макротаски"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The event loop executes synchronous code first, then processes all microtasks (promises, queueMicrotask), then moves to macrotasks (setTimeout, setInterval), and finally performs rendering. Microtasks always execute before the next macrotask.",
      "ru": "Event Loop выполняет синхронный код в первую очередь, затем обрабатывает все микротаски (promises, queueMicrotask), потом переходит к макротаскам (setTimeout, setInterval) и наконец выполняет отрисовку. Микротаски всегда выполняются перед следующим макротаском."
    }
  },
  {
    "id": "javascript-quiz-49",
    "topicId": "javascript",
    "question": {
      "en": "What is the result of the following coercion?\n```js\nconsole.log([] == false);\nconsole.log([] === false);\n```",
      "ru": "Какой результат приведения типов в следующем коде?\n```js\nconsole.log([] == false);\nconsole.log([] === false);\n```"
    },
    "options": {
      "en": [
        "true, true",
        "true, false",
        "false, false",
        "false, true"
      ],
      "ru": [
        "true, true",
        "true, false",
        "false, false",
        "false, true"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "With `==`, an empty array is coerced to an empty string, which coerces to 0, and false also coerces to 0, so they're equal. However, `===` performs strict equality without type coercion, so an array is never strictly equal to a boolean.",
      "ru": "При `==` пустой массив приводится к пустой строке, которая приводится к 0, а false тоже приводится к 0, поэтому они равны. Однако `===` выполняет строгое сравнение без приведения типов, поэтому массив никогда не равен булеву значению в строгом смысле."
    }
  },
  {
    "id": "javascript-quiz-50",
    "topicId": "javascript",
    "question": {
      "en": "What will be logged?\n```js\nfunction* generator() {\n  yield 1;\n  yield 2;\n  return 3;\n}\nconst gen = generator();\nconsole.log(gen.next());\nconsole.log(gen.next());\nconsole.log(gen.next());\n```",
      "ru": "Что будет выведено в консоль?\n```js\nfunction* generator() {\n  yield 1;\n  yield 2;\n  return 3;\n}\nconst gen = generator();\nconsole.log(gen.next());\nconsole.log(gen.next());\nconsole.log(gen.next());\n```"
    },
    "options": {
      "en": [
        "{value: 1, done: true}, {value: 2, done: true}, {value: 3, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: undefined, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: 3, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: 3, done: false}"
      ],
      "ru": [
        "{value: 1, done: true}, {value: 2, done: true}, {value: 3, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: undefined, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: 3, done: true}",
        "{value: 1, done: false}, {value: 2, done: false}, {value: 3, done: false}"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "A generator function uses `yield` to pause execution and return an object with `{value, done}`. When `return` is encountered, it sets `done: true` and the value becomes the return value. The first two calls yield 1 and 2 with `done: false`, and the third returns 3 with `done: true`.",
      "ru": "Функция-генератор использует `yield` для паузы выполнения и возврата объекта с `{value, done}`. Когда встречается `return`, устанавливается `done: true` и value становится возвращаемым значением. Первые два вызова возвращают 1 и 2 с `done: false`, а третий возвращает 3 с `done: true`."
    }
  }
]
