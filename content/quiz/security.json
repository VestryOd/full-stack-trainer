[
  {
    "id": "security-quiz-1",
    "topicId": "security",
    "question": {
      "en": "Which of the following best describes the difference between Stored XSS and Reflected XSS?",
      "ru": "Какое из следующих утверждений лучше всего описывает различие между Stored XSS и Reflected XSS?"
    },
    "options": {
      "en": [
        "Stored XSS persists in the database and affects all users; Reflected XSS needs a crafted link",
        "Reflected XSS uses DOM manipulation while Stored XSS uses server-side template injection",
        "Stored XSS only affects authenticated users, while Reflected XSS affects anonymous users",
        "Reflected XSS is more dangerous because it doesn't leave traces in server logs"
      ],
      "ru": [
        "Stored XSS сохраняется в базе данных и влияет на всех пользователей; Reflected XSS требует подготовленной ссылки",
        "Reflected XSS использует манипуляцию DOM, а Stored XSS использует инъекцию шаблонов на сервере",
        "Stored XSS влияет только на аутентифицированных пользователей, тогда как Reflected XSS влияет на анонимных пользователей",
        "Reflected XSS более опасен, потому что не оставляет следов в логах сервера"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Stored XSS persists because the malicious script is saved in the database and executed for every user who views it, whereas Reflected XSS is non-persistent and only affects users who follow the crafted link. The other options confuse XSS types with other attack vectors or make false claims about persistence and user impact.",
      "ru": "Stored XSS сохраняется, потому что вредоносный скрипт сохраняется в базе данных и выполняется для каждого пользователя, который его просматривает, тогда как Reflected XSS не сохраняется и влияет только на пользователей, которые перейдут по созданной ссылке. Остальные варианты путают типы XSS с другими векторами атак или делают ложные утверждения о сохранении и воздействии на пользователей."
    }
  },
  {
    "id": "security-quiz-2",
    "topicId": "security",
    "question": {
      "en": "What is the primary purpose of the 'SameSite' cookie flag in preventing CSRF attacks?\n```js\nSet-Cookie: sessionId=abc123; SameSite=Strict; HttpOnly; Secure\n```",
      "ru": "Какова основная цель флага 'SameSite' файла cookie при предотвращении атак CSRF?\n```js\nSet-Cookie: sessionId=abc123; SameSite=Strict; HttpOnly; Secure\n```"
    },
    "options": {
      "en": [
        "It encrypts the cookie value so that attackers cannot read or modify it",
        "It prevents the browser from sending the cookie in cross-site requests",
        "It restricts the cookie to only be accessible from the same subdomain, preventing cross-domain attacks",
        "It forces the browser to validate the origin header before sending any cookies to the server"
      ],
      "ru": [
        "Он шифрует значение cookie так, чтобы злоумышленники не могли его прочитать или изменить",
        "Он не даёт браузеру отправлять cookie в кросс-сайтовых запросах",
        "Он ограничивает cookie только доступом с одного и того же поддомена, предотвращая атаки между доменами",
        "Он заставляет браузер проверить заголовок происхождения перед отправкой каких-либо cookie на сервер"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "SameSite controls when cookies are sent with cross-site requests. With Strict, cookies are never sent in cross-site contexts, effectively neutralizing CSRF attacks that rely on automatic cookie submission. HttpOnly and Secure address XSS and man-in-the-middle attacks respectively, not CSRF. The subdomain-restriction option confuses domain restrictions with SameSite behavior, and the origin-header option misrepresents the role of origin header validation.",
      "ru": "SameSite контролирует, когда cookies отправляются при кросс-сайтовых запросах. С Strict cookies никогда не отправляются в кросс-сайтовых контекстах, эффективно нейтрализуя CSRF-атаки, которые полагаются на автоматическую отправку cookies. HttpOnly и Secure решают проблемы XSS и атак man-in-the-middle соответственно, а не CSRF. Вариант про ограничение поддоменом путает ограничения домена с поведением SameSite, а вариант про заголовок происхождения неправильно представляет роль проверки заголовка происхождения."
    }
  },
  {
    "id": "security-quiz-3",
    "topicId": "security",
    "question": {
      "en": "A JWT token has three parts separated by dots: header.payload.signature. If an attacker modifies the payload and recalculates a signature using a publicly known algorithm, what prevents them from creating a valid token?\n\neyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxMjMifQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c\n",
      "ru": "JWT-токен имеет три части, разделенные точками: header.payload.signature. Если атакующий изменит payload и пересчитает подпись, используя общеизвестный алгоритм, что предотвращает создание ими действительного токена?\n\neyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxMjMifQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c\n"
    },
    "options": {
      "en": [
        "The algorithm field in the header is encrypted, so attackers cannot determine which algorithm to use for signing",
        "The payload is base64-encoded and salted, making it impossible to decode and modify without breaking the encoding",
        "The server verifies the signature with a secret key known only to itself",
        "The signature is stored separately on the server, so any local modifications to the token are immediately detected"
      ],
      "ru": [
        "Поле алгоритма в заголовке зашифровано, поэтому атакующие не могут определить, какой алгоритм использовать для подписания",
        "Payload кодируется base64 и добавляется соль, что делает невозможным его декодирование и изменение без нарушения кодировки",
        "Сервер проверяет подпись секретным ключом, который известен только ему",
        "Подпись хранится отдельно на сервере, поэтому любые локальные изменения маркера немедленно обнаруживаются"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "JWT security relies on the server's secret key used during signature creation and verification. Without this key, attackers cannot generate a matching signature for a modified payload, even if they know the algorithm. The option about an encrypted algorithm field is false because the header is not encrypted. The option about base64 encoding and salting misunderstands them—the payload is easily decodable. The option claiming the signature is stored on the server is incorrect because the signature is stored within the token itself, not separately on the server.",
      "ru": "Безопасность JWT зависит от секретного ключа сервера, используемого при создании и проверке подписи. Без этого ключа злоумышленники не могут создать совпадающую подпись для измененного payload, даже если они знают алгоритм. Вариант про зашифрованное поле алгоритма неверен, так как заголовок не зашифрован. Вариант про кодирование base64 и добавление соли неправильно их понимает—payload легко декодируется. Вариант про хранение подписи на сервере неправильный, потому что подпись хранится внутри самого маркера, а не отдельно на сервере."
    }
  },
  {
    "id": "security-quiz-4",
    "topicId": "security",
    "question": {
      "en": "In the OAuth 2.0 Authorization Code flow, why does the server redirect to the client with an authorization code instead of directly issuing an access token to the user agent?",
      "ru": "В потоке Authorization Code OAuth 2.0 почему сервер перенаправляет клиента с кодом авторизации вместо прямой выдачи маркера доступа пользовательскому агенту?"
    },
    "options": {
      "en": [
        "The authorization code adds an extra layer of encryption that makes the access token unreadable to malicious users",
        "The authorization code is shorter than the access token, reducing bandwidth and improving performance",
        "The authorization code allows the resource owner to revoke access before the token is fully issued",
        "The authorization code is exchanged for an access token through a secure backend channel"
      ],
      "ru": [
        "Код авторизации добавляет дополнительный уровень шифрования, который делает маркер доступа нечитаемым для злоумышленников",
        "Код авторизации короче, чем маркер доступа, что снижает пропускную способность и повышает производительность",
        "Код авторизации позволяет владельцу ресурса отозвать доступ перед полной выдачей маркера",
        "Код авторизации обменивается на маркер доступа через безопасный канал бэкенда"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The Authorization Code flow separates user-facing interactions from backend token exchange. The browser never sees the actual access token—only the short-lived authorization code, which is exchanged for the token server-to-server where credentials remain hidden. This prevents token leakage via browser history, referrer headers, and logs. The options about an extra encryption layer, a shorter code, and pre-issue revocation misrepresent the security benefits and mechanics of the flow.",
      "ru": "Поток Authorization Code разделяет взаимодействие с пользователем и обмен маркерами в бэкенде. Браузер никогда не видит фактический маркер доступа—только кратковременный код авторизации, который обменивается на маркер от сервера к серверу, где учетные данные остаются скрытыми. Это предотвращает утечку маркера через историю браузера, заголовки referrer и логи. Варианты про дополнительный уровень шифрования, более короткий код и отзыв доступа до выдачи маркера неправильно представляют преимущества безопасности и механику потока."
    }
  },
  {
    "id": "security-quiz-5",
    "topicId": "security",
    "question": {
      "en": "Consider this SQL query construction:\n```js\nconst userId = request.query.id;\nconst query = `SELECT * FROM users WHERE id = ${userId}`;\ndb.execute(query);\n```\nWhich approach best prevents SQL injection while maintaining functionality?",
      "ru": "Рассмотрим это конструирование SQL-запроса:\n```js\nconst userId = request.query.id;\nconst query = `SELECT * FROM users WHERE id = ${userId}`;\ndb.execute(query);\n```\nКакой подход лучше всего предотвращает SQL-инъекцию, сохраняя при этом функциональность?"
    },
    "options": {
      "en": [
        "Use parameterized queries: db.execute('SELECT * FROM users WHERE id = ?', [userId])",
        "Escape special SQL characters in userId using a custom sanitization function before interpolation",
        "Use an ORM like Sequelize or TypeORM which automatically handles query construction",
        "Validate that userId is numeric and restrict input length to prevent malicious payloads"
      ],
      "ru": [
        "Используйте параметризованные запросы: db.execute('SELECT * FROM users WHERE id = ?', [userId])",
        "Экранируйте специальные символы SQL в userId с помощью пользовательской функции санитизации перед интерполяцией",
        "Используйте ORM, такой как Sequelize или TypeORM, который автоматически обрабатывает конструирование запросов",
        "Проверьте, что userId является числовым, и ограничьте длину ввода, чтобы предотвратить вредоносные нагрузки"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Parameterized queries are the gold standard for SQL injection prevention because they treat user input as data, not executable code, regardless of content. Custom escaping is error-prone and database-specific. While ORMs and input validation are helpful defense-in-depth measures, they are secondary to parameterized queries and do not provide the same level of guarantee against injection attacks.",
      "ru": "Параметризованные запросы являются стандартом для предотвращения SQL-инъекций, потому что они обрабатывают ввод пользователя как данные, а не исполняемый код, независимо от содержания. Пользовательское экранирование подвержено ошибкам и зависит от базы данных. Хотя ORM и валидация ввода полезны в качестве дополнительных мер, они являются вторичными по отношению к параметризованным запросам и не обеспечивают того же уровня защиты от атак инъекций."
    }
  },
  {
    "id": "security-quiz-6",
    "topicId": "security",
    "question": {
      "en": "What is the primary difference between Stored XSS and Reflected XSS attacks?",
      "ru": "В чем основное различие между атаками Stored XSS и Reflected XSS?"
    },
    "options": {
      "en": [
        "Reflected XSS persists in the database and affects all users, while Stored XSS only affects the user who clicks a malicious link",
        "Stored XSS persists in the database and affects all users, while Reflected XSS only affects the user who clicks a malicious link",
        "Stored XSS requires JavaScript to be enabled, while Reflected XSS works without JavaScript",
        "Both attack types are identical in impact; the terms are just different naming conventions"
      ],
      "ru": [
        "Reflected XSS сохраняется в базе данных и влияет на всех пользователей, а Stored XSS влияет только на пользователя, нажимающего на вредоносную ссылку",
        "Stored XSS сохраняется в базе данных и влияет на всех пользователей, а Reflected XSS влияет только на пользователя, нажимающего на вредоносную ссылку",
        "Stored XSS требует включения JavaScript, а Reflected XSS работает без JavaScript",
        "Оба типа атак идентичны по воздействию; термины - это просто разные соглашения об именовании"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Stored XSS saves malicious code in a database, affecting every user who retrieves that data, making it more dangerous. Reflected XSS is only sent back to the user who triggered it, typically through a crafted URL. The persistence in storage is the key differentiator.",
      "ru": "Stored XSS сохраняет вредоносный код в базе данных, влияя на каждого пользователя, который получает эти данные, что делает его более опасным. Reflected XSS отправляется только пользователю, который его вызвал, обычно через специально созданный URL. Сохранение в хранилище - это ключевое различие."
    }
  },
  {
    "id": "security-quiz-7",
    "topicId": "security",
    "question": {
      "en": "Consider a JWT token with header.payload.signature structure. If an attacker modifies the payload but keeps the original signature, what will happen when the server verifies the token?\n```js\n// Server verification\nconst decoded = jwt.verify(token, secretKey);\n```",
      "ru": "Рассмотрим JWT токен со структурой header.payload.signature. Если злоумышленник изменит payload, но оставит исходную подпись, что произойдет при проверке токена сервером?\n```js\n// Проверка на сервере\nconst decoded = jwt.verify(token, secretKey);\n```"
    },
    "options": {
      "en": [
        "The verification will succeed and the modified payload will be accepted as valid",
        "The server will accept the token but log a warning about the modification",
        "The verification will fail because the signature no longer matches the modified payload",
        "The verification will fail only if the payload contains sensitive information"
      ],
      "ru": [
        "Проверка пройдет успешно и измененный payload будет принят как действительный",
        "Сервер примет токен, но выведет предупреждение об изменении",
        "Проверка не пройдет, потому что подпись больше не соответствует измененному payload",
        "Проверка не пройдет только если payload содержит чувствительную информацию"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "JWT verification works by recalculating the signature using the secret key and the original payload. If the payload is modified, the recalculated signature won't match the original signature in the token, causing verification to fail. This is the security mechanism that prevents tampering.",
      "ru": "Проверка JWT работает путем пересчета подписи с использованием секретного ключа и исходного payload. Если payload изменен, пересчитанная подпись не совпадет с исходной подписью в токене, что приведет к сбою проверки. Это механизм безопасности, предотвращающий манипуляции."
    }
  },
  {
    "id": "security-quiz-8",
    "topicId": "security",
    "question": {
      "en": "Which HTTP-only cookie flag combination provides the strongest security posture for session tokens?",
      "ru": "Какая комбинация флагов HTTP-only cookie обеспечивает наиболее надежный уровень безопасности для токенов сеанса?"
    },
    "options": {
      "en": [
        "HttpOnly alone is sufficient to prevent all XSS and CSRF attacks",
        "Secure flag is most important; HttpOnly and SameSite are optional enhancements",
        "SameSite=Lax combined with HttpOnly is equivalent to all other security measures",
        "HttpOnly, Secure, SameSite=Strict, and appropriate Domain/Path restrictions"
      ],
      "ru": [
        "HttpOnly достаточно для предотвращения всех атак XSS и CSRF",
        "Флаг Secure является наиболее важным; HttpOnly и SameSite - это необязательные улучшения",
        "SameSite=Lax в сочетании с HttpOnly эквивалентен всем другим мерам безопасности",
        "HttpOnly, Secure, SameSite=Strict и соответствующие ограничения Domain/Path"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "A defense-in-depth approach combines HttpOnly (prevents JavaScript access), Secure (HTTPS only), SameSite=Strict (prevents CSRF), and Domain/Path restrictions (limits cookie scope). While each flag addresses specific threats, using all of them together provides comprehensive protection against multiple attack vectors.",
      "ru": "Подход многоуровневой защиты объединяет HttpOnly (предотвращает доступ JavaScript), Secure (только HTTPS), SameSite=Strict (предотвращает CSRF) и ограничения Domain/Path (ограничивает область действия cookie). Хотя каждый флаг устраняет определенные угрозы, использование всех них вместе обеспечивает комплексную защиту от множественных векторов атак."
    }
  },
  {
    "id": "security-quiz-9",
    "topicId": "security",
    "question": {
      "en": "In the OAuth 2.0 Authorization Code flow, why is the authorization code exchanged for an access token at the backend rather than returned directly to the frontend?",
      "ru": "В потоке OAuth 2.0 Authorization Code, почему код авторизации обменивается на токен доступа на серверной части, а не возвращается непосредственно на фронтенд?"
    },
    "options": {
      "en": [
        "The client secret must stay confidential on the backend",
        "Authorization codes are larger than access tokens and must be compressed on the backend",
        "The frontend lacks the computational power to perform token exchange cryptography",
        "OAuth 2.0 specification requires all token exchanges to happen asynchronously"
      ],
      "ru": [
        "Секрет клиента должен оставаться в тайне на серверной части",
        "Коды авторизации больше токенов доступа и должны быть сжаты на серверной части",
        "Фронтенд не обладает вычислительной мощностью для выполнения криптографии обмена токенов",
        "Спецификация OAuth 2.0 требует, чтобы все обмены токенами происходили асинхронно"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The client secret is sensitive credential that must never be exposed to the frontend or user's browser. By exchanging the authorization code on the backend, the application can securely authenticate with the authorization server using its client secret, which would be compromised if exposed to the frontend.",
      "ru": "Секрет клиента - это чувствительные учетные данные, которые никогда не должны быть раскрыты фронтенду или браузеру пользователя. Обмен кода авторизации на серверной части позволяет приложению безопасно аутентифицироваться на сервере авторизации, используя свой секрет клиента, который был бы скомпрометирован при раскрытии фронтенду."
    }
  },
  {
    "id": "security-quiz-10",
    "topicId": "security",
    "question": {
      "en": "A Content Security Policy header with `script-src 'self'` is set. What will happen if inline JavaScript in the HTML attempts to execute?\n```html\n<script>\n  console.log('Inline script');\n</script>\n```",
      "ru": "Установлен заголовок Content Security Policy с `script-src 'self'`. Что произойдет, если встроенный JavaScript в HTML попытается выполниться?\n```html\n<script>\n  console.log('Inline script');\n</script>\n```"
    },
    "options": {
      "en": [
        "The inline script will execute normally because it originates from 'self'",
        "The inline script will be blocked by CSP; only separate files from the same origin run",
        "The inline script will execute with a warning in the console but no blocking",
        "The CSP header only applies to scripts loaded from other origins, not to inline scripts in the page"
      ],
      "ru": [
        "Встроенный скрипт будет нормально выполняться, потому что он происходит из 'self'",
        "Встроенный скрипт будет заблокирован CSP; выполнятся только отдельные файлы с того же источника",
        "Встроенный скрипт будет выполняться с предупреждением в консоли, но без блокировки",
        "Заголовок CSP применяется только к скриптам с других источников, а не к встроенным скриптам на странице"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "CSP `script-src 'self'` means scripts can only load from the same origin, but inline scripts are explicitly blocked by default for security reasons. Only external script files from the same origin will execute. To allow inline scripts, you would need to use `'unsafe-inline'` or nonce/hash values, which weakens security.",
      "ru": "CSP `script-src 'self'` означает, что скрипты могут загружаться только с того же источника, но встроенные скрипты явно блокируются по умолчанию по соображениям безопасности. Выполняться будут только внешние файлы скриптов с того же источника. Чтобы разрешить встроенные скрипты, нужно использовать `'unsafe-inline'` или значения nonce/hash, что снижает безопасность."
    }
  },
  {
    "id": "security-quiz-11",
    "topicId": "security",
    "question": {
      "en": "Which of the following best describes the difference between Stored XSS and Reflected XSS?\n```html\n<!-- Stored: malicious script saved in database -->\n<!-- Reflected: malicious script in URL parameter -->\n```",
      "ru": "Какое из следующих утверждений лучше всего описывает разницу между Stored XSS и Reflected XSS?\n```html\n<!-- Stored: вредоносный скрипт сохранён в БД -->\n<!-- Reflected: вредоносный скрипт в параметре URL -->\n```"
    },
    "options": {
      "en": [
        "Reflected XSS persists in the database and affects all users; Stored XSS only affects users who click a malicious link",
        "Stored XSS requires a Content Security Policy to prevent; Reflected XSS cannot be prevented",
        "Stored XSS persists in the database and affects all users; Reflected XSS only affects users who click a malicious link",
        "Both types are equally dangerous and have identical prevention methods"
      ],
      "ru": [
        "Reflected XSS сохраняется в БД и влияет на всех пользователей; Stored XSS влияет только на пользователей, кликнувших на вредоносную ссылку",
        "Stored XSS требует Content Security Policy для предотвращения; Reflected XSS невозможно предотвратить",
        "Stored XSS сохраняется в БД и влияет на всех пользователей; Reflected XSS влияет только на пользователей, кликнувших на вредоносную ссылку",
        "Оба типа одинаково опасны и имеют идентичные методы предотвращения"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Stored XSS is more dangerous because the malicious script is permanently saved in the database and executes for every user who views the affected content. Reflected XSS only affects users who are tricked into clicking a malicious link with embedded payload in the URL. Both can be prevented through output encoding and input validation.",
      "ru": "Stored XSS более опасна, потому что вредоносный скрипт постоянно сохранён в БД и выполняется для каждого пользователя, просматривающего заражённый контент. Reflected XSS влияет только на пользователей, которые кликнули на вредоносную ссылку с встроенной полезной нагрузкой в URL. Оба типа можно предотвратить через кодирование выхода и валидацию входа."
    }
  },
  {
    "id": "security-quiz-12",
    "topicId": "security",
    "question": {
      "en": "You have a JWT token with the following structure. What is a critical security flaw if the 'secret' key is weak or hardcoded in the client code?\n\nHeader.Payload.Signature\n",
      "ru": "У вас есть JWT-токен со следующей структурой. Какая критическая уязвимость безопасности возникает, если 'secret' слабый или жёстко закодирован в клиентском коде?\n\nHeader.Payload.Signature\n"
    },
    "options": {
      "en": [
        "The JWT token becomes too large and causes performance issues",
        "The token cannot be decoded by the server, making authentication impossible",
        "The signature automatically expires after 24 hours regardless of expiration claims",
        "An attacker who knows the secret can forge valid tokens and impersonate any user"
      ],
      "ru": [
        "JWT-токен становится слишком большим и вызывает проблемы производительности",
        "Токен не может быть декодирован сервером, что делает аутентификацию невозможной",
        "Подпись автоматически истекает через 24 часа независимо от заявлений об истечении",
        "Злоумышленник, зная секрет, может создать поддельные токены и выдать себя за любого пользователя"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "If the secret key is weak or exposed (especially if hardcoded in client-side code), attackers can generate valid JWT tokens by computing the signature with that known secret. This bypasses authentication entirely. The secret should be strong, kept on the server only, and never exposed to clients. JWT size and automatic expiration are not related to secret strength.",
      "ru": "Если секретный ключ слабый или раскрыт (особенно если жёстко закодирован в клиентском коде), злоумышленники могут создать действительные JWT-токены, вычислив подпись с известным секретом. Это полностью обходит аутентификацию. Секрет должен быть сильным, храниться только на сервере и никогда не раскрываться клиентам. Размер JWT и автоматическое истечение не связаны с прочностью секрета."
    }
  },
  {
    "id": "security-quiz-13",
    "topicId": "security",
    "question": {
      "en": "Which HTTP header and cookie flag combination most effectively prevents CSRF attacks while maintaining secure session handling?\n\nSet-Cookie: sessionId=abc123; SameSite=Strict; Secure; HttpOnly\n",
      "ru": "Какая комбинация HTTP-заголовка и флага cookies наиболее эффективно предотвращает CSRF-атаки при сохранении безопасного управления сеансом?\n\nSet-Cookie: sessionId=abc123; SameSite=Strict; Secure; HttpOnly\n"
    },
    "options": {
      "en": [
        "SameSite=Strict blocks cross-site cookies; Secure requires HTTPS; HttpOnly blocks JavaScript",
        "SameSite=Lax alone already blocks every cross-site request, so Secure and HttpOnly add nothing here",
        "HttpOnly flag alone is enough to prevent CSRF attacks completely",
        "Secure flag is the only flag needed; SameSite and HttpOnly are redundant"
      ],
      "ru": [
        "SameSite=Strict блокирует кросс-сайтовые cookie; Secure требует HTTPS; HttpOnly закрывает доступ из JavaScript",
        "SameSite=Lax сам по себе блокирует любой кросс-сайтовый запрос, поэтому Secure и HttpOnly ничего не добавляют",
        "Флага HttpOnly достаточно для полной защиты от CSRF-атак",
        "Флаг Secure — единственный необходимый флаг; SameSite и HttpOnly избыточны"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "SameSite=Strict is the strongest CSRF protection by preventing cookies from being sent on cross-site requests. Secure ensures the cookie is only sent over HTTPS (preventing MITM attacks). HttpOnly prevents XSS from stealing the session token via JavaScript. While each flag serves a specific purpose, together they provide defense-in-depth. SameSite=Lax is weaker and doesn't prevent all CSRF vectors.",
      "ru": "SameSite=Strict — самая сильная защита от CSRF, предотвращая отправку cookies при кросс-сайтовых запросах. Secure гарантирует, что cookie отправляется только через HTTPS (предотвращая MITM-атаки). HttpOnly предотвращает XSS от кражи токена сеанса через JavaScript. Хотя каждый флаг служит определённой цели, вместе они обеспечивают защиту в глубину. SameSite=Lax слабее и не предотвращает все CSRF-векторы."
    }
  },
  {
    "id": "security-quiz-14",
    "topicId": "security",
    "question": {
      "en": "What is the primary security purpose of implementing a Content Security Policy (CSP) header like `Content-Security-Policy: script-src 'self'`?",
      "ru": "Какова основная цель безопасности реализации заголовка Content Security Policy (CSP) вроде `Content-Security-Policy: script-src 'self'`?"
    },
    "options": {
      "en": [
        "To encrypt all scripts transmitted over the network using TLS",
        "To restrict script execution to files from the same origin",
        "To automatically patch vulnerabilities in third-party JavaScript libraries",
        "To block all network requests from the website to improve performance"
      ],
      "ru": [
        "Зашифровать все скрипты, передаваемые по сети, используя TLS",
        "Ограничить выполнение скриптов файлами с того же источника",
        "Автоматически исправлять уязвимости в библиотеках JavaScript третьих сторон",
        "Блокировать все сетевые запросы с веб-сайта для улучшения производительности"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "`script-src 'self'` tells the browser to run scripts only from the page's own origin. An injected `<script>` from anywhere else, and inline script text, are refused.\n\nThat makes CSP a strong second layer, not the first one. It does not stop the injection itself: the input is still being reflected into the page, and the primary fix is contextual output encoding. CSP limits the damage when that fix is missing or imperfect.\n\nNote that `'self'` alone is coarse. If any script reachable on your origin can be abused — an old JSONP endpoint, an open redirect — the allowlist lets it through. Hashes or nonces are stricter.",
      "ru": "`script-src 'self'` говорит браузеру выполнять скрипты только с собственного источника страницы. Внедрённый `<script>` с чужого адреса и инлайновый код будут отклонены.\n\nЭто делает CSP сильным вторым слоем, но не первым. Саму инъекцию он не предотвращает: ввод по-прежнему попадает в страницу, и основное исправление — контекстное экранирование вывода. CSP ограничивает ущерб, когда этого исправления нет или оно неполное.\n\nУчтите, что одного `'self'` мало. Если на вашем же источнике есть скрипт, которым можно воспользоваться — старый JSONP-эндпоинт, открытый редирект, — список разрешённого его пропустит. Хеши или nonce строже."
    }
  },
  {
    "id": "security-quiz-15",
    "topicId": "security",
    "question": {
      "en": "In OAuth 2.0, what is the key security difference between the Authorization Code flow and the Implicit flow, particularly regarding token exposure?",
      "ru": "В OAuth 2.0, какова ключевая разница безопасности между потоком Authorization Code и потоком Implicit, особенно в отношении раскрытия токена?"
    },
    "options": {
      "en": [
        "Implicit flow is more secure because it reduces the number of HTTP requests required",
        "Authorization Code flow stores tokens in localStorage while Implicit stores them in cookies, making Implicit more secure",
        "Authorization Code exchanges a code for tokens over a backend channel; Implicit returns tokens in the URL fragment",
        "Both flows have identical security properties; the difference is only in the number of steps required"
      ],
      "ru": [
        "Поток Implicit безопаснее, потому что снижает количество необходимых HTTP-запросов",
        "Поток Authorization Code хранит токены в localStorage, а Implicit в cookies, делая Implicit более безопасным",
        "Authorization Code обменивает код на токены через канал бэкенда; Implicit возвращает токены в фрагменте URL",
        "Оба потока имеют идентичные свойства безопасности; разница только в количестве необходимых шагов"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Authorization Code flow is more secure because tokens are exchanged server-to-server via a backend channel, never exposed to the browser. Implicit flow sends tokens directly in the URL fragment, making them visible in browser history, referrer headers, and vulnerable to XSS. Modern recommendations deprecate Implicit flow for SPAs in favor of Authorization Code with PKCE. The number of requests and storage method are not the primary security distinction.",
      "ru": "Поток Authorization Code более безопасен, потому что токены обмениваются сервер-к-серверу через канал бэкенда, никогда не открываясь браузеру. Поток Implicit отправляет токены прямо в фрагменте URL, делая их видимыми в истории браузера, заголовках referrer и уязвимыми для XSS. Современные рекомендации исключают Implicit-поток для SPA в пользу Authorization Code с PKCE. Количество запросов и метод хранения не являются основным различием в безопасности."
    }
  },
  {
    "id": "security-quiz-16",
    "topicId": "security",
    "question": {
      "en": "A JWT token consists of three parts separated by dots. What is the correct order and what does each part represent?\n\neyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIn0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c\n",
      "ru": "JWT токен состоит из трёх частей, разделённых точками. Какой правильный порядок и что представляет каждая часть?\n\neyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIn0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c\n"
    },
    "options": {
      "en": [
        "Payload (claims), Header (algorithm & type), Signature",
        "Signature, Header (algorithm & type), Payload (claims)",
        "Header (algorithm & type), Signature, Payload (claims)",
        "Header (algorithm & type), Payload (claims), Signature"
      ],
      "ru": [
        "Полезная нагрузка (claims), Заголовок (алгоритм и тип), Подпись",
        "Подпись, Заголовок (алгоритм и тип), Полезная нагрузка (claims)",
        "Заголовок (алгоритм и тип), Подпись, Полезная нагрузка (claims)",
        "Заголовок (алгоритм и тип), Полезная нагрузка (claims), Подпись"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "JWT tokens always follow the format: Header.Payload.Signature. The header contains metadata like the algorithm and token type, the payload contains the claims and user data, and the signature ensures the token hasn't been tampered with. The order is fixed and cannot be rearranged.",
      "ru": "JWT токены всегда следуют формату: Заголовок.Полезная_нагрузка.Подпись. Заголовок содержит метаданные, такие как алгоритм и тип токена, полезная нагрузка содержит claims и данные пользователя, а подпись гарантирует, что токен не был подделан. Порядок является фиксированным и не может быть изменён."
    }
  },
  {
    "id": "security-quiz-17",
    "topicId": "security",
    "question": {
      "en": "Which of the following HTTP headers is most effective at preventing Reflected XSS attacks?\n\nContent-Security-Policy: default-src 'self'; script-src 'self' trusted.com\n",
      "ru": "Какой из следующих HTTP-заголовков наиболее эффективен для предотвращения атак Reflected XSS?\n\nContent-Security-Policy: default-src 'self'; script-src 'self' trusted.com\n"
    },
    "options": {
      "en": [
        "Content-Security-Policy header with strict script-src directives",
        "Set-Cookie header with the HttpOnly flag on the session cookie",
        "Access-Control-Allow-Origin header limited to trusted origins",
        "X-Frame-Options header set to DENY on every response"
      ],
      "ru": [
        "Заголовок Content-Security-Policy со строгими директивами script-src",
        "Заголовок Set-Cookie с флагом HttpOnly на cookie сеанса",
        "Заголовок Access-Control-Allow-Origin, ограниченный доверенными источниками",
        "Заголовок X-Frame-Options со значением DENY для каждого ответа"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "CSP headers directly control which scripts can execute on a page, effectively blocking inline scripts and scripts from unauthorized sources used in XSS attacks. While HttpOnly prevents JavaScript access to cookies and X-Frame-Options prevents clickjacking, CSP is the primary mitigation for XSS. Access-Control-Allow-Origin controls CORS, not XSS.",
      "ru": "Заголовки CSP напрямую контролируют, какие скрипты могут выполняться на странице, блокируя встроенные скрипты и скрипты из неавторизованных источников, используемых в XSS-атаках. Хотя HttpOnly предотвращает доступ JavaScript к cookies, а X-Frame-Options предотвращает clickjacking, CSP является основной защитой от XSS. Access-Control-Allow-Origin контролирует CORS, а не XSS."
    }
  },
  {
    "id": "security-quiz-18",
    "topicId": "security",
    "question": {
      "en": "Consider a web form that transfers money. What is the primary vulnerability if the application only validates the request came from the same origin using the Referer header?\n```html\n<form method=\"POST\" action=\"/transfer-money\">\n  <input type=\"hidden\" name=\"amount\" value=\"1000\">\n  <input type=\"hidden\" name=\"recipient\" value=\"attacker@evil.com\">\n  <input type=\"submit\" value=\"Claim Prize\">\n</form>\n```",
      "ru": "Рассмотрим веб-форму для передачи денег. Какова основная уязвимость, если приложение проверяет только источник запроса с помощью заголовка Referer?\n```html\n<form method=\"POST\" action=\"/transfer-money\">\n  <input type=\"hidden\" name=\"amount\" value=\"1000\">\n  <input type=\"hidden\" name=\"recipient\" value=\"attacker@evil.com\">\n  <input type=\"submit\" value=\"Claim Prize\">\n</form>\n```"
    },
    "options": {
      "en": [
        "XSS attack - the hidden input values are reflected back without encoding",
        "CSRF attack - the Referer header can be spoofed or missing",
        "SQL Injection - the recipient parameter is not sanitized",
        "Session Hijacking - the form uses POST instead of GET method"
      ],
      "ru": [
        "XSS атака - скрытые значения входных данных отражаются обратно без кодирования",
        "CSRF атака - заголовок Referer может быть подделан или отсутствовать",
        "SQL Injection - параметр recipient не санитизирован",
        "Session Hijacking - форма использует POST вместо GET метода"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Relying on the Referer header is a weak defence against CSRF (cross-site request forgery — a forged request sent from another site with your cookies). Browsers can omit that header, especially when moving from HTTPS to HTTP, and proxies can strip it. The proper defence is a synchronizer token: a per-session value the server issues and checks on every state-changing request. `SameSite` cookies complement that token but do not replace it. `SameSite` is a cookie attribute, not a token. Hidden input values do not indicate XSS, SQL injection, or session hijacking in this context.",
      "ru": "Полагаться на заголовок Referer — слабая защита от CSRF (cross-site request forgery — межсайтовая подделка запроса, когда чужой сайт отправляет запрос от вашего имени). Браузер может не отправить этот заголовок, особенно при переходе с HTTPS на HTTP, а прокси может его вырезать. Правильная защита — synchronizer token: значение на сессию, которое сервер выдаёт и проверяет при каждом запросе, меняющем состояние. Cookies с атрибутом `SameSite` дополняют такой токен, но не заменяют его. `SameSite` — это атрибут cookie, а не токен. Скрытые значения полей формы не указывают здесь ни на XSS, ни на SQL Injection, ни на перехват сессии."
    }
  },
  {
    "id": "security-quiz-19",
    "topicId": "security",
    "question": {
      "en": "In the OAuth 2.0 Authorization Code flow, which step poses the highest security risk if not implemented correctly?\n\n1. User clicks \"Login with Provider\"\n2. Browser redirected to: https://provider.com/authorize?client_id=xxx&redirect_uri=...\n3. User authenticates and grants permission\n4. Browser redirected to: https://app.com/callback?code=AUTH_CODE\n5. Backend exchanges code for access token using client secret\n",
      "ru": "В потоке OAuth 2.0 Authorization Code, какой шаг представляет наибольший риск безопасности, если реализован неправильно?\n\n1. Пользователь нажимает \"Войти через Provider\"\n2. Браузер перенаправляется на: https://provider.com/authorize?client_id=xxx&redirect_uri=...\n3. Пользователь проходит аутентификацию и даёт разрешение\n4. Браузер перенаправляется на: https://app.com/callback?code=AUTH_CODE\n5. Backend обменивает код на access token, используя client secret\n"
    },
    "options": {
      "en": [
        "Step 2 - client_id can be exposed in the URL query parameters",
        "Step 3 - user may not understand what permissions they are granting",
        "Step 4 - weak redirect_uri validation lets an attacker steal the authorization code",
        "Step 5 - the client secret may be transmitted insecurely to the backend"
      ],
      "ru": [
        "Шаг 2 - client_id может быть раскрыт в параметрах URL запроса",
        "Шаг 3 - пользователь может не понять, какие разрешения он даёт",
        "Шаг 4 - слабая проверка redirect_uri позволяет злоумышленнику украсть код авторизации",
        "Шаг 5 - client secret может быть передан небезопасно на backend"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The redirect_uri must be strictly validated against a whitelist on the provider's side. If validation is missing, an attacker can register a malicious redirect_uri and intercept the authorization code. While client_id exposure is not ideal, it's less critical since the code also requires the client_secret. Step 3 is a UX concern, not a technical vulnerability, and step 5 involves secure backend channels.",
      "ru": "redirect_uri должен быть строго проверен против whitelist на стороне провайдера. Если валидация отсутствует, злоумышленник может зарегистрировать вредоносный redirect_uri и перехватить код авторизации. Хотя раскрытие client_id не идеально, это менее критично, так как код также требует client_secret. Шаг 3 является проблемой UX, а не технической уязвимостью, а шаг 5 включает безопасные каналы backend."
    }
  },
  {
    "id": "security-quiz-20",
    "topicId": "security",
    "question": {
      "en": "A developer uses prepared statements to prevent SQL injection but still concatenates user input into dynamic table names. What vulnerability remains?\n```sql\nconst query = `SELECT * FROM ${tableName} WHERE id = ?`;\ndb.execute(query, [userId]);\n```",
      "ru": "Разработчик использует подготовленные утверждения для предотвращения SQL injection, но всё ещё конкатенирует пользовательский ввод в динамические имена таблиц. Какая уязвимость остаётся?\n```sql\nconst query = `SELECT * FROM ${tableName} WHERE id = ?`;\ndb.execute(query, [userId]);\n```"
    },
    "options": {
      "en": [
        "XSS vulnerability - the tableName is reflected in the query string without encoding",
        "The code is secure - prepared statements protect against all forms of SQL injection",
        "CSRF vulnerability - the query is executed server-side without token validation",
        "SQL Injection - table names cannot be parameterized by prepared statements"
      ],
      "ru": [
        "XSS уязвимость - tableName отражается в строке запроса без кодирования",
        "Код безопасен - подготовленные утверждения защищают от всех форм SQL Injection",
        "CSRF уязвимость - запрос выполняется на сервере без валидации токена",
        "SQL Injection - имена таблиц не могут быть параметризованы подготовленными утверждениями"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Prepared statements only protect parameterized values (like the WHERE clause), not SQL structure elements like table names. Since table names cannot be parameterized, they must be strictly validated against a whitelist of allowed tables. Using string concatenation for table names allows an attacker to access or modify unintended tables. XSS and CSRF are not relevant here as this is server-side SQL execution.",
      "ru": "Подготовленные утверждения защищают только параметризованные значения (например, WHERE clause), но не элементы структуры SQL, такие как имена таблиц. Так как имена таблиц не могут быть параметризованы, они должны быть строго проверены против whitelist разрешённых таблиц. Использование конкатенации строк для имён таблиц позволяет злоумышленнику получить доступ к неопределённым таблицам или их изменить. XSS и CSRF здесь не имеют значения, так как это выполнение SQL на сервере."
    }
  },
  {
    "id": "security-quiz-21",
    "topicId": "security",
    "question": {
      "en": "Which of the following best describes the difference between Stored XSS and Reflected XSS?\n```html\n<!-- Example: attacker injects <script>alert('xss')</script> -->\n```\nWhen is the payload permanently saved in the database versus temporarily reflected in the response?",
      "ru": "Какое из следующих утверждений лучше всего описывает разницу между Stored XSS и Reflected XSS?\n```html\n<!-- Пример: злоумышленник вводит <script>alert('xss')</script> -->\n```\nКогда полезная нагрузка постоянно сохраняется в базе данных, а не временно отражается в ответе?"
    },
    "options": {
      "en": [
        "Stored XSS saves the payload in the database; Reflected XSS lives only in a malicious link",
        "Reflected XSS saves the payload in the database; Stored XSS only affects the current user",
        "Both types save data permanently but Stored XSS requires user authentication",
        "Stored XSS and Reflected XSS are the same vulnerability with different naming conventions"
      ],
      "ru": [
        "Stored XSS сохраняет полезную нагрузку в базе данных; Reflected XSS существует только во вредоносной ссылке",
        "Reflected XSS сохраняет полезную нагрузку в базе данных; Stored XSS влияет только на текущего пользователя",
        "Оба типа сохраняют данные постоянно, но Stored XSS требует аутентификации пользователя",
        "Stored XSS и Reflected XSS - это одна и та же уязвимость с разными названиями"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Stored XSS persists in the database and is served to every user viewing that content, making it more dangerous. Reflected XSS exists only in the malicious URL and affects only users who click it. The other options swap the two types, claim that both store data, or treat them as one and the same bug.",
      "ru": "Stored XSS сохраняется в базе данных и отправляется всем пользователям, просматривающим это содержимое, что делает её более опасной. Reflected XSS существует только в вредоносном URL и влияет только на пользователей, нажимающих на него. Остальные варианты меняют типы местами, утверждают, что оба сохраняют данные, или считают их одной и той же уязвимостью."
    }
  },
  {
    "id": "security-quiz-22",
    "topicId": "security",
    "question": {
      "en": "A JWT token has three parts separated by dots. What is encoded in each section?\n\neyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.dozjgNryP4J3jVmNHl0w5N_XgL0n3I9PlFUP0THsR8U\n\nWhich part should be trusted for authentication decisions?",
      "ru": "JWT токен состоит из трёх частей, разделённых точками. Что кодируется в каждом разделе?\n\neyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.dozjgNryP4J3jVmNHl0w5N_XgL0n3I9PlFUP0THsR8U\n\nКакую часть нужно использовать для принятия решений по аутентификации?"
    },
    "options": {
      "en": [
        "Header (claims), Payload (algorithm info), Signature (verification); only the Payload should be trusted because it holds the user data",
        "Header (algorithm info), Payload (claims), Signature (verification); only the Signature should be trusted",
        "Header (encryption key), Payload (claims), Signature (verification); only the Header should be trusted",
        "All three parts are equally important and should all be trusted without verification"
      ],
      "ru": [
        "Заголовок (утверждения), Полезная нагрузка (информация об алгоритме), Подпись (проверка); следует доверять только Полезной нагрузке, так как в ней данные пользователя",
        "Заголовок (информация об алгоритме), Полезная нагрузка (утверждения), Подпись (проверка); следует доверять только Подписи",
        "Заголовок (ключ шифрования), Полезная нагрузка (утверждения), Подпись (проверка); следует доверять только Заголовку",
        "Все три части одинаково важны и должны приниматься без проверки"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The Header contains algorithm metadata, the Payload contains user claims (can be decoded but not trusted as-is), and the Signature cryptographically verifies the token's integrity. You must verify the signature before trusting the claims—the Payload alone is not secure because it's only Base64-encoded, not encrypted.",
      "ru": "Заголовок содержит метаданные алгоритма, Полезная нагрузка содержит утверждения пользователя (может быть декодирована, но не следует доверять как есть), и Подпись криптографически проверяет целостность токена. Вы должны проверить подпись перед доверием к утверждениям - Полезная нагрузка сама по себе не безопасна, так как она только Base64-кодирована, а не зашифрована."
    }
  },
  {
    "id": "security-quiz-23",
    "topicId": "security",
    "question": {
      "en": "What is the primary purpose of a Content Security Policy (CSP) header, and which of these is the most restrictive and secure default?\n\nContent-Security-Policy: default-src 'self';\n",
      "ru": "Какова основная цель заголовка Content Security Policy (CSP), и какой из этих вариантов является наиболее ограничивающим и безопасным по умолчанию?\n\nContent-Security-Policy: default-src 'self';\n"
    },
    "options": {
      "en": [
        "CSP encrypts all traffic between server and client; 'default-src 'self'' requires HTTPS on all resources",
        "CSP prevents CSRF attacks by validating token origins; 'default-src 'self'' allows inline scripts from the current domain",
        "CSP controls which sources may load resources; `default-src 'self'` allows only same-origin resources",
        "CSP is a performance optimization that caches external resources; 'default-src 'self'' speeds up page load times"
      ],
      "ru": [
        "CSP шифрует весь трафик между сервером и клиентом; 'default-src 'self'' требует HTTPS для всех ресурсов",
        "CSP предотвращает CSRF-атаки путём проверки источников токенов; 'default-src 'self'' разрешает встроенные скрипты из текущего домена",
        "CSP контролирует, какие источники могут загружать ресурсы; `default-src 'self'` разрешает только ресурсы того же источника",
        "CSP - это оптимизация производительности, которая кэширует внешние ресурсы; 'default-src 'self'' ускоряет загрузку страницы"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "CSP is a browser security mechanism that whitelists allowed resource origins, preventing inline script injection and external XSS vectors. 'default-src 'self'' is the most restrictive as it only allows resources from the same origin, blocking all inline code and external CDNs by default. This is the security best practice.",
      "ru": "CSP - это механизм безопасности браузера, который разрешает источники ресурсов, предотвращая встроенное внедрение скриптов и внешние XSS-векторы. 'default-src 'self'' является наиболее ограничивающим, так как разрешает только ресурсы с того же источника, блокируя по умолчанию весь встроенный код и внешние CDN. Это лучшая практика безопасности."
    }
  },
  {
    "id": "security-quiz-24",
    "topicId": "security",
    "question": {
      "en": "Consider a typical OAuth 2.0 authorization flow. Which of the following sequences correctly describes the Authorization Code flow?\n\nUser → App → Authorization Server → App → Resource Server\n\nWhat critical security reason justifies keeping the access token away from the browser?",
      "ru": "Рассмотрим типичный поток авторизации OAuth 2.0. Какая из следующих последовательностей правильно описывает поток Authorization Code?\n\nПользователь → Приложение → Сервер авторизации → Приложение → Сервер ресурсов\n\nКакая критическая причина безопасности оправдывает хранение токена доступа вне браузера?"
    },
    "options": {
      "en": [
        "The Authorization Code flow stores the access token directly in localStorage, which is visible to all JavaScript and protected by CSP",
        "The Authorization Code flow is used only for mobile apps and doesn't apply to web applications",
        "The Authorization Code flow requires the user to manually enter the access token on each request for security reasons",
        "The Authorization Code flow exchanges a short-lived code for an access token over a backend channel"
      ],
      "ru": [
        "Поток Authorization Code хранит токен доступа непосредственно в localStorage, который виден всему JavaScript и защищён CSP",
        "Поток Authorization Code используется только для мобильных приложений и не применяется к веб-приложениям",
        "Поток Authorization Code требует, чтобы пользователь вручную вводил токен доступа при каждом запросе по причинам безопасности",
        "Поток Authorization Code обменивает короткоживущий код на токен доступа через канал бэкенда"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The Authorization Code flow is specifically designed to keep the access token off the client side. The authorization server issues a short-lived code to the app, which exchanges it for an access token on the backend, where it's safely stored and used server-to-server. This prevents JavaScript from accessing the token, mitigating XSS risks.",
      "ru": "Поток Authorization Code специально разработан, чтобы держать токен доступа вне клиентской части. Сервер авторизации выдаёт короткоживущий код приложению, которое обменивает его на токен доступа на бэкенде, где он безопасно хранится и используется сервер-сервер. Это предотвращает доступ JavaScript к токену, снижая риски XSS."
    }
  },
  {
    "id": "security-quiz-25",
    "topicId": "security",
    "question": {
      "en": "Which combination of cookie flags provides the strongest defense against XSS and CSRF attacks, and what does each flag accomplish?\n```js\nres.setHeader('Set-Cookie', 'sessionId=abc123; Secure; HttpOnly; SameSite=Strict');\n```",
      "ru": "Какая комбинация флагов cookies обеспечивает наиболее сильную защиту от XSS и CSRF атак, и что достигается каждым флагом?\n```js\nres.setHeader('Set-Cookie', 'sessionId=abc123; Secure; HttpOnly; SameSite=Strict');\n```"
    },
    "options": {
      "en": [
        "Secure (HTTPS only), HttpOnly (JavaScript cannot read the cookie) and SameSite=Strict (not sent on cross-site requests)",
        "Secure (encrypt password), HttpOnly (visible to HTML), SameSite=Lax (allows same-domain requests) together enable flexibility while maintaining some protection",
        "Secure (requires authentication), HttpOnly (requires HTTPS), SameSite=None (allows all cross-site) provides maximum compatibility for third-party integrations",
        "Only HttpOnly is necessary; Secure and SameSite are legacy flags that don't impact modern security"
      ],
      "ru": [
        "Secure (только HTTPS), HttpOnly (JavaScript не может прочитать cookie) и SameSite=Strict (не отправляется в кросс-сайтовых запросах)",
        "Secure (шифрует пароль), HttpOnly (видна в HTML), SameSite=Lax (разрешает запросы внутри домена) вместе обеспечивают гибкость и некоторую защиту",
        "Secure (требует аутентификации), HttpOnly (требует HTTPS), SameSite=None (разрешает все кросс-сайт) обеспечивает максимальную совместимость для сторонних интеграций",
        "Только HttpOnly необходимо; Secure и SameSite - это устаревшие флаги, которые не влияют на современную безопасность"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The three flags cover three different things, and none of them stops XSS itself.\n\n- `Secure` — the cookie is only sent over HTTPS, so it cannot be read off a plain HTTP request\n- `HttpOnly` — JavaScript cannot read the cookie, so an XSS bug cannot steal the session token. The injected script still runs; it just cannot exfiltrate the cookie\n- `SameSite=Strict` — the browser does not attach the cookie to requests coming from another site, which is what blocks CSRF\n\nPreventing XSS is a separate job: encode output for its context and validate input.",
      "ru": "Три флага закрывают три разные задачи, и ни один из них не останавливает сам XSS.\n\n- `Secure` — cookie отправляется только по HTTPS, поэтому его не перехватить из обычного HTTP-запроса\n- `HttpOnly` — JavaScript не может прочитать cookie, поэтому XSS-уязвимость не украдёт токен сессии. Внедрённый скрипт всё равно выполнится, но вынести cookie не сможет\n- `SameSite=Strict` — браузер не прикрепляет cookie к запросам, пришедшим с другого сайта, и именно это блокирует CSRF\n\nПредотвращение XSS — отдельная задача: экранировать вывод под его контекст и валидировать ввод."
    }
  }
]