[
  {
    "id": "graphql-quiz-1",
    "topicId": "graphql",
    "question": {
      "en": "In GraphQL, what is the primary difference between a Query and a Mutation?\n\n```graphql\nquery GetUser {\n  user(id: \"1\") { name }\n}\n\nmutation UpdateUser {\n  updateUser(id: \"1\", name: \"John\") { name }\n}\n```",
      "ru": "В GraphQL, в чем основное различие между Query и Mutation?\n\n```graphql\nquery GetUser {\n  user(id: \"1\") { name }\n}\n\nmutation UpdateUser {\n  updateUser(id: \"1\", name: \"John\") { name }\n}\n```"
    },
    "options": {
      "en": [
        "Queries are read-only operations, while Mutations are write operations",
        "Queries can return multiple objects, while Mutations can only return a single object",
        "Queries are executed sequentially, while Mutations are executed in parallel for performance",
        "Queries require authentication, while Mutations are always public"
      ],
      "ru": [
        "Query — это операции чтения, а Mutation — это операции записи",
        "Query может возвращать несколько объектов, а Mutation может вернуть только один объект",
        "Query выполняются последовательно, а Mutation выполняются параллельно для оптимизации производительности",
        "Query требуют аутентификации, а Mutation всегда общедоступны"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Queries are intended for fetching data without side effects, while Mutations are explicitly designed for operations that modify data. This semantic distinction helps developers understand intent and allows GraphQL servers to optimize execution accordingly. Mutations are executed serially (not in parallel), and both can return any type of data.",
      "ru": "Query предназначены для получения данных без побочных эффектов, а Mutation явно разработаны для операций, которые изменяют данные. Это семантическое различие помогает разработчикам понять намерение и позволяет серверам GraphQL оптимизировать выполнение. Mutation выполняются последовательно (не параллельно), и оба могут возвращать любой тип данных."
    }
  },
  {
    "id": "graphql-quiz-2",
    "topicId": "graphql",
    "question": {
      "en": "What is the N+1 query problem in GraphQL, and how does it manifest?\n\n```graphql\nquery {\n  users {\n    id\n    name\n    posts {\n      title\n    }\n  }\n}\n```",
      "ru": "Что такое проблема N+1 запросов в GraphQL и как она проявляется?\n\n```graphql\nquery {\n  users {\n    id\n    name\n    posts {\n      title\n    }\n  }\n}\n```"
    },
    "options": {
      "en": [
        "When a GraphQL query fails, it sends N retry attempts plus 1 final error response to the client",
        "When resolving users and their posts, the server runs 1 query for users and N more, one per user's posts",
        "When using fragments, each fragment requires N+1 network roundtrips to resolve nested fields",
        "When Apollo Client caches data, it stores N versions of the same object plus 1 master copy, doubling memory usage"
      ],
      "ru": [
        "Когда запрос GraphQL не удается, он отправляет N попыток повтора плюс 1 окончательный ответ об ошибке клиенту",
        "При разрешении пользователей и их постов сервер выполняет 1 запрос для пользователей и ещё N — по одному на посты каждого пользователя",
        "При использовании фрагментов каждый фрагмент требует N+1 сетевых обменов для разрешения вложенных полей",
        "Когда Apollo Client кэширует данные, он сохраняет N версий одного объекта плюс 1 главную копию, удваивая использование памяти"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "The N+1 problem occurs when resolving nested fields requires multiple database queries: one for the parent list and one for each child entity. DataLoader and batch loading are common solutions to address this by combining multiple requests into a single database operation, reducing the total number of queries from N+1 to 2.",
      "ru": "Проблема N+1 возникает, когда разрешение вложенных полей требует нескольких запросов к базе данных: один для родительского списка и один для каждого дочернего объекта. DataLoader и пакетная загрузка — это распространенные решения для решения этой проблемы путем объединения нескольких запросов в одну операцию базы данных, сокращая общее количество запросов с N+1 до 2."
    }
  },
  {
    "id": "graphql-quiz-3",
    "topicId": "graphql",
    "question": {
      "en": "What is the primary advantage of using GraphQL Fragments in client code?",
      "ru": "Какое основное преимущество использования GraphQL Fragments в коде клиента?"
    },
    "options": {
      "en": [
        "Fragments reduce network bandwidth by automatically compressing query payloads sent to the server",
        "Fragments enable the server to execute queries in parallel instead of serially for better performance",
        "Fragments let you reuse common field selections across multiple queries and mutations",
        "Fragments prevent the Apollo Client cache from storing duplicate data in memory"
      ],
      "ru": [
        "Фрагменты уменьшают пропускную способность сети, автоматически сжимая полезные данные запроса, отправляемые на сервер",
        "Фрагменты позволяют серверу выполнять запросы параллельно вместо последовательно для улучшения производительности",
        "Фрагменты позволяют повторно использовать общие выборки полей в нескольких запросах и мутациях",
        "Фрагменты предотвращают сохранение кэшем Apollo Client дублирующихся данных в памяти"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Fragments are reusable selections of fields that can be shared across multiple queries, mutations, and subscriptions in your client code. They reduce duplication and make it easier to maintain field selections—when a component's data requirements change, you update the fragment once rather than multiple queries. While fragments don't directly compress bandwidth or enable server-side parallelization, they are a code organization tool.",
      "ru": "Фрагменты — это повторно используемые выборки полей, которые могут быть общими для нескольких запросов, мутаций и подписок в коде вашего клиента. Они уменьшают дублирование и упрощают обслуживание выборок полей — когда требования компонента к данным изменяются, вы обновляете фрагмент один раз, а не несколько запросов. Хотя фрагменты не сжимают пропускную способность напрямую и не включают параллелизм на стороне сервера, они являются инструментом организации кода."
    }
  },
  {
    "id": "graphql-quiz-4",
    "topicId": "graphql",
    "question": {
      "en": "How does Apollo Client's cache normalization work, and what is a key benefit?\n```js\nconst { data } = useQuery(GET_USER, { variables: { id: \"1\" } });\nconst { data: data2 } = useQuery(GET_POST, { variables: { userId: \"1\" } });\n```",
      "ru": "Как работает нормализация кэша Apollo Client и в чем основное преимущество?\n```js\nconst { data } = useQuery(GET_USER, { variables: { id: \"1\" } });\nconst { data2 } = useQuery(GET_POST, { variables: { userId: \"1\" } });\n```"
    },
    "options": {
      "en": [
        "Apollo Client compresses cached data using gzip algorithm, significantly reducing the size of data stored in browser localStorage",
        "Apollo Client creates a deep copy of every cached object to prevent accidental mutations from affecting the cache",
        "Apollo Client invalidates the entire cache whenever any mutation occurs, forcing all queries to refetch from the server",
        "Apollo Client stores all data in a flat key-value map by unique id, so one update shows up in every query"
      ],
      "ru": [
        "Apollo Client сжимает кэшированные данные, используя алгоритм gzip, значительно уменьшая размер данных, хранящихся в браузере localStorage",
        "Apollo Client создает глубокую копию каждого кэшированного объекта, чтобы предотвратить случайные мутации от влияния на кэш",
        "Apollo Client делает кэш недействительным каждый раз, когда происходит любая мутация, что заставляет все запросы повторно получать данные с сервера",
        "Apollo Client хранит все данные в плоской карте ключ-значение по уникальному id, поэтому одно обновление видно во всех запросах"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Apollo Client's normalized cache stores entities by their type and ID, creating a single source of truth for each entity. When the same entity (e.g., a User with id \"1\") appears in multiple queries, they all reference the same cached object. This means updating a user through a mutation automatically propagates to all queries that use that user, eliminating inconsistency and reducing unnecessary refetches.",
      "ru": "Нормализованный кэш Apollo Client хранит сущности по их типу и идентификатору, создавая единый источник истины для каждой сущности. Когда одна и та же сущность (например, User с id \"1\") появляется в нескольких запросах, они все ссылаются на один и тот же кэшированный объект. Это означает, что обновление пользователя через мутацию автоматически распространяется на все запросы, использующие этого пользователя, устраняя несогласованность и сокращая ненужные повторные выборки."
    }
  },
  {
    "id": "graphql-quiz-5",
    "topicId": "graphql",
    "question": {
      "en": "What is GraphQL introspection, and why is it important for developer experience?",
      "ru": "Что такое интроспекция (introspection) в GraphQL и почему это важно для опыта разработчика?"
    },
    "options": {
      "en": [
        "Introspection is a GraphQL feature that lets clients query the schema itself, which powers IDE tooling",
        "Introspection is a security mechanism that validates all GraphQL queries on the server before execution to prevent malicious code",
        "Introspection is a caching strategy that stores previously executed queries and their results to avoid redundant network requests",
        "Introspection is a performance optimization technique that pre-compiles GraphQL queries into machine code for faster execution"
      ],
      "ru": [
        "Интроспекция — это функция GraphQL, которая позволяет клиентам запрашивать саму схему, на этом и строятся инструменты IDE",
        "Интроспекция — это механизм безопасности, который проверяет все запросы GraphQL на сервере перед выполнением, чтобы предотвратить вредоносный код",
        "Интроспекция — это стратегия кэширования, которая хранит ранее выполненные запросы и их результаты, чтобы избежать ненужных сетевых запросов",
        "Интроспекция — это метод оптимизации производительности, который предварительно компилирует запросы GraphQL в машинный код для более быстрого выполнения"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "GraphQL introspection allows clients to query the __schema meta-field to discover available types, fields, arguments, and their descriptions. This enables tools like GraphQL IDE (GraphiQL), Apollo Studio, and IDE extensions to provide intelligent code completion, validation, and automatically generated documentation. Disabling introspection in production is sometimes recommended for security hardening, but the feature itself is crucial for development experience.",
      "ru": "Интроспекция (introspection) в GraphQL позволяет клиентам запрашивать мета-поле __schema для обнаружения доступных типов, полей, аргументов и их описаний. Это позволяет инструментам, таким как GraphQL IDE (GraphiQL), Apollo Studio и расширения IDE, предоставлять интеллектуальное автодополнение кода, проверку и автоматически сгенерированную документацию. Отключение интроспекции в production иногда рекомендуется для укрепления безопасности, но сама функция имеет решающее значение для опыта разработки."
    }
  },
  {
    "id": "graphql-quiz-6",
    "topicId": "graphql",
    "question": {
      "en": "What is the primary difference between a GraphQL Query and a Mutation?",
      "ru": "В чем основное различие между GraphQL Query и Mutation?"
    },
    "options": {
      "en": [
        "Queries can only request a single field, while mutations can request multiple fields",
        "Queries are read-only operations, while mutations modify server state",
        "Mutations are executed in parallel, while queries are executed sequentially",
        "Queries require authentication, while mutations are open to anonymous clients"
      ],
      "ru": [
        "Queries могут запросить только одно поле, а mutations могут запросить несколько полей",
        "Queries — операции только для чтения, а mutations модифицируют состояние сервера",
        "Mutations выполняются параллельно, а queries выполняются последовательно",
        "Queries требуют аутентификации, а mutations открыты для анонимных клиентов"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Queries are intended for fetching data without side effects, while mutations are designed for operations that modify data on the server. This semantic distinction helps clients understand the intent and allows servers to optimize execution accordingly. The other options describe incorrect or non-fundamental differences.",
      "ru": "Queries предназначены для получения данных без побочных эффектов, а mutations предназначены для операций, которые модифицируют данные на сервере. Это семантическое различие помогает клиентам понять намерение и позволяет серверам оптимизировать выполнение. Остальные варианты описывают неправильные или несущественные различия."
    }
  },
  {
    "id": "graphql-quiz-7",
    "topicId": "graphql",
    "question": {
      "en": "Consider this GraphQL query with an N+1 problem scenario:\n\n```graphql\nquery GetAuthors {\n  authors {\n    id\n    name\n    books {\n      id\n      title\n    }\n  }\n}\n```\n\nWhich technique BEST solves this at the resolver level?",
      "ru": "Рассмотрите этот GraphQL запрос с сценарием проблемы N+1:\n\n```graphql\nquery GetAuthors {\n  authors {\n    id\n    name\n    books {\n      id\n      title\n    }\n  }\n}\n```\n\nКакая техника ЛУЧШЕ всего решает это на уровне resolver?"
    },
    "options": {
      "en": [
        "Adding pagination limits to the books field to reduce the number of results",
        "Executing all book queries in parallel using Promise.all()",
        "Using DataLoader to batch requests and implement caching for book queries",
        "Moving the books field to a separate subscription query"
      ],
      "ru": [
        "Добавление лимитов pagination к полю books для уменьшения количества результатов",
        "Выполнение всех запросов книг параллельно с использованием Promise.all()",
        "Использование DataLoader для группировки запросов и реализации кэширования для запросов книг",
        "Перемещение поля books в отдельный subscription запрос"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "DataLoader is the industry-standard solution for the N+1 problem in GraphQL. It batches requests made during a single execution cycle and deduplicates identical requests, turning N+1 queries into a single batched query. Pagination reduces data but doesn't solve the underlying N+1 issue; Promise.all() executes requests in parallel but still creates multiple queries; subscriptions are unrelated to this problem.",
      "ru": "DataLoader — стандартное решение для проблемы N+1 в GraphQL. Оно группирует запросы, сделанные во время одного цикла выполнения, и дедублицирует идентичные запросы, превращая N+1 запросов в один групповой запрос. Pagination уменьшает данные, но не решает основную проблему N+1; Promise.all() выполняет запросы параллельно, но все еще создает несколько запросов; subscriptions не связаны с этой проблемой."
    }
  },
  {
    "id": "graphql-quiz-8",
    "topicId": "graphql",
    "question": {
      "en": "In Apollo Client, how does the cache-first fetch policy behave when a previously cached query is requested again?",
      "ru": "В Apollo Client, как себя ведет политика fetch policy \"cache-first\" при повторном запросе ранее закэшированного запроса?"
    },
    "options": {
      "en": [
        "It always fetches from the server first, then updates the cache if data has changed",
        "It returns cached data but also fetches from the server in the background to refresh it",
        "It discards the cache and refetches from the server on every request",
        "It returns the cached data immediately without checking the server for updates"
      ],
      "ru": [
        "Оно всегда сначала получает данные с сервера, затем обновляет кэш, если данные изменились",
        "Оно возвращает закэшированные данные, но также получает данные с сервера в фоне для их обновления",
        "Оно отбрасывает кэш и повторно получает данные с сервера при каждом запросе",
        "Оно возвращает закэшированные данные немедленно, не проверяя сервер на наличие обновлений"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "cache-first policy prioritizes cached data and returns it immediately if available, avoiding network requests entirely. This optimizes performance but risks serving stale data. cache-and-network would fetch from the server in the background; cache-only never checks the server; network-first checks the server first. Understanding fetch policies is critical for managing data freshness and performance tradeoffs.",
      "ru": "Политика cache-first приоритизирует закэшированные данные и возвращает их немедленно, если они доступны, избегая сетевых запросов полностью. Это оптимизирует производительность, но рискует доставить устаревшие данные. cache-and-network получала бы данные с сервера в фоне; cache-only никогда не проверяет сервер; network-first сначала проверяет сервер. Понимание fetch policies критически важно для управления актуальностью данных и компромиссами производительности."
    }
  },
  {
    "id": "graphql-quiz-9",
    "topicId": "graphql",
    "question": {
      "en": "What is a GraphQL Fragment, and when should you use it?\n\n```graphql\nfragment UserFields on User {\n  id\n  email\n  createdAt\n}\n```",
      "ru": "Что такое GraphQL Fragment и когда его следует использовать?\n\n```graphql\nfragment UserFields on User {\n  id\n  email\n  createdAt\n}\n```"
    },
    "options": {
      "en": [
        "A reusable set of fields on a type that you can include in many queries",
        "A partial query response that the server sends back in chunks to optimize network bandwidth",
        "A cached subset of data on the client that is updated when mutations occur",
        "A directive that filters query results based on user permissions"
      ],
      "ru": [
        "Переиспользуемый набор полей типа, который можно включать в разные запросы",
        "Частичный ответ запроса, который сервер отправляет порциями для оптимизации пропускной способности сети",
        "Закэшированное подмножество данных на клиенте, которое обновляется при выполнении mutations",
        "Directive, которая фильтрует результаты запроса на основе разрешений пользователя"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Fragments allow you to define a reusable selection set of fields that can be included in multiple queries, reducing code duplication and improving maintainability when field requirements change. They're a client-side abstraction tool, not related to server chunking, caching strategy, or permission directives. Fragments are essential for managing complex queries in large applications.",
      "ru": "Fragments позволяют определить переиспользуемый набор полей, который может быть включен в несколько запросов, уменьшая дублирование кода и улучшая поддерживаемость при изменении требований к полям. Это инструмент абстракции на клиенте, не связанный с разбивкой сервера на части, стратегией кэширования или директивами разрешений. Fragments необходимы для управления сложными запросами в больших приложениях."
    }
  },
  {
    "id": "graphql-quiz-10",
    "topicId": "graphql",
    "question": {
      "en": "Which of the following is a significant advantage of GraphQL over REST in terms of data fetching efficiency?",
      "ru": "Какое из следующих утверждений является значительным преимуществом GraphQL над REST с точки зрения эффективности получения данных?"
    },
    "options": {
      "en": [
        "GraphQL endpoints cache responses more effectively than REST endpoints",
        "Clients request only the fields they need, with no over-fetching or under-fetching",
        "GraphQL requires fewer HTTP requests because all queries use a single POST endpoint",
        "GraphQL automatically compresses responses, reducing bandwidth consumption compared to REST"
      ],
      "ru": [
        "Endpoints GraphQL кэшируют ответы более эффективно, чем endpoints REST",
        "Клиенты запрашивают только нужные им поля, без излишнего и недостаточного получения данных",
        "GraphQL требует меньше HTTP запросов, потому что все запросы используют один POST endpoint",
        "GraphQL автоматически сжимает ответы, уменьшая потребление полосы пропускания по сравнению с REST"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "GraphQL's key advantage is precise field selection — clients specify exactly which fields they need, eliminating over-fetching (requesting unnecessary fields) and under-fetching (needing multiple requests to get all required data). This is a fundamental architectural difference from REST, where endpoints return fixed data shapes. While GraphQL does use a single endpoint, that alone doesn't guarantee efficiency; caching and compression are implementation details not inherent to GraphQL.",
      "ru": "Ключевое преимущество GraphQL — точное выбор полей: клиенты точно указывают, какие поля им нужны, исключая излишнее получение (запрос ненужных полей) и недостаточное получение (необходимость нескольких запросов для получения всех требуемых данных). Это фундаментальное архитектурное различие с REST, где endpoints возвращают фиксированные структуры данных. Хотя GraphQL использует один endpoint, это само по себе не гарантирует эффективность; кэширование и сжатие являются деталями реализации, а не присущи GraphQL."
    }
  },
  {
    "id": "graphql-quiz-11",
    "topicId": "graphql",
    "question": {
      "en": "In GraphQL, what is the primary difference between a Query and a Mutation?\n\n```graphql\nquery GetUser {\n  user(id: 1) {\n    name\n  }\n}\n\nmutation CreateUser {\n  createUser(name: \"John\") {\n    id\n    name\n  }\n}\n```\n\nWhich statement best describes the distinction?",
      "ru": "В GraphQL, в чем основное различие между Query и Mutation?\n\n```graphql\nquery GetUser {\n  user(id: 1) {\n    name\n  }\n}\n\nmutation CreateUser {\n  createUser(name: \"John\") {\n    id\n    name\n  }\n}\n```\n\nКакое утверждение лучше всего описывает различие?"
    },
    "options": {
      "en": [
        "Queries can modify data while Mutations can only read data from the server",
        "Queries and Mutations are functionally identical; the distinction is only for documentation purposes",
        "Queries fetch data without changing it, while Mutations modify server state",
        "Mutations are deprecated in favor of using Queries with variables for all operations"
      ],
      "ru": [
        "Запросы могут изменять данные, а Mutations могут только читать данные с сервера",
        "Запросы и Mutations функционально идентичны; различие только в целях документации",
        "Запросы получают данные, не изменяя их, а Mutations изменяют состояние сервера",
        "Mutations устарели в пользу использования Queries с переменными для всех операций"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Queries fetch data without side effects, while Mutations are designed for operations that modify data and are guaranteed to execute serially, preventing race conditions. The semantic distinction helps developers understand intent and allows GraphQL servers to optimize execution. The three remaining options represent common misconceptions about the relationship between these operation types.",
      "ru": "Запросы получают данные без побочных эффектов, а Mutations предназначены для операций, которые изменяют данные и гарантированно выполняются последовательно, предотвращая условия гонки. Семантическое различие помогает разработчикам понять намерение и позволяет серверам GraphQL оптимизировать выполнение. Три остальных варианта представляют распространенные неправильные представления о взаимосвязи между этими типами операций."
    }
  },
  {
    "id": "graphql-quiz-12",
    "topicId": "graphql",
    "question": {
      "en": "What is the N+1 query problem in GraphQL and how does it typically manifest?\n\n```graphql\nquery GetUsers {\n  users(limit: 10) {\n    id\n    name\n    posts {\n      title\n    }\n  }\n}\n```\n\nAssuming no optimization, what database queries occur?",
      "ru": "Что такое проблема N+1 запросов в GraphQL и как она обычно проявляется?\n\n```graphql\nquery GetUsers {\n  users(limit: 10) {\n    id\n    name\n    posts {\n      title\n    }\n  }\n}\n```\n\nПредполагая отсутствие оптимизации, какие запросы к базе данных происходят?"
    },
    "options": {
      "en": [
        "GraphQL automatically batches all requests into a single database query regardless of structure",
        "Only 1 database query is executed because GraphQL combines nested fields into joins automatically",
        "Multiple queries execute in parallel, so the total count doesn't matter for performance",
        "1 query to fetch users + 10 queries (one per user) to fetch their posts"
      ],
      "ru": [
        "GraphQL автоматически объединяет все запросы в один запрос к базе данных независимо от структуры",
        "Выполняется только 1 запрос к базе данных, потому что GraphQL автоматически объединяет вложенные поля в join",
        "Несколько запросов выполняются параллельно, поэтому общее количество не имеет значения для производительности",
        "1 запрос для получения пользователей + 10 запросов (по одному на пользователя) для получения их постов"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Without optimization techniques like DataLoader, the resolver for posts is called once per user, resulting in N+1 queries (1 for users, N for each user's posts). The option claiming GraphQL automatically batches all requests into a single database query is false because GraphQL doesn't automatically batch at the database level without explicit implementation. The option that expects a single query from automatic joins ignores that GraphQL is resolver-based and doesn't know about database relationships. The option that dismisses the query count because requests run in parallel is incorrect because parallel execution still incurs the same total query cost.",
      "ru": "Без техник оптимизации, таких как DataLoader, резолвер постов вызывается один раз для каждого пользователя, что приводит к N+1 запросам (1 для пользователей, N для постов каждого пользователя). Вариант про автоматическое объединение всех запросов в один запрос к базе данных неверен, потому что GraphQL не объединяет автоматически на уровне базы данных без явной реализации. Вариант про один запрос за счет автоматических join игнорирует, что GraphQL основан на резолверах и не знает о связях в базе данных. Вариант про параллельное выполнение без учета количества запросов неверен, потому что параллельное выполнение все еще несет одинаковую общую стоимость запроса."
    }
  },
  {
    "id": "graphql-quiz-13",
    "topicId": "graphql",
    "question": {
      "en": "How does Apollo Client's cache management improve performance compared to traditional REST?\n```js\nconst { data } = useQuery(GET_USER, { variables: { id: 1 } });\n// User data is cached\nconst { data: data2 } = useQuery(GET_USER, { variables: { id: 1 } });\n// What happens on the second query?\n```",
      "ru": "Как управление кэшем Apollo Client улучшает производительность по сравнению с традиционным REST?\n```js\nconst { data } = useQuery(GET_USER, { variables: { id: 1 } });\n// Данные пользователя кэшируются\nconst { data: data2 } = useQuery(GET_USER, { variables: { id: 1 } });\n// Что происходит на втором запросе?\n```"
    },
    "options": {
      "en": [
        "Apollo returns cached data instantly for identical queries without hitting the network",
        "Apollo Client ignores caching entirely and always makes fresh network requests like traditional REST",
        "The cache only works within a single component and resets when the component unmounts",
        "Apollo stores data in localStorage and requires manual cache invalidation after every mutation"
      ],
      "ru": [
        "Apollo мгновенно возвращает кэшированные данные для идентичных запросов без обращения к сети",
        "Apollo Client полностью игнорирует кэширование и всегда делает свежие сетевые запросы, как традиционный REST",
        "Кэш работает только в одном компоненте и сбрасывается при размонтировании компонента",
        "Apollo сохраняет данные в localStorage и требует ручной инвалидации кэша после каждой мутации"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Apollo Client uses an in-memory normalized cache that returns identical query results instantly from cache, improving perceived performance and reducing network traffic. It can optionally refetch in the background via cache policies. The option saying Apollo ignores caching entirely and always hits the network ignores Apollo's core feature. The option that limits the cache to a single component is wrong because the cache is global and persists across components. The option about localStorage with manual invalidation after every mutation is incorrect because Apollo uses in-memory caching by default, not localStorage, and has intelligent cache invalidation strategies.",
      "ru": "Apollo Client использует нормализованный кэш в памяти, который возвращает идентичные результаты запросов мгновенно из кэша, улучшая воспринимаемую производительность и снижая сетевой трафик. Он может опционально переполучить данные в фоне через политики кэша. Вариант про полное игнорирование кэширования и постоянные сетевые запросы игнорирует основную функцию Apollo. Вариант, ограничивающий кэш одним компонентом, неверен, потому что кэш глобален и сохраняется между компонентами. Вариант про localStorage с ручной инвалидацией после каждой мутации неверен, потому что Apollo использует кэширование в памяти по умолчанию, а не localStorage, и имеет интеллектуальные стратегии инвалидации кэша."
    }
  },
  {
    "id": "graphql-quiz-14",
    "topicId": "graphql",
    "question": {
      "en": "What is a GraphQL Fragment and why is it useful in queries?\n\n```graphql\nfragment UserFields on User {\n  id\n  name\n  email\n}\n\nquery GetUsers {\n  users {\n    ...UserFields\n  }\n}\n```\n\nWhich benefit does this approach provide?",
      "ru": "Что такое GraphQL Fragment и почему это полезно в запросах?\n\n```graphql\nfragment UserFields on User {\n  id\n  name\n  email\n}\n\nquery GetUsers {\n  users {\n    ...UserFields\n  }\n}\n```\n\nКакое преимущество дает этот подход?"
    },
    "options": {
      "en": [
        "Fragments are required by GraphQL specification and all queries must use them",
        "Fragments enable reusable field selections across multiple queries",
        "Fragments improve database performance by automatically optimizing SQL joins",
        "Fragments allow mutations to execute faster than regular queries by skipping type checking"
      ],
      "ru": [
        "Фрагменты требуются спецификацией GraphQL и все запросы должны их использовать",
        "Фрагменты позволяют переиспользовать выборки полей в нескольких запросах",
        "Фрагменты улучшают производительность базы данных путем автоматической оптимизации SQL объединений",
        "Фрагменты позволяют мутациям выполняться быстрее, чем обычные запросы, пропуская проверку типов"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Fragments are reusable sets of fields that reduce query duplication and make schema changes easier to maintain. They're syntactic sugar for clarity, not a requirement. The option calling fragments a requirement of the specification is false because fragments are optional. The option about automatically optimizing SQL joins conflates client-side query construction with server-side optimization. The option about faster mutations and skipped type checking is incorrect because fragments don't affect mutation execution speed or type checking behavior.",
      "ru": "Фрагменты — это переиспользуемые наборы полей, которые снижают дублирование запросов и упрощают обслуживание изменений схемы. Они являются синтаксическим сахаром для ясности, а не требованием. Вариант про требование спецификации неверен, потому что фрагменты необязательны. Вариант про автоматическую оптимизацию SQL объединений смешивает конструирование запросов на стороне клиента с оптимизацией на стороне сервера. Вариант про ускорение мутаций и пропуск проверки типов неверен, потому что фрагменты не влияют на скорость выполнения мутаций или поведение проверки типов."
    }
  },
  {
    "id": "graphql-quiz-15",
    "topicId": "graphql",
    "question": {
      "en": "What are the key advantages of GraphQL over REST for API design, and what are realistic tradeoffs?",
      "ru": "Каковы ключевые преимущества GraphQL перед REST для проектирования API и каковы реальные компромиссы?"
    },
    "options": {
      "en": [
        "GraphQL is objectively superior to REST in all scenarios and has no downsides",
        "REST is faster than GraphQL because it uses HTTP caching, which GraphQL cannot leverage",
        "GraphQL removes over-fetching, but adds server complexity and can cause N+1 queries",
        "GraphQL replaces REST entirely and REST should never be used for new projects"
      ],
      "ru": [
        "GraphQL объективно превосходит REST во всех сценариях и не имеет недостатков",
        "REST работает быстрее, чем GraphQL, потому что использует кэширование HTTP, которое GraphQL не может использовать",
        "GraphQL исключает избыточную загрузку, но усложняет сервер и может привести к проблемам N+1 запросов",
        "GraphQL полностью заменяет REST, и REST никогда не следует использовать для новых проектов"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "GraphQL's key strength is precise data fetching (no over/under-fetching), but this comes with tradeoffs: increased server complexity, potential N+1 issues, and reduced effectiveness of standard HTTP caching. The option calling GraphQL superior in all scenarios with no downsides ignores real architectural tradeoffs. The option saying GraphQL cannot leverage HTTP caching is wrong because GraphQL can leverage HTTP caching and has query complexity tools. The option that retires REST entirely is false because REST remains appropriate for many use cases and the choice depends on requirements.",
      "ru": "Основная сила GraphQL — точная загрузка данных (без избыточности), но это сопровождается компромиссами: повышенная сложность сервера, потенциальные проблемы N+1 и сниженная эффективность стандартного кэширования HTTP. Вариант про превосходство GraphQL во всех сценариях без недостатков игнорирует реальные архитектурные компромиссы. Вариант про невозможность использовать кэширование HTTP в GraphQL неверен, потому что GraphQL может использовать кэширование HTTP и имеет инструменты сложности запросов. Вариант про полную замену REST неверен, потому что REST остается подходящим для многих случаев использования и выбор зависит от требований."
    }
  },
  {
    "id": "graphql-quiz-16",
    "topicId": "graphql",
    "question": {
      "en": "What is the primary difference between a GraphQL Query and a Mutation in terms of side effects?\n\n```graphql\nquery GetUser {\n  user(id: 1) { name email }\n}\n\nmutation UpdateUser {\n  updateUser(id: 1, name: \"John\") { name email }\n}\n```",
      "ru": "В чем основное различие между GraphQL Query и Mutation в отношении побочных эффектов?\n\n```graphql\nquery GetUser {\n  user(id: 1) { name email }\n}\n\nmutation UpdateUser {\n  updateUser(id: 1, name: \"John\") { name email }\n}\n```"
    },
    "options": {
      "en": [
        "Queries execute in parallel, while Mutations always execute sequentially",
        "Queries cannot return nested data, while Mutations can return complex objects",
        "Queries require authentication, while Mutations are always public",
        "Queries are read-only, while Mutations show intent to modify server state"
      ],
      "ru": [
        "Queries выполняются параллельно, а Mutations всегда выполняются последовательно",
        "Queries не могут возвращать вложенные данные, а Mutations могут возвращать сложные объекты",
        "Queries требуют аутентификацию, а Mutations всегда открыты",
        "Queries доступны только для чтения, а Mutations указывают намерение изменить состояние сервера"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Queries are designed for read-only operations with no side effects, while Mutations are the mechanism for operations that modify data. This distinction helps clients understand intent and allows servers to optimize caching and execution strategies. Sequential execution of Mutations is common but not required, and both can return complex nested data.",
      "ru": "Queries предназначены для операций только чтения без побочных эффектов, а Mutations — это механизм для операций, которые изменяют данные. Это различие помогает клиентам понять намерение и позволяет серверам оптимизировать кэширование и стратегии выполнения. Последовательное выполнение Mutations обычно, но не требуется, и оба могут возвращать сложные вложенные данные."
    }
  },
  {
    "id": "graphql-quiz-17",
    "topicId": "graphql",
    "question": {
      "en": "Which of the following best describes the N+1 query problem in GraphQL and how it differs from typical REST implementations?\n\n```graphql\nquery {\n  users { id name posts { id title } }\n}\n```",
      "ru": "Что лучше всего описывает проблему N+1 запроса в GraphQL и как она отличается от типичных реализаций REST?\n\n```graphql\nquery {\n  users { id name posts { id title } }\n}\n```"
    },
    "options": {
      "en": [
        "N+1 happens when the resolver for a nested field runs one query per parent item",
        "GraphQL automatically prevents N+1 by batching all queries, making it superior to REST",
        "N+1 only exists in REST APIs; GraphQL eliminates it by design with its single endpoint",
        "N+1 problems in GraphQL are solved by using fragments instead of nested selections"
      ],
      "ru": [
        "N+1 происходит, когда resolver для вложенного поля выполняет один запрос на элемент родителя",
        "GraphQL автоматически предотвращает N+1 путем объединения всех запросов, что делает его превосходнее REST",
        "N+1 существует только в REST API; GraphQL устраняет его по дизайну с помощью одной конечной точки",
        "Проблемы N+1 в GraphQL решаются использованием fragments вместо вложенных выборок"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "GraphQL does not automatically prevent N+1 queries—this is a common misconception. When fetching nested data, if each parent's posts are fetched individually, you get N+1 queries. Solutions include DataLoader for batching, query optimization, and caching. REST has similar issues but they're often less visible due to fixed response structures.",
      "ru": "GraphQL не предотвращает N+1 запросы автоматически — это распространенное заблуждение. При выборке вложенных данных, если посты каждого родителя выбираются индивидуально, вы получаете N+1 запросы. Решения включают DataLoader для объединения, оптимизацию запросов и кэширование. REST имеет похожие проблемы, но они часто менее видны из-за фиксированных структур ответа."
    }
  },
  {
    "id": "graphql-quiz-18",
    "topicId": "graphql",
    "question": {
      "en": "In Apollo Client, what is the primary purpose of fragments in a GraphQL query, and how do they relate to cache management?",
      "ru": "В Apollo Client, какова основная цель fragments в GraphQL запросе и как они связаны с управлением кэшем?"
    },
    "options": {
      "en": [
        "Fragments are primarily used to split large queries across multiple network requests for performance",
        "Fragments define reusable selection sets and keep cache keys consistent",
        "Fragments are a way to encrypt sensitive fields in the cache",
        "Fragments reduce bundle size by removing unused fields from the final query sent to the server"
      ],
      "ru": [
        "Fragments в основном используются для разделения больших запросов на несколько сетевых запросов для производительности",
        "Fragments задают переиспользуемые наборы выборок и держат ключи кэша согласованными",
        "Fragments — это способ зашифровать конфиденциальные поля в кэше",
        "Fragments уменьшают размер пакета, удаляя неиспользуемые поля из итогового запроса, отправляемого на сервер"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Fragments enable reusable selection sets and improve cache organization by consistently normalizing data—when the same fragment is used in multiple queries, Apollo can merge updates efficiently. They don't split network requests, encrypt data, or reduce bundle size. Fragments are essential for maintaining cache coherence across complex applications.",
      "ru": "Fragments позволяют переиспользовать наборы выборок и улучшают организацию кэша путем последовательной нормализации данных — когда один и тот же fragment используется в нескольких запросах, Apollo может эффективно объединять обновления. Они не разделяют сетевые запросы, не шифруют данные и не уменьшают размер пакета. Fragments являются важными для поддержания согласованности кэша в сложных приложениях."
    }
  },
  {
    "id": "graphql-quiz-19",
    "topicId": "graphql",
    "question": {
      "en": "GraphQL introspection allows a client to query the schema itself. What is a key security consideration when enabling introspection in production?\n\n```graphql\nquery {\n  __schema { types { name fields { name type } } }\n}\n```",
      "ru": "Интроспекция (introspection) в GraphQL позволяет клиенту запрашивать саму схему. Какое ключевое соображение безопасности возникает при её включении в production?\n\n```graphql\nquery {\n  __schema { types { name fields { name type } } }\n}\n```"
    },
    "options": {
      "en": [
        "Introspection queries are cached separately and can cause memory leaks if not properly managed",
        "Enabling introspection allows queries to run faster by skipping type validation",
        "Introspection can expose your whole API surface, including field names and types",
        "Introspection is only a security risk if you use REST endpoints alongside GraphQL"
      ],
      "ru": [
        "Запросы интроспекции кэшируются отдельно и могут вызвать утечки памяти, если ими не управлять",
        "Включение интроспекции позволяет запросам работать быстрее, пропуская проверку типов",
        "Интроспекция может раскрыть всю поверхность вашего API, включая имена полей и типы",
        "Интроспекция опасна только в том случае, если рядом с GraphQL вы используете REST-эндпоинты"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Introspection exposes the entire API schema, including all types, fields, arguments, and their descriptions—this gives attackers a complete map of your API surface for reconnaissance and exploitation. It's best practice to disable introspection in production unless specifically needed, or restrict it to authenticated users. The other options misrepresent technical facts about introspection.",
      "ru": "Интроспекция (introspection) раскрывает всю схему API: все типы, поля, аргументы и их описания. Это даёт злоумышленнику готовую карту вашего API для разведки и атаки. Хорошая практика — выключать интроспекцию в production, если она там не нужна, или оставлять её только аутентифицированным пользователям. Остальные варианты искажают технические факты об интроспекции."
    }
  },
  {
    "id": "graphql-quiz-20",
    "topicId": "graphql",
    "question": {
      "en": "What is a key tradeoff when choosing GraphQL over REST for your API?",
      "ru": "Какой ключевой компромисс существует при выборе GraphQL вместо REST для вашего API?"
    },
    "options": {
      "en": [
        "REST is simpler for clients because they don't need to learn query syntax, while GraphQL eliminates all server-side caching concerns",
        "GraphQL is always faster than REST because it uses a single endpoint, regardless of query complexity",
        "REST supports authentication while GraphQL does not, making GraphQL unsuitable for secure applications",
        "GraphQL allows precise field selection reducing over-fetching, but increases server complexity for nested queries"
      ],
      "ru": [
        "REST проще для клиентов, потому что им не нужно изучать синтаксис запросов, а GraphQL устраняет все проблемы кэширования на сервере",
        "GraphQL всегда быстрее REST, потому что использует одну конечную точку, независимо от сложности запроса",
        "REST поддерживает аутентификацию, а GraphQL — нет, что делает GraphQL непригодным для защищенных приложений",
        "GraphQL позволяет точный выбор полей, уменьшая избыточную загрузку, но увеличивает сложность сервера при разрешении вложенных запросов"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "GraphQL's primary advantage is precise client-specified data fetching (avoiding over-fetching), but this comes at the cost of increased server-side complexity—resolvers must handle dynamic nested queries, and subtle issues like N+1 problems can emerge. REST has simpler, predictable endpoints but clients often receive unnecessary data. Authentication works equally well in both, single endpoints don't guarantee speed, and GraphQL doesn't eliminate server caching needs.",
      "ru": "Основное преимущество GraphQL — точная выборка данных, указанная клиентом (избегание избыточной загрузки), но это происходит за счет повышенной сложности на сервере — resolvers должны обрабатывать динамические вложенные запросы, и могут возникнуть такие проблемы, как N+1. REST имеет более простые, предсказуемые конечные точки, но клиенты часто получают ненужные данные. Аутентификация работает одинаково хорошо в обоих случаях, одна конечная точка не гарантирует скорость, и GraphQL не устраняет необходимость кэширования сервера."
    }
  },
  {
    "id": "graphql-quiz-21",
    "topicId": "graphql",
    "question": {
      "en": "You're fetching user data with nested comments. Each user has 100 comments, and you're loading 50 users. Without proper optimization, what performance issue will you likely encounter?\n\n```graphql\nquery GetUsersWithComments {\n  users(limit: 50) {\n    id\n    name\n    comments {\n      id\n      text\n    }\n  }\n}\n```",
      "ru": "Вы получаете данные пользователей с вложенными комментариями. У каждого пользователя есть 100 комментариев, и вы загружаете 50 пользователей. Без надлежащей оптимизации какую проблему производительности вы, вероятно, встретите?\n\n```graphql\nquery GetUsersWithComments {\n  users(limit: 50) {\n    id\n    name\n    comments {\n      id\n      text\n    }\n  }\n}\n```"
    },
    "options": {
      "en": [
        "N+1 query problem causing 5000+ database queries",
        "Query string is too long and will be rejected by the server",
        "Apollo Client cache will automatically batch requests and solve the issue",
        "The GraphQL server will return incomplete data to prevent overload"
      ],
      "ru": [
        "Проблема N+1 запросов, вызывающая 5000+ запросов к базе данных",
        "Строка запроса слишком длинная и будет отклонена сервером",
        "Apollo Client автоматически пакетирует запросы и решает проблему",
        "Сервер GraphQL вернет неполные данные, чтобы предотвратить перегрузку"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The N+1 problem occurs when the resolver for each user separately queries the database for their comments, resulting in 1 query for users + 50 queries for comments = 51 queries total (or worse with nested resolution). This is a classic GraphQL performance issue solved by using dataloaders or batch queries. The options about the query string being rejected as too long and about the server returning incomplete data are not how GraphQL works, and the option about Apollo Client batching the requests automatically is incorrect because Apollo Client caches responses but doesn't prevent the initial N+1 queries on the server.",
      "ru": "Проблема N+1 возникает, когда резолвер для каждого пользователя отдельно запрашивает базу данных за их комментариями, в результате чего получается 1 запрос для пользователей + 50 запросов для комментариев = 51 запрос всего. Это классическая проблема производительности GraphQL, решаемая с помощью dataloaders или пакетных запросов. Варианты про отклонение слишком длинной строки запроса и про неполные данные от сервера не соответствуют тому, как работает GraphQL, а вариант про автоматическое пакетирование запросов в Apollo Client неверен, потому что Apollo Client кэширует ответы, но не предотвращает начальные N+1 запросы на сервере."
    }
  },
  {
    "id": "graphql-quiz-23",
    "topicId": "graphql",
    "question": {
      "en": "You are using Apollo Client and notice that after executing a mutation, your UI does not update even though the mutation succeeded. What is the most likely cause?",
      "ru": "Вы используете Apollo Client и замечаете, что после выполнения mutation ваш интерфейс не обновляется, хотя mutation выполнен успешно. Какова наиболее вероятная причина?"
    },
    "options": {
      "en": [
        "The Apollo Client cache needs to be manually cleared after every mutation",
        "The mutation response did not include the fields the UI queries",
        "The mutation should have been sent as a Query, since only Queries update the cache",
        "GraphQL subscriptions are required for cache updates to work"
      ],
      "ru": [
        "Кэш Apollo Client необходимо вручную очищать после каждого mutation",
        "Ответ mutation не включал поля, которые запрашивает интерфейс",
        "Вместо mutation нужно было отправить Query, ведь только Query обновляют кэш",
        "Подписки GraphQL необходимы для работы обновлений кэша"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Apollo Client's cache updates automatically when mutation responses match cached query fields. If the mutation response doesn't include fields that the UI queries, the cache won't update those fields. The option requiring the cache to be cleared manually after every mutation is incorrect because manual cache clearing defeats the purpose of automatic updates. The option suggesting a Query instead of the mutation misunderstands mutation purpose. The option that requires subscriptions for cache updates is wrong because subscriptions are for real-time updates, not standard cache synchronization.",
      "ru": "Кэш Apollo Client автоматически обновляется, когда ответы mutation совпадают с кэшированными полями запросов. Если ответ mutation не включает поля, которые запрашивает интерфейс, кэш не будет обновлять эти поля. Вариант про ручную очистку кэша после каждого mutation неверен, потому что ручная очистка кэша противоречит целям автоматических обновлений. Вариант про замену mutation на Query неправильно интерпретирует назначение mutation. Вариант про обязательные подписки для обновлений кэша неверен, потому что подписки предназначены для обновлений в реальном времени, а не для стандартной синхронизации кэша."
    }
  },
  {
    "id": "graphql-quiz-24",
    "topicId": "graphql",
    "question": {
      "en": "GraphQL introspection allows you to query the schema itself. Which of the following is a primary use case for introspection in production systems?",
      "ru": "Интроспекция (introspection) в GraphQL позволяет вам запрашивать саму схему. Какой из вариантов — основной случай её использования в production-системах?"
    },
    "options": {
      "en": [
        "Reducing the size of network requests by compressing schema information",
        "Replacing the need for API versioning and backwards compatibility",
        "Powering API documentation tools and IDE features like autocomplete",
        "Automatically generating SQL queries from GraphQL queries without developer input"
      ],
      "ru": [
        "Уменьшение размера сетевых запросов путем сжатия информации схемы",
        "Замена необходимости управления версиями API и обеспечения обратной совместимости",
        "Поддержка инструментов документации API и функций IDE вроде автодополнения",
        "Автоматическое создание SQL запросов из GraphQL запросов без участия разработчика"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Introspection is essential for building developer tools—IDEs like GraphQL Playground use it to provide real-time schema validation and autocomplete. The option about compressing schema information to shrink requests is incorrect because introspection adds overhead, not reduces it. The option about replacing API versioning is wrong because introspection doesn't replace versioning strategies. The option about automatically generating SQL queries is incorrect because introspection reveals schema structure but doesn't generate SQL; that's the resolver's responsibility.",
      "ru": "Интроспекция нужна для инструментов разработчика — IDE, такие как GraphQL Playground, используют её для валидации схемы в реальном времени и автодополнения. Вариант про сжатие информации схемы для уменьшения запросов неверен, потому что интроспекция добавляет нагрузку, а не уменьшает ее. Вариант про замену управления версиями API неверен, потому что интроспекция не заменяет стратегии управления версиями. Вариант про автоматическое создание SQL запросов неверен, потому что интроспекция раскрывает структуру схемы, но не генерирует SQL; это ответственность резолвера."
    }
  },
  {
    "id": "graphql-quiz-25",
    "topicId": "graphql",
    "question": {
      "en": "When comparing REST and GraphQL, which statement most accurately reflects a tradeoff rather than a clear advantage?\n\nREST: GET /api/users/1, GET /api/users/1/posts, GET /api/posts/5/comments\nGraphQL: query { user(id: 1) { posts { comments } } }\n",
      "ru": "При сравнении REST и GraphQL, какое утверждение наиболее точно отражает компромисс, а не явное преимущество?\n\nREST: GET /api/users/1, GET /api/users/1/posts, GET /api/posts/5/comments\nGraphQL: query { user(id: 1) { posts { comments } } }\n"
    },
    "options": {
      "en": [
        "GraphQL eliminates over-fetching, so a GraphQL client needs no caching layer at all",
        "REST requires multiple requests while GraphQL uses only one, making GraphQL always faster",
        "REST APIs are impossible to secure while GraphQL APIs have built-in security features",
        "GraphQL queries are harder to cache at the HTTP/CDN level because they use POST"
      ],
      "ru": [
        "GraphQL устраняет избыточное получение данных, поэтому клиенту GraphQL вообще не нужен слой кэширования",
        "REST требует нескольких запросов, а GraphQL использует только один, что делает GraphQL всегда быстрее",
        "REST API невозможно защитить, а GraphQL API имеют встроенные функции безопасности",
        "Запросы GraphQL сложнее кэшировать на уровне HTTP/CDN, потому что они идут через POST"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "GraphQL sends queries as POST bodies to one endpoint, so caches that key on the URL cannot help. That is a real trade-off: flexible queries cost you the free caching that REST gets from HTTP and any CDN (content delivery network). The option saying a GraphQL client needs no cache at all is false, because normalized client caches are exactly how GraphQL apps avoid refetching. The option that makes GraphQL always faster overstates the speed advantage, since that depends on the implementation. The option calling REST APIs impossible to secure is wrong: both styles need the same security work.",
      "ru": "GraphQL отправляет запросы телом POST на один эндпоинт, поэтому кэши, которые опираются на URL, тут бесполезны. Это настоящий компромисс: за гибкость запросов вы платите тем самым бесплатным кэшированием, которое REST получает от HTTP и любого CDN (content delivery network — сеть доставки контента). Вариант про то, что клиенту GraphQL кэш вообще не нужен, неверен: нормализованный кэш на клиенте — как раз то, чем такие приложения избегают повторных запросов. Вариант про то, что GraphQL всегда быстрее, переоценивает выигрыш в скорости: это зависит от реализации. Вариант про невозможность защитить REST API тоже неверен — обе архитектуры требуют одинаковой работы по безопасности."
    }
  }
]
