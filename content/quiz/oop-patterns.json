[
  {
    "id": "oop-patterns-quiz-1",
    "topicId": "oop-patterns",
    "question": {
      "en": "You need to ensure only one instance of a database connection class exists across your entire application. However, you're concerned about thread safety in a multi-threaded environment. Which statement about Singleton pattern pitfalls is most accurate?",
      "ru": "Вам нужно убедиться, что существует только один экземпляр класса подключения к базе данных во всем приложении. Однако вас беспокоит потокобезопасность в многопоточной среде. Какое утверждение о подводных камнях паттерна Singleton наиболее точно?"
    },
    "options": {
      "en": [
        "A poorly written Singleton can be bypassed by reflection or deserialization",
        "Lazy initialization in Singleton always guarantees thread safety without additional synchronization",
        "Singleton pattern eliminates the need for dependency injection in unit tests",
        "Global state from Singleton makes code more testable by reducing hidden dependencies"
      ],
      "ru": [
        "Плохо написанный Singleton можно обойти через рефлексию или десериализацию",
        "Ленивая инициализация в Singleton всегда гарантирует потокобезопасность без дополнительной синхронизации",
        "Паттерн Singleton устраняет необходимость в внедрении зависимостей при модульном тестировании",
        "Глобальное состояние от Singleton делает код более тестируемым, снижая скрытые зависимости"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "While Singleton controls instantiation, it's vulnerable to reflection and deserialization bypasses, allowing multiple instances to be created. Lazy initialization requires explicit synchronization (like double-checked locking) for thread safety, and global state actually complicates testing and introduces tight coupling—contradicting the other options.",
      "ru": "Хотя Singleton контролирует создание экземпляров, он уязвим для обхода через рефлексию и десериализацию, позволяя создавать несколько экземпляров. Ленивая инициализация требует явной синхронизации (например, двойной проверки блокировки) для потокобезопасности, а глобальное состояние усложняет тестирование и вводит тесную связанность, противоречащие другим вариантам."
    }
  },
  {
    "id": "oop-patterns-quiz-2",
    "topicId": "oop-patterns",
    "question": {
      "en": "Consider the following code structure and determine the pattern used:\n```js\nclass PaymentProcessor {\n  processVisa(payment) { /* Visa logic */ }\n  processMasterCard(payment) { /* MasterCard logic */ }\n  processPayPal(payment) { /* PayPal logic */ }\n  process(type, payment) {\n    switch(type) {\n      case 'visa': return this.processVisa(payment);\n      case 'mastercard': return this.processMasterCard(payment);\n      case 'paypal': return this.processPayPal(payment);\n    }\n  }\n}\n```\nWhich refactoring would best apply Factory vs Abstract Factory pattern here?",
      "ru": "Рассмотрите следующую структуру кода и определите используемый паттерн:\n```js\nclass PaymentProcessor {\n  processVisa(payment) { /* Visa logic */ }\n  processMasterCard(payment) { /* MasterCard logic */ }\n  processPayPal(payment) { /* PayPal logic */ }\n  process(type, payment) {\n    switch(type) {\n      case 'visa': return this.processVisa(payment);\n      case 'mastercard': return this.processMasterCard(payment);\n      case 'paypal': return this.processPayPal(payment);\n    }\n  }\n}\n```\nКакой рефакторинг лучше всего применит паттерн Factory vs Abstract Factory здесь?"
    },
    "options": {
      "en": [
        "Use Abstract Factory to create family of related payment processors with regional variations",
        "Use Factory Method to create the right PaymentProcessor subclass for the given type",
        "Use Abstract Factory for individual payment methods and Factory for regional payment systems",
        "Both patterns are equally suited; choose based on whether you prefer composition over inheritance"
      ],
      "ru": [
        "Используйте Abstract Factory для создания семейства связанных процессоров платежей с региональными вариациями",
        "Используйте Factory Method, чтобы создать нужный подкласс PaymentProcessor для заданного типа",
        "Используйте Abstract Factory для отдельных методов платежа и Factory для региональных платежных систем",
        "Оба паттерна одинаково подходят; выбирайте в зависимости от того, предпочитаете ли вы композицию наследованию"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "This scenario calls for Factory Method because you need to create different PaymentProcessor subclasses (VisaProcessor, MasterCardProcessor, PayPalProcessor) based on a single parameter. Abstract Factory is used when creating families of related objects that work together—unnecessary here. The switch statement should move into a factory function, not remain coupled to the class. The caller then works with one unified process() method, whatever subclass it receives.",
      "ru": "Этот сценарий требует Factory Method, потому что вам нужно создать разные подклассы PaymentProcessor (VisaProcessor, MasterCardProcessor, PayPalProcessor) на основе одного параметра. Abstract Factory используется при создании семейств связанных объектов, которые работают вместе—здесь это не нужно. Оператор switch должен переместиться в функцию factory, а не оставаться связанным с классом. Вызывающий код затем работает с единым методом process(), какой бы подкласс он ни получил."
    }
  },
  {
    "id": "oop-patterns-quiz-3",
    "topicId": "oop-patterns",
    "question": {
      "en": "Your team is debating between Observer and Pub/Sub patterns for a real-time notification system. Which characteristic best distinguishes Pub/Sub from Observer?",
      "ru": "Ваша команда обсуждает выбор между паттернами Observer и Pub/Sub для системы уведомлений в реальном времени. Какая характеристика лучше всего отличает Pub/Sub от Observer?"
    },
    "options": {
      "en": [
        "Pub/Sub is synchronous while Observer is always asynchronous",
        "Observer supports multiple event types while Pub/Sub only handles single events",
        "Observer keeps direct object references, while Pub/Sub goes through a broker",
        "Pub/Sub is implemented only in event-driven architectures while Observer works in any OOP context"
      ],
      "ru": [
        "Pub/Sub синхронна, а Observer всегда асинхронна",
        "Observer поддерживает несколько типов событий, а Pub/Sub обрабатывает только одиночные события",
        "Observer хранит прямые ссылки на объекты, а Pub/Sub работает через брокер",
        "Pub/Sub реализуется только в архитектурах, управляемых событиями, а Observer работает в любом контексте ООП"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Pub/Sub introduces a message broker as an intermediary, completely decoupling publishers from subscribers—they don't know about each other. Observer has direct references between subject and observers. Both can be synchronous or asynchronous, both support multiple event types, and both apply in various architectures. The key difference is the coupling and intermediary.",
      "ru": "Pub/Sub вводит брокер сообщений как посредника, полностью развязывая издателей от подписчиков—они не знают друг о друге. Observer имеет прямые ссылки между субъектом и наблюдателями. Оба могут быть синхронными или асинхронными, оба поддерживают несколько типов событий и применяются в различных архитектурах. Ключевое различие — в развязке и посреднике."
    }
  },
  {
    "id": "oop-patterns-quiz-4",
    "topicId": "oop-patterns",
    "question": {
      "en": "Analyze this code and predict the output:\n```js\nclass PaymentContext {\n  constructor(strategy) { this.strategy = strategy; }\n  setStrategy(strategy) { this.strategy = strategy; }\n  pay(amount) { return this.strategy.execute(amount); }\n}\n\nclass CreditCardStrategy { execute(amount) { return `Pay ${amount} via CC`; } }\nclass BitcoinStrategy { execute(amount) { return `Pay ${amount} via BTC`; } }\n\nconst context = new PaymentContext(new CreditCardStrategy());\nconsole.log(context.pay(100));\ncontext.setStrategy(new BitcoinStrategy());\nconsole.log(context.pay(100));\n```\nWhat pattern is demonstrated and why is it fundamentally different from State?",
      "ru": "Проанализируйте этот код и предскажите вывод:\n```js\nclass PaymentContext {\n  constructor(strategy) { this.strategy = strategy; }\n  setStrategy(strategy) { this.strategy = strategy; }\n  pay(amount) { return this.strategy.execute(amount); }\n}\n\nclass CreditCardStrategy { execute(amount) { return `Pay ${amount} via CC`; } }\nclass BitcoinStrategy { execute(amount) { return `Pay ${amount} via BTC`; } }\n\nconst context = new PaymentContext(new CreditCardStrategy());\nconsole.log(context.pay(100));\ncontext.setStrategy(new BitcoinStrategy());\nconsole.log(context.pay(100));\n```\nКакой паттерн демонстрируется и чем он принципиально отличается от State?"
    },
    "options": {
      "en": [
        "State pattern is shown; strategies cannot change dynamically like states can",
        "Both patterns are identical; the difference is only in naming conventions",
        "Strategy pattern is shown; State pattern is used when you need multiple inheritance to represent different behaviors",
        "Strategy is shown; the client switches behavior, while State changes state on its own"
      ],
      "ru": [
        "Демонстрируется паттерн State; стратегии не могут меняться динамически, как состояния",
        "Оба паттерна идентичны; различие только в соглашениях об именовании",
        "Демонстрируется паттерн Strategy; паттерн State используется, когда вам нужно множественное наследование для представления различных поведений",
        "Демонстрируется паттерн Strategy; клиент переключает поведение, а State меняет состояние сам"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "This is Strategy pattern: the client explicitly chooses and changes the algorithm (strategy). State pattern differs fundamentally because the context transitions between states automatically based on internal logic—the client doesn't directly call setState(). Strategy is about algorithm choice; State is about representing object behavior that depends on internal state transitions.",
      "ru": "Это паттерн Strategy: клиент явно выбирает и меняет алгоритм (стратегию). Паттерн State принципиально отличается тем, что контекст переходит между состояниями автоматически на основе внутренней логики—клиент не вызывает setState() напрямую. Strategy это выбор алгоритма; State это представление поведения объекта, которое зависит от переходов внутреннего состояния."
    }
  },
  {
    "id": "oop-patterns-quiz-5",
    "topicId": "oop-patterns",
    "question": {
      "en": "You're reviewing a React codebase and see Higher-Order Components (HOCs) being used heavily for cross-cutting concerns like logging and authentication. How does HOC relate to the Decorator pattern, and what is a key practical difference in how they're applied?",
      "ru": "Вы проверяете кодовую базу React и видите, что Higher-Order Components (HOC) интенсивно используются для сквозных проблем, таких как логирование и аутентификация. Как HOC связан с паттерном Decorator и какое ключевое практическое различие в том, как они применяются?"
    },
    "options": {
      "en": [
        "HOC is a Decorator in React; it wraps components at runtime, not at class definition",
        "Decorator pattern and HOC are completely unrelated; Decorator is for objects while HOC is only for React components",
        "HOC and Decorator are identical patterns; both add behavior by wrapping at any point in the lifecycle",
        "Decorator is more functional while HOC is more object-oriented; Decorator is preferred for pure functions"
      ],
      "ru": [
        "HOC — это Decorator в React; он оборачивает компоненты во время выполнения, а не при определении класса",
        "Паттерн Decorator и HOC совершенно не связаны; Decorator для объектов, а HOC только для компонентов React",
        "HOC и Decorator идентичны; оба добавляют поведение путем оборачивания в любой точке жизненного цикла",
        "Decorator более функционален, а HOC более объектно-ориентирован; Decorator предпочтителен для чистых функций"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "HOC is essentially a functional implementation of the Decorator pattern in React. Both add behavior by wrapping targets (objects in classic Decorator, components in HOC). The practical difference is timing: traditional Decorator is applied at class definition time via annotations/metadata, while HOC wraps at runtime to enhance components with additional props or lifecycle logic. This allows HOC to be more dynamic and composable in JavaScript/React.",
      "ru": "HOC это по сути функциональная реализация паттерна Decorator в React. Оба добавляют поведение путем оборачивания целей (объектов в классическом Decorator, компонентов в HOC). Практическое различие во времени: традиционный Decorator применяется во время определения класса через аннотации/метаданные, а HOC оборачивает во время выполнения, чтобы улучшить компоненты дополнительными пропсами или логикой жизненного цикла. Это позволяет HOC быть более динамичным и составляемым в JavaScript/React."
    }
  },
  {
    "id": "oop-patterns-quiz-6",
    "topicId": "oop-patterns",
    "question": {
      "en": "You're reviewing code where a Singleton pattern is implemented using a class with a static instance variable and lazy initialization. What is the primary pitfall to watch for in a multithreaded environment?",
      "ru": "Вы проверяете код, где паттерн Singleton реализован с использованием статической переменной экземпляра и ленивой инициализацией. Какова основная опасность в многопоточной среде?"
    },
    "options": {
      "en": [
        "The static field holds the instance for the whole process lifetime and leaks memory",
        "Race conditions can create several instances without proper synchronization",
        "The Singleton cannot be tested, because a static field cannot be swapped for a fake",
        "Lazy initialization is always slower than creating the instance when the class loads"
      ],
      "ru": [
        "Статическое поле держит экземпляр всё время работы процесса и приводит к утечке памяти",
        "Состояния гонки могут создать несколько экземпляров без должной синхронизации",
        "Singleton нельзя протестировать, потому что статическое поле нельзя подменить заглушкой",
        "Ленивая инициализация всегда медленнее создания экземпляра при загрузке класса"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Without proper synchronization (like double-checked locking), multiple threads can simultaneously check the instance and all find it null, each creating their own instance. The other options are either performance concerns or incorrect statements—Singletons can be tested via dependency injection, and memory isn't the issue.",
      "ru": "Без правильной синхронизации (например, double-checked locking) несколько потоков могут одновременно проверить экземпляр, найти его нулевым и каждый создать свой собственный. Остальные варианты либо являются проблемами производительности, либо неверны—Singletons могут быть протестированы через внедрение зависимостей, и память не является проблемой."
    }
  },
  {
    "id": "oop-patterns-quiz-7",
    "topicId": "oop-patterns",
    "question": {
      "en": "What is the key difference between Factory and Abstract Factory patterns? Consider a scenario where you're building UI components across multiple platforms.",
      "ru": "В чем ключевое различие между паттернами Factory и Abstract Factory? Рассмотрим сценарий, где вы создаете компоненты пользовательского интерфейса на нескольких платформах."
    },
    "options": {
      "en": [
        "Factory is always faster than Abstract Factory because it's simpler",
        "Abstract Factory is used only for creating objects from databases",
        "Factory makes one product type; Abstract Factory makes product families",
        "Factory and Abstract Factory are interchangeable names for the same pattern"
      ],
      "ru": [
        "Factory всегда быстрее Abstract Factory, потому что проще",
        "Abstract Factory используется только для создания объектов из баз данных",
        "Factory создаёт один тип продукта; Abstract Factory — семейства продуктов",
        "Factory и Abstract Factory—это взаимозаменяемые названия одного паттерна"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Factory handles creation of a single product type, while Abstract Factory manages creation of multiple related product families (e.g., Windows buttons, Mac buttons, Linux buttons as a cohesive set). This makes Abstract Factory ideal for cross-platform UI systems. The other options confuse performance, scope, or terminology.",
      "ru": "Factory обрабатывает создание одного типа продукта, а Abstract Factory управляет созданием нескольких семейств связанных продуктов (например, кнопки Windows, кнопки Mac, кнопки Linux как связный набор). Это делает Abstract Factory идеальным для кроссплатформенных систем пользовательского интерфейса. Остальные варианты путают производительность, область применения или терминологию."
    }
  },
  {
    "id": "oop-patterns-quiz-8",
    "topicId": "oop-patterns",
    "question": {
      "en": "Consider this Observer pattern implementation. What will be logged?\n```ts\nclass Subject {\n  private observers: Function[] = [];\n  notify(data: any) {\n    this.observers.forEach(obs => obs(data));\n  }\n  subscribe(observer: Function) {\n    this.observers.push(observer);\n  }\n}\n\nconst subject = new Subject();\nsubject.subscribe((data) => console.log('Observer 1:', data));\nsubject.subscribe((data) => console.log('Observer 2:', data));\nsubject.notify('event');\n```",
      "ru": "Рассмотрите эту реализацию паттерна Observer. Что будет выведено?\n```ts\nclass Subject {\n  private observers: Function[] = [];\n  notify(data: any) {\n    this.observers.forEach(obs => obs(data));\n  }\n  subscribe(observer: Function) {\n    this.observers.push(observer);\n  }\n}\n\nconst subject = new Subject();\nsubject.subscribe((data) => console.log('Observer 1:', data));\nsubject.subscribe((data) => console.log('Observer 2:', data));\nsubject.notify('event');\n```"
    },
    "options": {
      "en": [
        "Observer 1: event only (second observer is not called)",
        "Observer 2: event only (second observer overwrites the first)",
        "Nothing is logged (observers array is empty)",
        "Observer 1: event\\nObserver 2: event"
      ],
      "ru": [
        "Observer 1: event только (второй наблюдатель не вызывается)",
        "Observer 2: event только (второй наблюдатель перезаписывает первый)",
        "Ничего не регистрируется (массив observers пуст)",
        "Observer 1: event\\nObserver 2: event"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The subscribe method pushes each observer function into the array, so both observers are stored. The notify method iterates through all observers and calls each one with the data 'event'. Both will execute in order, logging both messages. This demonstrates the Observer pattern's one-to-many notification capability.",
      "ru": "Метод subscribe добавляет каждую функцию наблюдателя в массив, поэтому оба наблюдателя сохраняются. Метод notify выполняет итерацию по всем наблюдателям и вызывает каждого с данными 'event'. Оба будут выполняться по порядку, выводя оба сообщения. Это демонстрирует возможность уведомления один-ко-многим паттерна Observer."
    }
  },
  {
    "id": "oop-patterns-quiz-9",
    "topicId": "oop-patterns",
    "question": {
      "en": "How does the Strategy pattern differ from the State pattern in terms of responsibility and control flow?",
      "ru": "Чем паттерн Strategy отличается от паттерна State в плане ответственности и управления потоком?"
    },
    "options": {
      "en": [
        "Strategy lets the client pick the algorithm; State switches states on its own",
        "Strategy is for inheritance hierarchies; State is for composition",
        "They are identical patterns with different names depending on programming language",
        "State is faster because it caches state changes, while Strategy recalculates each time"
      ],
      "ru": [
        "Strategy позволяет клиенту выбрать алгоритм; State переключает состояния сам",
        "Strategy используется для иерархий наследования; State используется для композиции",
        "Это идентичные паттерны с разными названиями в зависимости от языка программирования",
        "State быстрее, потому что кэширует изменения состояния, а Strategy пересчитывает каждый раз"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Strategy gives the client explicit control over which algorithm to use, while State encapsulates state-dependent behavior and typically transitions automatically based on conditions. In Strategy, the client decides; in State, the object manages transitions internally. Both use composition, and performance is not a distinguishing factor.",
      "ru": "Strategy дает клиенту явный контроль над выбором алгоритма, в то время как State инкапсулирует поведение, зависящее от состояния, и обычно переходит автоматически на основе условий. В Strategy клиент решает; в State объект управляет переходами внутренне. Оба используют композицию, и производительность не является отличительным фактором."
    }
  },
  {
    "id": "oop-patterns-quiz-10",
    "topicId": "oop-patterns",
    "question": {
      "en": "When comparing the Decorator pattern with Higher-Order Components (HOC) in React, what is a significant architectural difference?",
      "ru": "При сравнении паттерна Decorator с компонентами высшего порядка (HOC) в React, какое существенное архитектурное различие?"
    },
    "options": {
      "en": [
        "HOC is always more performant than Decorator because it uses functional programming",
        "Decorator wraps object instances; HOC returns a wrapper React component",
        "Decorator can only be used in class-based languages; HOC is specific to JavaScript",
        "They solve the same problem, but Decorator is the older, deprecated approach"
      ],
      "ru": [
        "HOC всегда более производительный, чем Decorator, потому что использует функциональное программирование",
        "Decorator оборачивает экземпляры объектов; HOC возвращает компонент-обёртку React",
        "Decorator можно использовать только в языках, основанных на классах; HOC характерен для JavaScript",
        "Они решают одну и ту же проблему, но Decorator—это более старый, устаревший подход"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Decorator adds behavior to individual object instances through composition, while HOC returns a new component that wraps the original, preserving component identity and life cycle. Though conceptually similar, HOC is React-specific pattern adapted from Decorator principles. Performance depends on implementation, not the pattern itself.",
      "ru": "Decorator добавляет поведение к отдельным экземплярам объектов посредством композиции, в то время как HOC возвращает новый компонент, который оборачивает исходный, сохраняя идентификацию компонента и жизненный цикл. Хотя концептуально похожи, HOC—это паттерн, специфичный для React, адаптированный из принципов Decorator. Производительность зависит от реализации, а не от самого паттерна."
    }
  },
  {
    "id": "oop-patterns-quiz-11",
    "topicId": "oop-patterns",
    "question": {
      "en": "You're implementing a logger for a distributed system. A colleague suggests using the Singleton pattern to ensure only one logger instance exists globally. What is the primary pitfall of this approach in a multithreaded environment?\n\n```java\npublic class Logger {\n  private static Logger instance;\n  public static Logger getInstance() {\n    if (instance == null) {\n      instance = new Logger();\n    }\n    return instance;\n  }\n}\n```",
      "ru": "Вы реализуете логгер для распределённой системы. Коллега предлагает использовать паттерн Singleton, чтобы убедиться, что существует только один экземпляр логгера. Каков основной недостаток такого подхода в многопоточной среде?\n\n```java\npublic class Logger {\n  private static Logger instance;\n  public static Logger getInstance() {\n    if (instance == null) {\n      instance = new Logger();\n    }\n    return instance;\n  }\n}\n```"
    },
    "options": {
      "en": [
        "The pattern uses excessive memory due to static keyword",
        "It violates the Single Responsibility Principle by managing its own instantiation",
        "Race condition: two threads can pass the null check and each create an instance",
        "Singleton instances cannot be serialized in Java, so the logger state is lost on restart"
      ],
      "ru": [
        "Паттерн использует избыточную память из-за статического ключевого слова",
        "Это нарушает принцип единственной ответственности, управляя собственным созданием",
        "Состояние гонки: два потока могут пройти проверку на null и каждый создать экземпляр",
        "Экземпляры Singleton нельзя сериализовать в Java, поэтому состояние логгера теряется при перезапуске"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "This code is plain lazy initialization: one null check and no lock. Two threads can both see `instance == null`, both run the constructor, and one result overwrites the other. That breaks the Singleton guarantee. Double-checked locking is the cure, not the disease. It means check, take the lock, check again inside the lock, and declare the field `volatile`. The three remaining options are either false or not primary pitfalls of Singletons in concurrent code.",
      "ru": "Здесь обычная ленивая инициализация: одна проверка на null и никакой блокировки. Два потока могут оба увидеть `instance == null`, оба вызвать конструктор, и один результат перезапишет другой. Гарантия Singleton сломана. Двойная проверка с блокировкой (double-checked locking) — это лечение, а не болезнь. Она означает: проверка, захват блокировки, повторная проверка внутри блокировки и поле `volatile`. Три остальных варианта либо ложны, либо описывают не главную проблему Singleton в параллельном коде."
    }
  },
  {
    "id": "oop-patterns-quiz-12",
    "topicId": "oop-patterns",
    "question": {
      "en": "When should you choose Abstract Factory over Factory Method pattern?",
      "ru": "Когда вы должны выбрать паттерн Abstract Factory вместо паттерна Factory Method?"
    },
    "options": {
      "en": [
        "When creating a single type of object with minor variations",
        "When you want to reduce the number of classes in your codebase",
        "When performance is critical and you need to minimize instantiation overhead",
        "When you need families of related objects that must work together"
      ],
      "ru": [
        "Когда создаётся один тип объекта с незначительными вариациями",
        "Когда вы хотите сократить количество классов в кодовой базе",
        "Когда производительность критична и нужно минимизировать накладные расходы на создание экземпляров",
        "Когда нужны семейства связанных объектов, которые должны работать вместе"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Abstract Factory is designed for creating families of related objects (e.g., dark and light UI themes with buttons, dialogs, menus). Factory Method handles single object creation. The option about creating a single type of object with minor variations describes Factory Method's use case, while the options about reducing class count and minimizing instantiation overhead are not primary motivations for choosing Abstract Factory.",
      "ru": "Abstract Factory предназначена для создания семейств связанных объектов (например, тёмная и светлая UI темы с кнопками, диалогами, меню). Factory Method обрабатывает создание одного объекта. Вариант про создание одного типа объекта с незначительными вариациями описывает сценарий использования Factory Method, а варианты про сокращение количества классов и минимизацию накладных расходов не являются основными мотивами выбора Abstract Factory."
    }
  },
  {
    "id": "oop-patterns-quiz-13",
    "topicId": "oop-patterns",
    "question": {
      "en": "What is the key semantic difference between Observer pattern and Pub/Sub pattern?\n```ts\n// Observer: Direct reference\nsubject.attach(observer);\n\n// Pub/Sub: Indirect via broker\npublisher.publish('event-name', data);\nsubscriber.subscribe('event-name', handler);\n```",
      "ru": "Какова ключевая семантическая разница между паттерном Observer и паттерном Pub/Sub?\n```ts\n// Observer: Прямая ссылка\nsubject.attach(observer);\n\n// Pub/Sub: Косвенно через брокер\npublisher.publish('event-name', data);\nsubscriber.subscribe('event-name', handler);\n```"
    },
    "options": {
      "en": [
        "Observer couples subject and observer directly; Pub/Sub adds a broker",
        "Pub/Sub is synchronous while Observer is asynchronous",
        "Observer allows one subject only; Pub/Sub allows many publishers",
        "Pub/Sub uses more memory because it requires an event broker"
      ],
      "ru": [
        "Observer напрямую связывает subject и observer; Pub/Sub добавляет брокер",
        "Pub/Sub синхронна, а Observer асинхронна",
        "Observer допускает только один subject; Pub/Sub допускает много издателей",
        "Pub/Sub использует больше памяти, потому что требует брокера событий"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Observer has tight coupling—objects reference each other directly. Pub/Sub introduces a broker that mediates communication, removing direct dependencies. The options about synchronous versus asynchronous behavior and about the number of subjects and publishers describe variations but not the core difference. The option about Pub/Sub using more memory is a resource concern, not the semantic distinction.",
      "ru": "Observer имеет тесную связь—объекты напрямую ссылаются друг на друга. Pub/Sub вводит брокер, который опосредует коммуникацию, убирая прямые зависимости. Варианты про синхронность и асинхронность и про количество subject и издателей описывают вариации, но не основное отличие. Вариант про большее потребление памяти Pub/Sub — это проблема ресурсов, а не семантическое различие."
    }
  },
  {
    "id": "oop-patterns-quiz-14",
    "topicId": "oop-patterns",
    "question": {
      "en": "Compare Strategy and State patterns. Which statement is accurate?\n\n```java\n// Strategy: Client chooses algorithm\nprocessor.setStrategy(new FastAlgorithm());\n\n// State: Object changes behavior based on internal state\ntraffic.setState(new RedLightState());\n```",
      "ru": "Сравните паттерны Strategy и State. Какое утверждение точно?\n\n```java\n// Strategy: Клиент выбирает алгоритм\nprocessor.setStrategy(new FastAlgorithm());\n\n// State: Объект меняет поведение на основе внутреннего состояния\ntraffic.setState(new RedLightState());\n```"
    },
    "options": {
      "en": [
        "State pattern is faster than Strategy because it caches state transitions",
        "The client sets the Strategy; the object handles State transitions",
        "Both patterns are identical; State is just an older name for Strategy",
        "Strategy is used for conditional logic; State is used for polymorphism"
      ],
      "ru": [
        "Паттерн State быстрее, чем Strategy, потому что он кэширует переходы состояний",
        "Strategy задаёт клиент; переходы State объект обрабатывает сам",
        "Оба паттерна идентичны; State—просто старое имя для Strategy",
        "Strategy используется для условной логики; State используется для полиморфизма"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Strategy empowers the client to pick an algorithm at runtime. State manages internal state transitions within an object—the object itself decides when to transition. The option claiming State is faster because it caches transitions and the option claiming the two patterns are identical are factually incorrect. The option splitting them into conditional logic versus polymorphism conflates their purposes; both use polymorphism, but for different reasons.",
      "ru": "Strategy предоставляет клиенту возможность выбрать алгоритм во время выполнения. State управляет внутренними переходами состояний внутри объекта—сам объект решает, когда переходить. Вариант про то, что State быстрее из-за кэширования переходов, и вариант про идентичность паттернов фактически неверны. Вариант про условную логику и полиморфизм смешивает их назначения; оба используют полиморфизм, но по разным причинам."
    }
  },
  {
    "id": "oop-patterns-quiz-15",
    "topicId": "oop-patterns",
    "question": {
      "en": "In React, Higher-Order Components (HOCs) are often used instead of the Decorator pattern. What is the main reason HOCs are preferred over classical decorators in functional composition?\n\n```jsx\n// Decorator-like behavior in React\nconst EnhancedComponent = withTheme(withRouter(MyComponent));\n```",
      "ru": "В React Higher-Order Components (HOCs) часто используются вместо паттерна Decorator. Почему HOCs предпочитаются классическим декораторам при функциональной композиции?\n\n```jsx\n// Поведение, подобное Decorator, в React\nconst EnhancedComponent = withTheme(withRouter(MyComponent));\n```"
    },
    "options": {
      "en": [
        "Decorators cannot modify component props while HOCs can",
        "HOCs are more performant because they use memoization by default",
        "HOCs use function composition and return new wrapper components",
        "JavaScript does not support decorators natively, so HOCs are a workaround"
      ],
      "ru": [
        "Декораторы не могут изменять Props компонента, в то время как HOCs могут",
        "HOCs более производительны, потому что по умолчанию используют мемоизацию",
        "HOCs используют функциональную композицию и возвращают новые компоненты-обёртки",
        "JavaScript не поддерживает декораторы изначально, поэтому HOCs—это обходное решение"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "HOCs are functions that return new components, making them idiomatic to React's functional and compositional design. They naturally enhance components by wrapping and adding props. The option about decorators not being able to modify props is partially true but not the main reason. The option about memoization is false; HOCs don't memoize by default. The option claiming JavaScript has no native decorators is outdated—decorators are now part of JS standards.",
      "ru": "HOCs—это функции, которые возвращают новые компоненты, что делает их идиоматичными для функционального и композиционного дизайна React. Они естественно улучшают компоненты, обёртывая их и добавляя Props. Вариант про то, что декораторы не могут изменять Props, частично верен, но не основная причина. Вариант про memoization неверен; HOCs не используют memoization по умолчанию. Вариант про отсутствие нативной поддержки декораторов в JavaScript устарел—декораторы теперь являются частью стандартов JS."
    }
  },
  {
    "id": "oop-patterns-quiz-16",
    "topicId": "oop-patterns",
    "question": {
      "en": "You're reviewing code where a Singleton is accessed from multiple threads without synchronization. What is the primary pitfall this introduces?\n\n```java\npublic class DatabaseConnection {\n  private static DatabaseConnection instance;\n  \n  public static DatabaseConnection getInstance() {\n    if (instance == null) {\n      instance = new DatabaseConnection();\n    }\n    return instance;\n  }\n}\n```",
      "ru": "Вы проверяете код, где Singleton доступен из нескольких потоков без синхронизации. Какой основной подвох это вводит?\n\n```java\npublic class DatabaseConnection {\n  private static DatabaseConnection instance;\n  \n  public static DatabaseConnection getInstance() {\n    if (instance == null) {\n      instance = new DatabaseConnection();\n    }\n    return instance;\n  }\n}\n```"
    },
    "options": {
      "en": [
        "The Singleton pattern cannot be used with databases",
        "The getInstance method will always return null on the first call",
        "Thread safety is guaranteed because static fields are thread-safe by default",
        "Multiple instances of the Singleton can be created due to race conditions"
      ],
      "ru": [
        "Паттерн Singleton не может использоваться с базами данных",
        "Метод getInstance всегда вернёт null при первом вызове",
        "Потокобезопасность гарантирована, так как статические поля потокобезопасны по умолчанию",
        "Из-за условий гонки можно создать несколько экземпляров Singleton"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "There is a single null check here and no lock, so two threads can pass that check at the same time and create separate instances. Assigning the static field is itself atomic, but the check-then-act sequence around it is not. The repair has a name: double-checked locking, meaning check, lock, check again, plus a `volatile` field. The other options misunderstand either Singleton's applicability or Java's threading model.",
      "ru": "Здесь одна проверка на null и никакой блокировки, поэтому два потока могут пройти её одновременно и создать отдельные экземпляры. Само присваивание статического поля атомарно, а вот последовательность «проверил — создал» вокруг него — нет. У починки есть имя: двойная проверка с блокировкой (double-checked locking) — проверка, блокировка, повторная проверка и поле `volatile`. Остальные варианты неправильно понимают применимость Singleton или модель потоков Java."
    }
  },
  {
    "id": "oop-patterns-quiz-17",
    "topicId": "oop-patterns",
    "question": {
      "en": "When would you choose Abstract Factory over Factory Method pattern?\n\nA) When you need to create a single family of related objects\nB) When you need to instantiate objects from multiple families and ensure consistency\nC) When you want to defer object creation to subclasses\nD) When the creation logic is simple with few variations",
      "ru": "Когда вы выберете Abstract Factory вместо паттерна Factory Method?\n\nA) Когда нужно создать одно семейство связанных объектов\nB) Когда нужно инстанцировать объекты из нескольких семейств и обеспечить согласованность\nC) Когда хотите отложить создание объектов на подклассы\nD) Когда логика создания простая с несколькими вариантами"
    },
    "options": {
      "en": [
        "When you need to instantiate objects from multiple families and ensure consistency",
        "When you need to create a single family of related objects",
        "When you want to defer object creation to subclasses",
        "When the creation logic is simple with few variations"
      ],
      "ru": [
        "Когда нужно инстанцировать объекты из нескольких семейств и обеспечить согласованность",
        "Когда нужно создать одно семейство связанных объектов",
        "Когда хотите отложить создание объектов на подклассы",
        "Когда логика создания простая с несколькими вариантами"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Abstract Factory is designed for creating families of related objects (e.g., Windows UI components vs Mac UI components together), ensuring they work together consistently. Factory Method is better for single-object creation with deferred instantiation. The option about instantiating objects from multiple families captures this distinction—Abstract Factory manages multiple related object families, while the others describe Factory Method or simpler patterns.",
      "ru": "Abstract Factory предназначена для создания семейств связанных объектов (например, компоненты Windows UI и Mac UI вместе), обеспечивая их согласованную работу. Factory Method лучше подходит для создания одного объекта с отложенной инстанциацией. Вариант про инстанцирование объектов из нескольких семейств отражает это различие — Abstract Factory управляет несколькими семействами связанных объектов, остальные описывают Factory Method или более простые паттерны."
    }
  },
  {
    "id": "oop-patterns-quiz-18",
    "topicId": "oop-patterns",
    "question": {
      "en": "Study this code snippet:\n\n```javascript\nclass DataStore {\n  constructor() {\n    this.listeners = [];\n  }\n  \n  subscribe(callback) {\n    this.listeners.push(callback);\n  }\n  \n  emit(data) {\n    this.listeners.forEach(cb => cb(data));\n  }\n}\n\nconst store = new DataStore();\nstore.subscribe((d) => console.log('A:', d));\nstore.subscribe((d) => console.log('B:', d));\nstore.emit('update');\n```\n\nWhat pattern is this, and what is a key limitation?",
      "ru": "Изучите этот фрагмент кода:\n\n```javascript\nclass DataStore {\n  constructor() {\n    this.listeners = [];\n  }\n  \n  subscribe(callback) {\n    this.listeners.push(callback);\n  }\n  \n  emit(data) {\n    this.listeners.forEach(cb => cb(data));\n  }\n}\n\nconst store = new DataStore();\nstore.subscribe((d) => console.log('A:', d));\nstore.subscribe((d) => console.log('B:', d));\nstore.emit('update');\n```\n\nЭто какой паттерн и какое основное ограничение?"
    },
    "options": {
      "en": [
        "Pub/Sub pattern; it guarantees message delivery order across all subscribers",
        "Observer pattern; listeners are tightly coupled to the store instance",
        "Strategy pattern; it requires subscribers to implement a common interface",
        "Event Emitter pattern; it doesn't support unsubscribing from events"
      ],
      "ru": [
        "Паттерн Pub/Sub; гарантирует порядок доставки сообщений всем подписчикам",
        "Паттерн Observer; слушатели плотно связаны с экземпляром хранилища",
        "Паттерн Strategy; требует, чтобы подписчики реализовали общий интерфейс",
        "Паттерн Event Emitter; не поддерживает отписку от событий"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "This is the Observer pattern because listeners are directly registered on the subject (store). A key limitation is tight coupling: subscribers must know about and reference the specific store instance. Pub/Sub decouples this through a message broker. The code doesn't enforce an interface, and unsubscribe isn't shown but could be added.",
      "ru": "Это паттерн Observer, так как слушатели напрямую зарегистрированы на субъекте (store). Ключевое ограничение — плотная связанность: подписчики должны знать и ссылаться на конкретный экземпляр хранилища. Pub/Sub развязывает это через брокер сообщений. Код не требует интерфейс, и отписка не показана, но может быть добавлена."
    }
  },
  {
    "id": "oop-patterns-quiz-19",
    "topicId": "oop-patterns",
    "question": {
      "en": "What is the fundamental difference between Strategy and State patterns?\n\nConsider a Payment processor where:\n- Strategy allows switching between CreditCard, PayPal, or Bitcoin at runtime\n- State transitions an Order from Pending → Confirmed → Shipped → Delivered based on business rules",
      "ru": "В чем фундаментальное различие между паттернами Strategy и State?\n\nРассмотрим процессор платежей, где:\n- Strategy позволяет переключаться между CreditCard, PayPal или Bitcoin во время выполнения\n- State переводит Order из Pending → Confirmed → Shipped → Delivered на основе бизнес-правил"
    },
    "options": {
      "en": [
        "State requires inheritance; Strategy uses composition",
        "Strategy is used for conditional logic; State is only for workflows",
        "The client chooses the Strategy; the object drives State transitions",
        "Both patterns are identical; only the name differs"
      ],
      "ru": [
        "State требует наследования; Strategy использует композицию",
        "Strategy используется для условной логики; State только для рабочих процессов",
        "Strategy выбирает клиент; переходы State запускает сам объект",
        "Оба паттерна идентичны; отличается только название"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "In Strategy, the client explicitly chooses which algorithm to use (e.g., payment method). In State, transitions happen automatically based on the object's internal logic and state rules—the object controls when and how it changes state. Both use composition, and both handle conditional logic. The key distinction is who drives the change: external client vs. internal state machine.",
      "ru": "В Strategy клиент явно выбирает алгоритм (например, метод платежа). В State переходы происходят автоматически на основе внутренней логики объекта и правил состояния — объект управляет когда и как меняется состояние. Оба используют композицию и обработку условной логики. Ключевое различие — кто инициирует изменение: внешний клиент или внутренняя машина состояний."
    }
  },
  {
    "id": "oop-patterns-quiz-20",
    "topicId": "oop-patterns",
    "question": {
      "en": "Given a React component wrapped with multiple HOCs:\n\n```jsx\nconst Enhanced = withTheme(withRouter(withAuth(MyComponent)));\n```\n\nWhy would using the Decorator pattern (or composition of decorators) be problematic here compared to props drilling or context?",
      "ru": "Дан React компонент, обёрнутый несколькими HOC:\n\n```jsx\nconst Enhanced = withTheme(withRouter(withAuth(MyComponent)));\n```\n\nПочему использование паттерна Decorator (или композиция декораторов) здесь проблематично в сравнении с props drilling или context?"
    },
    "options": {
      "en": [
        "React natively forbids composing multiple HOCs together",
        "Decorators violate the Liskov Substitution Principle",
        "Context API automatically replaces all decorator patterns in modern React",
        "Deeply nested HOC wrappers are hard to debug and add render overhead"
      ],
      "ru": [
        "React по умолчанию запрещает композицию нескольких HOC вместе",
        "Декораторы нарушают принцип подстановки Лисков",
        "Context API автоматически заменяет все паттерны декоратора в современном React",
        "Глубоко вложенные обёртки HOC трудно отлаживать, и они замедляют отрисовку"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Multiple HOCs create wrapper hell with deep component trees, making the React DevTools harder to navigate and increasing render overhead. Neither React nor the Liskov Substitution Principle explicitly forbids this. Context API doesn't automatically replace HOCs—they serve different purposes. Modern React hooks are often preferred over HOCs, but the issue is the composition depth and its debugging/performance consequences.",
      "ru": "Несколько HOC создают ад обёрток с глубокими деревьями компонентов, затрудняя навигацию в React DevTools и увеличивая накладные расходы на отрисовку. React и принцип подстановки Лисков не запрещают это явно. Context API не заменяет HOC автоматически — они служат разным целям. Современные React hooks часто предпочтительнее HOC, но проблема в глубине композиции и её влиянии на отладку и производительность."
    }
  },
  {
    "id": "oop-patterns-quiz-21",
    "topicId": "oop-patterns",
    "question": {
      "en": "You're reviewing code that is meant to be a thread-safe Singleton. What is the primary pitfall you should watch out for?\n\n```java\npublic class Singleton {\n  private static Singleton instance;\n  \n  public static Singleton getInstance() {\n    if (instance == null) {\n      instance = new Singleton();\n    }\n    return instance;\n  }\n}\n```",
      "ru": "Вы проверяете код, который задуман как потокобезопасный Singleton. На какую основную ошибку вам следует обратить внимание?\n\n```java\npublic class Singleton {\n  private static Singleton instance;\n  \n  public static Singleton getInstance() {\n    if (instance == null) {\n      instance = new Singleton();\n    }\n    return instance;\n  }\n}\n```"
    },
    "options": {
      "en": [
        "Without synchronization, two threads can each create their own instance",
        "The Singleton class cannot be serialized, breaking persistence functionality",
        "Static initialization causes memory leaks in garbage-collected environments",
        "The getInstance() method returns null if called before the JVM fully loads"
      ],
      "ru": [
        "Без синхронизации два потока могут каждый создать свой экземпляр",
        "Класс Singleton не может быть сериализован, нарушая функциональность сохранения",
        "Статическая инициализация вызывает утечки памяти в среде со сборкой мусора",
        "Метод getInstance() возвращает null, если вызывается до полной загрузки JVM"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "The code is not thread-safe at all: one null check, no lock. Several threads can pass that check before any instance exists, so each of them creates its own. The real fix is called double-checked locking: check, enter a `synchronized` block, check again there, and declare the field `volatile`. Serialization and garbage collection are separate concerns, and `getInstance()` will not return null after class loading.",
      "ru": "Код вообще не потокобезопасен: одна проверка на null, никакой блокировки. Несколько потоков успевают пройти эту проверку до того, как экземпляр создан, и каждый создаёт свой. Настоящее решение называется двойной проверкой с блокировкой (double-checked locking): проверка, вход в `synchronized`-блок, повторная проверка там же и поле `volatile`. Сериализация и сборка мусора — отдельные темы, а `getInstance()` не вернёт null после загрузки класса."
    }
  },
  {
    "id": "oop-patterns-quiz-22",
    "topicId": "oop-patterns",
    "question": {
      "en": "When should you prefer Abstract Factory over Factory Method?",
      "ru": "Когда следует предпочесть Abstract Factory вместо Factory Method?"
    },
    "options": {
      "en": [
        "When you need to create a single object type with different implementations",
        "When you need families of related objects that work together",
        "When you want to reduce code coupling in a small, single-responsibility module",
        "When working with primitive types that require type conversion"
      ],
      "ru": [
        "Когда вам нужно создать один тип объекта с различными реализациями",
        "Когда вам нужны семейства связанных объектов, работающих вместе",
        "Когда вы хотите уменьшить связанность кода в небольшом модуле с единственной ответственностью",
        "При работе с примитивными типами, которые требуют преобразования типов"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Abstract Factory creates families of related objects (e.g., UI components for different OS themes) ensuring they work together consistently. Factory Method creates a single type with different implementations. Abstract Factory is needed when you have multiple interdependent object types that must be created as a cohesive unit.",
      "ru": "Abstract Factory создает семейства связанных объектов (например, компоненты UI для разных тем ОС), обеспечивая согласованную работу. Factory Method создает один тип с различными реализациями. Abstract Factory нужна, когда у вас есть несколько взаимозависимых типов объектов, которые должны быть созданы как единое целое."
    }
  },
  {
    "id": "oop-patterns-quiz-23",
    "topicId": "oop-patterns",
    "question": {
      "en": "What is the key difference between Observer and Pub/Sub patterns?\n```js\n// Observer: Direct coupling\nsubject.attach(observer);\n\n// Pub/Sub: Event bus mediates\neventBus.subscribe('event', callback);\neventBus.publish('event', data);\n```",
      "ru": "В чем ключевое отличие между паттернами Observer и Pub/Sub?\n```js\n// Observer: Прямая связь\nsubject.attach(observer);\n\n// Pub/Sub: Event bus опосредует\neventBus.subscribe('event', callback);\neventBus.publish('event', data);\n```"
    },
    "options": {
      "en": [
        "Observer uses callbacks, while Pub/Sub always delivers events as promises",
        "Pub/Sub is only for asynchronous operations, Observer is only for synchronous ones",
        "Observer couples subject and observers; Pub/Sub goes through an event bus",
        "Observer supports multiple subjects, Pub/Sub supports only a single publisher"
      ],
      "ru": [
        "Observer использует callbacks, а Pub/Sub всегда доставляет события как promises",
        "Pub/Sub предназначен только для асинхронных операций, Observer только для синхронных",
        "Observer связывает субъект и наблюдателей; Pub/Sub идёт через event bus",
        "Observer поддерживает несколько субъектов, Pub/Sub поддерживает только одного издателя"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The key distinction is coupling: Observer has direct references between subject and observer objects, while Pub/Sub decouples them through a central event bus that neither knows about each other. Both can be synchronous or asynchronous, and both support multiple entities. The mediator approach in Pub/Sub provides better scalability and loose coupling.",
      "ru": "Ключевое отличие — в связанности: Observer имеет прямые ссылки между объектами субъекта и наблюдателя, а Pub/Sub разделяет их через центральный event bus, где они друг друга не знают. Оба могут быть синхронными или асинхронными, оба поддерживают несколько сущностей. Подход с посредником в Pub/Sub обеспечивает лучшую масштабируемость и слабую связанность."
    }
  },
  {
    "id": "oop-patterns-quiz-24",
    "topicId": "oop-patterns",
    "question": {
      "en": "Examine this code. Which pattern is being used?\n```ts\nclass PaymentProcessor {\n  private strategy: PaymentStrategy;\n  \n  setStrategy(strategy: PaymentStrategy) {\n    this.strategy = strategy;\n  }\n  \n  process(amount: number) {\n    return this.strategy.pay(amount);\n  }\n}\n\nconst processor = new PaymentProcessor();\nprocessor.setStrategy(new CreditCardPayment());\n```",
      "ru": "Изучите этот код. Какой паттерн здесь используется?\n```ts\nclass PaymentProcessor {\n  private strategy: PaymentStrategy;\n  \n  setStrategy(strategy: PaymentStrategy) {\n    this.strategy = strategy;\n  }\n  \n  process(amount: number) {\n    return this.strategy.pay(amount);\n  }\n}\n\nconst processor = new PaymentProcessor();\nprocessor.setStrategy(new CreditCardPayment());\n```"
    },
    "options": {
      "en": [
        "State pattern — changing object behavior based on internal state transitions",
        "Template Method — defining algorithm skeleton in base class with step variations",
        "Adapter pattern — wrapping incompatible interfaces to work together",
        "Strategy pattern — swapping the algorithm at runtime through the context"
      ],
      "ru": [
        "Паттерн State — изменение поведения объекта на основе переходов внутреннего состояния",
        "Паттерн Template Method — определение скелета алгоритма в базовом классе с вариациями шагов",
        "Паттерн Adapter — обертывание несовместимых интерфейсов для совместной работы",
        "Паттерн Strategy — подмена алгоритма во время выполнения через контекст"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "This is Strategy pattern because the PaymentProcessor allows switching between different payment algorithms (strategies) at runtime without modifying its own logic. State pattern would track internal state transitions that trigger behavior changes, while Strategy lets the client explicitly choose which algorithm to use. Template Method defines a fixed algorithm skeleton, which is not the case here.",
      "ru": "Это паттерн Strategy, потому что PaymentProcessor позволяет переключаться между различными алгоритмами платежа (стратегиями) во время выполнения без изменения собственной логики. Паттерн State отслеживал бы переходы внутреннего состояния, которые вызывают изменения поведения, а Strategy позволяет клиенту явно выбрать, какой алгоритм использовать. Template Method определяет фиксированный скелет алгоритма, чего здесь нет."
    }
  },
  {
    "id": "oop-patterns-quiz-25",
    "topicId": "oop-patterns",
    "question": {
      "en": "In React, Higher-Order Components (HOC) serve a similar purpose to the Decorator pattern in OOP. What is the primary advantage of using HOC or Decorator over direct inheritance?",
      "ru": "В React компоненты высшего порядка (HOC) служат похожей цели, что и паттерн Decorator в ООП. Каково основное преимущество использования HOC или Decorator вместо прямого наследования?"
    },
    "options": {
      "en": [
        "They add and reuse behavior without changing the original class",
        "They are faster to execute because they avoid method lookup overhead",
        "They eliminate the need for constructor functions",
        "They guarantee memory efficiency by reducing object instantiation"
      ],
      "ru": [
        "Они добавляют и переиспользуют поведение без изменения исходного класса",
        "Они быстрее выполняются, потому что избегают накладных расходов поиска методов",
        "Они исключают необходимость в функциях-конструкторах",
        "Они гарантируют экономию памяти за счёт уменьшения создания объектов"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Decorators and HOCs add behavior through composition rather than inheritance, allowing you to wrap objects/components without modifying their source code. This enables reusing and combining multiple decorators flexibly. Direct inheritance creates rigid hierarchies and makes reusing code across unrelated classes difficult. Performance and memory efficiency are not primary advantages of this pattern—composition is about design flexibility.",
      "ru": "Decorators и HOCs добавляют поведение через композицию, а не наследование, позволяя оборачивать объекты/компоненты без изменения их исходного кода. Это обеспечивает гибкое переиспользование и комбинирование нескольких decorators. Прямое наследование создает жесткие иерархии и затрудняет переиспользование кода между несвязанными классами. Производительность и экономия памяти — не главные преимущества этого паттерна — композиция касается гибкости дизайна."
    }
  }
]