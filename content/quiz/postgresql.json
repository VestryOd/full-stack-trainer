[
  {
    "id": "postgresql-quiz-1",
    "topicId": "postgresql",
    "question": {
      "en": "You run EXPLAIN on a query and see 'Seq Scan' with a high actual time but 'Index Scan' shows estimated time is lower. What does this typically indicate?\n```sql\nEXPLAIN ANALYZE SELECT * FROM users WHERE age > 25;\n```",
      "ru": "Вы запускаете EXPLAIN на запросе и видите 'Seq Scan' с высоким фактическим временем, но 'Index Scan' показывает более низкое предполагаемое время. Что это обычно указывает?\n```sql\nEXPLAIN ANALYZE SELECT * FROM users WHERE age > 25;\n```"
    },
    "options": {
      "en": [
        "Statistics are outdated and the planner's cost estimates are inaccurate",
        "The query planner made a wrong choice; the index should always be used",
        "The index exists but is not being used because it violates MVCC rules",
        "Sequential scans are always faster in PostgreSQL regardless of index availability"
      ],
      "ru": [
        "Статистика устарела и оценки стоимости планировщика неточны",
        "Планировщик запросов сделал неправильный выбор; индекс всегда должен использоваться",
        "Индекс существует, но не используется, так как он нарушает правила MVCC",
        "Последовательные сканирования всегда быстрее в PostgreSQL независимо от наличия индекса"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "A big gap between the estimate and the real execution time usually means one thing: the planner's statistics are stale. Those statistics come from ANALYZE. Running ANALYZE refreshes them, so the planner can compare costs on real numbers again. Neither MVCC (Multi-Version Concurrency Control) nor sequential scan speed explains the mismatch.",
      "ru": "Большой разрыв между оценкой и реальным временем обычно значит одно: статистика планировщика устарела. Собирает её ANALYZE. Запустите ANALYZE, и она обновится, а планировщик снова будет сравнивать стоимости по настоящим числам. Ни MVCC (многоверсионность), ни скорость последовательного сканирования это расхождение не объясняют."
    }
  },
  {
    "id": "postgresql-quiz-2",
    "topicId": "postgresql",
    "question": {
      "en": "Consider the following scenario: Two transactions run concurrently. Transaction A reads a row, Transaction B updates that same row and commits. Then Transaction A reads the row again. Under READ COMMITTED isolation, what does Transaction A see on the second read?\n```sql\n-- Transaction A\nBEGIN ISOLATION LEVEL READ COMMITTED;\nSELECT value FROM account WHERE id = 1; -- sees 100\n-- ... Transaction B commits update: value = 150 ...\nSELECT value FROM account WHERE id = 1; -- sees ?\n```",
      "ru": "Рассмотрим следующий сценарий: две транзакции выполняются одновременно. Транзакция A читает строку, Транзакция B обновляет эту же строку и фиксирует. Затем Транзакция A читает строку снова. При уровне изоляции READ COMMITTED, что видит Транзакция A при втором чтении?\n```sql\n-- Transaction A\nBEGIN ISOLATION LEVEL READ COMMITTED;\nSELECT value FROM account WHERE id = 1; -- sees 100\n-- ... Transaction B commits update: value = 150 ...\nSELECT value FROM account WHERE id = 1; -- sees ?\n```"
    },
    "options": {
      "en": [
        "Always 100 (the value from the first read, due to snapshot isolation)",
        "150 (the updated value from Transaction B)",
        "A phantom read error because the row was modified",
        "It depends on how long Transaction B took to commit"
      ],
      "ru": [
        "Всегда 100 (значение из первого чтения, из-за снимка изоляции)",
        "150 (обновлённое значение из Транзакции B)",
        "Ошибка фантомного чтения, потому что строка была изменена",
        "Это зависит от того, как долго Транзакция B фиксировалась"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "READ COMMITTED isolation level gets a fresh snapshot for each statement, so the second SELECT sees Transaction B's committed changes (150). This is different from SERIALIZABLE or REPEATABLE READ, which would maintain a consistent snapshot. Phantom reads occur with INSERT/DELETE, not UPDATE, and timing is irrelevant once a transaction commits.",
      "ru": "Уровень изоляции READ COMMITTED получает свежий снимок для каждого оператора, поэтому второй SELECT видит зафиксированные изменения Транзакции B (150). Это отличается от SERIALIZABLE или REPEATABLE READ, которые сохраняют согласованный снимок. Фантомные чтения происходят при INSERT/DELETE, а не UPDATE, и время не важно после фиксации транзакции."
    }
  },
  {
    "id": "postgresql-quiz-3",
    "topicId": "postgresql",
    "question": {
      "en": "Which index type in PostgreSQL is best suited for supporting efficient range queries and naturally maintains sort order?\n```sql\nSELECT * FROM orders WHERE created_at BETWEEN '2024-01-01' AND '2024-12-31';\n```",
      "ru": "Какой тип индекса в PostgreSQL лучше всего подходит для поддержки эффективных диапазонных запросов и естественно сохраняет порядок сортировки?\n```sql\nSELECT * FROM orders WHERE created_at BETWEEN '2024-01-01' AND '2024-12-31';\n```"
    },
    "options": {
      "en": [
        "Hash index (fastest for equality checks)",
        "GiST index (generalized search tree for complex types)",
        "B-tree index (the default type in PostgreSQL)",
        "BRIN index (block range index for very large tables)"
      ],
      "ru": [
        "Хеш-индекс (самый быстрый для проверок равенства)",
        "GiST индекс (обобщенное дерево поиска для сложных типов)",
        "B-tree индекс (тип индекса по умолчанию в PostgreSQL)",
        "BRIN индекс (индекс диапазона блоков для очень больших таблиц)"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "B-tree is PostgreSQL's default index type, and the only one that keeps its keys sorted. That is what makes it ideal for range queries (BETWEEN, <, >) and for ORDER BY. Hash indexes support equality checks only. GiST (generalized search tree) targets geometry and full-text search. BRIN (block range index) trades precision for size on huge tables. Neither suits a plain range query.",
      "ru": "B-tree — индекс по умолчанию и единственный, который держит ключи отсортированными. Поэтому он идеален для диапазонных запросов (BETWEEN, <, >) и для ORDER BY. Хеш-индексы поддерживают только равенство. GiST (обобщённое дерево поиска) — для геометрии и полнотекстового поиска, BRIN (индекс диапазона блоков) — про экономию места на огромных таблицах. Обычному диапазонному запросу не подходит ни тот, ни другой."
    }
  },
  {
    "id": "postgresql-quiz-4",
    "topicId": "postgresql",
    "question": {
      "en": "What is the primary purpose of VACUUM in PostgreSQL and how does it relate to MVCC?\n```sql\nVACUUM ANALYZE table_name;\n```",
      "ru": "Какова основная цель VACUUM в PostgreSQL и как это связано с MVCC?\n```sql\nVACUUM ANALYZE table_name;\n```"
    },
    "options": {
      "en": [
        "VACUUM encrypts old row versions; MVCC ensures only the latest version is visible",
        "VACUUM deletes entire tables; MVCC is unrelated to VACUUM operations",
        "VACUUM moves data to archive storage; MVCC prevents all writes until VACUUM completes",
        "VACUUM removes dead row versions; MVCC is what creates those versions"
      ],
      "ru": [
        "VACUUM шифрует старые версии строк; MVCC гарантирует видимость только последней версии",
        "VACUUM удаляет целые таблицы; MVCC не связана с операциями VACUUM",
        "VACUUM перемещает данные в архивное хранилище; MVCC предотвращает все записи до завершения VACUUM",
        "VACUUM удаляет мёртвые версии строк; именно MVCC их создаёт"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "MVCC (Multi-Version Concurrency Control) lets several versions of one row exist at the same time. When a row is updated or deleted, the old version is only marked dead, not removed. VACUUM reclaims that space: it deletes the dead tuples no active transaction can see any more. Without it the table bloats. The other three options describe behaviour that does not exist.",
      "ru": "MVCC (многоверсионность) позволяет нескольким версиям одной строки существовать одновременно. Когда строку обновляют или удаляют, старая версия лишь помечается мёртвой, но не удаляется. VACUUM освобождает это место: он удаляет мёртвые кортежи, которых уже не видит ни одна активная транзакция. Без него таблица раздувается. Остальные три варианта описывают несуществующее поведение."
    }
  },
  {
    "id": "postgresql-quiz-5",
    "topicId": "postgresql",
    "question": {
      "en": "Which JOIN type returns rows only when there is a match in both tables? And how does NULL handling differ between the ON clause and the WHERE clause?\n```sql\nSELECT * FROM users \nINNER JOIN orders ON users.id = orders.user_id\nWHERE orders.discount IS NOT NULL;\n```",
      "ru": "Какой тип JOIN возвращает строки только при совпадении в обеих таблицах? И чем отличается обработка NULL в предложении ON от предложения WHERE?\n```sql\nSELECT * FROM users \nINNER JOIN orders ON users.id = orders.user_id\nWHERE orders.discount IS NOT NULL;\n```"
    },
    "options": {
      "en": [
        "INNER JOIN matches both tables; ON filters during the join, WHERE filters after it",
        "LEFT JOIN matches both tables; NULL in ON and WHERE behave identically",
        "FULL OUTER JOIN matches both tables; ON clause ignores NULLs while WHERE treats them as true",
        "CROSS JOIN matches both tables; both ON and WHERE use three-valued logic identically"
      ],
      "ru": [
        "INNER JOIN совпадает с обеими таблицами; ON фильтрует во время соединения, WHERE — после него",
        "LEFT JOIN совпадает с обеими таблицами; NULL в ON и WHERE ведут себя одинаково",
        "FULL OUTER JOIN совпадает с обеими таблицами; предложение ON игнорирует NULLs, а WHERE рассматривает их как истину",
        "CROSS JOIN совпадает с обеими таблицами; и ON, и WHERE используют трёхзначную логику одинаково"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "INNER JOIN returns only the rows that match in both tables. The ON clause belongs to the JOIN itself: a row whose condition is NULL or false never reaches the result. WHERE runs afterwards and filters the rows the JOIN already produced. That difference is very visible with LEFT JOIN and RIGHT JOIN, and almost invisible with INNER JOIN.",
      "ru": "INNER JOIN возвращает только те строки, которые совпали в обеих таблицах. Условие ON — часть самого соединения: строка, для которой оно даёт NULL или ложь, в результат не попадёт вовсе. WHERE применяется уже потом и фильтрует то, что соединение выдало. Для LEFT JOIN и RIGHT JOIN эта разница очень заметна, а для INNER JOIN почти незаметна."
    }
  },
  {
    "id": "postgresql-quiz-6",
    "topicId": "postgresql",
    "question": {
      "en": "You have a query that joins a users table with an orders table. The EXPLAIN output shows 'Hash Join' with a high buffer size. What does this indicate about the query execution?\n```sql\nSELECT u.id, COUNT(o.id)\nFROM users u\nJOIN orders o ON u.id = o.user_id\nGROUP BY u.id;\n```",
      "ru": "У вас есть запрос, который объединяет таблицу users с таблицей orders. Выход EXPLAIN показывает 'Hash Join' с большим размером буфера. Что это указывает о выполнении запроса?\n```sql\nSELECT u.id, COUNT(o.id)\nFROM users u\nJOIN orders o ON u.id = o.user_id\nGROUP BY u.id;\n```"
    },
    "options": {
      "en": [
        "PostgreSQL is using an index on the join condition, which is very efficient",
        "PostgreSQL is building an in-memory hash table from the smaller table",
        "PostgreSQL is performing a nested loop iteration, which is slower than merge join",
        "PostgreSQL found no suitable index and is performing a sequential scan on both tables"
      ],
      "ru": [
        "PostgreSQL использует индекс на условии JOIN, что очень эффективно",
        "PostgreSQL строит в памяти хеш-таблицу из меньшей таблицы",
        "PostgreSQL выполняет итерацию вложенного цикла, что медленнее, чем merge join",
        "PostgreSQL не нашёл подходящий индекс и выполняет последовательное сканирование обеих таблиц"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "Hash Join loads the smaller table into a hash table in memory for efficient lookups on the join key. The high buffer size in EXPLAIN indicates the hash table construction and memory usage. This is a legitimate and often efficient join strategy when indexes aren't available or when the join condition doesn't match indexed columns.",
      "ru": "Hash Join загружает меньшую таблицу в хеш-таблицу в памяти для эффективного поиска по ключу объединения. Большой размер буфера в EXPLAIN указывает на построение хеш-таблицы и использование памяти. Это законная и часто эффективная стратегия объединения, когда индексы недоступны или когда условие объединения не совпадает с индексированными столбцами."
    }
  },
  {
    "id": "postgresql-quiz-7",
    "topicId": "postgresql",
    "question": {
      "en": "In PostgreSQL, which index type is best suited for range queries like `WHERE price BETWEEN 100 AND 500`?",
      "ru": "В PostgreSQL какой тип индекса лучше всего подходит для диапазонных запросов типа `WHERE price BETWEEN 100 AND 500`?"
    },
    "options": {
      "en": [
        "Hash index, because it distributes values uniformly",
        "GiST index, because it's optimized for geometric data types",
        "B-tree index, because it keeps keys in sorted order",
        "BRIN index, because it uses block range indexing for large tables"
      ],
      "ru": [
        "Hash индекс, потому что он равномерно распределяет значения",
        "GiST индекс, потому что он оптимизирован для геометрических типов данных",
        "B-tree индекс, потому что он хранит ключи в отсортированном порядке",
        "BRIN индекс, потому что он использует индексирование блочного диапазона для больших таблиц"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "B-tree indexes keep their keys sorted, which is what makes them ideal for range queries and comparisons: BETWEEN, <, >, <=, >=. Hash indexes support equality checks only. GiST (generalized search tree) is built for specialized types such as geometry. BRIN (block range index) suits very large tables whose rows already lie in sequence, but it is less precise on ranges.",
      "ru": "B-tree держит ключи отсортированными, и поэтому идеален для диапазонных запросов и сравнений: BETWEEN, <, >, <=, >=. Хеш-индексы поддерживают только равенство. GiST (обобщённое дерево поиска) сделан для специальных типов вроде геометрии. BRIN (индекс диапазона блоков) подходит очень большим таблицам, где строки уже лежат по порядку, но на диапазонах он менее точен."
    }
  },
  {
    "id": "postgresql-quiz-8",
    "topicId": "postgresql",
    "question": {
      "en": "What is the purpose of VACUUM in PostgreSQL, and why is it necessary?\n```sql\nVACUUM ANALYZE table_name;\n```",
      "ru": "Какова цель VACUUM в PostgreSQL и почему он необходим?\n```sql\nVACUUM ANALYZE table_name;\n```"
    },
    "options": {
      "en": [
        "It locks the table exclusively to prevent concurrent access during cleanup",
        "It encrypts the table data and removes unused columns from the schema",
        "It defragments the disk and reorganizes all indexes into a single file",
        "It reclaims storage space from deleted rows and updates table statistics"
      ],
      "ru": [
        "Он исключительно блокирует таблицу, чтобы предотвратить одновременный доступ во время очистки",
        "Он шифрует данные таблицы и удаляет неиспользуемые столбцы из схемы",
        "Он дефрагментирует диск и переорганизует все индексы в один файл",
        "Он освобождает место от удалённых строк и обновляет статистику таблицы"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "VACUUM removes the dead tuples that MVCC (Multi-Version Concurrency Control) leaves behind and reclaims their disk space. ANALYZE updates the optimizer statistics. Together they improve query performance and storage efficiency. A plain VACUUM needs no exclusive lock, unlike VACUUM FULL, and it neither encrypts data, nor drops columns, nor defragments the file system.",
      "ru": "VACUUM удаляет мёртвые кортежи, которые оставляет после себя MVCC (многоверсионность), и освобождает занятое ими место на диске. ANALYZE обновляет статистику оптимизатора. Вместе они улучшают производительность запросов и эффективность хранения. Обычному VACUUM исключительная блокировка не нужна, в отличие от VACUUM FULL, и он не шифрует данные, не удаляет столбцы и не дефрагментирует диск."
    }
  },
  {
    "id": "postgresql-quiz-9",
    "topicId": "postgresql",
    "question": {
      "en": "What will be the result of this query with the given data?\n```sql\nSELECT id, name, salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LAG(salary) OVER (ORDER BY salary DESC) as prev_salary\nFROM employees;\n```\nAssuming employees with salaries: 50000, 60000, 60000, 70000",
      "ru": "Каким будет результат этого запроса с данными?\nПредполагая сотрудников с зарплатой: 50000, 60000, 60000, 70000\n```sql\nSELECT id, name, salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LAG(salary) OVER (ORDER BY salary DESC) as prev_salary\nFROM employees;\n```\nAssuming employees with salaries: 50000, 60000, 60000, 70000"
    },
    "options": {
      "en": [
        "LAG will return NULL for the first row, and ROW_NUMBER will assign numbers 1-4",
        "LAG will return NULL for all rows because it cannot look back, and ROW_NUMBER will assign ranks based on salary value",
        "LAG will ignore NULL values and ROW_NUMBER will assign the same rank to rows with identical salaries",
        "Both LAG and ROW_NUMBER will return NULL when encountering duplicate salary values"
      ],
      "ru": [
        "LAG вернёт NULL для первой строки, а ROW_NUMBER присвоит номера с 1 по 4",
        "LAG вернёт NULL для всех строк, потому что не может смотреть назад, а ROW_NUMBER присвоит ранги на основе значения зарплаты",
        "LAG будет игнорировать значения NULL и ROW_NUMBER присвоит одинаковый ранг строкам с одинаковой зарплатой",
        "Как LAG, так и ROW_NUMBER вернут NULL при встрече с дублирующимися значениями зарплаты"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "ROW_NUMBER numbers the rows one by one — 1, 2, 3, 4 — even when two of them hold the same salary. LAG (a window function that reads the previous row) has no previous row for the very first one, so it returns NULL. After that it gives 70000, 60000, 60000 for rows 2-4. Equal salaries disturb neither function: both handle repeated values correctly.",
      "ru": "ROW_NUMBER нумерует строки подряд — 1, 2, 3, 4 — даже если у двух из них одинаковая зарплата. LAG (оконная функция, которая читает предыдущую строку) для самой первой строки вернёт NULL. Дальше пойдут 70000, 60000, 60000 для строк 2-4. Одинаковые зарплаты не мешают ни той, ни другой функции: обе корректно обрабатывают повторы."
    }
  },
  {
    "id": "postgresql-quiz-10",
    "topicId": "postgresql",
    "question": {
      "en": "You're debugging a transaction that frequently encounters serialization conflicts. What does switching from READ COMMITTED to SERIALIZABLE isolation level guarantee?",
      "ru": "Вы отлаживаете транзакцию, которая часто встречает конфликты сериализации. Что гарантирует переключение с READ COMMITTED на уровень изоляции SERIALIZABLE?"
    },
    "options": {
      "en": [
        "All writes will be completed faster because no locks are acquired",
        "The transaction will see a consistent snapshot, and conflicts cause a rollback",
        "Dirty reads will be prevented, but phantom reads can still occur within the transaction",
        "The entire database will be locked exclusively for the duration of the transaction"
      ],
      "ru": [
        "Все записи будут выполнены быстрее, потому что блокировки не захватываются",
        "Транзакция увидит согласованный снимок, а конфликты приведут к откату",
        "Грязное чтение будет предотвращено, но фантомное чтение может всё ещё произойти в пределах транзакции",
        "Вся база данных будет исключительно заблокирована на время выполнения транзакции"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "SERIALIZABLE in PostgreSQL is built on SSI — Serializable Snapshot Isolation. The transaction gets one consistent snapshot, and PostgreSQL watches for conflicts between concurrent transactions. When their combined result is not serializable, one of them is rolled back. That prevents phantom reads and serialization anomalies. It does not lock the whole database, speed writes up, or ignore anomalies.",
      "ru": "SERIALIZABLE в PostgreSQL построен на SSI — Serializable Snapshot Isolation. Транзакция получает один согласованный снимок, а PostgreSQL следит за конфликтами между одновременными транзакциями. Если их совместный результат не сериализуем, одну из них откатывают. Это и защищает от фантомных чтений и аномалий сериализации. Базу данных он не блокирует, записи не ускоряет, аномалии не игнорирует."
    }
  },
  {
    "id": "postgresql-quiz-11",
    "topicId": "postgresql",
    "question": {
      "en": "You run EXPLAIN ANALYZE on a query and see 'Seq Scan' with high actual rows but low estimated rows. What is the most likely cause?\n```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE status = 'pending';\n```",
      "ru": "Вы запускаете EXPLAIN ANALYZE на запросе и видите 'Seq Scan' с высоким количеством реальных строк, но низким количеством оцененных строк. Какова наиболее вероятная причина?\n```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE status = 'pending';\n```"
    },
    "options": {
      "en": [
        "PostgreSQL always underestimates sequential scans by design",
        "The table has too many columns and EXPLAIN cannot estimate correctly",
        "The statistics on the status column no longer match the real data",
        "The query planner is broken and needs PostgreSQL to be restarted"
      ],
      "ru": [
        "PostgreSQL всегда недооценивает последовательные сканирования по замыслу",
        "Таблица имеет слишком много столбцов и EXPLAIN не может правильно оценить",
        "Статистика по столбцу status больше не совпадает с реальными данными",
        "Планировщик запросов сломан и требует перезагрузки PostgreSQL"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Stale statistics are the primary reason for row estimate mismatches. Running ANALYZE updates table statistics and improves planner decisions. The other options are misconceptions: PostgreSQL's estimates are generally reliable, the number of columns does not prevent accurate estimation, and the planner is highly optimized.",
      "ru": "Устаревшая статистика — главная причина расхождения между оценкой и фактическим числом строк. Запуск ANALYZE обновляет статистику таблицы и улучшает решения планировщика. Остальные варианты — заблуждения: оценки PostgreSQL обычно надёжны, количество столбцов точной оценке не мешает, а планировщик хорошо оптимизирован."
    }
  },
  {
    "id": "postgresql-quiz-12",
    "topicId": "postgresql",
    "question": {
      "en": "In MVCC, when Transaction A updates a row and Transaction B reads it before A commits, what does B see?\n```sql\n-- Transaction A\nBEGIN;\nUPDATE users SET name = 'Alice' WHERE id = 1;\n-- Transaction B (concurrent, started before A's UPDATE)\nSELECT name FROM users WHERE id = 1;\n```",
      "ru": "В MVCC, когда Транзакция A обновляет строку и Транзакция B читает её до фиксации A, что видит B?\n```sql\n-- Transaction A\nBEGIN;\nUPDATE users SET name = 'Alice' WHERE id = 1;\n-- Transaction B (concurrent, started before A's UPDATE)\nSELECT name FROM users WHERE id = 1;\n```"
    },
    "options": {
      "en": [
        "The new value, because updates are immediately visible to all transactions",
        "NULL, because the row is locked by A's update",
        "A mix of old and new values depending on which one is cached",
        "The old value, because B reads its own snapshot"
      ],
      "ru": [
        "Новое значение, потому что обновления немедленно видны всем транзакциям",
        "NULL, потому что строка заблокирована обновлением A",
        "Смесь старого и нового значений в зависимости от того, какое кэшировано",
        "Старое значение, потому что B читает свой снимок"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "MVCC (Multi-Version Concurrency Control) gives B a consistent snapshot as of the moment B started, so B still sees the old value. The changes made by A stay invisible until A commits. That is what prevents dirty reads, and it is PostgreSQL's default behaviour. Rows are not locked for reading during an update, and caching does not affect snapshot consistency.",
      "ru": "MVCC (Multi-Version Concurrency Control, многоверсионность) даёт B согласованный снимок на момент старта B, поэтому B всё ещё видит старое значение. Изменения A остаются невидимыми до фиксации A. Именно это защищает от грязного чтения и является поведением PostgreSQL по умолчанию. Строки при обновлении не блокируются для чтения, а кэширование на согласованность снимка не влияет."
    }
  },
  {
    "id": "postgresql-quiz-13",
    "topicId": "postgresql",
    "question": {
      "en": "Which index type is best for queries using range operators like >= and BETWEEN on numeric columns?",
      "ru": "Какой тип индекса лучше всего подходит для запросов, использующих операторы диапазона, такие как >= и BETWEEN, для числовых столбцов?"
    },
    "options": {
      "en": [
        "B-tree index, because it stores keys in sorted order",
        "Hash index, because it groups similar values together",
        "GiST index, because it supports all comparison operators",
        "BRIN index, because it compresses numeric data"
      ],
      "ru": [
        "Индекс B-tree, потому что он хранит ключи в отсортированном порядке",
        "Хеш-индекс, потому что он группирует похожие значения вместе",
        "Индекс GiST, потому что он поддерживает все операторы сравнения",
        "Индекс BRIN, потому что он сжимает числовые данные"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "B-tree is the default index type and the most efficient one for ranges. It keeps keys sorted, so a range scan can skip the values that cannot match. Hash indexes support equality only. GiST (generalized search tree) is for complex types. BRIN (block range index) is for very large tables with sequential data. Even there it is not always faster than B-tree.",
      "ru": "B-tree — индекс по умолчанию и самый эффективный для диапазонов. Он держит ключи отсортированными, поэтому сканирование диапазона пропускает значения, которые точно не подойдут. Хеш-индексы поддерживают только равенство. GiST (обобщённое дерево поиска) — для сложных типов. BRIN (индекс диапазона блоков) — для очень больших таблиц с последовательными данными. Даже там он не всегда быстрее B-tree."
    }
  },
  {
    "id": "postgresql-quiz-14",
    "topicId": "postgresql",
    "question": {
      "en": "What does VACUUM do and why is it important for long-running PostgreSQL instances?",
      "ru": "Что делает VACUUM и почему это важно для долгоживущих экземпляров PostgreSQL?"
    },
    "options": {
      "en": [
        "It deletes all rows marked as NULL and compresses the database file",
        "It removes dead tuples left by MVCC and reclaims their disk space",
        "It rebuilds all indexes to make them faster, similar to REINDEX",
        "It flushes the buffer cache to free up RAM and improve memory efficiency"
      ],
      "ru": [
        "Он удаляет все строки, отмеченные как NULL, и сжимает файл базы данных",
        "Он удаляет мёртвые кортежи, оставленные MVCC, и освобождает занятое ими место",
        "Он перестраивает все индексы, чтобы сделать их быстрее, аналогично REINDEX",
        "Он очищает кэш буфера, чтобы освободить ОЗУ и улучшить эффективность памяти"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "VACUUM reclaims the space of dead tuples, and it is critical against transaction id wraparound, a catastrophic event. In modern PostgreSQL the autovacuum daemon runs it for you. The other options confuse VACUUM with other operations. Rows holding NULL are not deleted in any special way. Rebuilding indexes is the job of REINDEX. And the buffer cache is not managed by VACUUM.",
      "ru": "VACUUM освобождает место, занятое мёртвыми кортежами, и критически важен против переполнения счётчика транзакций — катастрофического события. В современном PostgreSQL за вас его запускает демон autovacuum. Остальные варианты путают VACUUM с другими операциями. Строки со значением NULL никак особенно не удаляются. Перестройка индексов — работа REINDEX. А буферным кэшем VACUUM не управляет."
    }
  },
  {
    "id": "postgresql-quiz-15",
    "topicId": "postgresql",
    "question": {
      "en": "What is the result of the following query?\n```sql\nSELECT \n  ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary DESC) AS rank,\n  name,\n  salary\nFROM employees\nWHERE salary IS NOT NULL;\n```\nIf the employees table has 100 rows with NULL salaries, how many rows does this query return?",
      "ru": "Каков результат следующего запроса?\n```sql\nSELECT \n  ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary DESC) AS rank,\n  name,\n  salary\nFROM employees\nWHERE salary IS NOT NULL;\n```\nЕсли таблица employees содержит 100 строк с NULL зарплатами, сколько строк вернет этот запрос?"
    },
    "options": {
      "en": [
        "100 rows, because window functions ignore the WHERE clause",
        "All rows including NULLs, but they're sorted to the bottom",
        "Fewer than 100 rows, because WHERE filters rows before ranking",
        "Zero rows, because ROW_NUMBER() cannot be used with IS NOT NULL"
      ],
      "ru": [
        "100 строк, потому что оконные функции игнорируют предложение WHERE",
        "Все строки включая NULLs, но они отсортированы внизу",
        "Менее 100 строк, потому что WHERE фильтрует строки до нумерации",
        "Нулевые строки, потому что ROW_NUMBER() не может использоваться с IS NOT NULL"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "The WHERE clause is applied before the window functions run, so rows holding NULL are filtered out first. The window functions then work only on what is left. Aggregates behave differently: HAVING can filter them after grouping. Window functions are not immune to filtering, and ROW_NUMBER() works perfectly well together with a WHERE clause.",
      "ru": "Условие WHERE применяется раньше, чем выполняются оконные функции, поэтому строки со значением NULL отфильтровываются первыми. Дальше оконные функции работают только с тем, что осталось. С агрегатами иначе: их результат HAVING может отфильтровать уже после группировки. Оконные функции от фильтрации не защищены, а ROW_NUMBER() прекрасно работает вместе с условием WHERE."
    }
  },
  {
    "id": "postgresql-quiz-16",
    "topicId": "postgresql",
    "question": {
      "en": "Consider the following query execution:\n```sql\nSELECT * FROM orders WHERE user_id = 5 AND created_at > '2024-01-01';\n```\nYou run EXPLAIN and see that PostgreSQL performs a Sequential Scan despite having indexes on both user_id and created_at. What is the most likely reason?",
      "ru": "Рассмотрите следующий запрос:\n```sql\nSELECT * FROM orders WHERE user_id = 5 AND created_at > '2024-01-01';\n```\nВы запускаете EXPLAIN и видите, что PostgreSQL выполняет Sequential Scan, несмотря на индексы на user_id и created_at. Какова наиболее вероятная причина?"
    },
    "options": {
      "en": [
        "PostgreSQL does not support composite indexes on multiple columns",
        "The indexes are B-tree indexes and cannot be used with OR conditions",
        "The statistics are outdated and need to be refreshed with ANALYZE",
        "The table is small enough that a sequential scan is cheaper"
      ],
      "ru": [
        "PostgreSQL не поддерживает составные индексы на несколько столбцов",
        "Индексы являются B-tree индексами и не могут использоваться с условиями OR",
        "Статистика устарела и её нужно обновить с помощью ANALYZE",
        "Таблица достаточно маленькая, и последовательное сканирование дешевле"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "The planner compares costs before it picks a plan. On a small table, reading every row is often cheaper than walking two indexes and paying for the random reads that follow. The other options are misconceptions. A condition on two columns does not prevent index use, statistics are usually current under a normal workload, and PostgreSQL fully supports composite indexes.",
      "ru": "Планировщик сравнивает стоимости, прежде чем выбрать план. На маленькой таблице прочитать все строки часто дешевле, чем обойти два индекса и заплатить за следующие за ними случайные чтения. Остальные варианты — заблуждения. Условие по двум столбцам использованию индекса не мешает, статистика при обычной нагрузке обычно актуальна, а составные индексы PostgreSQL поддерживает полностью."
    }
  },
  {
    "id": "postgresql-quiz-17",
    "topicId": "postgresql",
    "question": {
      "en": "What is the primary difference between REPEATABLE READ and SERIALIZABLE isolation levels in PostgreSQL?",
      "ru": "В чём главное различие между уровнями изоляции REPEATABLE READ и SERIALIZABLE в PostgreSQL?"
    },
    "options": {
      "en": [
        "SERIALIZABLE aborts a transaction when it detects a serialization anomaly",
        "REPEATABLE READ uses row-level locking while SERIALIZABLE uses table-level locking",
        "SERIALIZABLE prevents all reads of uncommitted data while REPEATABLE READ allows dirty reads",
        "REPEATABLE READ requires explicit locks while SERIALIZABLE uses implicit locking automatically"
      ],
      "ru": [
        "SERIALIZABLE прерывает транзакцию, когда обнаруживает аномалию сериализации",
        "REPEATABLE READ использует блокировку на уровне строк, а SERIALIZABLE использует блокировку на уровне таблицы",
        "SERIALIZABLE предотвращает все чтения незафиксированных данных, а REPEATABLE READ допускает грязные чтения",
        "REPEATABLE READ требует явных блокировок, а SERIALIZABLE использует неявные блокировки автоматически"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "SERIALIZABLE detects conflicts via SSI — Serializable Snapshot Isolation. It tracks how transactions depend on each other and aborts one with a serialization failure. REPEATABLE READ holds one snapshot per transaction, but allows write skew: both see a valid snapshot and together break an invariant. Both use MVCC (Multi-Version Concurrency Control) snapshots, not locks; dirty reads cannot happen at READ COMMITTED.",
      "ru": "SERIALIZABLE ловит конфликты через SSI — Serializable Snapshot Isolation. Он следит за зависимостями между транзакциями и прерывает одну ошибкой сериализации. REPEATABLE READ держит один снимок на транзакцию, но допускает write skew: обе видят корректный снимок и вместе нарушают инвариант. Оба уровня опираются на снимки MVCC (многоверсионность), не на блокировки; грязное чтение невозможно уже на READ COMMITTED."
    }
  },
  {
    "id": "postgresql-quiz-18",
    "topicId": "postgresql",
    "question": {
      "en": "What will this query return?\n```sql\nSELECT id, name, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) as rank\nFROM employees;\n```\nIf there are 3 employees in the Sales department with salaries 50000, 50000, and 45000, what rank values will the two highest-paid employees receive?",
      "ru": "Что вернёт этот запрос?\n```sql\nSELECT id, name, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) as rank\nFROM employees;\n```\nЕсли в отделе Sales есть 3 сотрудника с зарплатой 50000, 50000 и 45000, какие значения ранга получат два самых высокооплачиваемых сотрудника?"
    },
    "options": {
      "en": [
        "Both will receive rank 1 because they have equal salaries",
        "They will receive rank 1 and rank 2 in an arbitrary order",
        "They will receive rank 1 and rank 1.5 (fractional rank for ties)",
        "One will receive rank 1 and the other rank 2, with the difference based on their ID"
      ],
      "ru": [
        "Оба получат ранг 1, так как у них одинаковая зарплата",
        "Они получат ранг 1 и ранг 2 в произвольном порядке",
        "Они получат ранг 1 и 1,5 (дробный ранг для одинаковых значений)",
        "Один получит ранг 1, а другой ранг 2, с разницей, основанной на их ID"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "ROW_NUMBER gives every row a unique sequential number, even when values are tied. Equal values do not share a number: each row gets its own. So two employees on the same salary get consecutive numbers, 1 and 2. Which of them comes first is decided by the internal row sequence. There is no tie rule here, unlike in RANK (that function does give tied rows one shared number).",
      "ru": "ROW_NUMBER присваивает каждой строке уникальный последовательный номер, даже при одинаковых значениях. Одинаковые значения номер не делят: у каждой строки свой. Поэтому два сотрудника с равной зарплатой получат подряд идущие номера, 1 и 2. Кто из них первый, решает внутренний порядок строк. Правила для повторов здесь нет, в отличие от RANK (эта функция как раз даёт повторам общий номер)."
    }
  },
  {
    "id": "postgresql-quiz-19",
    "topicId": "postgresql",
    "question": {
      "en": "What is the primary purpose of VACUUM in PostgreSQL?",
      "ru": "Какова основная цель VACUUM в PostgreSQL?"
    },
    "options": {
      "en": [
        "To rebuild all indexes to improve query performance",
        "To compress table data and reduce memory usage",
        "To reclaim disk space taken by deleted and updated rows",
        "To remove all NULL values from columns to optimize storage"
      ],
      "ru": [
        "Перестроить все индексы для повышения производительности запросов",
        "Сжать данные таблицы и снизить использование памяти",
        "Освободить дисковое пространство, занятое удалёнными и обновлёнными строками",
        "Удалить все значения NULL из столбцов для оптимизации хранилища"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "VACUUM removes dead tuples: old row versions left by DELETE and UPDATE, because MVCC (Multi-Version Concurrency Control) never overwrites a row in place. It also updates the visibility maps. That matters against transaction id wraparound, a catastrophe if the 32-bit counter hits its maximum. VACUUM does not rebuild indexes (REINDEX does), compress data, or remove NULLs. VACUUM FULL is harsher, but it locks the table.",
      "ru": "VACUUM удаляет мёртвые кортежи — старые версии строк после DELETE и UPDATE, ведь MVCC (многоверсионность) не перезаписывает строку на месте. Ещё он обновляет карты видимости. Это защита от переполнения счётчика транзакций: 32-битный счётчик дошёл до максимума — катастрофа. VACUUM не перестраивает индексы (это REINDEX), не сжимает данные и не удаляет NULL. VACUUM FULL жёстче, но блокирует таблицу."
    }
  },
  {
    "id": "postgresql-quiz-20",
    "topicId": "postgresql",
    "question": {
      "en": "What does this LEFT JOIN query return for rows where the right table has no match?\n```sql\nSELECT a.id, b.value FROM table_a a LEFT JOIN table_b b ON a.id = b.a_id;\n```\nAssuming table_a has an id=5 with no corresponding rows in table_b, what will the 'value' column contain?",
      "ru": "Что возвращает этот LEFT JOIN запрос для строк, где в правой таблице нет совпадения?\n```sql\nSELECT a.id, b.value FROM table_a a LEFT JOIN table_b b ON a.id = b.a_id;\n```\nПредположим, что table_a имеет id=5 без соответствующих строк в table_b, что будет содержать столбец 'value'?"
    },
    "options": {
      "en": [
        "An empty string or 0, depending on the data type of the column",
        "The row will be excluded from the result set entirely",
        "The default value defined in the table_b schema",
        "NULL, because there is no matching row in the right table"
      ],
      "ru": [
        "Пустую строку или 0 в зависимости от типа данных столбца",
        "Строка будет полностью исключена из результата запроса",
        "Значение по умолчанию, определённое в схеме table_b",
        "NULL, потому что в правой таблице нет совпадающей строки"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "LEFT JOIN keeps every row of the first table and fills the unmatched columns of the second table with NULL. That is its defining behaviour: unmatched columns become NULL, not 0, not an empty string, and not the schema default. The row itself is always in the result; only the columns from the second table are filled with NULL.",
      "ru": "LEFT JOIN сохраняет все строки первой таблицы, а несовпавшие столбцы второй таблицы заполняет значением NULL. Это его определяющее поведение: несовпавшие столбцы становятся NULL, а не нулём, не пустой строкой и не значением по умолчанию из схемы. Сама строка в результате остаётся всегда, заполняются значением NULL только столбцы второй таблицы."
    }
  },
  {
    "id": "postgresql-quiz-21",
    "topicId": "postgresql",
    "question": {
      "en": "PostgreSQL is doing a full table scan on a table with millions of rows. Which index type would be **least** useful for filtering on a range of numeric values?\n\nConsider: B-tree, Hash, GiST, BRIN",
      "ru": "PostgreSQL выполняет полное сканирование таблицы с миллионами строк. Какой тип индекса будет **наименее** полезен для фильтрации по диапазону числовых значений?\n\nРассмотрите: B-tree, Hash, GiST, BRIN"
    },
    "options": {
      "en": [
        "Hash index — it supports only equality comparisons",
        "B-tree index — it is too slow for large datasets",
        "GiST index — it cannot be used with numeric data types",
        "BRIN index — it requires sequential access patterns"
      ],
      "ru": [
        "Hash индекс — он поддерживает только сравнение на равенство",
        "B-tree индекс — он слишком медленный для больших наборов данных",
        "GiST индекс — он не может использоваться с числовыми типами данных",
        "BRIN индекс — он требует последовательного доступа к данным"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "Hash indexes support the equality operator (=) only, which makes them unsuitable for range queries such as < or BETWEEN. B-tree is in fact the standard choice for ranges over numeric columns. GiST (generalized search tree) and BRIN (block range index) can both work with numeric data, each with its own trade-offs.",
      "ru": "Хеш-индексы поддерживают только оператор равенства (=), и поэтому не годятся для диапазонных запросов вроде < или BETWEEN. B-tree, наоборот, стандартный выбор для диапазонов по числовым столбцам. GiST (generalized search tree, обобщённое дерево поиска) и BRIN (block range index, индекс диапазона блоков) с числовыми данными работать умеют оба, каждый со своими компромиссами."
    }
  },
  {
    "id": "postgresql-quiz-22",
    "topicId": "postgresql",
    "question": {
      "en": "What does the MVCC (Multi-Version Concurrency Control) mechanism in PostgreSQL primarily allow?",
      "ru": "Что в первую очередь позволяет делать механизм MVCC (многоверсионный контроль параллелизма) в PostgreSQL?"
    },
    "options": {
      "en": [
        "Multiple transactions to hold locks on the same row simultaneously",
        "Each transaction to see a consistent snapshot of the database at the moment it started",
        "Automatic rollback of conflicting transactions without user intervention",
        "Elimination of the need for transaction isolation levels"
      ],
      "ru": [
        "Нескольким транзакциям одновременно удерживать блокировки на одной строке",
        "Каждой транзакции видеть согласованный снимок базы данных на момент её начала",
        "Автоматический откат конфликтующих транзакций без вмешательства пользователя",
        "Исключение необходимости в уровнях изоляции транзакций"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "MVCC (Multi-Version Concurrency Control) lets every transaction see its own consistent view of the database, because the database keeps several versions of each row. That is what enables non-blocking reads and better concurrency. Lock-free reading is the key benefit here. MVCC does not prevent locks, and it does not remove the need for isolation levels, which still exist and still matter.",
      "ru": "MVCC (Multi-Version Concurrency Control, многоверсионность) даёт каждой транзакции свой согласованный вид базы данных, потому что база хранит несколько версий каждой строки. Именно это обеспечивает неблокирующее чтение и лучший параллелизм. Ключевое преимущество здесь — чтение без блокировок. MVCC не предотвращает блокировки и не отменяет уровни изоляции: они по-прежнему существуют и важны."
    }
  },
  {
    "id": "postgresql-quiz-23",
    "topicId": "postgresql",
    "question": {
      "en": "What will be the result of the following query?\n\n```sql\nSELECT \n  name,\n  salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LAG(salary) OVER (ORDER BY salary DESC) as prev_salary\nFROM employees\nWHERE salary > 50000\nORDER BY salary DESC;\n\n```\nWhich statement is correct about window functions here?",
      "ru": "Какой будет результат следующего запроса?\n\n```sql\nSELECT \n  name,\n  salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LAG(salary) OVER (ORDER BY salary DESC) as prev_salary\nFROM employees\nWHERE salary > 50000\nORDER BY salary DESC;\n\n```\nКакое утверждение об оконных функциях здесь верно?"
    },
    "options": {
      "en": [
        "The ORDER BY in the OVER clause must match the final ORDER BY exactly",
        "LAG will return the next salary value because it looks forward in the window frame",
        "The WHERE clause will be applied before window functions are calculated",
        "Window functions will process all rows from the table before the WHERE filter is applied"
      ],
      "ru": [
        "ORDER BY в предложении OVER должна точно совпадать с финальным ORDER BY",
        "LAG вернёт следующее значение зарплаты, потому что он смотрит вперёд в окне",
        "Предложение WHERE будет применено до расчёта оконных функций",
        "Оконные функции обработают все строки из таблицы до применения фильтра WHERE"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Window functions run after WHERE has filtered, never before. The ORDER BY inside OVER defines the sequence of rows within the window, independently of the final ORDER BY of the query. LAG (a window function that reads the previous row) looks backwards, not forwards. WHERE always filters the rows before the window functions process the result.",
      "ru": "Оконные функции выполняются после фильтрации по WHERE, а не до неё. Конструкция ORDER BY внутри OVER задаёт последовательность строк внутри окна, независимо от финального ORDER BY запроса. LAG (оконная функция, которая читает предыдущую строку) смотрит назад, а не вперёд. WHERE всегда отфильтровывает строки прежде, чем оконные функции обработают результат."
    }
  },
  {
    "id": "postgresql-quiz-24",
    "topicId": "postgresql",
    "question": {
      "en": "Consider the EXPLAIN output below. What does a high value in the 'Actual Loops' field most likely indicate?\n```txt\nSeq Scan on users (cost=0.00..35.50 rows=1000 width=100)\n  Filter: (status = 'active')\n  Actual Loops: 50\n  Actual Rows: 20000\n```",
      "ru": "Рассмотрите вывод EXPLAIN ниже. На что скорее всего указывает высокое значение в поле 'Actual Loops'?\n```txt\nSeq Scan on users (cost=0.00..35.50 rows=1000 width=100)\n  Filter: (status = 'active')\n  Actual Loops: 50\n  Actual Rows: 20000\n```"
    },
    "options": {
      "en": [
        "The query returned 50 times more rows than the planner estimated",
        "The query optimizer made 50 execution attempts before succeeding",
        "The table has 50 physical partitions that were scanned separately",
        "The scan node was executed 50 times inside a nested loop join"
      ],
      "ru": [
        "Запрос вернул в 50 раз больше строк, чем предсказал планировщик",
        "Оптимизатор запросов сделал 50 попыток выполнения перед успехом",
        "Таблица имеет 50 физических разделов, которые были отсканированы отдельно",
        "Узел сканирования был выполнен 50 раз внутри вложенного соединения"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "Actual Loops shows how many times this node was executed. A high loop count typically indicates the scan is part of a nested loop join where the outer relation drives the execution multiple times. The Actual Rows field (20000) is the total across all loops, not per loop. This is a performance red flag for nested loop joins on large outer relations.",
      "ru": "Actual Loops показывает, сколько раз был выполнен этот узел. Высокое количество циклов обычно указывает, что сканирование является частью вложенного соединения, где внешнее отношение управляет выполнением многократно. Поле Actual Rows (20000) — это общее значение для всех циклов, а не на цикл. Это красный флаг производительности для вложенных соединений на больших внешних отношениях."
    }
  },
  {
    "id": "postgresql-quiz-25",
    "topicId": "postgresql",
    "question": {
      "en": "What is the difference between READ COMMITTED and REPEATABLE READ isolation levels in PostgreSQL regarding NULL values?",
      "ru": "В чём разница между уровнями изоляции READ COMMITTED и REPEATABLE READ в PostgreSQL в отношении значений NULL?"
    },
    "options": {
      "en": [
        "Both levels handle NULL values identically in comparisons (NULL is neither equal nor not equal)",
        "REPEATABLE READ prevents NULL from being inserted by concurrent transactions",
        "NULL comparisons behave differently based on isolation level",
        "READ COMMITTED converts NULL to an empty string for consistency"
      ],
      "ru": [
        "Оба уровня обрабатывают значения NULL одинаково в сравнениях (NULL ни равно, ни не равно)",
        "REPEATABLE READ предотвращает вставку NULL параллельными транзакциями",
        "Сравнения NULL ведут себя по-разному в зависимости от уровня изоляции",
        "READ COMMITTED преобразует NULL в пустую строку для согласованности"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "NULL semantics do not depend on the isolation level. Under READ COMMITTED, under REPEATABLE READ and under every other level, NULL stays neither equal nor unequal to anything, including itself. The condition x = NULL never holds true, so the test to use becomes x IS NULL. Isolation levels govern row visibility and phantom reads, not the behaviour of NULL.",
      "ru": "Семантика NULL от уровня изоляции не зависит. И на READ COMMITTED, и на REPEATABLE READ, и на любом другом уровне NULL остаётся ни равным, ни не равным чему угодно, включая себя. Условие x = NULL не выполняется никогда, поэтому пишут x IS NULL. Уровни изоляции управляют видимостью строк и фантомными чтениями, а не поведением NULL."
    }
  },
  {
    "id": "postgresql-quiz-26",
    "topicId": "postgresql",
    "question": {
      "en": "What is the primary advantage of MVCC (Multi-Version Concurrency Control) in PostgreSQL?",
      "ru": "Каково основное преимущество MVCC (многоверсионного управления параллелизмом) в PostgreSQL?"
    },
    "options": {
      "en": [
        "It eliminates the need for indexes on frequently queried columns",
        "It lets readers and writers work without blocking each other",
        "It automatically compresses table data to reduce storage requirements",
        "It prevents NULL values from being inserted into any column"
      ],
      "ru": [
        "Оно исключает необходимость в индексах на часто запрашиваемых столбцах",
        "Оно позволяет читателям и писателям работать, не блокируя друг друга",
        "Оно автоматически сжимает данные таблицы, чтобы снизить требования к хранению",
        "Оно предотвращает вставку значений NULL в любой столбец"
      ]
    },
    "correctIndex": 1,
    "explanation": {
      "en": "MVCC (Multi-Version Concurrency Control) keeps several versions of each row, so reads and writes run at the same time without blocking each other. This is PostgreSQL's core concurrency mechanism. The other three options are wrong: MVCC does not replace indexes, does not compress table data, and has nothing to do with NULL values.",
      "ru": "MVCC (Multi-Version Concurrency Control, многоверсионность) хранит несколько версий каждой строки, поэтому чтение и запись идут одновременно и не блокируют друг друга. Это основной механизм параллелизма в PostgreSQL. Остальные три варианта неверны: MVCC не заменяет индексы, не сжимает данные таблицы и никак не связан со значениями NULL."
    }
  },
  {
    "id": "postgresql-quiz-27",
    "topicId": "postgresql",
    "question": {
      "en": "Consider the following query and its EXPLAIN output snippet. What does a high 'Buffers: shared hit' value indicate?\n```sql\nEXPLAIN (ANALYZE, BUFFERS) SELECT * FROM users WHERE id = 5;\n```\nOutput shows: Buffers: shared hit=9999 read=1",
      "ru": "Рассмотрите следующий запрос и фрагмент его вывода EXPLAIN. Что указывает высокое значение 'Buffers: shared hit'?\n```sql\nEXPLAIN (ANALYZE, BUFFERS) SELECT * FROM users WHERE id = 5;\n```\nВывод показывает: Buffers: shared hit=9999 read=1"
    },
    "options": {
      "en": [
        "The query is inefficient and should use an index to reduce buffer reads",
        "The table has 9999 rows that were scanned",
        "Most blocks came from the shared buffer cache, not from disk",
        "PostgreSQL allocated 9999 buffers to execute the query"
      ],
      "ru": [
        "Запрос неэффективен и должен использовать индекс для уменьшения операций чтения буфера",
        "Таблица содержит 9999 строк, которые были отсканированы",
        "Большинство блоков пришло из кэша общих буферов, а не с диска",
        "PostgreSQL выделил 9999 буферов для выполнения запроса"
      ]
    },
    "correctIndex": 2,
    "explanation": {
      "en": "Buffers: shared hit counts block accesses satisfied from the shared buffer cache (memory), while 'read' counts blocks fetched from disk. A high hit ratio indicates good cache performance and efficient memory usage. The options about needing an index, about 9999 scanned rows, and about 9999 allocated buffers misinterpret what the metrics represent.",
      "ru": "Buffers: shared hit подсчитывает доступы к блокам из кэша общих буферов (памяти), а 'read' подсчитывает блоки, загруженные с диска. Высокий коэффициент попаданий указывает на хорошую производительность кэша и эффективное использование памяти. Варианты про необходимость индекса, про 9999 отсканированных строк и про 9999 выделенных буферов неправильно интерпретируют, что представляют эти метрики."
    }
  },
  {
    "id": "postgresql-quiz-29",
    "topicId": "postgresql",
    "question": {
      "en": "What will be the result of executing the following query?\n```sql\nSELECT \n  employee_name,\n  salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LEAD(salary, 1) OVER (ORDER BY salary DESC) as next_salary\nFROM employees\nWHERE salary IS NOT NULL\nLIMIT 3;\n```\nAssuming employees table has salaries: 5000, 5000, 4000, 3000, 2000",
      "ru": "Каков результат выполнения следующего запроса?\n```sql\nSELECT \n  employee_name,\n  salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as rank,\n  LEAD(salary, 1) OVER (ORDER BY salary DESC) as next_salary\nFROM employees\nWHERE salary IS NOT NULL\nLIMIT 3;\n```\nПредполагая, что таблица employees имеет зарплаты: 5000, 5000, 4000, 3000, 2000"
    },
    "options": {
      "en": [
        "Returns 3 rows with all employees earning exactly 5000 because LEAD fetches the same value",
        "Returns 2 rows because the two duplicate salaries (5000) are treated as a single rank",
        "Returns 4 rows because LIMIT is applied before the window function",
        "Returns 3 rows with ranks 1, 2, 3 and next_salary from the following row"
      ],
      "ru": [
        "Возвращает 3 строки со всеми сотрудниками, получающими ровно 5000, потому что LEAD получает то же значение",
        "Возвращает 2 строки, потому что две одинаковые зарплаты (5000) рассматриваются как один ранг",
        "Возвращает 4 строки, потому что LIMIT применяется перед функцией окна",
        "Возвращает 3 строки с рангами 1, 2, 3 и next_salary из следующей строки"
      ]
    },
    "correctIndex": 3,
    "explanation": {
      "en": "ROW_NUMBER gives each row its own number, so the top three salaries get 1, 2 and 3. LEAD (a window function that reads the next row) returns that next salary: 5000, 4000, 3000. LIMIT is applied after the window functions are computed, which is why three rows come back and not four. The option with two rows confuses ROW_NUMBER with RANK (which gives tied rows one shared number).",
      "ru": "ROW_NUMBER даёт каждой строке свой номер, поэтому три наибольшие зарплаты получают 1, 2 и 3. LEAD (оконная функция, которая читает следующую строку) возвращает эту следующую зарплату: 5000, 4000, 3000. LIMIT применяется после того, как оконные функции посчитаны, поэтому строк возвращается три, а не четыре. Вариант с двумя строками путает ROW_NUMBER с RANK (эта функция даёт повторам общий номер)."
    }
  },
  {
    "id": "postgresql-quiz-30",
    "topicId": "postgresql",
    "question": {
      "en": "When should you use a BRIN index instead of a B-tree index in PostgreSQL?",
      "ru": "Когда следует использовать индекс BRIN вместо индекса B-tree в PostgreSQL?"
    },
    "options": {
      "en": [
        "When the table is very large and its data is naturally ordered",
        "When you need to enforce unique constraints across multiple columns",
        "When you need faster lookups for equality and range queries on small tables under 10MB",
        "When you want to replace VACUUM operations and reduce table bloat"
      ],
      "ru": [
        "Когда таблица очень большая и её данные естественным образом упорядочены",
        "Когда вам нужно обеспечить уникальные ограничения на нескольких столбцах",
        "Когда вам нужны более быстрые поиски для запросов равенства и диапазона в малых таблицах размером менее 10 МБ",
        "Когда вы хотите заменить операции VACUUM и снизить раздутие таблицы"
      ]
    },
    "correctIndex": 0,
    "explanation": {
      "en": "BRIN (Block Range INdex) is designed for very large tables whose data is already ordered. It keeps only minimal metadata per range of blocks. So it takes far less space than a B-tree and still performs well. The other options miss the point. Unique constraints are the job of a unique index. BRIN is for large tables, not small ones. And VACUUM is still needed.",
      "ru": "BRIN (индекс диапазона блоков) рассчитан на очень большие таблицы, данные в которых уже упорядочены. Он хранит лишь минимум метаданных на каждый диапазон блоков. Поэтому места он занимает намного меньше, чем B-tree, и всё равно работает хорошо. Остальные варианты мимо. Уникальные ограничения — дело уникального индекса. BRIN нужен большим таблицам, а не маленьким. А VACUUM всё равно необходим."
    }
  }
]
