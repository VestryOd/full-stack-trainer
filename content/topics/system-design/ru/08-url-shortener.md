<!-- verified: 2026-06-05, corrections: 0 -->
# Сервис коротких ссылок

## Requirements Clarification

Начинайте с фиксации требований, а не с рисования схемы. Это один из самых "стандартных" вопросов, и именно поэтому интервьюер ждёт структурированный подход, а не "сразу архитектуру". Сам пошаговый фреймворк разобран в статье про основы System Design этого топика.

**Functional Requirements:**

```txt
- по длинному URL создать короткий код (POST /shorten)
- по короткому коду редиректить на оригинальный URL (GET /:code)
- (уточнить) пользовательские алиасы ("app.com/my-link")?
- (уточнить) срок жизни ссылки (expiration)?
- (уточнить) аналитика по переходам — нужна ли, в каком виде?
```

**Non-Functional Requirements:**

```txt
- Read:Write ratio — обычно 100:1 или выше (одну ссылку создают раз,
  переходят по ней многократно) → read-heavy, нужен кеш
- Latency редиректа — должна быть минимальной (это в "горячем" пути UX)
- Уникальность кодов — обязательна, коллизии недопустимы
- Доступность — высокая (редирект не должен падать,
  даже если аналитика временно недоступна)
```

### Быстрая оценка масштаба

```txt
100M новых ссылок/месяц → ~40 writes/sec (средний)
Read:Write = 100:1     → ~4000 reads/sec (средний), пики выше

Storage: 100M ссылок/месяц * 500 байт (URL + метаданные) * 5 лет
  ≈ 100M * 12 * 5 * 500B ≈ 3 TB за 5 лет — не проблема для одной БД
  (объём НЕ требует шардинга сам по себе — требует только индекс по shortCode)
```

Вывод: три терабайта (TB) за пять лет — это немного, поэтому шардинг по объёму данных этой системе **не** нужен. Ключевая сложность в другом: генерация уникальных кодов на масштабе и кеширование read-пути.

## Генерация коротких кодов — главный вопрос темы

### Вариант 1: Hash (MD5/SHA256) + обрезка

MD5 — это хеш-функция: она превращает любой вход в строку фиксированной длины. SHA256 — более стойкая хеш-функция, которая делает то же самое. Дальше от результата берут только первые несколько символов.

```ts
import crypto from 'node:crypto';

function generateCode(longUrl: string): string {
  const hash = crypto.createHash('md5').update(longUrl).digest('hex');
  return hash.slice(0, 7); // первые 7 символов hex
}
```

- Плюсы: детерминированно (тот же URL → тот же код — можно использовать как дедупликацию).
- Минусы: **коллизии неизбежны** при обрезке хеша. Нужна проверка в БД (база данных) и стратегия "если занято — взять следующие N символов или добавить соль". Это превращает простую операцию в потенциально многошаговую, с повторными попытками.

### Вариант 2: Base62-кодирование auto-increment ID

```ts
const BASE62 = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';

function toBase62(num: number): string {
  if (num === 0) return BASE62[0];
  let result = '';
  while (num > 0) {
    result = BASE62[num % 62] + result;
    num = Math.floor(num / 62);
  }
  return result;
}
// toBase62(125) → "cb"
// 7 символов Base62 покрывают 62^7 ≈ 3.5 * 10^12 комбинаций
```

- Плюсы: гарантированная уникальность (ID уникален по определению — `PRIMARY KEY AUTO_INCREMENT`), код становится короче по мере роста, без коллизий.
- Минусы — и это **главный senior-нюанс**: auto-increment ID из одной БД — это **единая точка конкуренции** (single point of contention) на запись. На масштабе инстансов API много. И ни один из них не может безопасно "сам" сгенерировать следующий ID без похода в БД на каждый запрос.

### Решение для распределённой генерации: Pre-allocated ID ranges (Ticket Server)

```txt
Отдельный сервис (или таблица в БД с атомарным UPDATE) выдаёт
КАЖДОМУ инстансу API целый ДИАПАЗОН ID за раз:

  Instance A: запрашивает диапазон → получает [1..1000]
  Instance B: запрашивает диапазон → получает [1001..2000]

Каждый инстанс генерирует короткие коды из своего диапазона
ЛОКАЛЬНО, без обращения к БД на каждый запрос —
обращение к "тикет-серверу" происходит раз на 1000 запросов.
```

```sql
-- атомарное выделение следующего диапазона
UPDATE id_ranges SET current_max = current_max + 1000
WHERE id = 1
RETURNING current_max - 1000 AS range_start, current_max AS range_end;
```

Это классическое решение: так сделаны Ticket Server во Flickr и генерация ID в Instagram. Оно превращает "БД — узкое место на каждую запись" в "БД — узкое место раз на 1000 записей". Нагрузка падает на порядки.

### Вариант 3: Random + проверка коллизий

```ts
import { randomInt } from 'node:crypto';
import { db } from './db'; // уже созданный PrismaClient

const BASE62 = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';

function randomBase62String(length: number): string {
  let out = '';
  // randomInt, а не Math.random: код не должен быть предсказуемым
  for (let i = 0; i < length; i++) out += BASE62[randomInt(62)];
  return out;
}

async function generateUniqueCode(): Promise<string> {
  for (let attempt = 0; attempt < 5; attempt++) {
    const code = randomBase62String(7);
    const exists = await db.shortUrl.findUnique({ where: { code } });
    if (!exists) return code;
  }
  throw new Error('Failed to generate unique code after 5 attempts');
}
```

- При длине 7 символов Base62 (~3.5 * 10^12 комбинаций) вероятность коллизии при миллионах записей крайне мала — см. "birthday paradox". Но "крайне мала" — это всё же **не ноль**, поэтому проверка и повторная попытка нужны всегда. Важно явно проговорить: это вероятностный подход, а не гарантированный, в отличие от Base62 поверх auto-increment.

## Read Path: redirect — самый горячий путь системы

```txt
GET /abc123
  ↓
Redis: GET shortcode:abc123 → cache hit → 302 Redirect (быстро)
  ↓ (miss)
PostgreSQL: SELECT longUrl WHERE shortCode = 'abc123'
  ↓
Redis: SET shortcode:abc123 (с TTL)
  ↓
302 Redirect
```

### 301 vs 302 — неочевидный, но реальный senior-вопрос

```txt
301 Moved Permanently:
  Браузер КЕШИРУЕТ редирект локально — повторные обращения
  к /abc123 могут вообще не доходить до сервера.
  + меньше нагрузка на сервер
  - невозможно посчитать клики (браузер не делает повторный запрос)
  - невозможно изменить целевой URL для существующего короткого кода

302 Found (Temporary Redirect):
  Браузер НЕ кеширует — каждый переход доходит до сервера.
  + точная аналитика кликов
  + можно изменить целевой URL позже
  - больше нагрузка на сервер (компенсируется кешем в Redis)
```

Для URL-шортенера с аналитикой правильный выбор — **302**, и это нужно явно обосновать (часто кандидаты выбирают 301 "потому что permanent звучит правильно", не подумав про аналитику).

## Analytics — асинхронно, не в hot path

```txt
GET /abc123
  ↓ (синхронно, минимальная latency)
302 Redirect отправлен пользователю
  ↓ (асинхронно, fire-and-forget)
Queue: { shortCode: 'abc123', timestamp, userAgent, referrer, ip }
  ↓
Worker → batched insert в Analytics DB (ClickHouse/аналитический store)
```

Почему не `UPDATE clicks SET count = count + 1` синхронно на каждый редирект? Это лишняя запись в БД **на критическом пути** редиректа. При пиковой нагрузке именно она может стать узким местом и замедлить то, что должно быть максимально быстрым. Очередь развязывает "ответить пользователю" и "посчитать статистику" — сам паттерн разобран в статье про очереди сообщений.

## Custom Aliases и Expiration

```txt
Custom alias ("app.com/my-campaign"):
  - проверка уникальности перед созданием (UNIQUE constraint на shortCode)
  - не покрывается генерацией Base62/Ticket Server —
    отдельный путь записи с явной проверкой коллизий

Expiration:
  - поле expiresAt в таблице ShortUrls
  - проверка при редиректе: если expiresAt < now() → 404/410 Gone
  - периодическая job для удаления истёкших записей
    (или TTL на уровне Redis-кеша + lazy-проверка в БД)
```

## Финальная архитектура

```txt
                ┌───────────────┐
                │ Client        │
                └───────────────┘
                        │
                        ▼
                ┌───────────────┐
                │ Load Balancer │
                └───────────────┘
                        │
                        ▼
                ┌───────────────┐
                │ API Servers   │
                │ stateless     │
                └───────────────┘
        ▼                  ▼               ▼
┌──────────────┐  ┌─────────────────┐  ┌───────┐
│ Redis        │  │ PostgreSQL      │  │ Queue │
│ горячие коды │  │ источник правды │  │ клики │
└──────────────┘  └─────────────────┘  └───────┘
                                           ▼
                                 ┌──────────────────┐
                                 │ Analytics Worker │
                                 └──────────────────┘
                                           │
                                           ▼
                                 ┌──────────────────┐
                                 │ Analytics DB     │
                                 └──────────────────┘
```

Read replicas для PostgreSQL добавляются, если нагрузка редиректами после Redis-кеша всё ещё значима. В большинстве реалистичных сценариев это не первоочередная задача, потому что доля попаданий в Redis по популярным ссылкам высокая. Трафик по ссылкам распределён по Zipf: малая доля ссылок получает большую часть переходов.

## Типичные ошибки на интервью

- **Использовать `hash % N` или простую обрезку MD5 без обсуждения коллизий.** Без проверки и стратегии повторной попытки это не "решение", а источник скрытых багов на масштабе.

- **Auto-increment ID без обсуждения распределённой генерации.** На одном сервере это просто. Но "как это работает с 50 API-инстансами" — ожидаемый следующий вопрос, и ожидаемый ответ на него — заранее выданные диапазоны от Ticket Server.

- **Выбирать 301 redirect "по умолчанию".** Это делает аналитику невозможной, и пользователь может больше вообще не дойти до сервера.

- **Синхронный инкремент счётчика кликов** — запись в БД на каждый редирект, прямо в горячем пути, без очереди.

- **Не оценивать масштаб данных** — пытаться обосновать шардинг для системы, где 5 лет данных занимают пару терабайт и прекрасно живут в одной БД с индексом.

- **Игнорировать кеш как "опциональное улучшение".** При соотношении чтений к записям 100:1 и выше кеш — это не оптимизация, а центральный элемент архитектуры.
