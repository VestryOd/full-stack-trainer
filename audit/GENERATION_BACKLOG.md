# Бэклог генерации

## Состояние на 3 сентября 2026

| пункт | статус |
|---|---|
| 1. Диаграммы в банк `angular` | **закрыт**, `210cdc3` |
| 2. Оговорка про серверное состояние в `state-management` | **закрыт**, `210cdc3` |
| 3. Пять дефектов курса `python-fullstack` | **закрыт**, `99b72d2` |
| 4. Глубина `advanced`-ответов `web-performance` | **закрыт**, `210cdc3` + `37853fe` |
| 5. Три отсутствующих квиза (`python`, `angular`, `nx`) | **закрыт**, `485ca6d` + `5b55c8b` |
| 6. Меньшие пункты | **закрыт**, `5b55c8b` + `3761630` + `b2b85d6` + `fa802bd` |

**Бэклог пуст.** Покрытие: 95 зон из 95 — знаменатель вырос с 92, потому что три новых квиза
стали тремя новыми зонами.

Разделы ниже оставлены как постановки: чтобы было видно, что именно и на каком замере делалось.

## Главное, что этот файл сообщает следующему: его постановки неточны

**Шесть из шести.** Ни одна не совпала с деревом целиком, и все шесть расхождений нашлись за
один прогон перемера:

- по пункту 1 один из четырёх кандидатов аудита (`q-63`) **уже был сделан**;
- по пункту 3 премиса про `__init__.py` была неверна: конвенцию курс объясняет. Настоящих
  дефектов там нашлось два других, и один хуже заявленного — **`auth/__init__.py` не создавался
  вовсе, при шести импортах из него в главах 15 и 16**, то есть код главы не работал;
- по пункту 4 числа аудита (304/382/143) не совпали с деревом (224/273/94);
- по пункту 5 шаблон `QUIZ.md` описывал поле `difficulty`, которого **нет ни в типе
  `QuizQuestion`, ни в одной из 1100+ записей двадцати квизов**. Два агента независимо пошли за
  корпусом, а не за шаблоном, и были правы. Шаблон исправлен;
- по пункту 6, `keycloak-auth`: «статьи 04–07: 10 стен» — реально **14**, и львиная доля в
  файле, которого в заявке нет вовсе (`11-*-interview-questions.md` держал два прогона по 34 и
  111 абзацев, потому что `---` и жирные строки вопросов мерятся как проза);
- по пункту 6, курс `angular`: «глава 15 не упоминает гидратацию» — упоминает, 7 раз по-русски
  и 18 по-английски. И «~40% нового материала до обзорной секции» **инвертировано**: обзор уже
  стоит после всего нового, а 64% главы — новый материал до него. 40% это размер обзора;
- по пункту 6, банк `tdd`: «в `diagram_gen.py` нет билдера под простой цикл, нужна правка общего
  скрипта» — правка не нужна, цикл собирается из `vchain` и `with_title_and_notes`.

**Правило, теперь подтверждённое шесть раз из шести: постановку перемеряй перед работой, даже
если она из этого файла.** Дешевле всего это делает один прогон замера по зоне до запуска агента;
в промпт агента идут уже свои числа, а заявка — как гипотеза, которую он вправе опровергнуть.

## Что нашлось при закрытии и осталось долгом

- **18 box-диаграмм в `.md` не проходят `diagram_check`** (`architecture` 12, `nodejs` 4,
  `http-rest` 2). Записано ещё волной 4, чинится только перерисовкой.
- **`diagram_check.py` не читал JSON-рубрики вообще** — тоже записано волной 4. Инструмент
  починен (`5b55c8b`), восемь box-диаграмм в банках проверены впервые, все проходят. Заодно
  выяснилось, что разъехавшуюся диаграмму в `architecture-q-13`, на которую ссылалась запись
  волны 4, с тех пор уже кто-то исправил.
- **`en/11` в `keycloak-auth` дублировал абзац**, которого в `ru/11` один — закрыто `fa802bd`.
- **220 длинных строк в `starterCode`/`solution`** рубрики задач и около 25 задач, чей скелет не
  падает на проверках. Это долг волны 25, он описан в `audit/PROGRESS.md`.
- **Противоречие курса и банка `nx` про содержимое кэша**: главы 03 и 04 пишут, что запись
  кэша хранит вывод **и код возврата**, а `nx-q-12` — что кэшируются **только успешные прогоны**.
  Если так, код возврата всегда ноль. Найдено при генерации квиза, не тронуто.


Языковой проход закрыт (92 зоны из 92, 25 волн). Здесь собрано то, что **проход не мог
починить**, потому что это требует нового содержания, а не правки языка. Все пункты найдены
аудитом или волнами прохода, каждый с приложенным замером.

Рубрика **`content/tasks`** — 243 задачи, 4000 флагов — закрыта волной 25 и теперь входит в
`COVERAGE.md`. Её остаток долга не языковой и описан в `audit/PROGRESS.md`, волна 25.

Шаблоны формы — в `content/templates/`. Правила — `content/STYLE.md` §1–§11.

---

## 1. Диаграммы в банк `angular` — самое ценное по соотношению цены и эффекта

**Замер (мой, 3 сентября 2026):** на 104 записи приходится **2 диаграммы и 1 таблица на локаль**
— диаграммы в `q-57` и `q-63`, таблица в `q-27`. Аудит считает, что три-четыре схемы подняли бы
рубрику D с 4 до 5.

**Из четырёх кандидатов, названных аудитом, один уже сделан:** у `q-63` диаграмма есть.
Остаются:

| запись | что нарисовать |
|---|---|
| `angular-q-38` | порядок цепочки интерсепторов |
| `angular-q-51` | таблица триггеров `@defer` |
| `angular-q-57`, `angular-q-63` | **образцы стиля** — там визуалы уже есть, держись их |

**Ограничения:** банк закрыт по языку (русская локаль G=5.0), поэтому правится **только**
добавление визуала, текст не переписывается. Диаграммы — импортом `diagram_gen`, ширина ≤68,
`diagram_check` без FAIL. Каждая диаграмма — пересказ того, что в ответе уже сказано, без
нового факта. Обе локали.

**Проверка после:** флаги банка остаются нулём, `block_width.py` нулём.

---

## 2. `state-management`: тема противоречит себе по серверному состоянию

**Замер:** статьи 01, 02 и 04 подают «загрузить данные с сервера в стор» как обычную форму
async-действия — с флагами `loading`/`error` и без кеша. Собственная поправка темы приходит
только в 05 и 06, где ровно этот паттерн назван изобретением велосипеда.

**Чего не хватает:** главы про серверное состояние. Читатель, прочитавший 01–04 и остановившийся,
уносит паттерн, который сама тема считает неверным.

**Что делать:** новая статья между 04 и 05 либо явная оговорка в 01 и 02 с ссылкой вперёд.
Второе дешевле и не ломает нумерацию; первое честнее. Решение за автором зоны.

**Ограничения:** зона закрыта по языку, нумерация файлов сдвигается только если добавляется
статья — тогда ссылки внутри темы надо пересобрать (`[Название](./NN-slug.md)`).

---

## 3. Пять содержательных дефектов курса `python-fullstack`

Все пять найдены проходом волны 17, ни один не языковой:

- **`bcrypt` и пароль длиннее 72 байт.** Курс не упоминает ограничение; пароль дольше
  обрезается молча, и это классическая уязвимость авторизации;
- **`fcntl` под Windows.** Пример блокировки файла не работает на Windows, и курс об этом не
  говорит;
- **в главе 15 нет middleware** при том, что глава про авторизацию и запросы;
- **`BaseExceptionGroup`** не упомянут в главе про исключения (Python 3.11+);
- **ре-экспорты `__init__.py`** — конвенция, которую курс использует, но не объясняет.

Плюс два меньших: **`python-multipart` в зависимостях главы 15 без нижней границы версии** —
для пакета, разбирающего пользовательский ввод в auth-эндпоинте, минимум стоит зафиксировать;
и **в курсе ноль ASCII-диаграмм и одна таблица на 38 файлов** (волна добавила три простых
`txt`-схемы там, где аудит просил прямо, остальное — эта задача).

**Ограничения:** курс закрыт по языку. Каждая правка добавляет содержание в существующую главу,
поэтому после неё меряй флаги главы заново: новый абзац легко даёт длинное предложение.

---

## 4. `web-performance`: глубина ответов обратна заявленной сложности

**Замер (мой, 3 сентября 2026, русская локаль).** Числа аудита (304/382/143) были другими —
верь этим:

| сложность | записей | среднее слов |
|---|---|---|
| `middle` | 10 | 224 |
| `senior` | 10 | 273 |
| **`advanced`** | **5** | **94** |

Разрыв даже резче, чем говорил аудит: `advanced` в **2.9 раза тоньше** `senior`. Поштучно:
`q-23` — 73 слова, `q-22` — 78, `q-21` — 85, `q-25` — 117, `q-24` — 120.

**Что делать:** довести пять `advanced`-ответов до глубины `senior` (около 270 слов). Это около
**900 новых слов**, то есть генерация.

**Ограничения:** банк закрыт по языку. Новое содержание пишется по
`content/templates/QUESTION-BANK.md`: лид одним предложением, слот под список, слот под
таблицу или диаграмму, «Частая ловушка» — самая короткая проза ответа.

---

## 5. Отсутствующие квизы — ЗАКРЫТО

| квиз | заданий | ключи | что было каноном |
|---|---|---|---|
| `python` | 28 | 7/7/7/7 | курс на 19 глав + банк на 98 записей |
| `angular` | 30 | 7/8/8/7 | курс на 16 файлов + банк на 104 записи |
| `nx` | 20 | 5/5/5/5 | курс на 15 глав + банк на 20 записей |

Все три — ноль флагов в обеих локалях, ноль широких строк, ни одного бэктика в `options`, каждый
ключ проверен по главе-источнику. Слепые стратегии по сайту не поехали: индекс 0 — 25.5%,
«самый длинный» — 13.1%.

Один агент поймал у себя же работающую подсказку: подрезав правильные варианты, он сделал
стратегию «выбирай самый короткий» верной в 19 случаях из 28, и переделал двенадцать заданий,
удлинив дистракторы вместо укорачивания правильного ответа. До 7 из 28, то есть до случайного
уровня.

Генерируются по `content/templates/QUIZ.md`. Главное оттуда: **сначала корректность** — у
каждого задания правильный вариант существует, ключ указывает на него, объяснение не
противоречит ключу, и задание не противоречит статьям и банку той же темы (§9.5). `options`
рендерятся **обычным текстом**, бэктики в них печатаются буквально.

Источник материала — соответствующий курс и банк; они закрыты и служат каноном.

---

## 6. Меньшие пункты, требующие содержания

- ~~**`keycloak-auth`, статьи 04–07: 10 стен теории**~~ — **закрыто** (`3761630`). Реально было
  14 стен, и основная масса в `11-*-interview-questions.md`, которого в заявке нет: два прогона
  по 34 и 111 абзацев. 43 визуала на локаль, диаграммы зоны 10 → 86 PASS, стены 14 → 0;
- ~~**в банке `tdd` нет ни одной диаграммы**~~ — **закрыто** (`5b55c8b`). Правка общего скрипта
  не понадобилась: цикл собирается из `vchain` и `with_title_and_notes`. Билдер `phase_cycle`
  добавлен в `diagram_gen.py` (только добавление). Нарисованную стрелку возврата поставить
  нельзя — `diagram_check` считает `└` вне бокса орфанным углом, поэтому цикл замыкается
  подписью;
- ~~**глава 15 курса `angular` не упоминает гидратацию**~~ — **закрыто** (`b2b85d6`).
  Упоминает: 7 раз по-русски, 18 по-английски. Не хватало глубины, и все семь пунктов заявки
  подтвердились: `NG0506`, десятисекундный таймаут, откат к клиентскому рендерингу,
  `ngSkipHydration`, Trusted Types, `@defer (hydrate never)`, i18n с `--localize`. Все добавлены.
  Оговорка: `PendingTasks` был не только в банке — он есть в главе 03;
- ~~**глава 15 заявлена обзорной, но вводит ~40% нового материала до обзорной секции**~~ —
  **закрыто** (`b2b85d6`), и заявка была инвертирована: обзор уже стоял вторым H3 с конца, а
  новый материал до него — 64% главы. 40% это размер обзора. Верна была вторая половина
  претензии: в обзоре не было упражнения на ранние главы. Добавлено семь обратимых регрессий на
  готовом проекте, каждая с наблюдаемым результатом и ссылкой на главу;
- **`defaultVertexShader` в `canvas-graphics/05`** используется и нигде не определён. Требует
  имени экспорта Pixi v8, которого нельзя проверить по репозиторию (`pixi.js` в зависимостях
  проекта нет). **Решение пользователя от 3 сентября 2026: пропускаем**, разберётся точечно при
  прохождении темы.

---

## Открытый вопрос, ждущий решения

**На какую версию нацелен топик `nextjs`.** Топик написан под Next.js 15, документация уже 16.3.
Проход починил два места, где расхождение делало текст неверным, и не тронул третье:
`unstable_cache` в статье 05 несёт баннер «This API has been replaced by `use cache` in
Next.js 16». По топику 32 упоминания версии 15 против 2 упоминаний 16.

Переезд на 16 — отдельная работа по трём зонам: тема, банк (70 записей), квиз (30 заданий).
**Промежуточное решение: держим 15**, утверждения ограничены версией. Откатывается одной правкой.
